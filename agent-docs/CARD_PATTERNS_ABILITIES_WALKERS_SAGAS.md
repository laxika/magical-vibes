# Card Patterns: Activated Abilities, Planeswalkers & Sagas
- 3-chapter Saga with mill, own-graveyard exile/token, and Zombie count: `t/TymaretCallsTheDead.java` uses `SequenceEffect.of(MillEffect(3, CONTROLLER), ExileOwnGraveyardCardThenCreateTokenEffect(CREATURE or ENCHANTMENT, CreateTokenEffect.blackZombie(1)))` for chapters I/II and the same `PermanentCount(HasSubtype(ZOMBIE), CONTROLLER)` for chapter III life gain and scry.

All paths relative to `cards/`.

## Library and manifest effects

| Manifest the top card, then put counters on it | `f/FierceInvocation.java` / `w/Wildcall.java` | `ManifestTopCardAndPutCountersEffect(CounterType.PLUS_ONE_PLUS_ONE, 2)` / `ManifestTopCardAndPutCountersEffect(CounterType.PLUS_ONE_PLUS_ONE, new XValue())` |

| 3-chapter double-faced Saga with a delayed X-payment combat trigger on its back face | `t/TheRiseOfSozin.java` / `f/FireLordSozin.java` | Front: chapter I destroys all creatures, chapter II targets an opponent and uses `ChooseCardNameAndExileFromZonesEffect(..., 4)`, and chapter III uses `ExileSelfAndReturnTransformedEffect()`. Back: `ON_COMBAT_DAMAGE_TO_PLAYER` uses `PayXManaReanimateCreaturesWithTotalManaValueXEffect()` |

## Activated abilities

| Tap plus mana to force a chosen creature type to attack | `w/WalkingDesecration.java` | `(true, "{B}", CreaturesOfChosenTypeMustAttackThisTurnEffect)` - chooses the type on resolution and applies the transient must-attack requirement to matching creatures on every battlefield |

| Sacrifice white creature + per-attacking-red-creature payment | `h/Heroism.java` | SacrificePermanentCost(creature + WHITE, "Sacrifice a white creature") + PreventCombatDamageFromAttackingCreaturesUnlessPaysEffect(attacking creature + RED, "{2}{R}") - each matching attacker controller independently pays or that creature deals no combat damage this turn |

| Planeswalker with source-tracked exile, filtered X return, and opponent hand/graveyard exile | `a/AshiokNightmareWeaver.java` | `ExileTopCardsToSourceEffect(... TARGET_OPPONENT, true)` + `ReturnCardExiledWithSourceToBattlefieldEffect(CardTypePredicate(CREATURE), true, NIGHTMARE)` + `ExileAllOpponentsHandsEffect` + `ExileGraveyardCardsEffect(ALL_OPPONENTS)` |

| Pattern | Reference | Notes |
|---------|-----------|-------|
| Restricted mana-source tutor | `m/MoonsilverKey.java` | `SacrificeSelfCost` + `SearchLibraryEffect(CardAnyOf(CardAllOf(ARTIFACT, CardHasManaAbilityPredicate), basicLand()), HAND)` |
| Tap to damage any | `p/ProdigalPyromancer.java` | `(true, null, DealDamageToAnyTargetEffect, true)` |
| Tap to damage target + all same name | `i/IzzetStaticaster.java` | `(true, null, DealDamageToTargetCreatureAndAllWithSameNameEffect(1))` — only one target; same-name (incl. hexproof) still hit |
| Tap to damage player/PW (Desert-gated) | `w/WallOfForgottenPharaohs.java` | `DealDamageToTargetPlayerOrPlaneswalkerEffect(1)` + `.withActivationCondition(AnyOf(ControlsPermanent(DESERT), GraveyardCardThreshold(1, CardSubtypePredicate(DESERT))))` |
| Mana + tap to damage | `r/RodOfRuin.java` | `(true, "{3}", DealDamageToAnyTargetEffect, true)` |
| Damage + self-damage | `o/OrcishArtillery.java` | Two effects: damage target + DealDamageToPlayersEffect(3, DamageRecipient.CONTROLLER) |
| Pump self | `f/FurnaceWhelp.java` | `(false, "{R}", BoostSelfEffect, false)` |
| Discard at random, then any player may take damage instead of a self-pump | `b/BarbarianBully.java` | `DiscardRandomCardCost` + `AnyPlayerMayTakeDamageOrBoostSelfEffect(4, 2, 2)` with max activations per turn 1 |
| Pump self + conditional sacrifice | `d/DragonWhelp.java` | BoostSelfEffect activated ability + END_STEP_TRIGGERED ConditionalEffect(new ActivationCount(threshold, abilityIndex), SacrificeSelfEffect) |
| Self unblockable (Phyrexian mana) | `t/TrespassingSouleater.java` | `(false, "{U/P}", MakeCreatureUnblockableEffect(true), ...)` — Phyrexian mana activated, self-targeting unblockable |
| Pump target | `g/GhostWarden.java` | `(true, null, BoostTargetCreatureEffect, true)` |
| Copy target creature's activated abilities until end of turn | `q/QuicksilverElemental.java` | `ActivatedAbility("{U}", GainActivatedAbilitiesOfTargetCreatureUntilEndOfTurnEffect)` plus a STATIC `SpendBlueManaAsAnyColorForActivatedAbilitiesEffect` permission |
| Pump target + filter | `h/HateWeaver.java` | With PermanentPredicateTargetFilter |
| Tap to tap target | `i/IcyManipulator.java` | TapPermanentsEffect(TapUntapScope.TARGET) + PermanentPredicateTargetFilter |
| Tap to lock artifact | `r/RustTick.java` | MayNotUntapDuringUntapStepEffect (STATIC) + TapPermanentsEffect(TapUntapScope.TARGET) + DoesntUntapEffect.targetWhileSourceTapped() + PermanentIsArtifactPredicate |
| Tap to lock a tapped creature + drain it with -1/-1 counters | `g/GiantOyster.java` | MayNotUntapDuringUntapStepEffect (STATIC) + `{T}` ability TapPermanentsEffect(TapUntapScope.TARGET) + DoesntUntapEffect.targetWhileSourceTapped() with a tapped-creature filter, plus DRAW_TRIGGERED PutCountersOnUntapLockedPermanentsEffect(MINUS_ONE_MINUS_ONE, 1) and STATIC RemoveCountersWhenUntapLockEndsEffect(MINUS_ONE_MINUS_ONE) |
| Tap to draw | `a/ArcanisTheOmnipotent.java` | `(true, null, DrawCardEffect, false)` |
| Draw + discard | `m/MerfolkLooter.java` | DrawCardEffect + DiscardEffect(1, CONTROLLER) |
| Grant keyword to target | `m/MightWeaver.java` | GrantKeywordEffect with color filter |
| Sacrifice self for effect | `b/BottleGnomes.java` | SacrificeSelfCost + effect |
| Sacrifice self to remove all counters from target permanent | `v/VampireHexmage.java` | SacrificeSelfCost + `RemoveAllCountersFromTargetPermanentEffect` with `TargetFilters.permanent()`; removes every counter type without imposing a lasting counter lock |
| Sacrifice self for mana + sacrifice self to damage blocked creature | `t/TinderWall.java` | Two no-tap abilities: SacrificeSelfCost+AwardManaEffect(RED, 2) and {R}+SacrificeSelfCost+DealDamageToTargetCreatureEffect(2) with `PermanentAllOfPredicate(IsCreature, BlockedBySource)` — the sacrificed source's last known information keeps the target legal (CR 608.2b) |
| Sacrifice self + multi-target creature damage | `f/FireShrineKeeper.java` | tap+mana+SacrificeSelfCost+DealDamageToTargetCreatureEffect(3) with multi-target ability constructor (0-2 creature targets) — "up to two target creatures" pattern |
| Sacrifice self + damage player/pw | `v/VulshokReplica.java` | SacrificeSelfCost + DealDamageToAnyTargetEffect + PermanentIsPlaneswalkerPredicate filter (restricts to players + planeswalkers) |
| Sacrifice self + choose source prevention | `a/AuriokReplica.java` | SacrificeSelfCost + `PreventDamageFromChosenSourceEffect.allDamageToYou()` (controller-only). `b/BurrentonForgeTender.java` uses `allDamage(new PermanentColorInPredicate(Set.of(RED)), "red")` for global red-source prevention |
| Target-specific chosen-color prevention | `a/AvacynGuardianAngel.java` | `PreventAllDamageToTargetFromChosenColorEffect` on a target creature or player/planeswalker ability; the effect prompts for the color at resolution and installs a turn-scoped target shield |
| Sacrifice self + prevent target's combat damage | `r/ResistanceFighter.java` | SacrificeSelfCost + PreventDamageEffect.allCombatByTargetCreatures() — no-mana ability, single creature target; `combatOnly=true` prevents only combat damage |
| Target prevention from chosen source + life gain | `h/HealingGrace.java` | PreventDamageToTargetFromChosenSourceEffect(3) + GainLifeEffect(3) — target chosen on cast, source chosen on resolution |
| Sacrifice subtype for effect | `s/SiegeGangCommander.java` | SacrificePermanentCost(PermanentAllOfPredicate(creature + PermanentHasSubtypePredicate(GOBLIN)), description, false) + DealDamageToAnyTargetEffect |
| Exile creature for restricted mana | `f/FoodChain.java` | ExilePermanentCost(PermanentIsCreaturePredicate, "a creature", true, true) + AwardAnyColorManaEffect(Sum(Fixed(1), XValue), CREATURE_SPELL_ONLY) |
| Sacrifice subtype for +1/+1 counters on target | `d/DerangedOutcast.java` | SacrificePermanentCost(PermanentAllOfPredicate(creature + PermanentHasSubtypePredicate(HUMAN)), description, false) + PutCounterOnTargetPermanentEffect(PLUS_ONE_PLUS_ONE, 2) — source is itself a Human, so it can sacrifice itself |
| Sacrifice artifact for effect | `b/BarrageOgre.java` | SacrificePermanentCost(PermanentIsArtifactPredicate, "an artifact", false) + DealDamageToAnyTargetEffect (tap + sac artifact) |
| Sacrifice permanent (predicate) for effect | `m/MakeshiftMunitions.java` | SacrificePermanentCost(PermanentAnyOfPredicate) + DealDamageToAnyTargetEffect (mana + sac artifact or creature, no tap) |
| Sacrifice multiple permanents for tutor | `k/KuldothaForgemaster.java` | SacrificeMultiplePermanentsCost(3, PermanentIsArtifactPredicate) + SearchLibraryEffect(CardTypePredicate(ARTIFACT), LibrarySearchDestination.BATTLEFIELD) (tap + sac 3 artifacts) |
| Tap to +1/+1 counters on controlled filtered permanents | `s/SteelOverseer.java` | `(true, null, PutCounterOnEachControlledPermanentEffect(CounterType.PLUS_ONE_PLUS_ONE, 1, PermanentAllOfPredicate(artifact+creature)))` — no target, affects all matching permanents you control |
| Regenerate (self) | `d/DrudgeSkeletons.java` | `(false, "{B}", RegenerateEffect, false)` |
| Regenerate (target creature) | `a/Asceticism.java` | `RegenerateEffect(true)` + PermanentPredicateTargetFilter |
| Static effect grant (own creatures) | `a/Asceticism.java` | `GrantEffectEffect(TargetingRestrictionEffect.hexproof(), GrantScope.OWN_CREATURES)` |
| Create token | `d/DragonRoost.java` | CreateTokenEffect |
| Mill target | `m/Millstone.java` | `(true, "{2}", MillEffect(2, TARGET_PLAYER), true)` |
| Reveal until basic land (to hand, rest GY) | `h/HermitDruid.java` | `(true, "{G}", RevealUntilBasicLandToHandRestToGraveyardEffect)` — nonbasics do not stop |
| Reveal until chosen creature type to battlefield | `r/RiptideShapeshifter.java` | `SacrificeSelfCost` + `RevealUntilChosenCreatureTypeToBattlefieldEffect` — chooses the type on resolution, puts the first matching creature (including Changeling) onto the battlefield, then shuffles the other reveals |
| Mana dork (tap for color) | `b/BirdsOfParadise.java` | `(true, null, AwardAnyColorManaEffect, false)` |
| Mana rock (tap for N of any color) | `g/GildedLotus.java` | `(true, null, AwardAnyColorManaEffect(3), false)` |
| Mana rock (choose subtype + tap for any color restricted to chosen creature type) | `p/PillarOfOrigins.java` | ON_ENTER_BATTLEFIELD ChooseSubtypeOnEnterEffect + `(true, null, AwardAnyColorManaEffect(1, CHOSEN_SUBTYPE_CREATURE), false)` |
| Land (choose subtype + tap for any color restricted to chosen creature type, and that spell can't be countered) | `c/CavernOfSouls.java` | ON_ENTER_BATTLEFIELD ChooseSubtypeOnEnterEffect + `ManaAbilities.tapFor(COLORLESS)` + `AwardAnyColorManaEffect(1, CHOSEN_SUBTYPE_CREATURE_UNCOUNTERABLE)` |
| Land (restricted chosen-type spells or abilities) | `e/EclipsedRealms.java` | ON_ENTER_BATTLEFIELD `ChooseSubtypeOnEnterEffect(allowedTypes)` + `ManaAbilities.tapFor(COLORLESS)` + `AwardAnyColorManaEffect(1, CHOSEN_SUBTYPE_SPELL_OR_ABILITY)` |
| Mana dork (tap for N any color, creature spells only) | `s/SomberwaldSage.java` | `(true, null, AwardAnyColorManaEffect(3, CREATURE_SPELL_ONLY), false)` |
| Land (tap for any color restricted to multicolored spells) | `p/PillarOfTheParuns.java` | `(true, null, AwardAnyColorManaEffect(1, MULTICOLORED_SPELLS), false)` |
| Mana dork (ON_TAP) | `l/LlanowarElves.java` | addEffect(ON_TAP, AwardManaEffect) |
| Mana dork (G or U + conditional Nissa life) | `a/AvidReclaimer.java` | two `{T}` abilities: AwardManaEffect(GREEN\|BLUE) + ConditionalEffect(ControlsPermanent(NISSA), GainLifeEffect(2)) — conditional rider resolved inline on mana abilities |
| Animate self (X/X) | `c/ChimericStaff.java` | AnimatePermanentsEffect(XValue, XValue, subtypes, …, SELF, UEOT) |
| Sorcery-speed ability | `t/ThrullSurgeon.java` | ActivationTimingRestriction.SORCERY_SPEED |
| Metalcraft exile-flicker | `a/ArgentSphinx.java` | FlickerEffect.exileSelfReturnAtEndStep() + METALCRAFT restriction |
| Soulbond + paired flicker grant | `d/DeadeyeNavigator.java` | SoulbondChoosePartnerEffect + SoulbondPairWithEnteringEffect; STATIC ConditionalEffect(SourceIsPaired, GrantActivatedAbilityEffect(flickerSelfUnderYourControl, SELF_AND_PAIRED)) |
| Soulbond + paired triggered ability | `t/TandemLookout.java` | SoulbondChoosePartnerEffect + SoulbondPairWithEnteringEffect; STATIC ConditionalEffect(SourceIsPaired, GrantTriggeredAbilityEffect(ON_DAMAGE_TO_PLAYER, DrawCardEffect(1), SELF_AND_PAIRED)) |
| Soulbond + paired haste | `l/LightningMauler.java` | SoulbondChoosePartnerEffect + SoulbondPairWithEnteringEffect; STATIC ConditionalEffect(SourceIsPaired, GrantKeywordEffect(HASTE, SELF_AND_PAIRED)) |
| Metalcraft tap ability | `v/VedalkenCertarch.java` | ActivationTimingRestriction.METALCRAFT + target filter |
| Power-gated tap ability | `b/BloodshotTrainee.java` | ActivationTimingRestriction.POWER_4_OR_GREATER + creature target filter |
| Attack-only ability | `a/AncientHellkite.java` | ActivationTimingRestriction.ONLY_WHILE_ATTACKING + opponent creature filter (PermanentNotPredicate(PermanentControlledBySourceControllerPredicate)) |
| X-cost mass destroy (combat damage gated) | `s/SteelHellkite.java` | BoostSelfEffect pump + DestroyNonlandPermanentsWithManaValueXDealtCombatDamageEffect with maxActivationsPerTurn=1 — X-cost, once per turn, only affects damaged player's permanents |
| Loyalty (no target) | `a/AjaniOutlandChaperone.java` | `(+1, effects, false, description)` |
| Loyalty (with target filter) | `a/AjaniOutlandChaperone.java` | `(-2, effects, true, description, filter)` |

## Planeswalkers

| Pattern | Reference | Notes |
|---------|-----------|-------|
| Modal DFC with planeswalker-attack loyalty trigger and discard/reanimate/emblem back face | `m/MilaCraftyCompanion.java` / `l/LukkaWaywardBonder.java` | Front: `ON_ANY_PLAYER_ATTACKS` conditional on an opponent attacking a controlled planeswalker, plus a permanent-only opponent-target trigger for the optional draw. Back: `+1 DiscardCardThenEffect` with a creature-card alternate draw, `−2 ReturnCardFromGraveyardEffect` with haste and next-upkeep exile, `−7 CreateEmblemEffect` carrying the creature-entry source-power damage marker |
| Planeswalker with opponent sorcery-speed restriction + next-turn sorcery flash + optional bounce cantrip | `t/TeferiTimeRaveler.java` | STATIC `OpponentsCanCastSpellsOnlyAtSorcerySpeedEffect`; +1 `GrantFlashToCardTypeUntilNextTurnEffect(SORCERY)`; -3 `ReturnToHandEffect.target()` + `DrawCardEffect(1)` with an up-to-one artifact/creature/enchantment target group |
| **Any "you get an emblem with …" ultimate** | `s/SorinLordOfInnistrad.java` | `CreateEmblemEffect(List.of(<the emblem's static/marker effects>), "<emblem text, unquoted>")`. Declare the emblem's contents on the card — there is **no** per-card emblem effect class. Static payloads (`GrantKeywordEffect`, `GrantActivatedAbilityEffect`, `StaticBoostEffect`) are read by the layered pass; trigger markers by their owning service. For "**target opponent** gets an emblem" pass `EmblemRecipient.TARGET_PLAYER` and pair with `PlayerPredicateTargetFilter(OPPONENT)` |
| Planeswalker with until-next-turn mass pump, token, and opponent-upkeep sacrifice emblem | `s/SorinSolemnVisitor.java` | +1 `BoostAllOwnCreaturesEffect(1, 0, GrantDuration.UNTIL_YOUR_NEXT_TURN)` + `GrantKeywordEffect(LIFELINK, OWN_CREATURES, UNTIL_YOUR_NEXT_TURN)`; -2 `CreateTokenEffect("Vampire", 2, 2, BLACK, VAMPIRE, FLYING)`; -6 `CreateEmblemEffect` with `EmblemStepTriggerEffect(OPPONENT_UPKEEP, List.of(SacrificePermanentsEffect(1, PermanentIsCreaturePredicate, ACTIVE_PLAYER)))`. `StepTriggerService` puts the non-targeting emblem trigger on the stack with the active opponent as the effect recipient |
| Planeswalker with discard/sacrifice/pile separation | `l/LilianaOfTheVeil.java` | +1 DiscardEffect(1, EACH_PLAYER), -2 SacrificePermanentsEffect(1, PermanentIsCreaturePredicate, TARGET_PLAYER), -6 SeparatePermanentsIntoPilesAndSacrificeEffect |
| Planeswalker with reveal-to-hand opponent life loss, variable creature/planeswalker damage, and life-total-scaled tokens | `s/SorinGrimNemesis.java` | +1 `RevealTopCardPutIntoHandThenEachOpponentLosesLifeEffect`; −X `DealDamageToTargetCreatureOrPlaneswalkerEffect(XValue)` + `GainLifeEffect(XValue)`; −9 `CreateTokenEffect(HighestLifeTotalAmongPlayers, "Vampire Knight", 1/1, BLACK, VAMPIRE+KNIGHT, LIFELINK)` |
| Planeswalker ultimate creating a life-total-scaling token | `a/AjaniGoldmane.java` | −6 `CreateTokenEffect(1, "Avatar", 0, 0, WHITE, List.of(AVATAR), Set.of(), Set.of(), Map.of(STATIC, new SetPowerToughnessToAmountEffect(new ControllerLifeTotal(), new ControllerLifeTotal())))` — CDA P/T via the `tokenEffects` map, so the Avatar tracks the life total continuously |
| Planeswalker with opponent reveal choice + silver counter exile + artifact-scaling token | `k/KarnScionOfUrza.java` | +1 KarnScionRevealTwoOpponentChoosesEffect, -1 KarnScionReturnSilverCounterCardEffect, -2 CreateTokenEffect with STATIC BoostSelfEffect(PermanentCount(PermanentIsArtifactPredicate, CONTROLLER), same) via tokenEffects map |
| Planeswalker with Mulch reveal + any-number lands tapped + land-scaled Treefolk + GY permanents + no-max-hand | `w/WrennAndSeven.java` | +1 `LookAtTopCardsEffect.chooseNToHandRestToGraveyard(4,4,LAND,true)`. 0 `PutCardToBattlefieldEffect.tappedAnyNumber(LAND,"land")`. −3 `CreateTokenEffect` Treefolk/reach + STATIC `SetPowerToughnessToAmountEffect(PermanentCount(IsLand,CONTROLLER),same)`. −8 `ReturnCardFromGraveyardEffect` (permanent filter, returnAll, HAND) + `GrantNoMaximumHandSizeEffect(REST_OF_GAME)` |
| Planeswalker with draw + delayed untap + tuck + emblem | `t/TeferiHeroOfDominaria.java` | +1 DrawCardEffect + RegisterDelayedUntapPermanentsEffect(2), -3 PutTargetPermanentIntoLibraryNFromTopEffect(2) with nonland filter, -8 `CreateEmblemEffect(List.of(new ExileTargetOpponentPermanentOnDrawEffect()), "Whenever you draw a card, exile target permanent an opponent controls.")` — DrawService fires the marker on each draw |
| Planeswalker with combat-damage watch + up-to-two tap-lock + free-cast emblem | `t/TamiyoFieldResearcher.java` | +1 multi-target loyalty ability (`(false,null,effects,desc,null,+1,null,null,filters,0,2)`, filters = two `TargetFilters.creature()`) = `RegisterDelayedWatchedCreaturesCombatDamageEffect(List.of(new DrawCardEffect(1)))` — the delayed watch is controlled by the ability's controller, so "you draw" draws for them even off an opponent's creature. −2 same shape with two `TargetFilters.nonlandPermanent()` = `TapPermanentsEffect(TARGET)` + `SkipNextUntapEffect(TARGET)`. −7 `DrawCardEffect(3)` + `CreateEmblemEffect(List.of(new AlternativeCostForSpellsEffect("{0}", null, null, false, true)), "…")` — the emblem reuses Omniscience's hand-only free cast, scanned out of `GameData.emblems` by `CastingCostService` |
| Planeswalker with creature-count loyalty, creature-count pump, and persistent creature-entry draw emblem | `h/HuatliRadiantChampion.java` | +1 `PutCountersOnSelfEffect(LOYALTY, PermanentCount(IsCreature, CONTROLLER))`; −1 `BoostTargetCreatureEffect(PermanentCount(IsCreature, CONTROLLER), same)` with `TargetFilters.creature()`; −8 `CreateEmblemEffect(List.of(new DrawOnControlledCreatureEntersEffect()), "Whenever a creature you control enters, you may draw a card.")` |
| Planeswalker with tap-lock + tapped-creature draw + graveyard-recursion emblem | `t/TamiyoTheMoonSage.java` | +1 `TapPermanentsEffect(TARGET)` + `SkipNextUntapEffect(TARGET)` with **no** target filter (any permanent). -2 `DrawCardEffect(PermanentCount(AllOf(IsCreature, IsTapped), TARGET_PLAYER))` — the TARGET_PLAYER scope is what makes `DrawCardEffect.targetSpec()` advertise a player target, so no card-level filter is needed for "target player". -8 `GrantNoMaximumHandSizeEffect(REST_OF_GAME)` + `CreateEmblemEffect(List.of(new ReturnCardPutIntoGraveyardToHandEffect()), "…")` (the emblem carries only the graveyard-return marker) |
| Planeswalker with reveal-creatures dig + green creature from hand + creature-tutor emblem | `g/GarrukCallerOfBeasts.java` | +1 `new LookAtTopCardsEffect(Fixed(5), Fixed(5), CardTypePredicate(CREATURE), LookDestination.BOTTOM_OF_LIBRARY, true)` — `chooseCount == lookCount` makes it choice-free, the non-creatures raise a `LibraryReorder` for the bottom order. −3 `MayEffect(PutCardToBattlefieldEffect(AllOf(CREATURE, CardColorPredicate(GREEN)), "green creature"))`. −7 `CreateEmblemEffect` (marker `SearchCreatureToBattlefieldOnControllerCastsCreatureSpellEffect.Marker`; each creature cast queues `MayEffect(SearchLibraryEffect(creature, BATTLEFIELD))`) |
| Planeswalker with planeswalker removal + token + drain removal + opponent-side emblem | `g/GarrukApexPredator.java` | +1 `DestroyTargetPermanentEffect` filtered to `AllOf(IsPlaneswalker, Not(IsSourceCard))` (another planeswalker). +1 `CreateTokenEffect(Beast 3/3 black, DEATHTOUCH)`. −3 `GainLifeEffect(TargetToughness())` **listed before** `DestroyTargetPermanentEffect` so the toughness is read while the creature is still on the battlefield (Condemn ordering). −8 `CreateEmblemEffect(…, EmblemRecipient.TARGET_PLAYER)` + `PlayerPredicateTargetFilter(OPPONENT)` — the emblem lands under the *opponent*'s control and carries `BoostAttackingCreatureOnAttacksYouEffect(5, 5, {TRAMPLE})` |
| Planeswalker with scry+draw + bounce + first-spell-counter emblem | `j/JaceUnravelerOfSecrets.java` | +1 ScryEffect(1)+DrawCardEffect(1), -2 ReturnToHandEffect.target() with creature filter, -8 `CreateEmblemEffect(List.of(new CounterOpponentFirstSpellEachTurnEffect.Marker()), "…")`; TriggerCollectionService queues CounterSpellEffect (Zone.STACK) when an opponent's spellsCastThisTurnCount == 1 |
| Planeswalker that self-animates + prevents damage + conditional can't-lose emblem | `g/GideonOfTheTrials.java` | +1 `PreventDamageEffect.allByTargetPermanentUntilNextTurn()` (benign PERMANENT target filter). 0 (self-animate) `AnimatePermanentsEffect(4,4, [HUMAN,SOLDIER], {INDESTRUCTIBLE})` (SELF, selfTargeting auto-targets the source) + `PreventDamageEffect.allToSelf()`. 0 (emblem) `CreateEmblemEffect(List.of(new ConditionalEffect(ControlsPermanent(PermanentHasSubtypePredicate(GIDEON)), new CantLoseGameEffect())), "…")`. Two distinct 0-loyalty abilities are fine — selected by index |
| Planeswalker that taunts onto itself + tapped-creature removal + self-animate | `g/GideonJura.java` | +2 `MustAttackNextTurnEffect(TauntTarget.SOURCE_PERMANENT)` with `PlayerPredicateTargetFilter(PlayerRelationPredicate(OPPONENT))`. -2 `DestroyTargetPermanentEffect` filtered to `PermanentAllOfPredicate(IsCreature, IsTapped)`. 0 `AnimatePermanentsEffect(6,6,[HUMAN,SOLDIER], Set.of())` + `PreventDamageEffect.allToSelf()` |
| Creature transforming into a planeswalker + single-creature taunt | `k/KytheonHeroOfAkros.java` / `g/GideonBattleForged.java` | Front: ON_ATTACK `ConditionalEffect(MinimumAttackers(3), ExileSelfAtEndOfCombatAndReturnTransformedEffect())` (battalion shape) + `{2}{W}` `GrantKeywordEffect(INDESTRUCTIBLE, SELF)`. Back: +2 `TargetCreatureMustAttackSourcePermanentNextTurnEffect()` on an up-to-one multi-target loyalty ability (`minTargets 0`, `maxTargets 1`), +1 `GrantKeywordEffect(INDESTRUCTIBLE, TARGET, UNTIL_YOUR_NEXT_TURN)` + `UntapPermanentsEffect(TARGET, IsCreature)`, 0 `AnimatePermanentsEffect(4,4,[HUMAN,SOLDIER], Set.of(INDESTRUCTIBLE))` + `PreventDamageEffect.allToSelf()` |
| Planeswalker with a tap-one/untap-another loyalty ability + coin-flip extra turns | `r/RalZarek.java` | +1 multi-target loyalty ability (`(false,null,effects,desc,null,+1,null,null,filters,2,2)`, filters = two `TargetFilters.permanent()`) = `TapTargetThenUntapAnotherTargetEffect()` — the first target is tapped, the second untapped, and the multi-target path's duplicate-target rejection is what enforces "another". −2 `DealDamageToAnyTargetEffect(3)`. −7 `FlipCoinsPerHeadsEffect(5, new ControllerExtraTurnEffect(1))` — five coins, one extra turn per heads |
| Variable loyalty (-X) | `c/ChandraNalaar.java` | `ActivatedAbility.variableLoyaltyAbility(effects, desc, filter)` — loyalty cost is -X chosen by player, X stored in xValue |
| Enters with X loyalty + scry + conditional top-card play + multi-target land animation | `n/NissaStewardOfElements.java` | `{X}{G}{U}` planeswalker enters with **X loyalty** (no card-side code — `SpellCastingService` threads the paid X into the stack entry, `StackResolutionService.resolvePlaneswalkerSpell` reads it when the mana cost `hasX()`). +2 `ScryEffect(2)`. 0 `LookAtTopCardPutLandOrCreatureWithinLoyaltyEffect()`. −6 multi-target loyalty ability (`(false,null,effects,desc,null,-6,null,null,filters,0,2)`) = `UntapPermanentsEffect(ALL_TARGETS)` + `AnimatePermanentsEffect(5,5,[ELEMENTAL],{FLYING,HASTE},null,{},TARGET,UNTIL_END_OF_TURN)`; filters = two `ControlledPermanentPredicateTargetFilter(PermanentIsLandPredicate)` |
| Loyalty untap mixed creatures/lands + pump + any-number dig to BF | `n/NissaGenesisMage.java` | +2 multi-target loyalty (`0..4` identical creature-or-land filters) + `.withMultiTargetConstraint(AT_MOST_TWO_CREATURES_AND_TWO_LANDS)` + `UntapPermanentsEffect(ALL_TARGETS)`. −3 `BoostTargetCreatureEffect(5,5)`. −10 `LookAtTopCardsEffect.mayPutAnyNumberMatchingOntoBattlefieldRestOnBottomRandom(10, creatureOrLand)` |
| Loyalty draw+mill / mass mill / any-number-of-target-players draw | `j/JaceMemoryAdept.java` | +1 `DrawCardEffect(1)` + `MillEffect(1, TARGET_PLAYER)` (player target auto-derived from the mill's `targetSpec`); 0 `MillEffect(10, TARGET_PLAYER)`; −7 multi-target loyalty ability (`(false,null,effects,desc,null,-7,null,null,filters,0,2)`) = `DrawCardForTargetPlayerEffect(20)` with two `PlayerPredicateTargetFilter(PlayerRelationPredicate(ANY))` filters — the handler draws for each legal target |
| Loyalty conditional self-mill drain / -X/-X by tribe count / tribal graveyard-cast permission | `l/LilianaUntouchedByDeath.java` | +1 `MillControllerThenIfMilledEffect(3, CardSubtypePredicate(ZOMBIE), SequenceEffect(List.of(LoseLifeEffect(2, EACH_OPPONENT), GainLifeEffect(2))))` — the "if a matching card was milled this way" check lives in the effect, not a `ConditionalEffect`. −2 `BoostTargetCreatureEffect(Scaled(PermanentCount(PermanentHasSubtypePredicate(ZOMBIE), CONTROLLER), -1), same)` + a creature `PermanentPredicateTargetFilter` — "-X/-X where X is …" is a negated `Scaled` amount, never a new effect. −3 `AllowCastMatchingCardsFromGraveyardThisTurnEffect(CardSubtypePredicate(ZOMBIE))` — turn-scoped blanket graveyard-cast grant |
| Loyalty + mana + player damage | `c/ChandraBoldPyromancer.java` | +1 with AwardManaEffect + DealDamageToPlayersEffect(2, TARGET_PLAYER), -3 DealDamageToTargetCreatureOrPlaneswalkerEffect, -7 DealDamageToPlayersEffect(10, TARGET_PLAYER) + DealDamageToPermanentsTargetControlsEffect |
| Loyalty + optional player/PW damage + color impulse + red-spell emblem | `c/ChandraDressedToKill.java` | +1 AwardManaEffect(RED) + DealDamageToTargetPlayerOrPlaneswalkerEffect(1) with minTargets 0 / maxTargets 1. +1 ExileTopCardsMayCastMatchingThisTurnEffect(1, CardColorPredicate(RED)). −7 ExileTopCardsMayCastMatchingThisTurnEffect(5, RED) + `CreateEmblemEffect(List.of(new DealDamageEqualToManaSpentToCastToAnyTargetEffect(CardColorPredicate(RED))), "…")` — the emblem marker deals any-target damage equal to the mana spent |
| Planeswalker with resolution-time impulse cast-or-burn, mana, creature damage, and spell-cast damage emblem | `c/ChandraTorchOfDefiance.java` | +1 `ExileTopCardMayCastOrDealDamageEffect(2)`; the engine offers the exiled nonland card for a normal-cost cast during the ability's resolution and otherwise deals 2 to each opponent. Second +1 `AwardManaEffect(RED, 2)`, −3 `DealDamageToTargetCreatureEffect(4)`, −7 `CreateEmblemEffect(List.of(new DealDamageToAnyTargetOnControllerSpellCastEffect(5)), "…")` |
| Loyalty any-target ping + delayed spell copy + multi any-target damage | `c/ChandraTheFirebrand.java` | +1 `DealDamageToAnyTargetEffect(1)` (no filter). −2 `CopyNextInstantOrSorceryCastThisTurnEffect()`. −6 `DealDamageToEachTargetEffect(new Fixed(6))` on the full `ActivatedAbility` ctor with `multiTargetFilters = List.of()`, `minTargets 0`, `maxTargets 6` — the filterless group takes the multi-target path and is narrowed to "any target" from the effects' `targetSpec()` |
| Once-per-turn spell copying + any-color mana + filtered next-turn impulse cast + X multi-target damage | `c/ChandraHopesBeacon.java` | Static `OncePerTurnTriggerEffect(CopyControllerCastSpellOnSpellCastEffect(INSTANT|SORCERY))`; +2 `AwardAnyColorManaEffect(2, true)`; +1 `ExileTopCardsMayCastMatchingUntilNextTurnEffect(5, INSTANT|SORCERY)`; −X `DealDamageToEachTargetEffect(new XValue())` on the variable-loyalty multi-target constructor with min 0 / max 2 |
| Restricted mana rock + delayed non-mana exhaust copy | `p/PitAutomaton.java` | `{T}` `AwardRestrictedManaEffect(COLORLESS, 2, new ManaRestriction.Abilities())` plus `{2}, {T}` `CopyNextExhaustAbilityThisTurnEffect()` |
| Creature sacrifice + graveyard-only mana abilities | `l/LordOfTheForsaken.java` | `{B}, Sacrifice another creature:` `SacrificePermanentCost` + `MillEffect(3, TARGET_PLAYER)`; `Pay 1 life:` `PayLifeCost(1)` + `AwardRestrictedManaEffect(COLORLESS, 1, new ManaRestriction.GraveyardSpells())` |
| Loyalty player/PW ping + dependent creature target + exile-and-copy ultimate | `c/ChandraPyromaster.java` | +1 multi-target loyalty ability (`(false,null,effects,desc,null,+1,null,null,filters,1,2)`) = `DealDamageToTargetAndUpToCreaturesThatPlayerControlsEffect(1,1,1,false,true)`; filters = `AnyTargetPredicateTargetFilter(PermanentIsPlaneswalkerPredicate, PlayerRelationPredicate(ANY))` + `PermanentPredicateTargetFilter(PermanentIsCreaturePredicate)` with `.withMultiTargetConstraint(MultiTargetConstraint.CONTROLLED_BY_FIRST_TARGET)` — the constraint is the only way to say "a creature **that player** controls". 0 `ExileTopCardMayPlayThisTurnEffect(false)`. −7 `ExileTopCardsChooseSpellAndCastCopiesEffect(10, 3)` |
| Transform DFC planeswalker + optional pump + wolf/transform + haste/power-tap emblem | `a/ArlinnKord.java` / `a/ArlinnEmbracedByTheMoon.java` | Front: +1 `BoostTargetCreatureEffect(2,2)` + `GrantKeywordEffect(VIGILANCE+HASTE, TARGET)` with minTargets 0 / maxTargets 1; 0 `CreateTokenEffect(Wolf 2/2)` + `TransformSelfEffect`. Back: +1 `BoostAllOwnCreaturesEffect(1,1)` + `GrantKeywordEffect(TRAMPLE, OWN_CREATURES)`; −1 `DealDamageToAnyTargetEffect(3)` + `TransformSelfEffect`; −6 `CreateEmblemEffect` (OWN_PERMANENTS+IsCreature haste + `{T}: SourcePower any-target`) |
| Looting creature transforming into a planeswalker + graveyard-cast permission + mill emblem | `j/JaceVrynsProdigy.java` / `j/JaceTelepathUnbound.java` | Front: `{T}` `[DrawCardEffect(1), DiscardEffect(1, CONTROLLER), ConditionalEffect(GraveyardCardThreshold(5, CardTruePredicate), ExileSelfAndReturnTransformedEffect())]` — the immediate exile-and-return sibling of Kytheon's end-of-combat one. Back: +1 `BoostTargetCreatureEffect(-2, 0, GrantDuration.UNTIL_YOUR_NEXT_TURN)` on an up-to-one multi-target loyalty ability (minTargets 0 / maxTargets 1); −3 `AllowCastTargetCardFromGraveyardThisTurnEffect(instant-or-sorcery, CONTROLLERS_GRAVEYARD, true)` (whole-turn permission, targets a graveyard card); −9 `CreateEmblemEffect(List.of(MillEffect(5, TARGET_PLAYER)), "...")` |
| Pinging creature transforming into a planeswalker on cumulative damage, with a damage-emblem back face | `c/ChandraFireOfKaladesh.java` / `c/ChandraRoaringFlame.java` | Front: `ON_CONTROLLER_CASTS_SPELL` `SpellCastTriggerEffect(CardColorPredicate(RED), [UntapPermanentsEffect(SELF)])` (mandatory, unlike Cinder Pyromancer's `MayEffect`); `{T}` `[DealDamageToTargetPlayerOrPlaneswalkerEffect(1), ConditionalEffect(SelfDealtDamageThisTurn(3), ExileSelfAndReturnTransformedEffect())]` — the ping resolves first so it counts toward the three. Back: +1 `DealDamageToTargetPlayerOrPlaneswalkerEffect(2)`; −2 `DealDamageToTargetCreatureEffect(2)`; −7 `DealDamageToPlayersEffect(6, EACH_OPPONENT)` + `CreateEmblemEffect([EmblemStepTriggerEffect(UPKEEP, [DealDamageToPlayersEffect(3, CONTROLLER)], …)], …, EmblemRecipient.EACH_PLAYER_DEALT_DAMAGE_THIS_WAY)` — fired by `StepTriggerService` (see also Ajani, Adversary of Tyrants for the END_STEP variant) |
| Creature transforming into a planeswalker on an ally death + "if you do" token, with a discard/-X reanimate/emblem back face | `l/LilianaHereticalHealer.java` / `l/LilianaDefiantNecromancer.java` | Front: `ON_ALLY_NONTOKEN_CREATURE_DIES` `ExileSelfAndReturnTransformedEffect(CreateTokenEffect.blackZombie(1))` — the token rides the transform as the effect's "if you do" payload, so a Liliana already off the battlefield makes no Zombie. Back: +2 `DiscardEffect(1, EACH_PLAYER, false)`; −X `ActivatedAbility.variableLoyaltyAbility` with `ReturnCardFromGraveyardEffect.builder().destination(BATTLEFIELD).filter(CardAllOfPredicate(creature, CardNotPredicate(CardSupertypePredicate(LEGENDARY)))).targetGraveyard(true).requiresManaValueEqualsX(true)` (X pays the loyalty AND narrows the legal graveyard targets); −8 `CreateEmblemEffect(List.of(RegisterDelayedReturnDyingCreatureUnderControlEffect(false, null, 0, null, null)), "...")` — the only emblem carrying a death trigger, scanned by `TriggerCollectionService#collectEmblemCreatureDeathTriggers` |
| Transforming creature with a death-trigger emblem | `s/SephirothFabledSoldier.java` / `s/SephirothOneWingedAngel.java` | Back face uses `CreateEmblemEffect(List.of(new EmblemCreatureDeathTriggerEffect(payload, opponentFilter)), "Whenever a creature dies, …")`; the marker is dispatched by `TriggerCollectionService` and retains the explicit opponent target filter |
| Loyalty destroy/control-steal/player-or-pw damage+discard+sacrifice | `n/NicolBolasPlaneswalker.java` | +3 DestroyTargetPermanentEffect with `PermanentPredicateTargetFilter(PermanentNotPredicate(PermanentIsCreaturePredicate))` (noncreature); -2 GainControlOfTargetEffect(PERMANENT) with creature filter; -9 DealDamageToTargetPlayerOrPlaneswalkerEffect(7) + DiscardEffect(7, TARGET_PLAYER_OR_PERMANENT_CONTROLLER) + SacrificePermanentsEffect(7, PermanentTruePredicate(), TARGET_PLAYER_OR_PERMANENT_CONTROLLER) — discard+sacrifice piggyback on the damage target |
| Loyalty dig-cast / hand exile / opponent any-target burn / mass exile | `n/NicolBolasGodPharaoh.java` | +2 `ExileTopUntilNonlandOfTargetOpponentMayCastThisTurnEffect` (OPPONENT player filter); +1 `EachOpponentExilesFromHandEffect(2)`; −4 `DealDamageToAnyTargetEffect(7)` + `AnyTargetPredicateTargetFilter(Not(ControlledBySourceController), OPPONENT)`; −12 `ExileAllPermanentsEffect(AllOf(Not(IsLand), Not(ControlledBySourceController)))` |
| Loyalty one-pass Torment punisher / destroy+draw / each-opponent burn+draw | `n/NicolBolasTheDeceiver.java` | +3 `TormentOfHailfireEffect.once(3)` (fixed 1 iteration of each-opponent three-way punisher); −3 `DestroyTargetPermanentEffect` + `DrawCardEffect(1)` with creature filter; −11 `DealDamageToPlayersEffect(7, EACH_OPPONENT)` + `DrawCardEffect(7)` |
| Loyalty keywords+conditional counter / may-sac reflexive any-target / hand Vampire | `s/SorinImperiousBloodlord.java` | +1 GrantKeywordEffect(DEATHTOUCH+LIFELINK, TARGET) + ConditionalEffect(TargetPermanentMatches(VAMPIRE), PutCounterOnTargetPermanentEffect(+1/+1)) with ControlledPermanentPredicateTargetFilter(creature); +1 MayEffect(SacrificePermanentThenEffect(VAMPIRE, SequenceEffect(DealDamageToAnyTargetEffect(3), GainLifeEffect(3)))) — sac first, then any-target chosen as reflexive trigger goes on stack; −3 MayEffect(PutCardToBattlefieldEffect(creature+VAMPIRE)) |
| Multi-target tap ability (equip mover) | `b/BrassSquire.java` | `ActivatedAbility(true, null, effects, desc, multiTargetFilters, 2, 2)` — tap to attach Equipment to creature, instant speed, uses `AttachTargetEquipmentToTargetCreatureEffect` |
| Multi-target ability with shared creature-type restriction | `s/SecretTunnel.java` | `ActivatedAbility(true, "{4}", List.of(new MakeCreatureUnblockableEffect()), desc, List.of(TargetFilters.creatureYouControl(), TargetFilters.creatureYouControl()), 2, 2).withMultiTargetConstraint(SHARE_CREATURE_TYPES)` |
| Transform DFC | `b/BloodlineKeeper.java` | Front face with `setBackFaceCard(new LordOfLineage())` + `getBackFaceClassName()` override. Activated ability with `TransformSelfEffect` + subtype count restriction (`CardSubtype.VAMPIRE, 5`). Back face is a separate Card subclass (`LordOfLineage`) |
| Activated fight + delayed death-return transform DFC | `g/GoldenGuardian.java` / `g/GoldForgeGarrison.java` | Front: `{2}` `SourceFightsTargetCreatureEffect()` + `RegisterDelayedSelfReturnTransformedEffect()` targeting another creature you control; the registration is created only if the source remains on the battlefield through resolution. Back: `{T}` `AwardAnyColorManaEffect(2)` + `{4},{T}` `CreateTokenEffect("Golem", 4, 4, null, [GOLEM], {}, {ARTIFACT})` |
| Transforming creature into a planeswalker + discard/reanimate/library-exile loyalty abilities | `n/NicolBolasTheRavager.java` / `n/NicolBolasTheArisen.java` | Front: ETB `DiscardEffect(1, EACH_OPPONENT)` + sorcery-speed `{4}{U}{B}{R}` `ExileSelfAndReturnTransformedEffect()`. Back: `+2 DrawCardEffect(2)`, `−3 DealDamageToTargetCreatureOrPlaneswalkerEffect(10)`, `−4` targeted all-graveyards `ReturnCardFromGraveyardEffect` filtered to creature/planeswalker, `−12 ExileAllButBottomCardOfTargetLibraryEffect()` |
| Creature-to-Saga DFC with an opponent-permanent target group and front-face reset | `s/Sheoldred.java` / `t/TheTrueScriptures.java` | Front: ETB `SacrificePermanentsEffect(1, nontoken creature-or-planeswalker, EACH_OPPONENT)` + sorcery-speed `{4}{B}` `ExileSelfAndReturnTransformedEffect()` gated by `OpponentGraveyardAtLeast(8)`. Back: chapter I uses `target(creature-or-planeswalker an opponent controls, 0, 99)` with `AT_MOST_ONE_PER_CONTROLLER` and `DestroyEachTargetPermanentEffect`; chapter II discards and mills three per opponent; chapter III returns all creature cards from all graveyards, then uses `ExileSelfAndReturnFrontEffect()` |
| Dies-return transform DFC (creature to Equipment) | `h/HarvestHand.java` / `s/ScroungedScythe.java` | Front: ON_DEATH `ReturnSourceTransformedFromGraveyardEffect()` — immediate transformed return under your control. Back (Equipment): STATIC `StaticBoostEffect(1, 1, EQUIPPED_CREATURE)` + `GrantKeywordEffect(MENACE, EQUIPPED_CREATURE, PermanentHasSubtypePredicate(HUMAN))` + `EquipActivatedAbility("{2}")` |
| Dies-return transform DFC attached to an opposing permanent | `v/VengefulStrangler.java` / `s/StranglingGrasp.java` | Front: targeted ON_DEATH `ReturnSourceTransformedFromGraveyardAttachedToTargetPermanentEffect()` for an opposing creature or planeswalker. Back (Aura): `ENCHANTED_PERMANENT_CONTROLLER_UPKEEP_TRIGGERED` `SequenceEffect.of(SacrificePermanentsEffect(1, Not(IsLand), TARGET_PLAYER), LoseLifeEffect(1, TARGET_PLAYER))` |
| Aura death-return transform DFC (Aura to land) | `j/JourneyToEternity.java` / `a/AtzalCaveOfEternity.java` | Front: `ON_ENCHANTED_PERMANENT_PUT_INTO_GRAVEYARD` `ReturnEnchantedCreatureAndSourceTransformedOnDeathEffect()` — returns the enchanted creature under the Aura controller's control, then returns the Aura transformed. Back: `{T}` `AwardAnyColorManaEffect()` + `{3}{B}{G},{T}` targeted creature-card `ReturnCardFromGraveyardEffect` |
| Sacrifice-return transform DFC | `b/BiolumeEgg.java` | ETB `ScryEffect(2)` + ON_DEATH `RegisterDelayedReturnSourceTransformedEffect(true, true)` (sacrifice-only, under owner's control). Back face (`BiolumeSerpent`): `SacrificeMultiplePermanentsCost(2, ISLAND)` + `MakeCreatureUnblockableEffect(true)` |
| Werewolf transform DFC | `d/DaybreakRanger.java` | Innistrad werewolf pattern: front face uses EACH_UPKEEP_TRIGGERED with `ConditionalEffect(new NoSpellsCastLastTurn(), TransformSelfEffect())`. Back face (`NightfallPredator`) uses EACH_UPKEEP_TRIGGERED with `ConditionalEffect(new TwoOrMoreSpellsCastLastTurn(), TransformSelfEffect())`. Both faces have their own activated abilities |
| Werewolf lord DFC | `i/InstigatorGang.java` | Werewolf transform + STATIC `StaticBoostEffect(power, 0, OWN_CREATURES, PermanentIsAttackingPredicate())` on both faces (front +1/+0, back +3/+0). Back face (`WildbloodPack`) also has Trample (auto-loaded from Scryfall) |
| Upkeep reveal transform DFC | `d/DelverOfSecrets.java` | Non-werewolf upkeep transform: front face uses UPKEEP_TRIGGERED with `LookAtTopCardMayRevealTypeTransformEffect(Set.of(INSTANT, SORCERY))`. Looks at top card, if matching type offers may reveal + transform. Back face (`InsectileAberration`) is vanilla (no transform back trigger) |
| Upkeep reveal and reward transform DFC | `s/SidequestCatchAFish.java` / `c/CookingCampsite.java` | Front enchantment uses UPKEEP_TRIGGERED `LookAtTopCardMayRevealMatchingToHandEffect(ARTIFACT or CREATURE, false, [CreateTokenEffect(Food), TransformSelfEffect])`; the reward effects resolve only after the matching card is actually put into hand. Back land taps for `{W}` and has a sorcery-speed `{3}, {T}, Sacrifice an artifact` ability that puts +1/+1 counters on each creature you control |
| Upkeep mill transform DFC | `a/AberrantResearcher.java` | Front face: UPKEEP_TRIGGERED `MillAndTransformIfTypesEffect(Set.of(INSTANT, SORCERY))` — mill 1, transform if milled card is I/S. Back face (`PerfectedForm`) is vanilla flying Insect Horror. Unlike Delver, mill is mandatory and there is no may-reveal |
| Tap mill + graveyard colorless transform DFC | `g/GrizzledAngler.java` / `g/GrislyAnglerfish.java` | Front: `{T}` mills 2 then `ConditionalEffect(GraveyardCardThreshold(1, CREATURE+CardIsColorlessPredicate), TransformSelfEffect)`. Back: `{6}` `MatchingCreaturesMustAttackThisTurnEffect` on opponents' creatures (Suicidal Charge predicate) |
| Enchantment-to-land transform DFC (attack trigger) | `l/LegionsLanding.java` | Front face: ON_ENTER_BATTLEFIELD with CreateTokenEffect (1/1 Vampire with lifelink). ON_ALLY_CREATURES_ATTACK with ConditionalEffect(new MinimumAttackers(3), TransformSelfEffect()). Back face (`AdantoTheFirstFort`): two ActivatedAbilities — tap for {W} (AwardManaEffect) and {2}{W},{T} for CreateTokenEffect. Pattern: enchantment that transforms into utility land when attacking with 3+ creatures |
| Enchantment-to-land transform DFC (exiled-creature threshold) | `p/ProfaneProcession.java` / `t/TombOfTheDuskRose.java` | Front: `{3}{W}{B}` `ExileTargetPermanentAndTrackWithSourceEffect` + `ConditionalEffect(new SourceExiledCardsThreshold(3), TransformSelfEffect())`; the threshold counts non-token cards exiled with the source. Back: `ManaAbilities.tapForAnyColor()` + `{2}{W}{B},{T}` `ReturnCardExiledWithSourceToBattlefieldEffect(CardTypePredicate(CREATURE), false, null)` |
| Enchantment-to-land transform DFC (end step trigger) | `g/GrowingRitesOfItlimoc.java` | Front face: ON_ENTER_BATTLEFIELD with LookAtTopCardsEffect.mayRevealOneToHandRestOnBottom(4, CardTypePredicate(CREATURE)). CONTROLLER_END_STEP_TRIGGERED with ConditionalEffect(new ControlsPermanentCount(4, PermanentIsCreaturePredicate), TransformSelfEffect()). Back face (`ItlimocCradleOfTheSun`): two mana abilities — tap for {G} and tap for {G} per creature. Pattern: enchantment that transforms into mana land at end step with 4+ creatures |
| Enchantment transform DFC (invitation counters + attack branch) | `w/WeddingAnnouncement.java` / `w/WeddingFestivity.java` | Front: CONTROLLER_END_STEP_TRIGGERED `SequenceEffect.of(PutCountersOnSelfEffect(INVITATION), ConditionalReplacementEffect(AttackedWithCreaturesThisTurn(2), Human token, DrawCardEffect(1)), ConditionalEffect(SourceCounterThreshold(3, INVITATION), TransformSelfEffect))`. Keeps counters on transform. Back: STATIC StaticBoostEffect(1, 1, OWN_CREATURES) |
| Enchantment-to-land transform DFC (land-count trigger) | `t/TwistsAndTurns.java` / `m/MycoidMaze.java` | Front: STATIC explore replacement marker, targeted ETB `ExploreEffect`, and `ON_ALLY_LAND_ENTERS_BATTLEFIELD` conditional on seven controlled lands with `TransformSelfEffect`. Back: green mana ability and `{3}{G}` top-four creature-card search to hand with the rest bottom random |
| Artifact-to-land transform DFC (counter threshold + tokens) | `t/TreasureMap.java` | Front face: ActivatedAbility({1}, tap, [ScryEffect(1), PutCounterOnSelfThenTransformIfThresholdEffect(LANDMARK, 3, false, [CreateTokenEffect.ofTreasureToken(3)])]). Back face (`TreasureCove`): two ActivatedAbilities — tap for {C} and {T}+SacrificePermanentCost(TREASURE) for DrawCardEffect. Pattern: artifact with scry+counter activated ability that transforms into utility land and creates tokens on transform |
| Artifact transform DFC (all-players life threshold) | `c/CryptolithFragment.java` / `a/AuroraOfEmrakul.java` | Front: STATIC `EntersTappedEffect` + mana ability `ActivatedAbility(tap, null, [AwardAnyColorManaEffect(), LoseLifeEffect(1, EACH_PLAYER)])` (life loss rides along inside the mana ability — it never uses the stack) + UPKEEP_TRIGGERED `ConditionalEffect(new EachPlayerLifeAtMost(10), TransformSelfEffect())`. Back: ON_ATTACK `LoseLifeEffect(3, EACH_OPPONENT)` |
| Artifact transform DFC (life threshold) | `c/ChaliceOfLife.java` | Front face: ActivatedAbility(tap, [GainLifeEffect(1), ConditionalEffect(new ControllerLifeAtLeast(30), TransformSelfEffect())]). Back face (`ChaliceOfDeath`): ActivatedAbility(tap, [LoseLifeEffect(5, TARGET_PLAYER)]). Pattern: artifact that gains life and transforms when controller has 30+ life; back face deals 5 damage via targeted tap ability |

## Sagas

| Pattern | Reference Card | Key Code |
|---------|-----------|-------|
| 3-chapter Saga with damage/life/token | `c/ChainersTorment.java` | SAGA_CHAPTER_I/II: DealDamageToPlayersEffect(2, DamageRecipient.EACH_OPPONENT) + GainLifeEffect(2). SAGA_CHAPTER_III: CreateTokenFromHalfLifeTotalAndDealDamageEffect. Lore counters auto-managed: ETB adds 1st in StackResolutionService, precombat main adds subsequent in StepTriggerService. Sacrifice SBA in StateBasedActionService when lore >= final chapter and no chapter ability on stack |
| 3-chapter Saga with mass destruction + graveyard return | `f/FallOfTheThran.java` | SAGA_CHAPTER_I: DestroyAllPermanentsEffect(PermanentIsLandPredicate). SAGA_CHAPTER_II/III: EachPlayerReturnsCardsFromGraveyardToBattlefieldEffect(2, CardTypePredicate(LAND)). Each player returns up to 2 lands; auto-returns when <= maxCount, queues graveyard choices when > maxCount |
| 3-chapter Saga with targeting chapter + board wipe + graveyard exile | `p/PhyrexianScriptures.java` | SAGA_CHAPTER_I: PutCounterOnTargetPermanentEffect(PLUS_ONE_PLUS_ONE, 1) + AddCardTypeToTargetPermanentEffect(ARTIFACT, true). Chapter I targets "up to one creature" — uses pending saga chapter target queue. SAGA_CHAPTER_II: DestroyAllPermanentsEffect(AllOf(IsCreature, Not(IsArtifact))). SAGA_CHAPTER_III: ExileGraveyardCardsEffect(GraveyardExileScope.ALL_OPPONENTS). First Saga with chapter targeting |
| 3-chapter Saga with opponent sacrifice + discard + graveyard reanimate | `t/TheEldestReborn.java` | SAGA_CHAPTER_I: SacrificePermanentsEffect(1, AnyOf(IsCreature, IsPlaneswalker), EACH_OPPONENT). SAGA_CHAPTER_II: DiscardEffect(1, EACH_OPPONENT). SAGA_CHAPTER_III: ReturnCardFromGraveyardEffect.builder().destination(BATTLEFIELD).source(ALL_GRAVEYARDS).filter(CardAnyOfPredicate(creature, planeswalker)).build() |
| 3-chapter Saga with opponent-only targeting + tap lock + mass bounce | `t/TimeOfIce.java` | SAGA_CHAPTER_I/II: TapPermanentsEffect(TapUntapScope.TARGET) + DoesntUntapEffect.targetWhileSourceOnBattlefield(). Uses `setSagaChapterTargetFilter()` with opponent-only filter. SAGA_CHAPTER_III: ReturnToHandEffect.allPermanentsMatching(AllOf(PermanentIsCreaturePredicate, PermanentIsTappedPredicate)). Per-chapter target filters via `sagaChapterTargetFilters` |
| 3-chapter Saga with one target per opponent + discard/mill + creature reanimation + front-face reset | `t/TheTrueScriptures.java` | Chapter I declares one optional multi-target group (`target(..., 0, 99)`) with `MultiTargetConstraint.AT_MOST_ONE_PER_CONTROLLER` and destroys the chosen creature or planeswalker targets. Chapter II uses `DiscardEffect(3, EACH_OPPONENT)` plus `MillEffect(3, EACH_OPPONENT)`. Chapter III returns all creature cards from all graveyards, then `ExileSelfAndReturnFrontEffect()` returns the transformed Saga as its front creature |
| 3-chapter Saga with "until your next turn" ability grant + counters + keywords | `s/SongOfFreyalise.java` | SAGA_CHAPTER_I/II: GrantActivatedAbilityEffect(mana ability, OWN_CREATURES, null, UNTIL_YOUR_NEXT_TURN). Grants tap-for-any-color ability to all creatures at resolution. SAGA_CHAPTER_III: PutCounterOnEachControlledPermanentEffect(PLUS_ONE_PLUS_ONE, 1, new PermanentIsCreaturePredicate()) + GrantKeywordEffect(VIGILANCE, TRAMPLE, INDESTRUCTIBLE, OWN_CREATURES) |
| 3-chapter Saga with greatest-power targeting + counters + keyword grant | `t/TriumphOfGerrard.java` | SAGA_CHAPTER_I/II: PutCounterOnTargetPermanentEffect.withTargetRestriction(PLUS_ONE_PLUS_ONE, 1, PermanentHasGreatestPowerAmongControlledCreaturesPredicate()). SAGA_CHAPTER_III: GrantKeywordEffect(FLYING, FIRST_STRIKE, LIFELINK, TARGET, predicate) |
| 3-chapter Saga with graveyard targeting chapters + spell copy | `t/TheMirariConjecture.java` | SAGA_CHAPTER_I: ReturnCardFromGraveyardEffect(HAND, instant). SAGA_CHAPTER_II: ReturnCardFromGraveyardEffect(HAND, sorcery). Uses its graveyard-targeting spec for graveyard target selection. SAGA_CHAPTER_III: GrantInstantSorceryCopyUntilEndOfTurnEffect |
| 3-chapter Saga with opponent permanent exile, temporary spell tax, and typed graveyard return | `e/ElspethConquersDeath.java` | SAGA_CHAPTER_I: `ExileTargetPermanentEffect` with an opponent-controlled mana-value floor and Saga target filter. SAGA_CHAPTER_II: `IncreaseSpellCostUntilNextTurnEffect` for noncreature spells with opponent scope. SAGA_CHAPTER_III: targeted `ReturnCardFromGraveyardEffect`, then `ChooseOneEffect` for `PutCounterOnReferencedPermanentEffect(RETURNED, ...)` counter selection. |
| 4-chapter Saga with optional nonland removal, draw, and dynamic opponent damage | `s/SummonBahamut.java` | SAGA_CHAPTER_I/II: DestroyTargetPermanentEffect with a nonland predicate (Saga target selection supplies the up-to-one skip). SAGA_CHAPTER_III: DrawCardEffect(2). SAGA_CHAPTER_IV: DealDamageToPlayersEffect(PermanentManaValueSum(other-than-source, CONTROLLER), EACH_OPPONENT) |
| 3-chapter Saga with token/life, batched delayed combat trigger, and outside-game top-deck choice | `t/TheRavensWarning.java` | SAGA_CHAPTER_I: CreateTokenEffect(Bird, flying) + GainLifeEffect(2). SAGA_CHAPTER_II: RegisterDelayedCombatDamageLookAtHandAndDrawEffect(flying), which fires once per player-damage step and reuses LookAtHandEffect + DrawCardEffect. SAGA_CHAPTER_III: SearchOutsideGameForCardToTopOfLibraryEffect |
| 3-chapter Saga with surveil, filtered creature-spell copy, and named combat-damage draw | `t/TheCloneSaga.java` | SAGA_CHAPTER_I: SurveilEffect(3). SAGA_CHAPTER_II: `CopyNextSpellCastThisTurnEffect(CardTypePredicate(CREATURE), Set.of(LEGENDARY))`, which creates a token copy without the legendary supertype. SAGA_CHAPTER_III: `ChooseCardNameForDelayedCreatureCombatDamageEffect(List.of(new DrawCardEffect()))` |
| 4-chapter Saga with scry, a resolution-time name choice, first chosen-name spell draw, and library peeks | `m/MedomaisProphecy.java` | SAGA_CHAPTER_I: `ScryEffect(2)`. SAGA_CHAPTER_II: `ChooseCardNameAtResolutionEffect()`. SAGA_CHAPTER_III: `ChosenNameSpellCastTriggerEffect(List.of(new DrawCardEffect(2)))`. SAGA_CHAPTER_IV: `LookAtTopCardOfEachLibraryEffect()` |
| Loyalty counters + temporary keyword pump + damage-prevention emblem | `a/AjaniSteadfast.java` | +1 `PutCounterOnTargetPermanentEffect` + `GrantKeywordEffect(FIRST_STRIKE+VIGILANCE+LIFELINK, TARGET)` with minTargets 0 / maxTargets 1; -2 creature counters plus loyalty counters for other planeswalkers; -7 `CreateEmblemEffect(List.of(new PreventAllButOneDamageToControllerAndPlaneswalkersEffect()), "…")` |
| Loyalty mana + chosen impulse draw + creature-copy token + divided creature/PW damage | `c/ChandraFlameshaper.java` | +2 `AwardManaEffect(RED, 3)` + `ExileTopCardsChooseOneMayPlayThisTurnEffect(3)`; +1 `CreateTokenCopyOfTargetPermanentEffect(true, false, true)` with a creature-you-control target filter; -4 `DealDividedDamageEffect.chosenAmongTargetCreaturesAndPlaneswalkers(8)` |
