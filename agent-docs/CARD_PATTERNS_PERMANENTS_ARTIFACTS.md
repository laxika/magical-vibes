# Card Patterns: Artifacts, Vehicles & Equipment

All paths relative to `cards/`.

| Pattern | Reference | Notes |
|---------|-----------|-------|
| Target opponent's library until instant/sorcery + free cast | `c/ChaosWand.java` | `{4}`+tap `ActivatedAbility` with `PlayerPredicateTargetFilter(OPPONENT)` + `RevealTopCardsOfTargetPlayerUntilInstantOrSorceryAndCastEffect()` — the target opponent's library is revealed from the top until an instant or sorcery is found, the controller may cast it for free, and the remaining cards are bottomed randomly |

## Artifacts

| Pattern | Reference | Notes |
|---------|-----------|-------|
| Life-gain trigger with capped pay-X draw-X | `w/WellOfLostDreams.java` | ON_CONTROLLER_GAINS_LIFE PayXManaDrawXCardsEffect — snapshot the life gained for each trigger, then choose and pay generic X at resolution to draw X cards |
| ETB/LTB controller chooses one land of each basic land type and destroys them | `s/SunderingTitan.java` | Register the same non-targeting resolution effect in `ON_ENTER_BATTLEFIELD` and `ON_SELF_LEAVES_BATTLEFIELD`; the controller chooses across all battlefields and the selected lands are destroyed simultaneously |
| Graveyard-punisher + permanent handoff | `j/JinxedRing.java` | `ON_ALLY_NONTOKEN_PERMANENT_PUT_INTO_GRAVEYARD_FROM_BATTLEFIELD` with `DealDamageToPlayersEffect(1, CONTROLLER)` + activated `SacrificeCreatureCost` and `TargetPlayerGainsControlOfSourceCreatureEffect` targeting an opponent |
| Hand-reveal imprint + matching spell-cast damage | `t/ThoughtPrison.java` | ON_ENTER_BATTLEFIELD `MayEffect(ChooseCardsFromTargetHandEffect(1, [LAND], EXILE, true))` + ON_ANY_PLAYER_CASTS_SPELL `SpellCastTriggerEffect` gated by `StackEntrySharesColorOrManaValueWithImprintedCardPredicate` and dealing 2 damage to the triggering player |
| Static coin-flip replacement artifact | `k/KrarksThumb.java` | `EffectSlot.STATIC` + `KrarksThumbEffect`; route every logical coin flip through the shared coin-flip service |
| Mana rock with a mulligan-time hand replacement | `s/SerumPowder.java` | `ManaAbilities.tapFor(ManaColor.COLORLESS)` plus `EffectSlot.MULLIGAN_ACTION` `SerumPowderEffect`; the mulligan service offers the special action before the normal mulligan |
| Imprint + matching land-tap mana bonus | `e/ExtraplanarLens.java` | ON_ENTER_BATTLEFIELD `MayEffect(ExileTargetPermanentAndImprintEffect)` on `TargetFilters.landYouControl()` + ON_ANY_PLAYER_TAPS_LAND `AddOneOfEachManaTypeProducedByLandEffect(false, true)` |
| Enters tapped + X-scaled multi-target sac ability | `r/RunedArch.java` | STATIC `EntersTappedEffect` + `{X}`,`{T}`,`SacrificeSelfCost` ability with `MakeCreatureUnblockableEffect`, a power-≤2 creature `PermanentPredicateTargetFilter`, `minTargets 0` / `maxTargets 100` and `.withXScaledTargets()` — "X target creatures with power 2 or less can't be blocked this turn"; the paid X bounds the target count |
| Hexproof-piercing static + sac pump | `g/GlaringSpotlight.java` | STATIC `IgnoreOpponentCreatureHexproofEffect` (target opponents' hexproof creatures as though they had none) + `{3}`, `SacrificeSelfCost` ability with `GrantKeywordEffect(HEXPROOF, OWN_CREATURES)` + `MakeAllCreaturesUnblockableEffect.ownCreatures()` |
| Hexproof-piercing land | `d/DetectionTower.java` | `{1}`, `{T}` `IgnoreOpponentHexproofUntilEndOfTurnEffect` floating exemption (your opponents and their hexproof creatures can be targeted as though they had none until end of turn) + `{T}: Add {C}` |
| CU + play-a-card self-sac + life ability | `j/JujuBubble.java` | UPKEEP_TRIGGERED CumulativeUpkeepEffect("{1}") + ON_CONTROLLER_CASTS_SPELL SpellCastTriggerEffect(null, [SacrificeSelfEffect]) + ON_CONTROLLER_PLAYS_LAND SacrificeSelfEffect + `{2}` GainLifeEffect(1) — "play a card" = cast or land play (not put-onto-battlefield) |
| Extra loyalty activations + end-step punisher | `t/TheChainVeil.java` | `{4},{T}` `GrantExtraLoyaltyActivationToPlaneswalkersEffect()` (each planeswalker you control gets one extra loyalty activation this turn) + CONTROLLER_END_STEP_TRIGGERED `ConditionalEffect(DidntActivateLoyaltyAbilityThisTurn(), LoseLifeEffect(2))` — intervening-if gated at trigger and resolution |
| Targeted ETB + activated ability | `c/ContagionClasp.java` | ON_ENTER_BATTLEFIELD PutCounterOnTargetPermanentEffect(MINUS_ONE_MINUS_ONE) + tap+mana ProliferateEffect activated ability |
| Lowest-mana-value upkeep destruction | `c/CullingScales.java` | `PermanentHasLowestManaValueAmongAllNonlandPermanentsPredicate` target filter + `UPKEEP_TRIGGERED` `ConditionalEffect(TargetPermanentMatches(same predicate), DestroyTargetPermanentEffect())` — choose among tied lowest-mana-value nonland permanents at trigger time and recheck the condition on resolution |
| Charge-up lock artifact (can't-attack + detain gated on counters) | `e/EdificeOfAuthority.java` | Ability 1: `{1},{T}` LockTargetPermanentEffect(true,false,false,UNTIL_END_OF_TURN) + PutCountersOnSelfEffect(BRICK) — "target creature can't attack this turn" + accrue a brick counter. Ability 2: `{1},{T}` LockTargetPermanentEffect(true,true,true,UNTIL_YOUR_NEXT_TURN) `.withRequiredSourceCounters(BRICK, 3)` — Detain, gated on three brick counters on the source. Both target a creature via the effect's `harmful(CREATURE)` targetSpec (no explicit targetFilter). |
| Spend white mana as red (mana-color permission) | `s/SunglassesOfUrza.java` | STATIC `SpendWhiteManaAsRedEffect()` — per-player permission read via `GameQueryService.canSpendWhiteManaAsRed`, honored by `ManaCost.canPay`/`pay` through the `ManaPool.isWhiteSpendableAsRed` flag |
| Cast cycling spells from GY + exile non-cycled cycling cards | `a/AbandonedSarcophagus.java` | STATIC `CastSpellsWithCyclingFromGraveyardEffect` + `ExileOwnCyclingCardsUnlessCycledEffect` — cast any number of cycling spells from GY for normal cost; cycling cards are exiled on GY entry unless they were just cycled |
| Sac creature for counter + sac self for draw | `c/CullingDais.java` | Two abilities: tap+SacrificeCreatureCost+PutCountersOnSelfEffect(CHARGE), mana+SacrificeSelfCost+DrawCardEffect(CountersOnSource(CHARGE)) |
| ETB + death draw (simple) | `i/IchorWellspring.java` | ON_ENTER_BATTLEFIELD DrawCardEffect + ON_DEATH DrawCardEffect — draws on ETB and when destroyed/sacrificed |
| Opponent picks which of your top two graveyard cards is exiled | `p/PhyrexianGrimoire.java` | `{4}` + tap `ActivatedAbility` with a `PlayerPredicateTargetFilter(OPPONENT)` + `OpponentChoosesOneOfTopTwoGraveyardCardsEffect()` — the targeted opponent chooses one of the top two cards of your graveyard, that one is exiled and the other goes to your hand (one card ⇒ exiled, nothing to hand) |
| Opponent picks which of your top three library cards is milled, then draw two | `t/ThranTome.java` | `{5}` + tap `ActivatedAbility` with `PlayerPredicateTargetFilter(OPPONENT)` + `ThranTomeRevealTopThreeOpponentChoosesEffect()` + `DrawCardEffect(2)` — the targeted opponent chooses from the revealed top cards; the shared library-search choice moves the chosen card to the controller's graveyard and leaves the rest in place |
| Sac self to restock a player's library from their graveyard + delayed draw | `l/LodestoneBauble.java` | Tap+{1}+`SacrificeSelfCost` + `RegisterDrawCardsAtNextUpkeepEffect.targetGraveyardOwner(1)` + `PutTargetCardsFromGraveyardOnTopOfLibraryEffect.fromAnyPlayerGraveyard(CardPredicateUtils.basicLand(), 4)` — "up to four target basic land cards from a player's graveyard on top of their library"; graveyard targets ride in on `Zone.GRAVEYARD` `targetIds`, and the draw is listed first so "that player" can still be read off them |
| Spellbomb (sac for effect + may-pay draw) | `f/FlightSpellbomb.java` | Tap+SacrificeSelfCost+GrantKeywordEffect(TARGET) ability + ON_DEATH MayPayManaEffect("{U}", DrawCardEffect(1)) — Spellbomb cycle pattern |
| Mana rock that doesn't untap + pay-to-untap + draw-step self-damage | `m/ManaVault.java` | STATIC `DoesntUntapEffect.self()` + `UPKEEP_TRIGGERED` `MayPayManaEffect("{4}", UntapPermanentsEffect(TapUntapScope.SELF))` + `DRAW_TRIGGERED` `ConditionalEffect(new SourceIsTapped(), DealDamageToPlayersEffect(1, DamageRecipient.CONTROLLER))` + `ON_TAP` `AwardManaEffect(COLORLESS, 3)` |
| Tap to pump a creature while tapped | `t/TawnossWeaponry.java` | STATIC `MayNotUntapDuringUntapStepEffect()` + `{2}`,`{T}` activated `BoostTargetCreatureWhileSourceTappedEffect(1, 1)` (`PermanentIsCreaturePredicate` target filter) — +1/+1 that persists for as long as the artifact stays tapped, ends when it untaps or leaves |
| Charge counter trigger + activated token | `g/GolemFoundry.java` | MayEffect(PutChargeCounterOnSelfOnArtifactCastEffect) on ON_ANY_PLAYER_CASTS_SPELL + activated RemoveCounterFromSourceCost(3, CHARGE) + CreateTokenEffect |
| Upkeep counter accrual + draw-step damage to all + any-player counter removal | `a/ArmageddonClock.java` | UPKEEP_TRIGGERED PutCountersOnSelfEffect(DOOM) + DRAW_TRIGGERED DealDamageToPlayersEffect(CountersOnSource(DOOM), EACH_PLAYER) + `{4}` RemoveCounterFromSourceEffect(DOOM, 1) ability with ONLY_DURING_ANY_UPKEEP + `.withActivatableByAnyPlayer()` — escalating damage doomsday clock any player can slow (cf. Infinite Hourglass) |
| Upkeep hand-size intervening-if damage to opponent or planeswalker | `s/ScaldingTongs.java` | UPKEEP_TRIGGERED `ConditionalEffect(new CardsInHandAtMost(3), DealDamageToTargetPlayerOrPlaneswalkerEffect(1, PlayerRelation.OPPONENT))` — no card-level target filter needed: `TriggerTargetCollector` reads the effect's `PlayerRelation.OPPONENT` through `CardEffect.targetPlayerRelation()` and offers opponents and planeswalkers only. The `PLAYER_OR_PLANESWALKER` spec cannot supply that on its own — the plain "target player or planeswalker" wording declares the same spec and there the controller is a legal choice. The intervening-if is checked at trigger time (`StepTriggerService` drops unmet any-target `ConditionalEffect`s before queueing the target choice) and again at resolution |
| Enters with fixed charge counters + tap-remove ability | `n/NecrogenCenser.java` | EnterWithCountersEffect(CHARGE, Fixed(2)) + tap+RemoveCounterFromSourceCost(1, CHARGE)+LoseLifeEffect(2, TARGET_PLAYER) (loss-only, no life gain) |
| Omen-counter transform DFC (creature cards to GY) | `s/SoulcipherBoard.java` / `c/CipherboundSpirit.java` | EnterWithCountersEffect(OMEN, Fixed(3)) + `{1}{U},{T}` LookAtTopCardsOfTargetLibraryEffect(2, PUT_ONE_INTO_GRAVEYARD) (no target = own library) + ON_ALLY_CREATURE_CARD_PUT_INTO_GRAVEYARD_FROM_ANYWHERE SequenceEffect(RemoveCounterFromSourceEffect(OMEN,1), ConditionalEffect(NotCondition(SourceCounterThreshold(1,OMEN)), TransformSelfEffect)). Back: STATIC CanBlockOnlyIfAttackerMatchesPredicateEffect(Flying) + `{3}{U}` DrawCardEffect(2)+DiscardEffect(1, CONTROLLER) |
| Counter ⇄ token shuttle (two upkeep triggers) | `t/Tetravus.java` | ON_ENTER_BATTLEFIELD EnterWithCountersEffect(PLUS_ONE_PLUS_ONE, Fixed(3)) + UPKEEP_TRIGGERED RemoveCountersToCreateTetraviteTokensEffect(1/1 colorless Tetravite artifact creature token: flying + STATIC CantBeEnchantedByOtherAurasEffect) + UPKEEP_TRIGGERED ExileTetraviteTokensToPutCountersOnSelfEffect — remove any number of +1/+1 counters to create that many Tetravite tokens (0..N number choice); exile any number of tokens created with it to put that many +1/+1 counters back. Tokens tracked in GameData.sourceCreatedTokens (token id set per source permanent id) |
| Discard predicate card cost + gain indestructible | `s/SanctumSpirit.java` | DiscardCardTypeCost(null, CardIsHistoricPredicate, "historic") + GrantKeywordEffect(INDESTRUCTIBLE, SELF) — discard a card matching a predicate as cost, no mana, no tap |
| Study counter accumulation + sacrifice for mass reanimate | `g/GrimoireOfTheDead.java` | Ability 1: tap+{1}+DiscardCardTypeCost(null,null)+PutCountersOnSelfEffect(STUDY). Ability 2: tap+RemoveCounterFromSourceCost(3,STUDY)+SacrificeSelfCost+ReturnCardFromGraveyardEffect(returnAll, ALL_GRAVEYARDS, grantColor BLACK, grantSubtype ZOMBIE) |
| Dual sacrifice abilities (damage + counter removal) | `g/GremlinMine.java` | Two abilities: tap+{1}+SacrificeSelfCost+DealDamageToTargetCreatureEffect(4) with artifact creature filter + tap+{1}+SacrificeSelfCost+RemoveCounterFromTargetPermanentEffect(CHARGE, null, 4) with noncreature artifact filter |
| Exile self cost + exile target creature | `b/BrittleEffigy.java` | tap+{4}+ExileSelfCost+ExileTargetPermanentEffect with PermanentIsCreaturePredicate filter. ExileSelfCost exiles self as cost (not sacrifice) |
| Exile self cost + each opponent keeps N graveyard cards | `w/WatchersOfTheDead.java` | no-tap/no-mana ExileSelfCost + ExileGraveyardCardsEffect(2, GraveyardExileScope.EACH_OPPONENT_KEEP) — "Exile this creature: Each opponent chooses two cards in their graveyard and exiles the rest" |
| Type-changing tap ability | `l/LiquimetalCoating.java` | Tap: AddCardTypeToTargetPermanentEffect(ARTIFACT) — target permanent becomes artifact until end of turn |
| Hand-imprint + cast a copy | `e/EliteArcanist.java`, `i/IsochronScepter.java` | ON_ENTER_BATTLEFIELD MayEffect(ExileFromHandToImprintEffect(...)) + activated tap copy effect — Elite Arcanist pays {X} equal to the imprinted card's mana value; Isochron Scepter pays {2}; both may-cast the copy for free |
| X-cost hand-imprint + upkeep cast copy | `p/PanopticMirror.java` | `{X}, {T}` ability wraps `ExileFromHandToImprintEffect.withManaValueX(instantOrSorcery, ...)` in `MayEffect`; `UPKEEP_TRIGGERED` wraps `CopyImprintedCardAndMayCastCopyEffect(false)` in a second `MayEffect` for the optional copy and optional free cast |
| Hand-imprint + X-cost token | `p/PrototypePortal.java` | ON_ENTER_BATTLEFIELD MayEffect(ExileFromHandToImprintEffect(CardTypePredicate(ARTIFACT))) + activated {X}+tap CreateTokenCopyOfImprintedCardEffect(false, false) — imprint artifact from hand on ETB, pay X (imprinted card's mana cost) to create permanent token copy |
| Tap+sac: GY exile → 1/1 flying Spirit copy + stat-matched Zombie | `s/SoulSeparator.java` | `ActivatedAbility(true, "{5}", [SacrificeSelfCost, ExileTargetCardFromGraveyardAndCreateTokenCopyEffect(CREATURE, ownGraveyardOnly, [SPIRIT], false, false, null, 1, 1, Set.of(FLYING), true)])` — one effect makes both tokens: the 1/1 flying Spirit copy and the black Zombie whose P/T are the exiled card's P/T |
| Graveyard exile + pay cost + token copy | `b/BackFromTheBrink.java` | Activated sorcery-speed ExileCardFromGraveyardCost(CREATURE, true, true) + CreateTokenCopyOfExiledCostCardEffect — exile creature from graveyard, pay its mana cost, create token copy. Dynamic mana cost from exiled card |
| Face-down hand vault, emptied on control loss | `g/GusthasScepter.java` | tap ability `ExileCardFromHandFaceDownWithSourceEffect(true)` + second tap ability `PutCardExiledWithSourceIntoHandEffect()` — stash hand cards face down under the artifact (only its controller sees them), take one back; the `true` flag makes `StateBasedActionService` bin every stashed card when the artifact changes controller or leaves the battlefield |
| Hand-exile + noted-mana bank | `i/IceCauldron.java` | `{X}`+tap ability with `withActivationCondition(NotCondition(SourceCounterThreshold(1, CHARGE)), …)` → MayEffect(ExileFromHandToImprintEffect(nonland, …, grantCastPermission=true)) + PutCountersOnSelfEffect(CHARGE) + NoteManaSpentForActivationEffect; second tap ability RemoveCounterFromSourceCost(1, CHARGE) + AddNotedManaForLastExiledCardEffect — exile a nonland card castable from exile, bank the mana spent, later re-add it earmarked for that card |
| Noted-mana filter (one mana banked) | `j/JeweledAmulet.java` | `{1}`+tap ability with `withActivationCondition(NotCondition(SourceCounterThreshold(1, CHARGE)), …)` → PutCountersOnSelfEffect(CHARGE) + NoteManaSpentForActivationEffect; second tap ability RemoveCounterFromSourceCost(1, CHARGE) + AddNotedManaEffect — unrestricted mana of the noted type |
| Hand-imprint + cost reduction | `s/SemblanceAnvil.java` | ON_ENTER_BATTLEFIELD MayEffect(ExileFromHandToImprintEffect(CardNotPredicate(CardTypePredicate(LAND)))) + STATIC ReduceOwnCastCostForSharedCardTypeWithImprintEffect(Fixed(2)) — imprint nonland card from hand on ETB, spells sharing card type with imprinted card cost {2} less |
| Graveyard ability cost reduction + tap-a-Zombie mill | `e/EmbalmersTools.java` | STATIC ReduceGraveyardCardActivatedAbilityCostEffect(CardTypePredicate(CREATURE), 1) + activated (no {T}) TapMultiplePermanentsCost(1, PermanentHasSubtypePredicate(ZOMBIE)) + MillEffect(1, TARGET_PLAYER) — creature cards' graveyard abilities (incl. Embalm/Eternalize) cost {1} less; tap a Zombie you control to mill target player 1 |
| Static + upkeep trigger | `v/VensersJournal.java` | STATIC NoMaximumHandSizeEffect + UPKEEP_TRIGGERED GainLifeEffect(CardsInHand(CONTROLLER)) — no max hand size + gain life equal to hand size each upkeep |
| Each-upkeep library comparison | `t/Timesifter.java` | EACH_UPKEEP_TRIGGERED `TimesifterEffect()` — each player exiles their top library card; greatest mana value wins an extra turn, with tied players repeating the comparison |
| Each-upkeep bounce above active player's hand size | `n/NoeticScales.java` | EACH_UPKEEP_TRIGGERED `ReturnToHandEffect.allPermanentsMatching(PermanentAllOfPredicate(PermanentIsCreaturePredicate, PermanentControlledByActivePlayerPredicate, PermanentPowerGreaterThanActivePlayerHandSizePredicate))` — returns only creatures controlled by the active player whose effective power exceeds that player's current hand size |
| No max hand size + discards to library | `l/LibraryOfLeng.java` | STATIC NoMaximumHandSizeEffect + STATIC DiscardToTopOfLibraryInsteadEffect — no max hand size; the controller's effect-caused discards go on top of their library instead of the graveyard (replacement checked in GraveyardService.discardCard) |
| All players no max hand + draw-step loot | `a/AnvilOfBogardan.java` | STATIC PlayersHaveNoMaximumHandSizeEffect + EACH_DRAW_TRIGGERED SequenceEffect(DrawCardForTargetPlayerEffect(1), DiscardEffect(1, TARGET_PLAYER)) — players have no max hand size; each draw step that player draws an extra card then discards |
| Set chosen opponent's max hand size | `c/CursedRack.java` | STATIC SetOpponentMaximumHandSizeEffect(4) — the chosen opponent's maximum hand size is set to four (chosen opponent = the controller's opponent(s), implicit like Booby Trap). Folded with reductions in timestamp order by TurnCleanupService.getMaxHandSize |
| Sacrifice creature + search by MV | `b/BirthingPod.java` | Tap+mana(Phyrexian)+SacrificeCreatureCost(true)+SearchLibraryEffect(CardTypePredicate(CREATURE), LibrarySearchDestination.BATTLEFIELD, new ManaValueBound(true, 1)) — sorcery speed, sacrifice creature to tutor creature with MV+1 to battlefield. `trackSacrificedManaValue=true` stores sacrificed MV in xValue |
| Choose color + static boost + mana bonus | `c/CagedSun.java` | ON_ENTER_BATTLEFIELD ChooseColorOnEnterEffect + STATIC BoostCreaturesOfChosenColorEffect(1,1) + ON_ANY_PLAYER_TAPS_LAND AddExtraManaOfChosenColorOnLandTapEffect — choose color on enter, creatures of chosen color get +1/+1, lands tapped for chosen color add extra mana |
| Symmetric extra land play + land-tap bounce | `s/StormCauldron.java` | STATIC EachPlayerPlaysAdditionalLandEffect + ON_ANY_PLAYER_TAPS_LAND ReturnTappedLandToHandEffect — every player gets one extra land play per turn; any land tapped for mana returns to its owner's hand (mana kept) |
| Shrine (upkeep + spell trigger charge + sac-for-mana) | `s/ShrineOfBoundlessGrowth.java` | UPKEEP_TRIGGERED PutCountersOnSelfEffect(CHARGE) + ON_CONTROLLER_CASTS_SPELL SpellCastTriggerEffect(CardColorPredicate) + tap+SacrificeSelfCost+AwardManaEffect(COLORLESS, CountersOnSource(CHARGE)) — charge counters on upkeep and colored spell cast, sacrifice for mana |
| Upkeep + spell-cast charge counters + sac damage | `s/ShrineOfBurningRage.java` | UPKEEP_TRIGGERED PutCountersOnSelfEffect(CHARGE) + ON_CONTROLLER_CASTS_SPELL SpellCastTriggerEffect(CardColorPredicate(RED)) + activated {3}+tap+SacrificeSelfCost+DealDamageToAnyTargetEffect(new CountersOnSource(CHARGE)) — counters resolve from the entry's source snapshot after the sacrifice |
| Target-player library filter ({3},{T}) | `a/AshnodsCylix.java` | `{3}`+tap ActivatedAbility with `LookAtTopCardsOfTargetLibraryEffect(3, TargetLibraryAction.KEEP_ONE_ON_TOP_EXILE_REST)` — target player looks at their top three, keeps one on top, rest exiled. Player target comes from the effect's own `targetSpec()`, no target filter needed |
| Shrine (upkeep + spell trigger charge + sac-for-library-pick) | `s/ShrineOfPiercingVision.java` | UPKEEP_TRIGGERED PutCountersOnSelfEffect(CHARGE) + ON_CONTROLLER_CASTS_SPELL SpellCastTriggerEffect(CardColorPredicate(BLUE)) + tap+SacrificeSelfCost+LookAtTopCardsEffect.chooseOneToHandRestOnBottom(new CountersOnSource(CHARGE)) — charge counters on upkeep and blue spell cast, sacrifice to look at top X and pick one to hand (X read via source snapshot after the sacrifice) |
| Upkeep random graveyard return + spell-cast self-boost | `c/CharmbreakerDevils.java` | UPKEEP_TRIGGERED ReturnCardFromGraveyardEffect.builder().destination(HAND).filter(instant/sorcery filter).returnAtRandom(true).build() + ON_CONTROLLER_CASTS_SPELL SpellCastTriggerEffect(instant/sorcery filter, BoostSelfEffect(4,0)) — return random instant/sorcery from graveyard at upkeep, +4/+0 when casting instant/sorcery |
| Multi-target player ability (exchange life) | `s/SoulConduit.java` | Activated {6}+tap ExchangeTargetPlayersLifeTotalsEffect with multi-target PlayerPredicateTargetFilter(ANY) — two target players exchange life totals |
| Upkeep may multi-player target (exchange life) | `a/AxisOfMortality.java` | UPKEEP_TRIGGERED MayEffect(ExchangeTargetPlayersLifeTotalsEffect) — at beginning of your upkeep, you may have two target players exchange life totals. Uses `UpkeepMultiPlayerTargetTrigger` queue for two-step player target selection, then MayEffect at resolution time |
| Modal ("choose one") upkeep trigger | `e/EtherwroughtPage.java` | UPKEEP_TRIGGERED `ChooseOneEffect(List.of(option, option, option))` with non-targeting single-effect modes (GainLifeEffect / SurveilEffect / LoseLifeEffect(EACH_OPPONENT)). A triggered ability has no cast, so the mode is picked as it resolves: `ChooseOneEffectHandler` (normalfx) → `ChoiceContext.ChooseModeChoice` list pick → the chosen mode's effects are spliced into the paused resolution. StepTriggerService already pushes the raw ChooseOneEffect (its `targetSpec` is NONE). Test answers the mode with `harness.handleListChoice(player, "<label>")`; a surveil mode then answers the may with `handleMayAbilityChosen` |
| Untap own artifacts on opponent's untap | `u/UnwindingClock.java` | STATIC UntapAllPermanentsYouControlDuringEachOtherPlayersStepEffect(UNTAP, PermanentIsArtifactPredicate) — filtered variant of Seedborn Muse (which uses null filter for all permanents) |
| Global untap mill trigger | `m/MesmericOrb.java` | ON_ANY_PERMANENT_BECOMES_UNTAPPED `MillEffect(1, MillRecipient.UNTAPPED_PERMANENT_CONTROLLER)` — each permanent that becomes untapped causes its controller to mill one; the trigger snapshots that controller |
| Untap-step type-restriction lock | `s/StorageMatrix.java` | STATIC StorageMatrixEffect() — while untapped, each player's untap step pauses; the active player picks artifact/creature/land and only that type untaps. Untap-step wiring in `UntapStepService`/`TurnProgressionService`; choice via `ChoiceContext.StorageMatrixUntapChoice` |
| Untap-step count-restriction lock | `s/StaticOrb.java` | STATIC StaticOrbEffect() — while untapped, players can't untap more than two permanents; the untap step pauses (only when >2 would untap) so the active player picks up to two. Untap-step wiring in `UntapStepService`/`TurnProgressionService`; choice via `MultiPermanentChoiceContext.StaticOrbUntap` |
| Untap-step count lock, filtered/always-on | `s/StoicAngel.java`, `s/Smoke.java` | STATIC `StaticOrbEffect(1, new PermanentIsCreaturePredicate(), false)` — parameterized Static Orb lock: cap 1, creatures only, applies whenever on battlefield (not only while untapped). Non-creatures untap normally (Stoic Angel and Smoke share the same `StaticOrbEffect(1, creatures, false)` config) |
| Untap-step count lock, filtered/while-untapped | `w/WinterOrb.java` | STATIC `StaticOrbEffect(1, new PermanentIsLandPredicate(), true)` — cap 1 on lands only, active only while the source artifact is untapped; non-lands untap normally |
| Untap-step count lock, filtered/unconditional | `i/ImiStatue.java` | STATIC `StaticOrbEffect(1, new PermanentIsArtifactPredicate(), false)` — "Players can't untap more than one artifact during their untap steps"; no "while untapped" clause, so the lock holds even while the Statue itself is tapped |
| Skip untap steps + simultaneous A/C/L tap flip each upkeep | `s/SandsOfTime.java` | STATIC `PlayersSkipUntapStepEffect()` (skips entire untap step incl. phasing; works while tapped) + EACH_UPKEEP_TRIGGERED `SimultaneouslyFlipControlledPermanentsTapStatesEffect(artifact\|creature\|land)` — active player's matching permanents flip tap state from a snapshot |
| Global upkeep-step skip | `e/EonHub.java` | STATIC `PlayersSkipUpkeepStepEffect()` — every player skips upkeep while any copy is on the battlefield; the turn engine advances from untap to precombat main |
| Targeted coin flip activated ability | `g/GoblinLyre.java` | SacrificeSelfCost + FlipCoinWinEffect(damage to target opponent/planeswalker = creatures you control, damage to you = creatures they control) with an `AnyTargetPredicateTargetFilter` for opponent-only |
| Coin flip activated ability | `s/SorcerersStrongbox.java` | Tap+{2} FlipCoinWinEffect(SacrificeSelfThenEffect(new DrawCardEffect(3))) — flip a coin, if you win sacrifice self and draw 3 |
| Token creation + tap-X-subtype destruction | `a/AryelKnightOfWindgrace.java` | Ability 1: tap+{2}{W} CreateTokenEffect(Knight 2/2 white vigilance). Ability 2: tap+{B}+TapMultiplePermanentsCost(new XValue(), Knight, excludeSource)+DestroyTargetPermanentEffect with PermanentPowerAtMostXPredicate target filter — tap X untapped Knights to destroy creature with power X or less |

### Activated abilities with dynamic/game-state target filters

| Pattern | Reference | Notes |
|---------|-----------|-------|
| Steal creature (power <= controlled creature count) | `b/BeguilerOfWills.java` | Tap ability + GainControlOfTargetEffect(PERMANENT) + PermanentAllOfPredicate(PermanentIsCreaturePredicate, PermanentPowerAtMostControlledCreatureCountPredicate) target filter — gain control of creature with power <= your creature count |
| Steal creature (power <= controlled subtype count) | `v/VedalkenShackles.java` | Static may-not-untap + tap+{2} ability + GainControlOfTargetEffect(WHILE_SOURCE_REMAINS_TAPPED) + PermanentPowerAtMostControlledSubtypeCountPredicate(ISLAND) target filter |
| Destroy creature (power <= X tapped permanents) | `a/AryelKnightOfWindgrace.java` | Tap+{B}+TapMultiplePermanentsCost(new XValue(), Knight)+DestroyTargetPermanentEffect + PermanentPowerAtMostXPredicate — tap X Knights to destroy creature with power X or less |
| Grant flying (toughness < source power, controlled) | `s/StoneGiant.java` | Tap ability + GrantKeywordEffect(FLYING) + ControlledPermanentPredicateTargetFilter with PermanentAllOfPredicate(PermanentIsCreaturePredicate, PermanentToughnessLessThanSourcePowerPredicate) — target controlled creature with toughness less than Stone Giant's power |
| P/T becomes function of in-combat creature's P/T | `s/SwornDefender.java` | {1} + `SetSelfPowerToughnessFromTargetCreatureEffect(-1, 1, PermanentAllOfPredicate(IsCreature, InCombatWithSource))` + the same predicate as `PermanentPredicateTargetFilter` — source's power becomes target's toughness minus 1 and toughness becomes target's power plus 1 until EOT, locked in at resolution |
| Deal toughness damage (in-combat target) | `s/SteadfastArmasaur.java` | {1}{W}+tap + DealDamageToPermanentEqualToSourceToughnessEffect + PermanentInCombatWithSourcePredicate target filter — deals toughness damage to creature blocking or blocked by it |
| Tap multiple subtype cost + steal | `c/CaptivatingVampire.java` | TapMultiplePermanentsCost(5, Vampire) + GainControlOfTargetEffect(PERMANENT, VAMPIRE) + PermanentIsCreaturePredicate target filter — tap 5 Vampires to steal creature, granting it Vampire subtype |
| Tap multiple subtype cost + token creation (morbid) | `s/SkirsdagHighPriest.java` | Tap + TapMultiplePermanentsCost(2, creature) + CreateTokenEffect + MORBID timing restriction — tap 2 other creatures to create 5/5 Demon token, only if a creature died this turn |
| ETB destroy (dealt damage this turn) | `f/FathomFleetCutthroat.java` | ON_ENTER_BATTLEFIELD DestroyTargetPermanentEffect + PermanentAllOfPredicate(PermanentIsCreaturePredicate, PermanentDealtDamageThisTurnPredicate, PermanentControlledBySourceControllerPredicate with NOT) — destroy opponent's creature that was dealt damage this turn |
| Saga (greatest power among controlled) | `t/TriumphOfGerrard.java` | Saga chapters target creature you control with greatest power — uses PermanentHasGreatestPowerAmongControlledCreaturesPredicate + ControlledPermanentPredicateTargetFilter |

## Vehicles

| Pattern | Reference | Notes |
|---------|-----------|-------|
| Vehicle with Crew + combat trigger | `w/Weatherlight.java` | CrewCost(3) + AnimatePermanentsEffect.crew() activated ability (no tap, no mana). ON_COMBAT_DAMAGE_TO_PLAYER LookAtTopCardsEffect.mayRevealOneToHandRestOnBottom(5, CardIsHistoricPredicate()). Crew cost auto-pays when all creatures must be tapped; prompts interactively otherwise |

## Equipment

| Pattern | Reference | Notes |
|---------|-----------|-------|
| Simple boost equip | `l/LeoninScimitar.java` | STATIC StaticBoostEffect(X, Y, GrantScope.EQUIPPED_CREATURE) + EquipActivatedAbility |
| Global anthem relative to equipped creature + attach restriction | `k/KondasBanner.java` | `setAttachRestriction(PermanentHasSupertypePredicate(LEGENDARY))` + STATIC `StaticBoostEffect(1, 1, ALL_CREATURES, PermanentSharesColorWithEquippedCreaturePredicate())` + `StaticBoostEffect(1, 1, ALL_CREATURES, PermanentSharesCreatureTypeWithEquippedCreaturePredicate())` + `EquipActivatedAbility("{2}", legendary, message)`. Both anthems cover the equipped creature itself (it shares its own color and types), so it gets +2/+2; the attach restriction is also a state-based check (CR 704.5n) |
| Color-count boost equip | `c/CivicSaber.java` | STATIC AttachedBoostEffect(AttachedPermanentColorCount(), Fixed(0), EQUIPPED_CREATURE) + equip — equipped creature gets +1/+0 for each of its colors |
| Boost + keywords equip | `l/LoxodonWarhammer.java` | Boost + GrantKeywordEffect(EQUIPPED_CREATURE) + equip |
| Conditional token-combo Equipment | `h/HelmOfKaldra.java` | STATIC GrantKeywordEffect(FIRST_STRIKE/TRAMPLE/HASTE, EQUIPPED_CREATURE) + `{1}` ConditionalEffect(SequenceEffect(CreateTokenEffect(legendary Avatar), AttachMatchingEquipmentToCreatedPermanentEffect(Kaldra Equipment filter))) + EquipActivatedAbility(`{2}`) |
| Boost + conditional keyword equip | `j/JoustingLance.java` | STATIC StaticBoostEffect(2, 0, EQUIPPED_CREATURE) + ConditionalEffect(new ControllerTurn(), GrantKeywordEffect(FIRST_STRIKE, EQUIPPED_CREATURE)) + equip — keyword only applies during controller's turn |
| Boost + can't block equip | `c/CopperCarapace.java` | STATIC StaticBoostEffect(X, Y, GrantScope.EQUIPPED_CREATURE) + CantBlockEffect + equip |
| ETB attach + sacrifice equip | `p/PistonSledge.java` | ON_ENTER_BATTLEFIELD AttachSourceEquipmentToTargetCreatureEffect + STATIC StaticBoostEffect(X, Y, GrantScope.EQUIPPED_CREATURE) + equip with SacrificePermanentCost(PermanentIsArtifactPredicate, "an artifact", false) (no mana). Uses ControlledPermanentPredicateTargetFilter on card for ETB targeting |
| Boost + shroud + attach-to-entering-subtype equip | `c/CloakAndDagger.java` | STATIC StaticBoostEffect(2, 0, EQUIPPED_CREATURE) + GrantKeywordEffect(SHROUD, EQUIPPED_CREATURE) + ON_ANY_OTHER_CREATURE_ENTERS_BATTLEFIELD TriggeringCardConditionalEffect(CardSubtypePredicate(ROGUE), AttachSourceEquipmentToEnteringCreatureEffect) + equip. "Whenever a Rogue creature enters, you may attach this Equipment to it" — non-targeting may-attach to the entering creature (any controller) |
| Living weapon equip | `s/Strandwalker.java` | ON_ENTER_BATTLEFIELD LivingWeaponEffect + STATIC StaticBoostEffect(X, Y, GrantScope.EQUIPPED_CREATURE) + GrantKeywordEffect(EQUIPPED_CREATURE) + equip |
| Living weapon + per-subtype boost equip | `l/Lashwrithe.java` | ON_ENTER_BATTLEFIELD LivingWeaponEffect + STATIC AttachedBoostEffect(PermanentCount(PermanentHasSubtypePredicate(SWAMP), CONTROLLER), same, GrantScope.EQUIPPED_CREATURE) + Phyrexian mana equip |
| Boost + color/subtype + death trigger equip | `n/NimDeathmantle.java` | STATIC StaticBoostEffect(X, Y, GrantScope.EQUIPPED_CREATURE) + GrantKeywordEffect(EQUIPPED_CREATURE) + GrantColorEffect(BLACK, EQUIPPED_CREATURE) + GrantSubtypeEffect(ZOMBIE, EQUIPPED_CREATURE) + ON_ANY_NONTOKEN_CREATURE_DIES MayPayManaEffect("{4}", ReturnDyingCreatureToBattlefieldEffect(true)) + equip |
| Boost + subtype-conditional reanimate + exile-on-own-death equip | `o/OathkeeperTakenosDaisho.java` | STATIC StaticBoostEffect(3, 1, EQUIPPED_CREATURE) + ON_EQUIPPED_CREATURE_DIES TriggeringCardConditionalEffect(CardSubtypePredicate(SAMURAI), ReturnDyingCreatureToBattlefieldEffect(false)) + ON_DEATH ExileEquippedCreatureEffect + EquipActivatedAbility("{2}"). The reanimate puts the Samurai back under the Equipment controller's control without reattaching; the ON_DEATH half binds the Equipment's last-known `attachedTo` at trigger time because the Equipment is already in the graveyard when it resolves |
| Boost + exile-self-to-make-a-token that returns it on death | `t/TatsumasaTheDragonsFang.java` | STATIC StaticBoostEffect(5, 5, EQUIPPED_CREATURE) + ActivatedAbility("{6}", [ExileSelfCost, CreateTokenEffect(5/5 blue Dragon Spirit with flying, tokenEffects ON_DEATH ReturnExiledCardToBattlefieldUnderOwnerControlEffect(null))]) + EquipActivatedAbility("{3}"). The return rides on the token as its own death trigger; `CreateTokenEffectHandler` binds the exiled Equipment's card id when the token is created, and it comes back under its **owner's** control |
| Keyword + graveyard-count boost equip | `r/RunechantersPike.java` | STATIC GrantKeywordEffect(FIRST_STRIKE, EQUIPPED_CREATURE) + AttachedBoostEffect(CardsInGraveyard(instant+sorcery filter, CONTROLLER), Fixed(0), EQUIPPED_CREATURE) + equip — +X/+0 where X = instants+sorceries in controller's graveyard |
| Imprint + land-name boost equip | `s/StrataScythe.java` | ON_ENTER_BATTLEFIELD SearchLibraryForCardTypeToExileAndImprintEffect(LAND) + STATIC AttachedBoostEffect(LandsMatchingImprintedName(), same, GrantScope.EQUIPPED_CREATURE) + equip — imprint land from library on ETB, equipped creature gets +1/+1 per matching land on all battlefields |
| Transforming Equipment (boost + "when equipped creature transforms, transform this") | `n/NeglectedHeirloom.java` (back face `a/AshmouthBlade.java`) | STATIC StaticBoostEffect(1, 1, EQUIPPED_CREATURE) + ON_EQUIPPED_CREATURE_TRANSFORMS TransformSelfEffect + EquipActivatedAbility("{1}"); back face = StaticBoostEffect(3, 3) + GrantKeywordEffect(FIRST_STRIKE, EQUIPPED_CREATURE) + EquipActivatedAbility("{3}"). The slot is fired by `AnimationSupport.fireEquipmentTransformTriggers` for every Equipment attached to a permanent that just transformed; the Equipment stays attached because its back face is also an Equipment |
| Boost + equipped creature death trigger equip | `s/SylvokLifestaff.java` | STATIC StaticBoostEffect(1, 0, GrantScope.EQUIPPED_CREATURE) + ON_EQUIPPED_CREATURE_DIES GainLifeEffect(3) + equip — triggers when the creature it's attached to dies |
| Boost + keyword + granted upkeep draw + death control-handoff equip | `a/AvariceAmulet.java` | STATIC StaticBoostEffect(2, 0, EQUIPPED_CREATURE) + GrantKeywordEffect(VIGILANCE, EQUIPPED_CREATURE) + GrantTriggeredAbilityEffect(UPKEEP_TRIGGERED, DrawCardEffect(1), EQUIPPED_CREATURE) + ON_EQUIPPED_CREATURE_DIES TargetPlayerGainsControlOfSourceCreatureEffect + setCastTimeTargetFilter(PlayerPredicateTargetFilter(OPPONENT)) + EquipActivatedAbility("{2}"). The draw ability is granted to the creature, so the creature's controller draws (collected in `StepTriggerService` via `GrantedUpkeepEffectSupport`); the death trigger targets an opponent through the Death pipeline and hands them the Equipment permanently |
| Boost + keyword + subtype-conditional death trigger equip | `a/AvacynsCollar.java` | STATIC StaticBoostEffect(1, 0, EQUIPPED_CREATURE) + GrantKeywordEffect(VIGILANCE, EQUIPPED_CREATURE) + ON_EQUIPPED_CREATURE_DIES TriggeringCardConditionalEffect(CardSubtypePredicate(HUMAN), CreateTokenEffect(Spirit 1/1 white flying)) + equip — triggers only if equipped creature was a Human |
| Boost + keywords + death reattach equip | `f/ForebearsBlade.java` | STATIC StaticBoostEffect(3, 0, EQUIPPED_CREATURE) + GrantKeywordEffect(VIGILANCE, EQUIPPED_CREATURE) + GrantKeywordEffect(TRAMPLE, EQUIPPED_CREATURE) + ON_EQUIPPED_CREATURE_DIES AttachSourceEquipmentToTargetCreatureEffect + setCastTimeTargetFilter(ControlledPermanentPredicateTargetFilter) + equip. Targeted death trigger: when equipped creature dies, controller chooses a creature to reattach to |
| Boost + death "put creature from hand + attach" equip | `d/Deathrender.java` | STATIC StaticBoostEffect(2, 2, EQUIPPED_CREATURE) + ON_EQUIPPED_CREATURE_DIES PutCardToBattlefieldEffect(CardTypePredicate(CREATURE), "creature", …, attachSourceEquipment=true) + equip. Declinable card choice: on resolution you may put a creature card from hand onto the battlefield and the source Equipment attaches to it |
| Granted pump + damage-splash-on-blocker equip | `k/KusariGama.java` | STATIC GrantActivatedAbilityEffect(ActivatedAbility("{2}", BoostSelfEffect(1, 0)), EQUIPPED_CREATURE) + ON_ALLY_CREATURE_DEALS_DAMAGE_TO_CREATURE EquipmentDamagesOtherDefendingCreaturesEffect + EquipActivatedAbility("{3}"). "Whenever equipped creature deals damage to a blocking creature, this Equipment deals that much damage to each other creature defending player controls" — the trigger lives on the Equipment and still fires when the equipped creature dies to the same combat damage (last-known attachment) |
| Combat-damage counters + three counter-spending modes equip | `u/UmezawasJitte.java` | ON_EQUIPPED_CREATURE_DEALS_COMBAT_DAMAGE PutCountersOnSelfEffect(CHARGE, 2) + three activated abilities sharing RemoveCounterFromSourceCost(1, CHARGE): BoostEquippedCreatureUntilEndOfTurnEffect(2, 2), BoostTargetCreatureEffect(-1, -1), or GainLifeEffect(2) + EquipActivatedAbility("{2}"). The modes are separate activated abilities because the -1/-1 mode has a distinct target requirement |
| Grant tap-damage ability + creature-death untap + subtype attach | `t/ThornbiteStaff.java` | STATIC GrantActivatedAbilityEffect(ActivatedAbility(tap+"{2}", DealDamageToAnyTargetEffect(1)), EQUIPPED_CREATURE) + ON_ANY_CREATURE_DIES UntapPermanentsEffect(TapUntapScope.ENCHANTED) + ON_ANY_OTHER_CREATURE_ENTERS_BATTLEFIELD TriggeringCardConditionalEffect(CardSubtypePredicate(SHAMAN), AttachSourceEquipmentToEnteringCreatureEffect) + EquipActivatedAbility("{4}"). Untap trigger modeled on the Equipment (fizzles if unattached), like Diviner's Wand's draw trigger |
| Grant ability + doesn't untap equip | `h/HeavyArbalest.java` | STATIC EquippedCreatureDoesntUntapEffect + GrantActivatedAbilityEffect(EQUIPPED_CREATURE) + equip |
| Boost + attack untap-lock + sacrifice-on-unattach equip | `s/StitchersGraft.java` | STATIC StaticBoostEffect(3, 3, EQUIPPED_CREATURE) + ON_ATTACK SkipNextUntapEffect(TapUntapScope.ENCHANTED) + STATIC SacrificeOnUnattachEffect + EquipActivatedAbility("{2}"). The ENCHANTED scope locks the attached creature (not the Equipment) when it attacks |
| Per-blocker trigger equip | `i/InfiltrationLens.java` | ON_BECOMES_BLOCKED MayEffect(DrawCardEffect(2)) + `TriggerMode.PER_BLOCKER` + equip. Use `addEffect(slot, effect, TriggerMode.PER_BLOCKER)` for "becomes blocked by a creature" triggers that fire once per blocker |
| Becomes-target trigger equip | `l/LivewireLash.java` | ON_BECOMES_TARGET_OF_SPELL DealDamageToAnyTargetEffect(2) + STATIC StaticBoostEffect(2, 0, GrantScope.EQUIPPED_CREATURE) + equip. Grants triggered ability to equipped creature: when it becomes the target of a spell, deal 2 damage to any target |
| Boost + animate-self-not-Equipment equip | `h/HauntedPlateMail.java` | STATIC StaticBoostEffect(4, 4, EQUIPPED_CREATURE) + `{0}` ActivatedAbility AnimatePermanentsEffect(4, 4, [SPIRIT], []) + LoseSubtypesUntilEndOfTurnEffect(EQUIPMENT) gated by `withActivationCondition(ControlsPermanentCountAtMost(0, IsCreature))` + EquipActivatedAbility("{4}"). "Activate only if you control no creatures"; while animated, equip has no effect |
| Draw trigger boost/flying + granted draw ability equip | `d/DivinersWand.java` | ON_CONTROLLER_DRAWS BoostEquippedCreatureAndGrantKeywordUntilEndOfTurnEffect(1, 1, FLYING) + STATIC GrantActivatedAbilityEffect(ActivatedAbility("{4}", DrawCardEffect(1)), EQUIPPED_CREATURE) + ON_ANY_OTHER_CREATURE_ENTERS_BATTLEFIELD TriggeringCardConditionalEffect(CardSubtypePredicate(WIZARD), AttachSourceEquipmentToEnteringCreatureEffect) + equip. "Whenever you draw a card, equipped creature gets +1/+1 and gains flying until end of turn" modeled as a draw trigger on the Equipment that boosts the attached creature (fizzles when unattached) |
| Spell-cast trigger boost equip | `l/LeeringEmblem.java` | ON_CONTROLLER_CASTS_SPELL BoostEquippedCreatureUntilEndOfTurnEffect(Fixed(2), Fixed(2)) + EquipActivatedAbility("{2}"). "Whenever you cast a spell, equipped creature gets +2/+2 until end of turn" — the boost effect placed directly on the ON_CONTROLLER_CASTS_SPELL slot (dedicated collector carries the source Equipment id; fizzles if unattached) |
| Attack/block trigger per-attacker boost + attach-to-subtype equip | `v/VeteransArmaments.java` | ON_ATTACK + ON_BLOCK BoostEquippedCreatureUntilEndOfTurnEffect(PermanentCount(PermanentIsAttackingPredicate, ANY_PLAYER), same) + ON_ANY_OTHER_CREATURE_ENTERS_BATTLEFIELD TriggeringCardConditionalEffect(CardSubtypePredicate(SOLDIER), AttachSourceEquipmentToEnteringCreatureEffect) + equip. "Equipped creature has 'Whenever this creature attacks or blocks, it gets +1/+1 until end of turn for each attacking creature'" — granted triggered ability modeled as ON_ATTACK/ON_BLOCK slots on the Equipment that boost the attached creature by the attacking-creature count (ANY_PLAYER so a blocker counts the opponent's attackers) |
| Ally creature targeted by opponent enchantment | `s/ShapersSanctuary.java` | ON_ALLY_CREATURE_BECOMES_TARGET_OF_OPPONENT_SPELL_OR_ABILITY MayEffect(DrawCardEffect(), "Draw a card?"). Global enchantment that monitors all your creatures — triggers on opponent spells and abilities |
| Boost + grant card type equip | `s/SilverskinArmor.java` | STATIC StaticBoostEffect(1, 1, GrantScope.EQUIPPED_CREATURE) + GrantCardTypeEffect(ARTIFACT, EQUIPPED_CREATURE) + equip. Makes equipped creature an artifact in addition to its other types (counts for metalcraft, artifact targeting, etc.) |
| Sword cycle (boost + protection + combat trigger) | `s/SwordOfWarAndPeace.java` | STATIC StaticBoostEffect(2, 2, GrantScope.EQUIPPED_CREATURE) + ProtectionFromColorsEffect(RED, WHITE) + ON_COMBAT_DAMAGE_TO_PLAYER DealDamageToPlayersEffect(CardsInHand(TARGET_PLAYER), DamageRecipient.TARGET_PLAYER) + GainLifeEffect(CardsInHand(CONTROLLER)) + equip. Also: `SwordOfFeastAndFamine` (discard + untap lands), `SwordOfBodyAndMind` (token + mill) |
| Subtype-conditional granted damage ability equip | `s/SorcerersWand.java` | STATIC GrantActivatedAbilityEffect(EQUIPPED_CREATURE) with ConditionalReplacementEffect(new SourceHasSubtype(subtype), baseEffect, upgradedEffect)(WIZARD, DealDamageToAnyTargetEffect(1), DealDamageToAnyTargetEffect(2)) + PermanentPredicateTargetFilter(PermanentIsPlaneswalkerPredicate) + equip. Equipped creature taps to deal 1 damage to player/planeswalker (2 if Wizard). Target filter restricts permanent targets to planeswalkers |
| Evasion + sacrifice-equip-to-deal-damage | `b/BlazingTorch.java` | STATIC CanBeBlockedOnlyByFilterEffect(PermanentNotPredicate(vampires/zombies)) + GrantActivatedAbilityEffect(EQUIPPED_CREATURE) with SacrificeSourceEquipmentCost + DealDamageToAnyTargetEffect + equip |
| Grant counter-charging + spend-counters-for-damage equip | `h/Hankyu.java` | STATIC GrantActivatedAbilityEffect(ActivatedAbility(tap, null, PutCountersOnGrantingEquipmentEffect(AIM)), EQUIPPED_CREATURE) + STATIC GrantActivatedAbilityEffect(ActivatedAbility(tap, null, [RemoveAllCountersAsCostEffect(AIM, true), DealDamageToAnyTargetEffect(new XValue())]), EQUIPPED_CREATURE) + EquipActivatedAbility("{4}"). Both counter operations act on the granting Equipment, not on the creature activating them; the damage source is the equipped creature |
| Bait-counter activation + equipped-creature untap Fish trigger | `f/FishingPole.java` | STATIC GrantActivatedAbilityEffect(ActivatedAbility(tap+"{1}", [TapGrantingEquipmentCost, PutCountersOnGrantingEquipmentEffect(BAIT)]), EQUIPPED_CREATURE) + ON_ANY_PERMANENT_BECOMES_UNTAPPED TriggeringPermanentConditionalEffect(source attachment host, ConditionalEffect.unless(SourceCounterThreshold(1, BAIT), SequenceEffect(RemoveCounterFromSourceEffect(BAIT, 1), CreateTokenEffect(Fish 1/1 blue)))) + EquipActivatedAbility("{2}"). The trigger watches the attached creature even when the Equipment is controlled by another player; the counter check is resolution-time. |
| Double combat damage dealt/received equip | `i/InquisitorsFlail.java` | STATIC DoubleEquippedCreatureCombatDamageEffect + equip. Doubles combat damage dealt by and received by equipped creature |
| Attack-trigger power damage to attacked target equip | `m/MageSlayer.java` | ON_ATTACK EquippedCreatureDealsPowerToAttackedTargetEffect() + EquipActivatedAbility("{3}"). "Whenever equipped creature attacks, it deals damage equal to its power to the player or planeswalker it's attacking." Non-targeting: the handler finds the equipped creature via the Equipment's `attachedTo` and the recipient via the creature's live `attackTarget`; equipped creature is the damage source. Fires even when the creature is blocked |
| Equipment granting shroud + combat-damage immunity | `g/GeneralsKabuto.java` | STATIC GrantKeywordEffect(SHROUD, EQUIPPED_CREATURE) + PreventAllCombatDamageToAttachedCreatureEffect + EquipActivatedAbility("{2}") — "Equipped creature has shroud. Prevent all combat damage that would be dealt to equipped creature." The creature still deals its own combat damage and still takes noncombat damage |
| Prevent combat damage to your attackers | `d/DolmenGate.java` | STATIC PreventCombatDamageToAttackingCreaturesYouControlEffect — prevents all combat damage dealt to attacking creatures you control |
| Per-land boost + dual equip cost | `b/BlackbladeReforged.java` | STATIC AttachedBoostEffect(PermanentCount(PermanentIsLandPredicate, CONTROLLER), same, EQUIPPED_CREATURE) + equip legendary creature {3} (ActivatedAbility with PermanentAllOfPredicate(creature + legendary)) + EquipActivatedAbility("{7}") |
| Combat-triggered token copy equip | `h/HelmOfTheHost.java` | BEGINNING_OF_COMBAT_TRIGGERED CreateTokenCopyOfEquippedCreatureEffect(true, true) + EquipActivatedAbility("{5}") — at beginning of combat on your turn, create a token copy of equipped creature (not legendary, gains haste) |
| Combat-triggered GY exile → 4/4 black Zombie copy | `g/GodPharaohsGift.java` | BEGINNING_OF_COMBAT_TRIGGERED ExileOwnCreatureFromGraveyardCreateZombieTokenCopyEffect — non-targeting up-to-one creature from your GY; token is 4/4 black Zombie (types replaced, not added) with haste until EOT |
| Boost + subtype combat destroy equip | `w/WoodenStake.java` | STATIC StaticBoostEffect(1, 0, EQUIPPED_CREATURE) + ON_BLOCK DestroySubtypeCombatOpponentEffect(VAMPIRE, true) + ON_BECOMES_BLOCKED DestroySubtypeCombatOpponentEffect(VAMPIRE, true) PER_BLOCKER + equip. Destroys Vampires that block or are blocked by equipped creature (can't regenerate) |
| Combat-damage transform equip (→ creature) | `e/Elbrus.java` (back face `w/WithengarUnbound.java`) | STATIC StaticBoostEffect(1, 0, EQUIPPED_CREATURE) + ON_COMBAT_DAMAGE_TO_PLAYER TransformSelfEffect (mandatory, not wrapped in MayEffect) + EquipActivatedAbility("{1}"). On transform, `resolveTransformSelf` auto-unattaches the Equipment because the back face (Withengar Unbound, a creature) is not an Equipment (CR 301.5c). Back face has ON_PLAYER_LOSES_GAME PutCountersOnSelfEffect(PLUS_ONE_PLUS_ONE, 13) — multiplayer-only, never resolves in the 2-player engine. Compare to DowsingDagger which wraps TransformSelfEffect in MayEffect (optional) |
| Pay-to-copy your activated abilities | `r/RingsOfBrighthearth.java` | ON_CONTROLLER_ACTIVATES_NONMANA_ABILITY CopyControllerActivatedAbilityTriggerEffect("{2}") — "whenever you activate a non-mana ability, you may pay {2} to copy it (may choose new targets)." Trigger built in `TriggerCollectionService.checkControllerActivatesNonManaAbilityTriggers` after the ability is on the stack; single-target copies are retargetable |
| Copy equipped creature's activated abilities | `i/IllusionistsBracers.java` | ON_CONTROLLER_ACTIVATES_NONMANA_ABILITY CopyControllerActivatedAbilityTriggerEffect(null, null, true) + EquipActivatedAbility("{3}") — free, mandatory copy of any non-mana ability of the creature this Equipment is attached to; copy is retargetable and controlled by the Equipment's controller |
| Pay-to-copy your entering nontoken creatures | `m/MinionReflector.java` | ON_ALLY_NONTOKEN_CREATURE_ENTERS_BATTLEFIELD MayPayManaEffect("{2}", CreateTokenCopyOfTargetPermanentEffect(true, false, true)) — "whenever a nontoken creature you control enters, you may pay {2} to create a token copy with haste, sacrificed at the next end step." Slot bakes the entering creature's id and skips tokens (no loop); `sacrificeAtEndStep=true` sends the token to the graveyard so its dies-triggers fire |
| Sorcery-speed copy of an opponent-chosen creature | `e/EchoChamber.java` | `{4}`, tap `ActivatedAbility` with `ActivationTimingRestriction.SORCERY_SPEED` + `OpponentChoosesCreatureTheyControlTokenCopyEffect()` — an opponent chooses a creature they control, you get a hasty token copy exiled at the next end step |
| Boost + keyword + "damaged-by-equipped-creature dies" exile-for-token equip | `u/UnscytheKillerOfKings.java` | STATIC StaticBoostEffect(3, 3, EQUIPPED_CREATURE) + GrantKeywordEffect(FIRST_STRIKE, EQUIPPED_CREATURE) + ON_DAMAGED_CREATURE_DIES MayEffect(ExileDyingCreatureCardAndCreateTokenEffect(CreateTokenEffect.blackZombie(1))) + EquipActivatedAbility("{2}"). "Whenever a creature dealt damage by equipped creature this turn dies, you may exile that card. If you do, create a 2/2 black Zombie." The ON_DAMAGED_CREATURE_DIES slot fires for the currently-equipped creature's damage — `GraveyardService.triggerDamagedCreatureDiesAbilities` also scans Equipment attached to the damaging creature (per rulings, only the currently-equipped creature is checked even if Unscythe wasn't attached when the damage was dealt). The dying card id is bound onto the wrapped effect via `DyingCreatureCardAwareEffect` (the may flow drops the stack entry's triggering-card id); the exile — and thus the token — only happens if the card is still in a graveyard (token / removed-in-response → nothing) |
| Mandatory copy of entering nontoken subtype | `n/Necroduality.java` | ON_ALLY_NONTOKEN_CREATURE_ENTERS_BATTLEFIELD TriggeringCardConditionalEffect(CardSubtypePredicate(ZOMBIE), CreateTokenCopyOfTargetPermanentEffect()) — "whenever a nontoken Zombie you control enters, create a token that's a copy of that creature." Enter collector bakes entering id as targetId; tokens skipped by the slot |
| Choose two colors + per-color spell-cast life | `t/TabletOfTheGuilds.java` | ON_ENTER_BATTLEFIELD ChooseColorOnEnterEffect(2) + ON_CONTROLLER_CASTS_SPELL GainLifeForEachChosenColorSpellCastEffect — store two distinct colors, then gain one life for each chosen color in every spell the controller casts |
| Choose one color + spell-cast life | `p/PrismRing.java` | ON_ENTER_BATTLEFIELD ChooseColorOnEnterEffect() + ON_CONTROLLER_CASTS_SPELL GainLifeForEachChosenColorSpellCastEffect — single-color variant of the Tablet shape; gain 1 life per spell of the chosen color |
