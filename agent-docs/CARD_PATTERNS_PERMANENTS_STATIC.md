# Card Patterns: Static Permanents & Auras

| Attack cost paid by returning a permanent | `f/FloodtideSerpent.java` | STATIC `CantAttackUnlessEffect(new ControlsPermanentCount(1, filter), desc)` + `CantAttackUnlessReturnToHandEffect(1, filter, desc)` — the controller returns a matching permanent to its owner's hand as attackers are declared |

| Choose a creature type as an additional cast cost; P/T equal matching creatures | `c/CallerOfTheHunt.java` | SPELL ChooseCreatureTypeCost + STATIC SetPowerToughnessToAmountEffect(PermanentCount(AllOf(IsCreature, PermanentHasSourceChosenSubtypePredicate), ANY_PLAYER), same) - the cast-time choice is stored on the entering permanent and the count updates with the battlefield |

All paths relative to `cards/`.

| Aura that grants a keyword to an enchanted Equipment's equipped creature | `r/RuneOfSustenance.java` | target(`TargetFilters.permanent()`) + ON_ENTER_BATTLEFIELD DrawCardEffect(1) + STATIC GrantKeywordEffect(LIFELINK, ENCHANTED_PERMANENT, PermanentIsCreaturePredicate) + STATIC GrantKeywordToEquippedCreatureOfEnchantedEquipmentEffect(LIFELINK) |

| Pattern | Reference | Notes |
|---------|-----------|-------|
| Pay-life trigger + counter-scaled activated destruction | `f/FontOfAgonies.java` | `ON_CONTROLLER_PAYS_LIFE PutCountersOnSelfEffect(BLOOD, EventValue())` + `{1}{B}` `RemoveCounterFromSourceCost(4, BLOOD)` + `DestroyTargetPermanentEffect()` targeting a creature — the pay-life trigger snapshots the amount actually paid |
| Global draw replacement plus end-step sacrifice-or-discard | `p/PossessedPortal.java` | STATIC `PossessedPortalDrawReplacementEffect` plus END_STEP_TRIGGERED `EachPlayerSacrificesPermanentUnlessDiscardEffect` |
| Extra-turn skip + self-exile extra-turn replacement | `u/UginsNexus.java` | STATIC `UginNexusReplacementEffect` — skips an extra turn as it would begin while the Nexus remains on the battlefield; if the Nexus would go from the battlefield to a graveyard, exiles it and queues an extra turn for its last controller |

| Aura with fading, a static boost, and destructive leave trigger | `p/ParallaxDementia.java` | target(creature) + ON_ENTER_BATTLEFIELD EnterWithCountersEffect(FADE, Fixed(1)) + UPKEEP_TRIGGERED RemoveCounterOrSacrificeSelfEffect(FADE) + STATIC StaticBoostEffect(3, 2, ENCHANTED_CREATURE) + ON_SELF_LEAVES_BATTLEFIELD DestroyEnchantedCreatureOnLeaveEffect() |

## Static permanents

| Pattern | Reference | Notes |
|---------|-----------|-------|
| Oil-counter trigger plus counter-based anthem | `i/IchorplateGolem.java` | ON_ALLY_CREATURE_ENTERS_BATTLEFIELD `EnteringCreatureHasCountersConditionalEffect(OIL, PutCountersOnEnteringCreatureEffect(OIL, 1, false, OIL))` plus STATIC `StaticBoostEffect(1, 1, ALL_OWN_CREATURES, PermanentHasCountersPredicate(OIL))` |
| Buyback cost reduction | `m/MemoryCrystal.java` | STATIC `ReduceBuybackCostEffect(2)` — the generic mana component of every player's mana buyback costs {2} less |
| Foretell cost reduction and timing permission | `c/CosmosCharger.java` | STATIC `ForetellCostReductionEffect(1, true)` — your foretell action costs {1} less and may be taken during any player's turn |
| Opponents' creatures can't get +1/+1 counters | `b/Blightbeetle.java` | STATIC GrantEffectEffect(CantHavePlusOnePlusOneCountersEffect, OPPONENT_CREATURES) — the narrow counter lock leaves other counter types unaffected |
| Subtype lord (all) | `g/GoblinKing.java` | STATIC StaticBoostEffect with PermanentHasAnySubtypePredicate filter, ALL_CREATURES scope |
| Subtype lord (own) + keyword | `k/KnightExemplar.java` | STATIC StaticBoostEffect(1, 1, Set.of(INDESTRUCTIBLE), OWN_CREATURES, PermanentHasAnySubtypePredicate) â€” +1/+1 and indestructible to other Knights you control |
| Legendary-creature count lord (own) | `h/HeroesPodium.java` | STATIC BoostLegendaryCreaturesByOtherLegendaryCreaturesEffect(1, 1) â€” each legendary creature you control gets +1/+1 for each other legendary creature you control; a legendary source creature also receives the bonus |
| Anthem (all own) | `g/GloriousAnthem.java` | STATIC StaticBoostEffect with OWN_CREATURES scope, no filter |
| Mana-retaining self boost | `o/OmnathLocusOfMana.java` | STATIC `PreventManaDrainEffect(ManaColor.GREEN)` + `BoostSelfEffect(new UnspentMana(ManaColor.GREEN), same)` |
| Dynamic anthem from creatures entering this turn + combat token | `k/Kinbinding.java` | STATIC DynamicStaticBoostEffect(CreaturesEnteredBattlefieldThisTurn(CONTROLLER), same, OWN_CREATURES) + BEGINNING_OF_COMBAT_TRIGGERED green and white Kithkin token |
| Multi-subtype lord + transform lock | `i/Immerwolf.java` | STATIC StaticBoostEffect(1, 1, OWN_CREATURES, PermanentHasAnySubtypePredicate({WOLF, WEREWOLF})) + STATIC PreventTransformEffect(PermanentAllOfPredicate(WEREWOLF, NOT HUMAN)) â€” +1/+1 to other Wolves/Werewolves you control and "Non-Human Werewolves you control can't transform" |
| Color-filtered anthem + cost lock | `a/AngelOfJubilation.java` | STATIC StaticBoostEffect(1, 1, OWN_CREATURES, PermanentNotPredicate(PermanentColorInPredicate(BLACK))) + PlayersCantPayLifeOrSacrificeCreaturesEffect â€” +1/+1 to other nonblack creatures you control, and no player may pay life or sacrifice creatures as a spell/ability cost |
| Forced-sacrifice immunity | `s/SigardaHostOfHerons.java` | STATIC OpponentEffectsCantCauseSacrificeEffect â€” "Spells and abilities your opponents control can't cause you to sacrifice permanents"; keywords (flying, hexproof) come from Scryfall so the constructor holds only the one static |
| Forced-discard and sacrifice immunity | `t/TamiyoCollectorOfTales.java` | STATIC OpponentEffectsCantCauseDiscardEffect + OpponentEffectsCantCauseSacrificeEffect â€” opponent-controlled effects cannot cause the controller to discard cards or sacrifice permanents |
| Supertype lord (own) | `a/ArvadTheCursed.java` | STATIC StaticBoostEffect(2, 2, OWN_CREATURES, PermanentHasSupertypePredicate(LEGENDARY)) â€” +2/+2 to other legendary creatures you control |
| Subtype lord (own) + damage reflection | `g/GreatbowDoyen.java` | STATIC StaticBoostEffect(1, 1, OWN_CREATURES, PermanentHasSubtypePredicate(ARCHER)) + ON_ALLY_CREATURE_DEALS_DAMAGE_TO_CREATURE ReflectAllyDamageToDamagedCreatureControllerEffect(PermanentHasSubtypePredicate(ARCHER)) â€” +1/+1 to other Archers you control, and whenever an Archer you control deals damage to a creature it also deals that much to that creature's controller |
| Color boost/debuff | `a/AscendantEvincar.java` | STATIC StaticBoostEffect with PermanentColorInPredicate / PermanentNotPredicate filter |
| Choose color + conditional white anthem + sac state trigger | `c/CallToArms.java` | ON_ENTER_BATTLEFIELD ChooseColorOnEnterEffect + STATIC ConditionalEffect(ChosenColorStrictlyMostCommonAmongOpponentNontokens, StaticBoostEffect(1,1, ALL_CREATURES, WHITE)) + STATE_TRIGGERED sacrifice when condition fails (single-opponent model) |
| Own boost + opponent debuff | `e/EleshNornGrandCenobite.java` | STATIC StaticBoostEffect(2, 2, OWN_CREATURES) + StaticBoostEffect(-2, -2, OPPONENT_CREATURES) |
| Color keyword lord | `b/BellowingTanglewurm.java` | STATIC GrantKeywordEffect with PermanentColorInPredicate filter, OWN_CREATURES scope |
| Color keyword lord (source qualifies) | `c/CorrosiveMentor.java` | STATIC GrantKeywordEffect(WITHER, ALL_OWN_CREATURES, PermanentColorInPredicate(BLACK)) â€” "Black creatures you control have wither"; ALL_OWN_CREATURES because the source is itself black and must pass the same filter |
| Keyword lord + spell trigger | `h/HandOfThePraetors.java` | STATIC StaticBoostEffect with PermanentHasKeywordPredicate(INFECT) filter, OWN_CREATURES scope + ON_CONTROLLER_CASTS_SPELL GivePoisonCountersEffect(1, PoisonRecipient.TARGET_PLAYER, CardAllOfPredicate(CREATURE, INFECT)) |
| Attachment self-buff | `c/ChampionOfTheFlame.java` | STATIC BoostSelfEffect(Scaled(AttachmentsOnSource(true, true), 2), same) â€” +2/+2 for each Aura and Equipment attached. Equipment-only: AttachmentsOnSource(false, true) (Goblin Gaveleer). Auras-only: AttachmentsOnSource(true, false) (Evershrike) |
| Aura self-buff + X graveyard reanimate-with-Aura | `e/Evershrike.java` | STATIC BoostSelfEffect(Scaled(AttachmentsOnSource(true, false), 2), same) (+2/+2 per Aura) + graveyard `{X}{W/B}{W/B}` ability: ReturnCardFromGraveyardEffect(selfâ†’BATTLEFIELD, returnAll) + PutAuraFromHandOntoSelfWithinXManaValueOrExileEffect â€” returns itself, then may attach a hand Aura with mana value â‰¤ X or is exiled |
| Tribal combat trigger (subtype counter lord) | `r/RakishHeir.java` | ON_ALLY_CREATURE_COMBAT_DAMAGE_TO_PLAYER AllyCombatDamageTriggerEffect(PermanentHasSubtypePredicate(VAMPIRE), PutCountersOnSourceEffect(1,1,1), bindSourceToDealer=true) â€” when a Vampire you control deals combat damage to a player, put +1/+1 counter on it |
| Aura combat-window counter growth/decay | `s/ShapeOfTheWiitigo.java` | target(creature) + ON_ENTER_BATTLEFIELD PutCounterOnReferencedPermanentEffect(PLUS_ONE_PLUS_ONE, 6) + UPKEEP_TRIGGERED PutOrRemoveCounterIfAttackedOrBlockedSinceLastUpkeepEffect(ATTACHED, PLUS_ONE_PLUS_ONE) — the enchanted creature gains a counter after attacking or blocking and loses one otherwise |
| Ally combat trigger scaled by damage dealt | `n/NecropolisRegent.java` | ON_ALLY_CREATURE_COMBAT_DAMAGE_TO_PLAYER AllyCombatDamageTriggerEffect(PermanentIsCreaturePredicate(), PutCountersOnSelfEffect(PLUS_ONE_PLUS_ONE, EventValue()), bindSourceToDealer=true) — "whenever a creature you control deals combat damage to a player, put that many +1/+1 counters on it". `CombatDamageService` sets the ally-trigger stack entry's `eventValue` to the damage dealt, which `EventValue` reads at resolution |
| Choose subtype + grant to own | `x/Xenograft.java` | ON_ENTER_BATTLEFIELD ChooseSubtypeOnEnterEffect + STATIC GrantChosenSubtypeToOwnCreaturesEffect |
| Choose subtype + grant to own creature Vehicles + other chosen-type lord | `l/LifecraftEngine.java` | ON_ENTER_BATTLEFIELD ChooseSubtypeOnEnterEffect + STATIC GrantChosenSubtypeToOwnCreaturesEffect.ownCreaturesMatching(PermanentHasSubtypePredicate(VEHICLE)) + STATIC BoostCreaturesOfChosenSubtypeEffect.otherOwnCreatures(1,1) |
| Choose subtype + grant to own graveyard creature cards | `a/AshesOfTheFallen.java` | ON_ENTER_BATTLEFIELD ChooseSubtypeOnEnterEffect + STATIC GrantChosenSubtypeToOwnGraveyardCreatureCardsEffect |
| Choose subtype + become that type + "other" lord | `a/AdaptiveAutomaton.java` | ON_ENTER_BATTLEFIELD ChooseSubtypeOnEnterEffect + STATIC GrantChosenSubtypeToOwnCreaturesEffect.toSelf() + STATIC BoostCreaturesOfChosenSubtypeEffect.otherOwnCreatures(1,1) â€” the source itself becomes the chosen type but is excluded from its own boost |
| Choose subtype on a **creature** + tribe-wide protection from it | `r/RidersOfGavony.java` | ON_ENTER_BATTLEFIELD ChooseSubtypeOnEnterEffect + STATIC GrantProtectionFromChosenTypeToOwnCreaturesEffect(HUMAN) â€” the as-enters creature-type choice also works for creature spells (BattlefieldEntryService defers the ETB pipeline until it is made) |
| Choose subtype + lord boost + cast draw | `v/VanquishersBanner.java` | ON_ENTER_BATTLEFIELD ChooseSubtypeOnEnterEffect + STATIC BoostCreaturesOfChosenSubtypeEffect(1,1) + ON_CONTROLLER_CASTS_SPELL ChosenSubtypeSpellCastTriggerEffect(DrawCardEffect) â€” choose type on enter, +1/+1 to own creatures of that type, draw on casting creature of that type |
| Choose subtype + charge on cast + counter-scaled lord | `d/DoorOfDestinies.java` | ON_ENTER_BATTLEFIELD ChooseSubtypeOnEnterEffect + ON_CONTROLLER_CASTS_SPELL ChosenSubtypeSpellCastTriggerEffect(PutCountersOnSelfEffect(CHARGE), false) + STATIC BoostCreaturesOfChosenSubtypeEffect(1,1,CounterType.CHARGE) â€” charge counter whenever you cast any spell of the chosen type (creatureSpellOnly=false), own creatures of that type get +1/+1 per charge counter |
| Shared-type pump | `c/CoatOfArms.java` | STATIC BoostBySharedCreatureTypeEffect |
| Choose subtype + copy matching spells as tokens | `r/ReflectionsOfLittjara.java` | ON_ENTER_BATTLEFIELD ChooseSubtypeOnEnterEffect + ON_CONTROLLER_CASTS_SPELL CopyControllerCastSpellOnSpellCastEffect(CardHasSourceChosenSubtypePredicate(false), tokenCopy=true) — copies any spell carrying the chosen creature subtype; permanent-spell copies resolve as tokens |
| Chroma anthem (own creatures) | `l/LightFromWithin.java` | STATIC BoostOwnCreaturesByManaSymbolEffect(ManaColor.WHITE, 1, 1) â€” each creature you control gets +1/+1 per white mana symbol in its own cost; hybrid/Phyrexian symbols of that color count |
| Color-count anthem (other multicolored) | `k/KnightOfNewAlara.java` | STATIC BoostOtherMulticoloredCreaturesByColorCountEffect(1, 1) â€” each other multicolored creature you control gets +1/+1 per color it has; monocolored/colorless creatures and the source itself get nothing (layer-5-aware color count) |
| Can't block | `s/SpinelessThug.java` | STATIC CantBlockEffect |
| Fliers can't block your creatures | `b/BowerPassage.java` | STATIC MatchingCreaturesCantBlockMatchingCreaturesEffect(new PermanentHasKeywordPredicate(Keyword.FLYING), new PermanentControlledBySourceControllerPredicate(), "â€¦") â€” global restriction with a controller-relative attacker predicate |
| Can't block unless (block-only) | `m/MaraudingBoneslasher.java` | STATIC CantBlockUnlessEffect(new ControlsOtherPermanentCount(1, new PermanentHasSubtypePredicate(ZOMBIE)), "you control another Zombie") â€” can't block unless you control another Zombie; attacking stays unrestricted (unlike CantAttackOrBlockUnlessEffect) |
| Can't block high-power (fixed) | `i/IronclawOrcs.java` | STATIC CantBlockCreaturesWithPowerAtLeastEffect(2) â€” can't block attackers with power â‰Ą 2 (hard restriction, no cost; resolved in GameQueryService.findBlockDenial) |
| Aura granting protection from a chosen colour, instant-speed with cleanup sacrifice | `w/WardOfLights.java` | `target(TargetFilters.creature())` + STATIC FlashCastWithCleanupSacrificeEffect + ON_ENTER_BATTLEFIELD ChooseColorOnEnterEffect + STATIC ProtectionFromChosenColorEffect(ENCHANTED_CREATURE) â€” the Mirage flash clause plus a chosen-colour protection grant that never detaches the Aura itself |
| Aura curse (-0/-1 + can't block high-power) | `i/IronclawCurse.java` | STATIC StaticBoostEffect(0, -1, ENCHANTED_CREATURE) + CantBlockCreaturesWithPowerGreaterOrEqualToOwnToughnessEffect â€” can't block attackers with power â‰Ą enchanted creature's own toughness (self-referential, resolved in GameQueryService.findBlockDenial) |
| Must attack | `b/BloodrockCyclops.java` | STATIC MustAttackEffect |
| Haste lord + your creatures must attack | `h/HellraiserGoblin.java` | STATIC GrantKeywordEffect(HASTE, ALL_OWN_CREATURES) + MustAttackEffect(ALL_OWN_CREATURES) — "creatures you control have haste and attack each combat if able", source included |
| Keyword to creatures in combat with the source | `a/AlmsBeast.java` | STATIC GrantKeywordEffect(LIFELINK, ALL_CREATURES, PermanentInCombatWithSourcePredicate) — "creatures blocking or blocked by this creature have lifelink"; ALL_CREATURES reaches both players' creatures and excludes the source, so the Beast never gains lifelink itself. The grant lapses on its own when combat state clears |
| Monstrosity activation with monstrous self-bonuses | `h/HundredHandedOne.java` | ACTIVATED `MonstrosityEffect(N)` gated by `NotCondition(SourceIsMonstrous)` + STATIC `ConditionalEffect(SourceIsMonstrous, GrantKeywordEffect(..., SELF))` and `GrantAdditionalBlockEffect` |
| Must attack unless you control a named creature | `m/MaraudingMaulhorn.java` | STATIC ConditionalEffect(NotCondition(ControlsPermanent(PermanentAllOfPredicate(PermanentIsCreaturePredicate, PermanentNamedPredicate("Advocate of the Beast")))), MustAttackEffect) |
| Conditional must attack (alongside others) | `e/EkunduCyclops.java` | STATIC MustAttackIfAnotherCreatureAttacksEffect — must attack only when another creature you control is declared as an attacker |
| Global must-block requirement + attacking-player block control | `i/InvasionPlans.java` | STATIC AllCreaturesMustBlockEachCombatEffect + AttackingPlayerChoosesBlockersEffect — every creature able to block must block, and the active player declares those blocks |
| Other subtype creatures you control must attack + combat token + scaling attack boost | `g/GoblinRabblemaster.java` | STATIC MatchingCreaturesMustAttackEffect(PermanentAllOfPredicate(IsCreature, HasSubtype(GOBLIN), PermanentControlledBySourceControllerPredicate, Not(PermanentIsSourceCardPredicate))) + BEGINNING_OF_COMBAT_TRIGGERED CreateTokenEffect(1/1 red Goblin, HASTE) + ON_ATTACK BoostSelfEffect(PermanentCount(AllOf(IsAttacking, HasSubtype(GOBLIN)), ANY_PLAYER, excludeSource=true), Fixed(0)) |
| Subtype-wide must attack + upkeep token | `g/GoblinAssault.java` | EACH_UPKEEP_TRIGGERED CreateTokenEffect(1/1 red Goblin, HASTE) + STATIC MatchingCreaturesMustAttackEffect(PermanentHasSubtypePredicate(GOBLIN)) — all Goblins (any controller) attack each combat if able |
| Evasion (blocked only by) | `e/ElvenRiders.java` | STATIC CanBeBlockedOnlyByFilterEffect |
| Evasion (lesser power can't block) | `s/ShrillHowler.java` | STATIC CantBeBlockedByCreaturesWithLessPowerEffect â€” creatures with power < this creature's power can't block it |
| Block limit | `s/StalkingTiger.java` | STATIC CanBeBlockedByAtMostNCreaturesEffect |
| Aura granting hexproof + block limit | `a/AlphaAuthority.java` | `target(TargetFilters.creature())` + STATIC GrantKeywordEffect(HEXPROOF, ENCHANTED_CREATURE) + STATIC CanBeBlockedByAtMostNCreaturesEffect(1) — the block cap in an Aura's STATIC slot applies to the enchanted creature via `GameQueryService.getMaxBlockersAllowed` |
| Aura: damaged-by-enchanted-creature die→exile | `k/KumanosBlessing.java` | `target(creature)` + STATIC `ExileCreaturesDamagedBySourceInsteadOfDyingEffect` (Flash keyword-driven). Same Frostwielder replacement; `PermanentRemovalService` matches the effect on an Aura attached to the damaging permanent |
| Block limit (team-wide) | `y/YuanShaoTheIndecisive.java` | STATIC EachControlledCreatureCanBeBlockedByAtMostNCreaturesEffect â€” each creature you control can't be blocked by more than N |
| Global combat creature limit | `d/DuelingGrounds.java` | STATIC MaximumCombatCreaturesEffect(1, 1) â€” no more than one creature can attack and no more than one distinct creature can block each combat |
| Block limit (team-wide filtered) | `c/ChallengerTroll.java` | STATIC EachControlledCreatureCanBeBlockedByAtMostNCreaturesEffect(1, PermanentPowerAtLeastPredicate(4)) — creatures you control with power 4 or greater can't be blocked by more than one creature |
| Unblockable | `p/PhantomWarrior.java` | STATIC CantBeBlockedEffect |
| Conditional unblockable | `s/ScrapdiverSerpent.java` | STATIC CantBeBlockedIfDefenderControlsMatchingPermanentEffect |
| Conditional negative evasion (defender gate) | `a/ArcticFoxes.java` | STATIC CantBeBlockedByCreaturesMatchingPredicateEffect(powerâ‰Ą2, snow land) â€” restriction only while defender controls matching permanent |
| Snow landwalk (printed, fixed type) | `l/LegionsOfLimDL.java` | STATIC CantBeBlockedIfDefenderControlsMatchingPermanentEffect(PermanentAllOf(HasSubtype(SWAMP), HasSupertype(SNOW)), true) â€” NOT a landwalk `Keyword`, which checks the subtype only; the trailing `true` flags it AS a landwalk ability so Staff of the Ages switches it off |
| Landwalk switched off board-wide | `s/StaffOfTheAges.java` | STATIC LandwalkIgnoredForBlockingEffect() â€” creatures with landwalk can be blocked as though they didn't have it (keyword landwalk, snow landwalk, and until-EOT snow landwalk grants) |
| Snow landwalk of a chosen type, until EOT + bounce | `b/BarbarianGuides.java` | `{2}{R}, {T}` ActivatedAbility(TargetFilters.creatureYouControl()) with GrantChosenSnowLandwalkToTargetEffect() + ReturnTargetPermanentToHandAtEndStepEffect() on the shared target â€” transient form of the row above, stored in `Permanent.unblockableIfDefenderControlsUntilEndOfTurn` |
| Unblockable while attacking alone | `d/DreamProwler.java` | STATIC CantBeBlockedIfAttackingAloneEffect |
| Own creatures matching a filter can't be blocked | `t/TetsukoUmezawaFugitive.java` | STATIC ControlledCreaturesMatchingCantBeBlockedEffect(PermanentAnyOfPredicate(PermanentPowerAtMostPredicate(1), PermanentToughnessAtMostPredicate(1))) — CR 613.11 rules-modifier applied after the layer system, so the set reads fully layered P/T. Do NOT use GrantEffectEffect(CantBeBlockedEffect, ALL_OWN_CREATURES, filter) for a P/T filter: a layer-6 grant's scope filter is evaluated mid-pass, before layer 7 exists |
| Attack restriction (defender controls) | `s/SeaMonster.java` | STATIC CantAttackUnlessEffect(new DefendingPlayerControlsPermanent(predicate), desc) — can't attack unless defending player controls a matching permanent |
| Opponent combat may exile from graveyard or lose attacks | `w/WebOfInertia.java` | OPPONENT_BEGINNING_OF_COMBAT_TRIGGERED `MayEffect(ActivePlayerExilesCardFromGraveyardEffect(...), ACTIVE_PLAYER)`; decline or an empty graveyard stamps a player-scoped `CreaturesCantAttackControllerUnlessPredicateEffect` until end of turn |
| Attack/block restriction (controller has more lands) | `m/MonstrousHound.java` | STATIC CantAttackUnlessEffect + CantBlockUnlessEffect(new ControllerControlsMoreLandsThanOpponent(), desc) — both restrictions require the controller to control strictly more lands than the combat opponent |
| Attack restriction (controller controls) | `d/DesperateCastaways.java` | STATIC CantAttackUnlessEffect(new ControlsPermanentCount(1, predicate), desc) — can't attack unless controller controls a matching permanent |
| Attack/block restriction (more creatures than opponent) | `m/MoggToady.java` | STATIC CantAttackUnlessEffect + CantBlockUnlessEffect, both using ControlsMoreCreaturesThanOpponent — attacks compare you to defending player; blocks compare you to attacking player |
| Attack restriction (battlefield count) | `h/HarborSerpent.java` | STATIC CantAttackUnlessEffect(new AnyPlayerControlsPermanentCount(5, predicate), desc) — can't attack unless N+ matching permanents across all battlefields |
| Attack restriction (defender poisoned) | `c/ChainedThroatseeker.java` | STATIC CantAttackUnlessEffect(new DefendingPlayerPoisoned(), desc) — can't attack unless defending player is poisoned |
| Attack tax paid by sacrifice (global) | `f/FloodedWoodlands.java` | STATIC CreaturesCantAttackUnlessSacrificeEffect(greenCreature, 1, PermanentIsLandPredicate, "a land") — matching creatures can't attack unless their controller sacrifices one land per matching attacker |
| Attackers-you shrink + damage-gated self-exile | `b/BoardedWindow.java` | STATIC StaticBoostEffect(-1, 0, ALL_CREATURES, PermanentIsAttackingSourceControllerPredicate) + END_STEP_TRIGGERED ConditionalEffect(ControllerDealtDamageThisTurn(4), ExileSelfEffect()) — "creatures attacking you get -1/-0" plus "at the beginning of each end step, if you were dealt 4 or more damage this turn, exile this artifact" |
| Attack restriction (opponent damage) | `b/BloodcrazedGoblin.java` | STATIC CantAttackUnlessEffect(new OpponentDealtDamageThisTurn(1), desc) — can't attack unless an opponent was dealt damage this turn |
| Attack/block restriction (greater-power ally) | `o/Okk.java` | STATIC CantAttackOrBlockUnlessGreaterPowerAlsoDoesEffect() — can't attack/block unless another declared attacker/blocker has strictly greater power (combat-set-dependent, validated at declaration time) |
| Attack/block restriction (N other allies) | `o/OrcishConscripts.java` | STATIC CantAttackOrBlockUnlessCountAlsoDoesEffect(2) — can't attack/block unless at least N other creatures are also declared as attackers/blockers (combat-set-dependent, validated at declaration time) |
| Global attack lock (power vs. hand size) | `e/EnsnaringBridge.java` | STATIC CreaturesWithPowerGreaterThanAmountCantAttackEffect(new CardsInHand(CountScope.CONTROLLER)) — any creature with power greater than the source controller's hand size can't attack |
| Global attack lock (attacked last controller turn) | `h/HallsOfMist.java` | STATIC CreaturesCantAttackUnlessPredicateEffect(PermanentNotPredicate(PermanentAttackedDuringControllersLastTurnPredicate)) + UPKEEP_TRIGGERED CumulativeUpkeepEffect("{1}") — creatures that attacked during their controller's last turn can't attack |
| Own-team attack lock (subtype exemption) | `e/EvilEyeOfOrmsByGore.java` | STATIC ControlledCreaturesCantAttackUnlessPredicateEffect(PermanentHasSubtypePredicate(EYE)) — non-Eye creatures you control can't attack; + CanBeBlockedOnlyByFilterEffect(PermanentHasSubtypePredicate(WALL), "Walls") for "can't be blocked except by Walls" |
| Grant unblockable to own creatures | `t/TetsukoUmezawaFugitive.java` | STATIC GrantEffectEffect(CantBeBlockedEffect, ALL_OWN_CREATURES, PermanentAnyOfPredicate(power/toughness filter)) |
| Block restriction | `c/CloudElemental.java` | STATIC CanBlockOnlyIfAttackerMatchesPredicateEffect |
| Aura granting flying + block restriction | `s/StratusWalk.java` | ON_ENTER_BATTLEFIELD DrawCardEffect(1) + STATIC GrantKeywordEffect(FLYING, ENCHANTED_CREATURE) + STATIC CanBlockOnlyIfAttackerMatchesPredicateEffect(PermanentHasKeywordPredicate(FLYING), "creatures with flying") — the restriction is picked up from the attached Aura |
| Blocks shadow without having it | `w/WallOfDiffusion.java` | STATIC CanBlockCreaturesWithShadowEffect — lifts the shadow blocking restriction one-way; carrier gains no shadow |
| Tapped creatures can block | `m/MasakoTheHumorless.java` | STATIC ControlledCreaturesCanBlockAsThoughUntappedEffect — controller-relative permission read by block legality |
| Protection from colors | `p/PaladinEnVec.java` | STATIC ProtectionFromColorsEffect |
| Protection from the colors of permanents you control | `e/EmptyShrineKannushi.java` | STATIC ProtectionFromColorsOfPermanentsYouControlEffect — state-dependent colour set, evaluated in `GameQueryService.hasProtectionFrom` |
| Aura: protection from colors of permanents you control | `p/PledgeOfLoyalty.java` | `target(TargetFilters.creature())` + STATIC `ProtectionFromColorsOfPermanentsYouControlEffect(ENCHANTED_CREATURE)` — colors are evaluated from the Aura controller's battlefield and the Aura stays attached despite its own white protection |
| Aura: enchanted creature gets +1/+1 and has protection from creatures | `s/SpiritMantle.java` | `target(TargetFilters.creature())` + STATIC StaticBoostEffect(1, 1, GrantScope.ENCHANTED_CREATURE) + STATIC GrantEffectEffect(ProtectionFromCardTypesEffect(CREATURE), GrantScope.ENCHANTED_CREATURE) â€” card-type protection granted to another permanent is read from the static bonus's grantedEffects by `GameQueryService.hasProtectionFromSourceCardTypes` |
| Aura: enchanted creatures you control get +1/+1 and return to hand instead of dying | `n/NecromancersMagemark.java` | `target(TargetFilters.creature())` + STATIC StaticBoostEffect(1, 1, GrantScope.OWN_CREATURES, PermanentIsEnchantedPredicate) + STATIC ReturnEnchantedCreaturesYouControlToHandInsteadOfDyingEffect |
| Aura entering with X counters that boost the enchanted creature | `e/EndlessScream.java` | `target(TargetFilters.creature())` + ON_ENTER_BATTLEFIELD EnterWithCountersEffect(SCREAM, new XValue()) + STATIC StaticBoostEffect(1, 0, GrantScope.ENCHANTED_CREATURE, CounterType.SCREAM) — the scaling-counter ctor multiplies the flat boost by the counters on the Aura itself (the static handler's `context.source()`), so no per-card effect is needed |
| Protection from mana value N or greater | `m/MistmeadowSkulk.java` | STATIC ProtectionFromManaValueEffect(N) |
| Grant protection from colors to greatest-MV creatures | `f/FavorOfTheMighty.java` | STATIC GrantEffectEffect(ProtectionFromColorsEffect(all 5 colors), ALL_CREATURES, PermanentHasGreatestManaValueAmongAllCreaturesPredicate) â€” granted protection is read by GameQueryService.hasProtectionFrom via the static bonus's grantedEffects |
| Prevent all damage to self | `c/ChoMannoRevolutionary.java` | STATIC PreventAllDamageEffect |
| Static prevention that any player can switch off | `g/GlitteringLion.java` | STATIC PreventAllDamageEffect + `{3}` `SuppressStaticEffectUntilEndOfTurnEffect(PreventAllDamageEffect.class)` with `.withActivatableByAnyPlayer()` |
| Wall-only targeting restriction | `w/WallOfShadows.java` | STATIC WallOnlyTargetingRestrictionEffect + PreventAllDamageToSelfFromCreaturesItBlocksEffect — the targeting marker is checked against the source spell/ability's full legal target shape, including modal mode unions |
| CU + prevent all damage to your creatures | `i/InnerSanctum.java` | UPKEEP_TRIGGERED CumulativeUpkeepEffect.life(2) + STATIC PreventDamageToCreaturesEffect.youControl(false) — `false` covers combat damage too; `true` is Mark of Asylum's noncombat-only variant. `PreventDamageToCreaturesEffect.all()` is the ungated both-players version (`b/BubbleMatrix.java`) |
| Prevent all damage to you during your turn | `p/PersonalSanctuary.java` | STATIC PreventAllDamageToControllerEffect(true) â€” turn-gated variant of Glacial Chasm's prevention; applies only while the controller is the active player |
| Aura: prevent all damage dealt by enchanted creature | `d/Defang.java` | target(TargetFilters.creature()) + STATIC PreventAllDamageDealtByEnchantedCreatureEffect â€” damage dealt to it is unaffected; use PreventAllDamageToAndByEnchantedCreatureEffect (Heart of Light) for the to-and-by version |
| Prevent X damage per source to you, X = subtype count | `b/BattletideAlchemist.java` | STATIC PreventDamageToControllerPerClericEffect â€” "you may prevent X of that damage, X = Clerics you control", per source, combat + noncombat; hooked in DamageSupport (noncombat) + CombatDamageService.accumulatePlayerDamage (combat) |
| Cumulative upkeep + scalable combat creature damage prevention | `c/CoverOfWinter.java` | UPKEEP_TRIGGERED `CumulativeUpkeepEffect("{S}")` + STATIC `PreventXDamagePerSourceToControllerAndCreaturesEffect(CountersOnSource(AGE), true, true)` + `{S}` `PutCountersOnSelfEffect(AGE)` ability — prevents age-counter damage from each creature source to the controller and their creatures, only during combat |
| Cumulative upkeep paid by discarding per age counter + death draw | `v/VexingSphinx.java` | UPKEEP_TRIGGERED `CumulativeUpkeepEffect.discardCard()` + ON_DEATH `DrawCardForEachDyingSourceCounterEffect(AGE)` — the discard payment is one card per age counter; the death trigger snapshots age counters before drawing |
| Prevent fixed damage per source to you | `u/UrzasArmor.java` | STATIC PreventFixedDamagePerSourceToControllerEffect(N) â€” "If a source would deal damage to you, prevent N of that damage", per source, combat + noncombat; hooked in DamageSupport (noncombat) + CombatDamageService.accumulatePlayerDamage (combat) |
| ETB tutor a planeswalker + prevent fixed damage per source to your planeswalkers | `d/DjeruWithEyesOpen.java` | ON_ENTER_BATTLEFIELD MayEffect(SearchLibraryEffect(CardTypePredicate(PLANESWALKER))) + STATIC PreventFixedDamageToPlaneswalkersYouControlEffect(N) â€” "If a source would deal damage to a planeswalker you control, prevent N of that damage", per source, combat + noncombat; hooked in the planeswalker branches of DamageSupport.dealCreatureDamage / resolveAnyTargetDamage + CombatDamageService.accumulatePlayerDamage. Vigilance from Scryfall |
| Can't lose game | `p/PlatinumAngel.java` | STATIC CantLoseGameEffect |
| Can't lose from life + damage as infect | `p/PhyrexianUnlife.java` | STATIC CantLoseGameFromLifeEffect + DamageDealtAsInfectBelowZeroLifeEffect |
| Can't lose from life + X counters soak damage (opponent opts in each upkeep) | `s/SoulEcho.java` | ON_ENTER_BATTLEFIELD EnterWithCountersEffect(ECHO, XValue()) + STATIC CantLoseGameFromLifeEffect + `target(PlayerPredicateTargetFilter(PlayerRelationPredicate(OPPONENT)))` UPKEEP_TRIGGERED SoulEchoUpkeepEffect — sacrifices itself when out of echo counters, otherwise offers the targeted opponent the "each 1 damage removes an echo counter instead" replacement until the controller's next upkeep |
| Excess lands/artifacts/creatures of target player phase out | `e/Equipoise.java` | `target(PlayerPredicateTargetFilter(ANY))` UPKEEP_TRIGGERED EquipoiseEffect — controller chooses excess permanents per type (lands → artifacts → creatures) of the targeted player to phase out; can target self |
| Your attackers phase out after combat | `t/TeferisVeil.java` | ON_ALLY_CREATURE_ATTACKS PhaseOutAttackingCreatureAtEndOfCombatEffect — one trigger per attacking creature you control; the phasing is delayed to end of combat so the attacker deals its damage first |
| Each upkeep choose type; nontoken permanents of that type phase out | `t/TeferisRealm.java` | EACH_UPKEEP_TRIGGERED PhaseOutChosenTypeNontokenPermanentsEffect — active player picks artifact/creature/land/non-Aura enchantment; all controllers' nontoken matches phase out. World enchantment (world rule = SBA) |
| Replace own loss with game reset | `l/LichsMirror.java` | STATIC ReplaceControllerLossWithGameResetEffect — "If you would lose, instead shuffle hand/graveyard/owned permanents into library, draw 7, life becomes 20." Implemented by `GameResetLossReplacer` (a `LossReplacer` bean), reached from the single gate `GameOutcomeService.resolveLoss(gameData, playerId, LossReason)` that every rules-loss source calls. Poison is NOT reset (one-shot: the Mirror shuffles itself away). Does NOT apply to an opponent's "wins the game" effect. |
| Replace own loss with exile and starting life | `e/ExquisiteArchangel.java` | STATIC ReplaceControllerLossWithExileAndStartingLifeEffect — "If you would lose, instead exile this creature and your life total becomes equal to your starting life total." Implemented by `ExquisiteArchangelLossReplacer` through the same centralized loss gate; the source is exiled, so the replacement is one-shot. |
| Damage can't reduce life below 1 (if control creature) | `w/Worship.java` | STATIC DamageLifeFloorEffect(1, LifeFloorCondition.CONTROLS_A_CREATURE) |
| ETB set life to 7 if below + damage can't reduce life below 7 | `e/ElderscaleWurm.java` | ON_ENTER_BATTLEFIELD ConditionalEffect(ControllerLifeAtMost(6), SetLifeTotalEffect(7)) + STATIC DamageLifeFloorEffect(7, LifeFloorCondition.LIFE_AT_LEAST_FLOOR) |
| Can't lose + life gain draw + life loss exile + LTB lose | `l/LichsMastery.java` | STATIC CantLoseGameEffect + GrantKeywordEffect(HEXPROOF, SELF), ON_CONTROLLER_GAINS_LIFE DrawCardsEqualToLifeGainedEffect, ON_CONTROLLER_LOSES_LIFE ExileForEachLifeLostEffect, ON_SELF_LEAVES_BATTLEFIELD ControllerLosesGameOnLeavesEffect |
| Life-loss sacrifice/discard per point + BB draw | `o/OathOfLimDL.java` | ON_CONTROLLER_LOSES_LIFE SacrificeOtherPermanentUnlessDiscardForEachLifeLostEffect + ActivatedAbility `{B}{B}` DrawCardEffect |
| Controller shroud | `t/TrueBeliever.java` | STATIC GrantControllerKeywordEffect(Keyword.SHROUD) |
| All creatures untargetable by spells | `d/DenseFoliage.java` | STATIC GrantEffectEffect(TargetingRestrictionEffect.spells(), ALL_CREATURES) — "Creatures can't be the targets of spells"; abilities can still target. Checked by `GameQueryService.cantBeTargetedByAnySpell` in the spell-target paths only |
| Graveyard cards untargetable + ETB draw | `g/GroundSeal.java` | ON_ENTER_BATTLEFIELD DrawCardEffect(1) + STATIC GraveyardCardsCantBeTargetedEffect — "Cards in graveyards can't be the targets of spells or abilities". Checked by `GameQueryService.canGraveyardCardsBeTargeted` in the target-validation, valid-target-computation and graveyard-prompt paths; non-targeting graveyard effects still work |
| Shroud to other enchantments + your enchanted creatures | `g/GreaterAuramancy.java` | STATIC GrantKeywordEffect(SHROUD, OWN_PERMANENTS, PermanentIsEnchantmentPredicate) + GrantKeywordEffect(SHROUD, OWN_CREATURES, PermanentIsEnchantedPredicate) — "other enchantments you control" via OWN_PERMANENTS (source excluded from static computation), "enchanted creatures you control" via new PermanentIsEnchantedPredicate (has an Aura attached) |
| Tap to give another creature you control shroud while tapped | `h/HisokasGuard.java` | STATIC `MayNotUntapDuringUntapStepEffect()` + `{1}{U}`,`{T}` activated `BoostTargetCreatureWhileSourceTappedEffect(0, 0, Set.of(Keyword.SHROUD))` — keyword-only use of the Tawnos's Weaponry effect; "other than this creature" via `ControlledPermanentPredicateTargetFilter(AllOf(IsCreature, Not(IsSourceCard)), …)` |
| Shroud to all noncreature artifacts while untapped | `s/SpectralGuardian.java` | STATIC ConditionalEffect(SourceUntapped, GrantKeywordEffect(SHROUD, ALL_PERMANENTS, AllOf(IsArtifact, Not(IsCreature)))) — global grant across every battlefield; `GrantScope.ALL_PERMANENTS` on `GrantKeywordEffect` matches the filter only (no creature requirement, source not auto-excluded), and the untapped gate is re-evaluated each static pass |
| Can't cast type | `s/SteelGolem.java` | STATIC CantCastSpellTypeEffect |
| ETB return opponents' blue creatures + blue creature-spell lock | `l/LlawanCephalidEmpress.java` | ON_ENTER_BATTLEFIELD ReturnAllPermanentsOfColorToHandEffect(BLUE, creature, opponentsOnly=true) + STATIC OpponentsCantCastSpellsMatchingPredicateEffect(blue + creature) |
| Can't cast noncreature MV/X spells | `g/GaddockTeeg.java` | STATIC NoncreatureSpellsCantBeCastEffect(4, true) — add `false` as the third argument for a controller-only lock (Nullhide Ferox) |
| Opponents can't cast MV â‰¤ N | `b/BriselaVoiceOfNightmares.java` | STATIC OpponentsCantCastSpellsWithManaValueAtMostEffect(3) |
| Opponents locked out during your turn | `g/GrandAbolisher.java` | STATIC OpponentsCantCastOrActivateDuringYourTurnEffect |
| Cast/activate only on own turns | `c/CityOfSolitude.java` | STATIC PlayersCanCastAndActivateOnlyDuringOwnTurnEffect — symmetric; mana abilities included |
| No instants / non-mana abilities in combat | `h/HandToHand.java` | STATIC PlayersCantCastInstantsOrActivateNonManaAbilitiesDuringCombatEffect — symmetric; only during combat steps |
| Aura: enchanted creature's controller can't cast creature spells | `b/BrandOfIllOmen.java` | STATIC EnchantedPermanentControllerCantCastSpellTypeEffect(Set.of(CREATURE)) + UPKEEP_TRIGGERED CumulativeUpkeepEffect("{R}") |
| Opponents with more permanents of a type can't cast/play it | `w/WardOfBones.java` | STATIC WardOfBonesEffect |
| Leader restriction with sacrifice-to-ignore action | `d/DampingEngine.java` | STATIC DampingEngineEffect + once-per-turn `SacrificePermanentCost` ability with `IgnoreSourceDampingEngineEffectUntilEndOfTurnEffect` |
| No life gain + each-upkeep drain + punish targeting | `f/ForsakenWastes.java` | STATIC PlayersCantGainLifeEffect + EACH_UPKEEP_TRIGGERED LoseLifeEffect(1, TARGET_PLAYER) + ON_BECOMES_TARGET_OF_SPELL TargetSpellControllerLosesLifeEffect(5) — World enchantment, world rule handled by the SBA (nothing on the card) |
| Each upkeep: that player may sac a creature or lose 5 and you sac this | `p/PillarTombsOfAku.java` | EACH_UPKEEP_TRIGGERED ForcedCostOrElseEffect.enchantedControllerMayPay(SacrificePermanentCost(IsCreature), [LoseLifeEffect(5, TARGET_PLAYER), SacrificeSelfEffect]) — active player is prompted; World rule = SBA |
| Each upkeep: exile random from hand, may play this turn; unplayed → GY at next end step | `e/ElkinLair.java` | EACH_UPKEEP_TRIGGERED ExileRandomCardFromHandMayPlayThisTurnEffect — World enchantment; world rule = SBA |
| Two players name cards; those spells/lands are locked | `n/NullChamber.java` | ON_ENTER_BATTLEFIELD YouAndOpponentChooseCardNamesOnEnterEffect + STATIC SpellsAndLandsWithChosenNamesCantBePlayedEffect — World enchantment (world rule = CR 704.5k SBA); symmetric lock on casting AND land plays |
| Unique-name enforcer (ETB wipe + enter trigger) | `e/EyeOfSingularity.java` | ON_ENTER_BATTLEFIELD DestroyAllPermanentsEffect(Not(basicLand)+SharesNameWithAnother, cantRegen) + ON_ANY_PERMANENT_ENTERS_BATTLEFIELD TriggeringCardConditionalEffect(Not(basicLandCard), DestroyOtherPermanentsWithEnteringNameEffect) — World enchantment; world rule = SBA |
| Entering-creature shared-color wipe | `s/SpreadingPlague.java` | ON_ANY_PERMANENT_ENTERS_BATTLEFIELD TriggeringCardConditionalEffect(CardTypePredicate(CREATURE), DestroyOtherCreaturesSharingColorWithEnteringCreatureEffect) — destroys other creatures sharing any color with the entering creature; the entering creature is excluded and regeneration is disallowed |
| Limit spells (all players) | `r/RuleOfLaw.java`, `a/ArcaneLaboratory.java` | STATIC LimitSpellsPerTurnEffect(1, EACH_PLAYER) |
| Limit noncreature spells (all players) | `d/DeafeningSilence.java` | STATIC LimitSpellsPerTurnEffect(1, EACH_PLAYER, CardNotPredicate(CardTypePredicate(CREATURE))) |
| Limit spells (enchanted player) | `c/CurseOfExhaustion.java` | STATIC LimitSpellsPerTurnEffect(1, ENCHANTED_PLAYER) — Curse Aura on a player |
| Tax attackers (player only) | `g/GhostlyPrison.java`, `k/KoskunFalls.java`, `w/WindbornMuse.java` | STATIC `RequirePaymentToAttackEffect.playerOnly` — uniform per-attacker tax from defender side |
| CU + color hard-deny + attack tax | `e/ElephantGrass.java` | UPKEEP_TRIGGERED CumulativeUpkeepEffect("{1}") + STATIC CreaturesCantAttackControllerUnlessPredicateEffect(Not(ColorIn(BLACK))) + STATIC RequirePaymentToAttackEffect.playerOnly(2) — black can't attack you; nonblack pay {2} |
| Tax the enchanted attacker (Aura) | `b/Brainwash.java` | STATIC EnchantedCreatureCombatTaxEffect(3, CombatTaxKind.ATTACK) — enchanted creature can't attack unless its controller pays {3}; per-creature, not uniform |
| Tax blocks of the enchanted creature (Aura) | `a/AwesomePresence.java` | STATIC EnchantedCreatureCombatTaxEffect(3, CombatTaxKind.BE_BLOCKED_BY) — enchanted creature can't be blocked unless the defending player pays {3} for each creature blocking it; charged per declared block in `CombatBlockService` |
| Tax every combat action of the enchanted creature (Aura) | `o/OppressiveRays.java` | STATIC EnchantedCreatureCombatTaxEffect(3, CombatTaxKind.ATTACK) + EnchantedCreatureCombatTaxEffect(3, CombatTaxKind.BLOCK_WITH) + IncreaseActivatedAbilityCostEffect(PermanentIsHostOfSourceAuraPredicate, 3) — one kind per taxed action, each summed independently; `BLOCK_WITH` charges the creature's own controller, unlike `BE_BLOCKED_BY` |
| Activated abilities of creatures cost {1} less | `h/Heartstone.java` | STATIC ReduceActivatedAbilityCostEffect(PermanentIsCreaturePredicate, 1) — symmetric reduction for every creature permanent; colored requirements remain, and a one-mana activation remains one mana |
| Other permanents' Exhaust abilities cost {2} less | `b/BoomScholar.java` | STATIC ReduceExhaustAbilityCostEffect(2) — controller-scoped reduction for other controlled permanents only; `.withExhaust()` marks the affected abilities |
| Tax attackers + tax all blockers, both conditional | `a/ArchangelOfTithes.java` | STATIC RequirePaymentToAttackEffect(1, new SourceUntapped()) + STATIC RequirePaymentToBlockEffect(1, new SourceIsAttacking()) — attack tax live only while the source is untapped (planeswalkers protected), board-wide block tax live only while it attacks |
| Tax attackers, scaling | `s/SphereOfSafety.java` | STATIC RequirePaymentToAttackEffect with a `PermanentCount` DynamicAmount (enchantments you control) |
| Tax opponent spells | `a/AuraOfSilence.java` | STATIC IncreaseSpellCostEffect(CardAnyOfPredicate(CardTypePredicate(ARTIFACT), CardTypePredicate(ENCHANTMENT)), 2, OPPONENT) |
| Tax opponents during your turn | `t/TitheTaker.java` | STATIC `ConditionalEffect(ControllerTurn, IncreaseSpellCostEffect(CardTruePredicate, 1, OPPONENT))` + `ConditionalEffect(ControllerTurn, IncreaseActivatedAbilityCostEffect.opponentNonMana(PermanentTruePredicate, 1))` — the second modifier excludes mana abilities |
| Tax matching spells (all players) | `t/ThaliaGuardianOfThraben.java` | STATIC IncreaseSpellCostEffect(CardNotPredicate(CardTypePredicate(CREATURE)), 1, ALL) — symmetric, affects all players |
| Enters tapped | `r/RootMaze.java` | STATIC EnterPermanentsOfTypesTappedEffect |
| Controlled lands enter untapped | `s/Spelunking.java` | STATIC ControlledLandsEnterUntappedEffect |
| Opponent creatures enter tapped + haste lord | `u/UrabraskTheHidden.java` | STATIC GrantKeywordEffect(HASTE, OWN_CREATURES) + EnterPermanentsOfTypesTappedEffect(CREATURE, opponentsOnly=true) |
| Opponent creatures + nonbasic lands enter tapped | `t/ThaliaHereticCathar.java` | STATIC `EnterPermanentsOfTypesTappedEffect.matching(creature OR (land AND NOT basic), opponentsOnly=true)` |
| P/T = lands | `m/MolimoMaroSorcerer.java` | STATIC SetPowerToughnessToAmountEffect(a, a) where a = PermanentCount(PermanentIsLandPredicate, CONTROLLER) |
| P/T = all snow permanents + ice-counter snow grant | `r/RimefeatherOwl.java` | STATIC `SetPowerToughnessToAmountEffect(PermanentCount(HasSupertype(SNOW), ANY_PLAYER), same)` + a layer-4 static supertype grant for permanents with ice counters; activated ability uses `PutCounterOnTargetPermanentEffect(ICE)` |
| P/T = creatures | `s/ScionOfTheWild.java` | STATIC SetPowerToughnessToAmountEffect(a, a) where a = PermanentCount(PermanentIsCreaturePredicate, CONTROLLER) |
| P/T = subtype | `n/Nightmare.java` | STATIC SetPowerToughnessToAmountEffect(a, a) where a = PermanentCount(PermanentHasSubtypePredicate(SWAMP), CONTROLLER) |
| P/T = GY creatures | `m/Mortivore.java` | STATIC SetPowerToughnessToAmountEffect(a, a) where a = CardsInGraveyard(CardTypePredicate(CREATURE), ANY_PLAYER) |
| P/T = hand size + draw trigger | `p/PsychosisCrawler.java` | STATIC SetPowerToughnessToAmountEffect(a, a) where a = CardsInHand(CONTROLLER) + ON_CONTROLLER_DRAWS LoseLifeEffect(1, EACH_OPPONENT) |
| Self boost per lands + GY lands | `m/MultaniYavimayasAvatar.java` | STATIC BoostSelfEffect(PermanentCount(PermanentIsLandPredicate, CONTROLLER), same) + BoostSelfEffect(CardsInGraveyard(CardTypePredicate(LAND), CONTROLLER), same) — +1/+1 per land you control and per land card in your graveyard |
| Gain activated abilities from typed GY cards | `n/NecroticOoze.java`, `m/MirranSafehouse.java` | STATIC `GainActivatedAbilitiesOfCardsInAllGraveyardsEffect(CardType)` — selfOnly, gains all activated abilities of matching cards in all graveyards; `ON_TAP` mana abilities are exposed as equivalent tap abilities |
| Grant unearth to GY creatures | `s/SedrisTheTraitorKing.java` | STATIC GrantGraveyardAbilityToCreatureCardsEffect(unearth {2}{B} ActivatedAbility) — each creature card in your graveyard gains the given graveyard-activated ability. The ability is the Fatestitcher unearth (ReturnCardFromGraveyardEffect self→BATTLEFIELD, returnAll, grantHaste, exileAtEndStep, SORCERY_SPEED). Scanned via the `GraveyardAbilityGrantingEffect` capability by `GameQueryService.computeGrantedGraveyardAbilitiesForOwnedCard` |
| Grant unearth to GY artifacts | `m/MishraTamerOfMakFawa.java` | STATIC GrantTriggeredAbilityEffect(ON_BECOMES_TARGET_OF_OPPONENT_SPELL, CounterUnlessSacrificesEffect, OWN_PERMANENTS) plus GrantGraveyardAbilityToArtifactCardsEffect(unearth {1}{B}{R} ActivatedAbility). The unearth ability returns the artifact card to the battlefield with haste, exiles it at the next end step or if it would leave the battlefield, and is sorcery-speed. |
| Grant scavenge (cost = mana cost) to GY creatures | `v/VarolzTheScarStriped.java` | STATIC GrantScavengeEqualToManaCostToCreatureCardsEffect() — same capability, ability built per card from its mana cost via `Card.scavengeAbility(cost)`. Second ability: `SacrificeCreatureCost(false, false, false, true)` + `RegenerateEffect()` ("Sacrifice another creature: Regenerate ~") |
| Tap to grant Embalm (cost = mana cost) to a target GY creature | `c/CurseclothWrappings.java` | Activated `GrantEmbalmToTargetCreatureCardEffect()` targets a creature card in your graveyard; resolution records the card until end of turn, and the effective graveyard abilities expose `Card.embalmAbility(card.getManaCost())` |
| Grant madness (cost = mana cost) to owned off-BF cards | `f/FalkenrathGorger.java` | STATIC GrantMadnessEqualToManaCostEffect(CardAllOf(CREATURE, VAMPIRE)) — Vampire creature cards you own not on the battlefield have madness; cost = mana cost. Capability `MadnessGrantingEffect`; discard path snapshots cost onto MadnessMayCastEffect |
| Top-of-library instant/sorcery casting + copy | `m/MelekIzzetParagon.java` | STATIC PlayWithTopCardRevealedEffect + AllowCastFromTopOfLibraryEffect(INSTANT, SORCERY) + ON_CONTROLLER_CASTS_SPELL CopyControllerCastSpellOnSpellCastEffect(instant/sorcery filter, Zone.LIBRARY) — the zone argument makes the mandatory copy fire only on library-top casts |
| Attack-enabled top-library play | `t/TheLunarWhale.java` | STATIC LookAtTopCardOfOwnLibraryEffect + ConditionalEffect(SourceAttackedThisTurn, PlayLandsFromTopOfLibraryEffect) + ConditionalEffect(SourceAttackedThisTurn, AllowCastFromTopOfLibraryEffect(all nonland card types)) + CrewCost(1) |
| Gain top-library-card abilities | `s/SkillBorrower.java` | STATIC PlayWithTopCardRevealedEffect + GainActivatedAbilitiesOfTopLibraryCardEffect — selfOnly, gains the top library card's activated abilities while it's an artifact or creature |
| Gain loyalty abilities of other planeswalkers | `n/NicolBolasDragonGod.java` | STATIC GainLoyaltyAbilitiesOfOtherPlaneswalkersEffect — selfOnly, scans the battlefield and grants only loyalty abilities of other planeswalkers |
| Top-library creature color anthem + reorder ability | `c/CrownOfConvergence.java` | STATIC PlayWithTopCardRevealedEffect + BoostCreaturesSharingColorWithTopCardEffect(1,1) + `{G}{W}` PutTopCardsOfLibraryOnBottomEffect(1) — matching creatures you control get +1/+1 while the top card is a creature sharing a color, and the activated ability moves that card to the bottom |
| +1/+1 per same name | `r/RelentlessRats.java` | STATIC BoostByOtherCreaturesWithSameNameEffect(1, 1) — any controller |
| +1/+1 per same name you control | `t/TimberpackWolf.java` | STATIC BoostByOtherCreaturesWithSameNameEffect(1, 1, true) — only creatures you control count |
| +1/+0 per other subtype you control | `r/RatColony.java` | STATIC BoostSelfEffect(PermanentCount(PermanentHasSubtypePredicate(RAT), CONTROLLER, excludeSource=true), Fixed(0)) |
| Cost reduction if opponent has more creatures | `a/AvatarOfMight.java` | STATIC ConditionalEffect(OpponentControlsMoreCreatures(4), ReduceOwnCastCostEffect(Fixed(6))) — costs {6} less if an opponent controls 4+ more creatures than you |
| Subtype cost reduction | `d/DanithaCapashenParagon.java` | STATIC ReduceCastCostForMatchingSpellsEffect(CardAnyOfPredicate(CardSubtypePredicate(AURA), CardSubtypePredicate(EQUIPMENT)), 1, SELF) — from battlefield permanent |
| Equip cost reduction + conditional subtype anthem | `a/AuriokSteelshaper.java` | STATIC ReduceEquipCostEffect(1) + ConditionalEffect(Equipped, StaticBoostEffect(1, 1, OWN_CREATURES, PermanentHasAnySubtypePredicate({SOLDIER, KNIGHT}))) |
| Symmetric card-type cost reduction | `a/ArcaneMelee.java` | STATIC ReduceCastCostForMatchingSpellsEffect(CardAnyOfPredicate(CardTypePredicate(INSTANT), CardTypePredicate(SORCERY)), 2, ALL) — ALL scope affects every player |
| Symmetric all-spells cost reduction | `h/HelmOfAwakening.java` | STATIC ReduceCastCostForMatchingSpellsEffect(CardTruePredicate(), 1, ALL) — "Spells cost {1} less"; CardTruePredicate matches every spell |
| Color-specific cost reduction | `t/TheDarknessCrystal.java` | STATIC ReduceCastCostForMatchingSpellsEffect(CardColorPredicate(BLACK), 1, SELF) — black spells you cast cost {1} less |
| Second-spell cost reduction | `h/HighspireBellRinger.java` | STATIC ReduceSecondSpellCastCostEffect(1) — the source controller's second spell cast each turn costs {1} less |
| Subtype cost reduction scaling with counters on the source | `h/HeraldOfWar.java` | ON_ATTACK PutCountersOnSourceEffect(1,1,1) + STATIC ReduceCastCostForMatchingSpellsEffect(CardAnyOfPredicate(CardSubtypePredicate(ANGEL), CardSubtypePredicate(HUMAN)), CountersOnSource(PLUS_ONE_PLUS_ONE), SELF) — "cost {1} less for each +1/+1 counter on this creature"; the amount is evaluated against the source permanent |
| Chosen subtype creature-spell cost reduction | `u/UrzasIncubator.java` | ON_ENTER_BATTLEFIELD ChooseSubtypeOnEnterEffect + STATIC ReduceCastCostForChosenSubtypeSpellsEffect(2) — creature spells of the source permanent's chosen type cost {2} less |
| Cost reduction per creature card in graveyard | `g/Ghoultree.java` | STATIC ReduceOwnCastCostEffect(CardsInGraveyard(CardTypePredicate(CREATURE), CONTROLLER)) — this spell costs {1} less per creature card in your graveyard |
| Cost reduction per instant/sorcery card in graveyard | `t/TheDawningArchaic.java` | STATIC ReduceOwnCastCostEffect(CardsInGraveyard(CardAnyOfPredicate(CardTypePredicate(INSTANT), CardTypePredicate(SORCERY)), CONTROLLER)) — compose the two type predicates, do not add a new amount |
| Cost reduction per distinct card type in graveyard | `e/EmrakulThePromisedEnd.java` | STATIC ReduceOwnCastCostEffect(CardTypesAmongCardsInGraveyard()) + ON_SELF_CAST ControlTargetPlayerNextTurnEffect(true) + STATIC ProtectionFromCardTypesEffect(INSTANT) — Emrakul |
| Cost reduction per creature on battlefield | `b/BlasphemousAct.java` | STATIC ReduceOwnCastCostEffect(PermanentCount(PermanentIsCreaturePredicate, ANY_PLAYER)) — this spell costs {1} less for each creature on the battlefield |
| Cost reduction by total mana value of noncreature artifacts | `m/MetalworkColossus.java` | STATIC ReduceOwnCastCostEffect(PermanentManaValueSum(AllOf(PermanentIsArtifactPredicate, Not(PermanentIsCreaturePredicate)), CONTROLLER)) — sum the mana values rather than count the artifacts |
| Cost reduction per card cycled/discarded this turn | `h/HollowOne.java` | STATIC ReduceOwnCastCostEffect(Scaled(CardsDiscardedOrCycledThisTurn(), 2)) + hand Cycling {2} — costs {2} less per card you've cycled or discarded this turn |
| Creature mana only | `m/MyrSuperion.java` | setRequiresCreatureMana(true) — can only be cast with mana produced by creatures |
| No max hand size | `s/Spellbook.java` | STATIC NoMaximumHandSizeEffect |
| Toughness as combat damage (controller) | `b/BelligerentBrontodon.java` | STATIC AssignCombatDamageWithToughnessEffect(ALL_OWN_CREATURES) â€” all your creatures assign combat damage equal to toughness |
| Toughness as combat damage (global) | `d/DoranTheSiegeTower.java` | STATIC AssignCombatDamageWithToughnessEffect(ALL_CREATURES) â€” every creature assigns combat damage equal to toughness |
| Toughness as combat damage when greater than power | `b/BedrockTortoise.java` | STATIC AssignCombatDamageWithToughnessEffect(ALL_OWN_CREATURES, PermanentToughnessGreaterThanPowerPredicate) â€” only your creatures with toughness greater than power use toughness |
| Defending player assigns combat damage | `d/DefensiveFormation.java` | STATIC DefensiveFormationEffect() â€” the controller assigns combat damage dealt by creatures attacking them |
| Toughness as combat damage (Aura, always) | `g/GauntletsOfLight.java` | STATIC AssignCombatDamageWithToughnessEffect(ENCHANTED_CREATURE, true) â€” enchanted creature assigns combat damage equal to toughness even when its power is greater |
| Double damage (global) | `f/FurnaceOfRath.java` | STATIC DoubleDamageEffect |
| Double damage (controller's all sources) | `a/AngrathsMarauders.java` | STATIC DoubleControllerDamageEffect(null, true) — doubles all damage from sources you control (combat, spells, abilities) |
| Double damage (controller's spells by color) | `f/FireServant.java` | STATIC DoubleControllerDamageEffect(AllOf[TypeIn(INSTANT,SORCERY), ColorIn(RED)], false) — doubles only red instant/sorcery damage |
| Double damage (sources of chosen creature type) | `c/CollectiveInferno.java` | ON_ENTER_BATTLEFIELD ChooseSubtypeOnEnterEffect + STATIC DoubleDamageFromChosenSubtypeEffect — doubles damage from your sources carrying the chosen subtype |
| Plus N damage (controller's red spells / red planeswalkers) | `p/PyromancersGauntlet.java` | STATIC AdditionalControllerDamageEffect(2, AnyOf[AllOf(TypeIn(INSTANT,SORCERY), ColorIn(RED)), AllOf(CardTypeIn(PLANESWALKER), ColorIn(RED))]) — +2 to matching sources only |
| Plus N damage (another source of a color you control) | `e/EmbermawHellion.java` | STATIC AdditionalColorSourceDamageEffect(1, CardColor.RED) — +1 to every other red source you control, combat included; excludes itself |
| Plus N damage (red or artifact sources to opponents) | `m/MechanizedWarfare.java` | STATIC AdditionalDamageToOpponentsFromRedOrArtifactSourcesEffect(1) — +1 from your red or artifact sources to opponents and their permanents |
| Plus N damage (red sources to opponents) | `t/TorbranThaneOfRedFell.java` | STATIC AdditionalDamageToOpponentsFromColorSourcesEffect(2, CardColor.RED) — +2 from your red sources to opponents and their permanents |
| Reduce all spell damage by N | `b/BenevolentUnicorn.java` | STATIC ReduceSpellDamageEffect(1) — "if a spell would deal damage to a permanent or player, it deals that much damage minus 1 instead"; global replacement effect hooked in DamageSupport via GameQueryService.getSpellDamageReduction |
| Prevent all instant/sorcery spell damage + flying untap lock | `e/EnergyStorm.java` | UPKEEP_TRIGGERED CumulativeUpkeepEffect("{1}") + STATIC PreventDamageFromInstantAndSorcerySpellsEffect + STATIC MatchingPermanentsDoesntUntapEffect(Flying) — global spell-damage prevention (DamageSupport via GameQueryService); fliers don't untap |
| Grant deathtouch to your instant and sorcery spells | `p/PestilentSpirit.java` | STATIC GrantDeathtouchToControllerSpellsEffect — controller-scoped spell-source marker, checked by the noncombat damage pipeline |
| Counter every tapped permanent + counters replace its untap | `f/FreyalisesWinds.java` | ON_ALLY_PERMANENT_BECOMES_TAPPED + ON_OPPONENT_PERMANENT_BECOMES_TAPPED PutCounterOnReferencedPermanentEffect(PermanentReference.TRIGGERING, CounterType.WIND) + STATIC RemoveCountersInsteadOfUntappingEffect(CounterType.WIND). Both tap slots are needed for "whenever *a* permanent becomes tapped"; the untap replacement fires in `UntapStepService` in place of the untap, so the permanent stays tapped one extra turn per accumulated tap |
| Double damage to enchanted player (Curse) | `c/CurseOfBloodletting.java` | STATIC DoubleDamageToEnchantedPlayerEffect — enchant player curse; doubles all damage dealt to the enchanted player (spell/ability via DamageResolutionService.dealDamageToPlayer, combat via CombatDamageService.applyPlayerDamage), using GameQueryService.getEnchantedPlayerDamageMultiplier. Stacks multiplicatively with Furnace of Rath |
| Double damage to opponents + prevent half to you | `g/GiselaBladeOfGoldnight.java` | STATIC DoubleDamageToOpponentsAndTheirPermanentsEffect + STATIC PreventHalfDamageToControllerAndTheirPermanentsEffect — recipient-scoped pair; the doubler is read via GameQueryService.getDamageToRecipientMultiplier at the four damage entry points, the half-prevention via DamagePreventionService.applyHalfDamagePrevention from applyPlayerPreventionShield / applyCreaturePreventionShield. Never both on one damage event: the doubler skips its own controller, the prevention only covers its own controller |
| Spell damage bonus to opponents + spell damage prevention to you | `r/RemKarolusStalwartSlayer.java` | STATIC AdditionalDamageToOpponentsAndTheirPermanentsEffect(1) + STATIC PreventSpellDamageToControllerAndPermanentsEffect — recipient-scoped pair; only damage from spells on the stack is affected, and the bonus applies regardless of spell controller |
| Double damage to controller and self | `g/GoldnightCastigator.java` | STATIC DoubleDamageToControllerAndSelfEffect — recipient-scoped replacement; doubles all damage dealt to its controller or to the Castigator itself, but not to other permanents its controller controls |
| Double mana produced (tap for mana) | `m/ManaReflection.java` | STATIC ManaReflectionEffect — tapping any permanent for mana produces twice as much of that mana (2^count, controller-scoped) via GameQueryService.manaProductionMultiplier |
| Twist basic-land mana colors | `n/NakedSingularity.java` | UPKEEP_TRIGGERED CumulativeUpkeepEffect("{3}") + STATIC NakedSingularityManaEffect — Plains→{R}, Island→{G}, Swamp→{W}, Mountain→{U}, Forest→{B}; same plumbing as Reality Twist |
| Lands produce fixed color | `i/InfernalDarkness.java` | UPKEEP_TRIGGERED CumulativeUpkeepEffect("{B}", 1) + STATIC ReplaceLandManaWithColorEffect(BLACK) — amount unchanged; global via GameQueryService.fixedLandManaColor |
| Symmetric land-tap mana doubling | `m/ManaFlare.java` | ON_ANY_PLAYER_TAPS_LAND `AddOneOfEachManaTypeProducedByLandEffect(false)` — every player's land tap adds one extra mana of a type it produced (controller-scoped variant `true` = Vorinclex) |
| Controller's creature-tap mana boost | `l/LeylineOfAbundance.java` | ON_CONTROLLER_TAPS_CREATURE_FOR_MANA `AddManaWhenCreatureTappedForManaEffect(ManaColor.GREEN)` — the controller's creatures add one additional green when tapped for mana |
| Deferred opponent land-tap color lock | `m/ManaWeb.java` | ON_ANY_PLAYER_TAPS_LAND `TapLandsThatCouldProduceSameManaAsTappedLandEffect()` — queues a trigger, then at resolution taps that player's lands that could produce any type the triggering land could produce |
| Parity-branching upkeep with turn-scoped mana riders | `c/ChaosMoon.java` | Two complementary EACH_UPKEEP_TRIGGERED `ConditionalEffect`s (`NotCondition(TotalPermanentCountEven())` / `TotalPermanentCountEven()`), each wrapping a `SequenceEffect` of `BoostAllCreaturesEffect(±1, ±1, PermanentColorInPredicate(RED))` + a turn-scoped mana rider (`LandsOfSubtypeAddExtraManaUntilEndOfTurnEffect` / `LandsOfSubtypeProduceFixedManaColorUntilEndOfTurnEffect`). Parity is counted on resolution, not as an intervening-if |
| Island-tap CU-restricted mana + snow bonus | `s/Snowfall.java` | UPKEEP_TRIGGERED CumulativeUpkeepEffect("{U}") + ON_ANY_PLAYER_TAPS_LAND `AddRestrictedManaWhenLandOfSubtypeTappedForManaEffect(ISLAND, BLUE, 1, 2, CumulativeUpkeepCosts())` — Island → +{U} CU-only; snow Island → +{U}{U} instead; "may" auto-accepted |
| Snow-land tap mana boost + symmetric untap lock | `w/WintersNight.java` | ON_ANY_PLAYER_TAPS_LAND `AddProducedManaWhenSnowLandTappedEffect()` + `TappedSnowLandDoesntUntapEffect()` — World enchantment; every snow land tapped for mana pays its controller an extra mana of the produced type and then skips its next untap step |
| Play lands from GY | `c/CrucibleOfWorlds.java` | STATIC PlayLandsFromGraveyardEffect |
| Play lands from top of library + public reveal + landfall life gain | `c/CourserOfKruphix.java` | STATIC PlayWithTopCardRevealedEffect + PlayLandsFromTopOfLibraryEffect + ON_ALLY_LAND_ENTERS_BATTLEFIELD GainLifeEffect(1) |
| Cast cycling spells from GY + exile non-cycled cycling cards | `a/AbandonedSarcophagus.java` | STATIC `CastSpellsWithCyclingFromGraveyardEffect` + `ExileOwnCyclingCardsUnlessCycledEffect` |
| Draw replacement | `a/Abundance.java` | STATIC AbundanceDrawReplacementEffect |
| Optional draw replacement — skip a draw | `o/ObstinateFamiliar.java` | STATIC ObstinateFamiliarDrawReplacementEffect — the controller is offered a may-choice for each card they would draw; accepting leaves that card on top of the library |
| Draw replacement — empty hand draws two, lose 1 life | `b/BloodScrivener.java` | STATIC EmptyHandDrawExtraCardAndLoseLifeEffect — controller-scoped, detected in `DrawService.resolveDrawCard`; only applies while the drawer's hand is empty |
| Draw replacement — reveal top 3, creatures to hand | `s/SagesOfTheAnima.java` | STATIC SagesOfTheAnimaDrawReplacementEffect — if you would draw, instead reveal top 3, creatures → hand, rest → bottom in any order. Detected in `DrawService` via the capability interface `RevealTopCardsCreaturesToHandDrawReplacementEffect` (no concrete instanceof). Fully replaces the draw (no draw triggers, no empty-library loss) |
| Draw replacement — look at top 3, one to hand | `t/TomorrowAzamisFamiliar.java`, `u/UnderrealmLich.java` | STATIC top-three hand replacement — Tomorrow, Azami's Familiar puts the rest on the bottom in any order; Underrealm Lich puts the rest into the graveyard. Detected in `DrawService` via `LookAtTopCardsChooseOneToHandDrawReplacementEffect` (no concrete instanceof). Fully replaces the draw (no draw triggers, no empty-library loss) |
| Draw replacement — reveal draw, discard creature unless pay life | `b/BreathstealersCrypt.java` | STATIC BreathstealersCryptDrawReplacementEffect — every draw revealed; creature → discard unless pay 3 life. Detected in `DrawService.performDrawCard`; mayfx pay-or-discard |
| Draw replacement — Shared Fate exile-and-play | `s/SharedFate.java` | STATIC SharedFateDrawReplacementEffect + AllowCastFromCardsExiledWithSourceEffect(false, EXILER) — each drawer exiles the top card of an opponent's library face down and may look at/play/cast the cards they exiled with the source while it remains on the battlefield |
| Return-from-GY-instead-of-draw + self-exile GY | `f/ForbiddenCrypt.java` | STATIC ReturnFromGraveyardInsteadOfDrawEffect + STATIC ExileOwnCardsInsteadOfGraveyardEffect — draws are replaced by returning a GY card to hand (lose if GY empty); your cards are exiled instead of hitting your GY |
| Bushido + battlefield permanent graveyard replacement | `s/SamuraiOfThePaleCurtain.java` | ON_BLOCK + ON_BECOMES_BLOCKED `BoostSelfEffect(1, 1)` + STATIC `ExilePermanentsInsteadOfGraveyardEffect` — exiles permanents leaving the battlefield, while spells and other cards entering a graveyard from other zones are unaffected |
| Grant flash to spell type | `s/ShimmerMyr.java` | STATIC GrantFlashToCardTypeEffect(ARTIFACT) — controller may cast artifact spells as though they had flash |
| Grant flash to all spells + leyline | `l/LeylineOfAnticipation.java` | ON_OPENING_HAND_REVEAL MayEffect(LeylineStartOnBattlefieldEffect) + STATIC GrantFlashToCardTypeEffect(null) — may start on battlefield from opening hand, grants flash to all spells |
| Grant legendary to all nonland permanents + leyline | `l/LeylineOfSingularity.java` | ON_OPENING_HAND_REVEAL MayEffect(LeylineStartOnBattlefieldEffect) + STATIC GrantSupertypeToAllNonlandPermanentsEffect(LEGENDARY) — may start on battlefield from opening hand, makes every nonland permanent legendary |
| Any player may cast small creature spells for free at instant speed | `a/Aluren.java` | STATIC AlternativeCostForSpellsEffect("{0}", CardAllOf(CREATURE, CardMaxManaValue(3)), null, false, false, appliesToAllPlayers=true) + STATIC GrantFlashToCardTypeEffect(same predicate, appliesToAllPlayers=true) — both halves are symmetric permissions; `CastingCostService.findFreeCastSource` and `CastingPermissionService.hasFlashGrantForCard` scan every battlefield and honour an opponent's source only when the flag is set |
| Grant conspire to your spells + ETB tokens | `w/WortTheRaidmother.java` | ON_ENTER_BATTLEFIELD CreateTokenEffect(2 Goblin Warrior) + STATIC GrantSpellCastingAbilityToSpellsEffect(Keyword.CONSPIRE, CardAllOfPredicate(type instant/sorcery, color red/green)) — red or green instant/sorcery spells you cast have conspire |
| Grant convoke to artifact spells | `c/ChiefEngineer.java` | STATIC GrantSpellCastingAbilityToSpellsEffect(Keyword.CONVOKE, CardTypePredicate(ARTIFACT)) — artifact spells you cast can use convoke |
| Grant improvise to nonartifact spells | `i/InspiringStatuary.java` | STATIC GrantSpellCastingAbilityToSpellsEffect(Keyword.IMPROVISE, CardNotPredicate(CardTypePredicate(ARTIFACT))) — nonartifact spells you cast can use improvise |
| Grant jump-start to exactly two-color graveyard instants and sorceries | `n/NivMizzetSupreme.java` | STATIC GrantSpellCastingAbilityToSpellsEffect.fromZone(Keyword.JUMP_START, CardAllOfPredicate(CardAnyOfPredicate(INSTANT, SORCERY), CardHasExactlyTwoColorsPredicate), Zone.GRAVEYARD) — matching cards can be cast from the controller's graveyard by discarding a card, then are exiled |
| Grant replicate equal to mana cost to instants and sorceries | `d/DjinnIlluminatus.java` | STATIC GrantSpellCastingAbilityToSpellsEffect(Keyword.REPLICATE, CardAnyOfPredicate(type instant/sorcery)) — matching spells gain a repeatable additional cost equal to their mana cost and copy themselves once per payment |
| Conditional keywords while any creature has -1/-1 | `t/TenaciousHunter.java` | STATIC ConditionalEffect(AnyPlayerControlsPermanent(AllOf(IsCreature, HasCounters(MINUS_ONE_MINUS_ONE))), GrantKeywordEffect(Set.of(VIGILANCE, DEATHTOUCH), SELF)) |
| Metalcraft keyword | `a/AuriokEdgewright.java` | STATIC ConditionalEffect(new Metalcraft(), GrantKeywordEffect(DOUBLE_STRIKE, SELF)) |
| Metalcraft keyword + boost | `a/AuriokSunchaser.java` | STATIC ConditionalEffect(new Metalcraft(), GrantKeywordEffect) + ConditionalEffect(new Metalcraft(), StaticBoostEffect) |
| Metalcraft boost only | `c/CarapaceForger.java` | STATIC ConditionalEffect(new Metalcraft(), StaticBoostEffect(2, 2, SELF)) |
| Metalcraft boost + ignore defender | `s/SpireSerpent.java` | STATIC ConditionalEffect(new Metalcraft(), StaticBoostEffect) + ConditionalEffect(new Metalcraft(), CanAttackAsThoughNoDefenderEffect) |
| Metalcraft become creature | `r/RustedRelic.java` | STATIC ConditionalEffect(new Metalcraft(), AnimatePermanentsEffect(5, 5, [GOLEM], [])) â€” noncreature becomes creature with fixed P/T and subtypes (4-arg SELF/UEOT ctor) |
| Opponent-turn become creature | `w/WardenOfTheWall.java` | STATIC EntersTappedEffect + mana ability + ConditionalEffect(new NotControllerTurn(), AnimatePermanentsEffect(2, 3, [GARGOYLE], [FLYING])) â€” artifact animates on opponent's turn only |
| Combat-only self-animate until end of combat | `j/JadeStatue.java` | Activated ability `{2}` with `ActivationTimingRestriction.ONLY_DURING_COMBAT` + `AnimatePermanentsEffect(3, 6, [GOLEM], Set.of(), null, Set.of(), GrantScope.SELF, EffectDuration.UNTIL_END_OF_COMBAT)` â€” artifact becomes a 3/6 creature only during combat; reverts when combat ends |
| Animate all noncreature artifacts (March-style) | `m/MarchOfTheMachines.java` | STATIC `AnimateNoncreatureArtifactsEffect()` â€” every noncreature artifact (both players') becomes a creature with P/T = its mana value while the enchantment is out |
| Animate your own enchantments above a count | `s/StarfieldOfNyx.java` | STATIC `AnimateControlledEnchantmentsEffect(5)` - while you control five or more enchantments, each other non-Aura enchantment you control is a creature with base P/T = its mana value |
| Animate all noncreature artifacts + strip abilities | `t/TitaniasSong.java` | STATIC `AnimateNoncreatureArtifactsEffect(true)` â€” like March of the Machines but the artifacts also lose all abilities (layer 6), so e.g. Mox/mana rocks can no longer tap for mana. (The "if this enchantment leaves, the effect continues until end of turn" rider is not modeled â€” a rare corner case with no engine support.) |
| Metalcraft burn spell | `g/GalvanicBlast.java` | SPELL ConditionalReplacementEffect(new Metalcraft(), baseEffect, upgradedEffect)(DealDamageToAnyTargetEffect(2), DealDamageToAnyTargetEffect(4)) â€” picks base/upgrade at resolution |
| Morbid burn spell | `b/BrimstoneVolley.java` | SPELL ConditionalReplacementEffect(new Morbid(), baseEffect, upgradedEffect)(DealDamageToAnyTargetEffect(3), DealDamageToAnyTargetEffect(5)) â€” picks base/upgrade at resolution based on creature death this turn |
| Morbid land search | `c/CaravanVigil.java` | SPELL ConditionalReplacementEffect(new Morbid(), baseEffect, upgradedEffect)(SearchLibraryEffect(CardPredicateUtils.basicLand()), SearchLibraryEffect(basicLand filter, LibrarySearchDestination.BATTLEFIELD)) â€” search for basic land to hand, or to battlefield untapped if morbid |
| Morbid ETB counters | `f/FesterhideBoar.java` | ON_ENTER_BATTLEFIELD ConditionalEffect(new Morbid(), PutCountersOnSourceEffect(1, 1, 2)) â€” enters with +1/+1 counters if a creature died this turn |
| Other subtype creatures enter with extra +1/+1 counter + counter-removal draw | `s/SageOfFables.java` | STATIC ControlledCreaturesEnterWithAdditionalCountersEffect(WIZARD, 1) + ActivatedAbility("{2}", [RemoveCounterFromControlledCreatureCost(1, PLUS_ONE_PLUS_ONE), DrawCardEffect(1)]) â€” each other Wizard you control enters with an additional counter; remove a counter to draw |
| Other creatures enter with counters equal to source power + extra type | `m/MasterBiomancer.java` | STATIC ControlledCreaturesEnterWithSourcePowerCountersEffect(MUTANT) — each other creature you control enters with +1/+1 counters equal to this creature's power and as a Mutant in addition to its other types |
| Convert +1/+1 counters into an X/X token | `o/OozeFlux.java` | ActivatedAbility("{1}{G}", [RemoveOneOrMoreCountersFromControlledCreaturesCost(PLUS_ONE_PLUS_ONE), CreateTokenEffect("Ooze", XValue, XValue, GREEN, [OOZE])]).withXValueFromControlledCreatureCounters() — X is the number of counters removed as the cost, split freely across your creatures |
| Other subtype enter with counter + your countered creatures get keyword | `b/BramblewoodParagon.java` | STATIC ControlledCreaturesEnterWithAdditionalCountersEffect(WARRIOR, 1) + GrantKeywordEffect(TRAMPLE, GrantScope.ALL_OWN_CREATURES, PermanentHasCountersPredicate(PLUS_ONE_PLUS_ONE)) â€” other Warriors enter with a +1/+1 counter; every creature you control that has a +1/+1 counter (including this one) has trample. `ALL_OWN_CREATURES` so the source qualifies too |
| Metalcraft damage + can't block | `c/ConcussiveBolt.java` | SPELL DealDamageToPlayersEffect(4, DamageRecipient.TARGET_PLAYER) + ConditionalEffect(new Metalcraft(), new CantBlockThisTurnEffect(TapUntapScope.TARGET_PLAYERS_PERMANENTS)) â€” damage always, metalcraft adds mass can't-block on target player's creatures (rides the shared player target) |
| Life threshold boost + keyword | `s/SerraAscendant.java` | STATIC ConditionalEffect(new ControllerLifeAtLeast(30), StaticBoostEffect(5, 5, [FLYING], SELF)) â€” +5/+5 and flying as long as controller has 30+ life |
| Two-sided life threshold boost + protection | `b/BloodBaronOfVizkopa.java` | STATIC ProtectionFromColorsEffect(WHITE, BLACK) + STATIC ConditionalEffect(AllConditions(ControllerLifeAtLeast(30), AnOpponentLifeAtMost(10)), StaticBoostEffect(6, 6, [FLYING], SELF)) |
| Controls-another-multicolored self-boost + keyword | `e/EsperStormblade.java` | STATIC ConditionalEffect(new ControlsAnotherPermanent(new PermanentIsMulticoloredPredicate()), StaticBoostEffect(1, 1, [FLYING], SELF)) â€” +1/+1 and flying as long as you control another multicolored permanent (self is multicolored, so `ControlsAnotherPermanent` excludes it) |
| Life threshold transform (activated) | `c/ChaliceOfLife.java` | ActivatedAbility(tap, [GainLifeEffect(1), ConditionalEffect(new ControllerLifeAtLeast(30), TransformSelfEffect())]) â€” gain 1 life then transform if 30+ life |
| Few-cards-in-hand self-boost | `t/ThresherLizard.java` | STATIC ConditionalEffect(new CardsInHandAtMost(1), StaticBoostEffect(1, 2, SELF)) â€” +1/+2 as long as you have one or fewer cards in hand |
| Graveyard card threshold boost + keyword | `g/GhituLavarunner.java` | STATIC ConditionalEffect(new GraveyardCardThreshold(2, instant/sorcery filter), StaticBoostEffect(1, 0, [HASTE], SELF)) â€” +1/+0 and haste as long as 2+ instant/sorcery cards in controller's graveyard |
| Controls-subtype self-boost | `t/TeferisSentinel.java` | STATIC ConditionalEffect(new ControlsPermanent(PermanentHasSubtypePredicate(TEFERI)), StaticBoostEffect(4, 0, SELF)) â€” +4/+0 as long as you control a Teferi planeswalker |
| Controls-subtype self-boost + unblockable | `j/JacesSentinel.java` | STATIC ConditionalEffect(new ControlsPermanent(PermanentHasSubtypePredicate(JACE)), StaticBoostEffect(1, 0, SELF)) + ConditionalEffect(new ControlsPermanent(PermanentHasSubtypePredicate(JACE)), GrantEffectEffect(CantBeBlockedEffect, SELF)) â€” +1/+0 and can't be blocked as long as you control a Jace planeswalker |
| Escalating upkeep symmetrical exile | `d/DescentIntoMadness.java` | UPKEEP_TRIGGERED `SequenceEffect.of(PutCountersOnSelfEffect(CounterType.DESPAIR), EachPlayerExilesPermanentsOrCardsFromHandEffect(CountersOnSource(CounterType.DESPAIR)))` â€” one stack entry, so the exile count reads the counter the same trigger just added; players pick freely across their battlefield and hand and the enchantment can exile itself |

| Opponent end-step quest counter + threshold token ability | `l/LuminarchAscension.java` | OPPONENT_END_STEP_TRIGGERED `ConditionalEffect(ControllerDidntLoseLifeThisTurn, MayEffect(PutCountersOnSelfEffect(QUEST)))` + activated `CreateTokenEffect(4/4 Angel, FLYING)` gated by `withRequiredSourceCounters(QUEST, 4)` |
| Each player's end step: damage for untapped lands | `c/CitadelOfPain.java` | END_STEP_TRIGGERED `DealDamageToEndStepPlayerEffect(PermanentCount(AllOf(LAND, Not(TAPPED)), TARGET_PLAYER))` — the amount is evaluated at resolution for the active end-step player |
| Any card enters your graveyard + graveyard reset ability | `q/QuestForAncientSecrets.java` | `ON_ALLY_CARD_PUT_INTO_GRAVEYARD_FROM_ANYWHERE MayEffect(PutCountersOnSelfEffect(QUEST))` + activated `RemoveCounterFromSourceCost(5, QUEST)` + `SacrificeSelfCost` + `ShuffleGraveyardIntoLibraryEffect(true)` |

## Auras

| Pattern | Reference | Notes |
|---------|-----------|-------|
| Lockdown (can't attack/block) | `p/Pacifism.java` | STATIC EnchantedCreatureCantAttackOrBlockEffect |
| Lockdown (can't attack/block/crew Vehicles) | `r/RevokePrivileges.java` | STATIC EnchantedCreatureCantAttackOrBlockEffect + EnchantedCreatureCantCrewEffect |
| Lockdown (can't attack/block/transform) + sorcery once-per-turn reattach | `b/BoundByMoonsilver.java` | STATIC EnchantedCreatureCantAttackOrBlockEffect + EnchantedCreatureCantTransformEffect; activated `SacrificePermanentCost(PermanentTruePredicate, â€¦)` + `AttachSourceAuraToTargetCreatureEffect` with `maxActivationsPerTurn=1` + `SORCERY_SPEED` |
| Untargetable by spells + can't be enchanted | `a/AntiMagicAura.java` | STATIC GrantEffectEffect(TargetingRestrictionEffect.spells(), ENCHANTED_CREATURE) + GrantEffectEffect(CantBeEnchantedByOtherAurasEffect(), ENCHANTED_CREATURE) â€” "can't be the target of spells" reuses Dense Foliage's marker scoped to the enchanted creature; the new CantBeEnchantedByOtherAurasEffect marker is enforced at the Aura-spell targeting path |
| Predicate-conditional aura | `b/BondsOfFaith.java` | STATIC EnchantedPermanentConditionalEffect(PermanentHasSubtypePredicate(HUMAN), StaticBoostEffect(2, 2, GrantScope.ENCHANTED_CREATURE), EnchantedCreatureCantAttackOrBlockEffect()) â€” composes existing effects, +2/+2 if Human, can't attack/block otherwise |
| Color-conditional aura (independent bonuses) | `s/SteelOfTheGodhead.java` | STATIC StaticBoostEffect(1, 1, Set.of(LIFELINK), ENCHANTED_CREATURE, PermanentColorInPredicate(WHITE)) + StaticBoostEffect(1, 1, ENCHANTED_CREATURE, PermanentColorInPredicate(BLUE)) + EnchantedPermanentConditionalEffect(PermanentColorInPredicate(BLUE), CantBeBlockedEffect(), null) â€” gate boosts/keywords by the enchanted creature's color via StaticBoostEffect's `filter` (ENCHANTED_CREATURE scope honors it); for a non-boost effect use a single-branch EnchantedPermanentConditionalEffect with a null inactive branch |
| Lockdown (can't attack) + self-bounce | `f/ForcedWorship.java` | STATIC EnchantedCreatureCantAttackOrBlockEffect(true, false) — attack-only; the creature can still block — + activated ReturnToHandEffect.self() |
| Full lockdown (can't attack/block/activate) | `a/Arrest.java` | STATIC EnchantedCreatureCantAttackOrBlockEffect + EnchantedCreatureCantActivateAbilitiesEffect |
| Lockdown (can't attack/block, only {T} abilities locked) + upkeep sacrifice-unless-pay | `s/SerraBestiary.java` | UPKEEP_TRIGGERED ForcedCostOrElseEffect(PayManaCost("{W}{W}"), List.of(SacrificeSelfEffect()), true) + STATIC EnchantedCreatureCantAttackOrBlockEffect + EnchantedCreatureCantActivateTapAbilitiesEffect — the tap-only variant (non-tap activated abilities stay usable), unlike Arrest |
| Global flier lockdown (can't attack/block + {T} abilities) + Phasing | `k/KatabaticWinds.java` | STATIC MatchingCreaturesCantAttackOrBlockEffect(PermanentHasKeywordPredicate(FLYING), …) + MatchingPermanentsCantActivateTapAbilitiesEffect(FLYING) — board-wide Serra Bestiary pair; Phasing is printed keyword |
| O-ring exile creature + upkeep sac unless any player pays | `i/IcyPrison.java` | ON_ENTER_BATTLEFIELD ExileTargetPermanentUntilSourceLeavesEffect (creature target) + UPKEEP_TRIGGERED ForcedCostOrElseEffect(PayManaCost("{3}"), List.of(SacrificeSelfEffect()), true, true) — anyPlayerMayPay APNAP offer; LTB return is implicit O-ring linkage |
| ETB may exile target + all same name until leaves | `d/DetentionSphere.java` | MayEffect(ExileTargetPermanentAndAllWithSameNameUntilSourceLeavesEffect) + AllOf(Not(IsLand), Not(Named("Detention Sphere"))); LTB return is implicit O-ring linkage |
| P/T override + ability strip aura | `d/DeepFreeze.java` | STATIC SetBasePowerToughnessEffect(0, 4, ENCHANTED_CREATURE) + GrantKeywordEffect(DEFENDER, ENCHANTED_CREATURE) + LosesAllAbilitiesEffect(ENCHANTED_CREATURE) + GrantColorEffect(BLUE, ENCHANTED_CREATURE) + GrantSubtypeEffect(WALL, ENCHANTED_CREATURE) — sets base P/T, grants defender, strips all original abilities, adds color and type |
| Global subtype-filtered base P/T | `h/HarmoniousArchon.java` | STATIC SetBasePowerToughnessEffect(3, 3, ALL_CREATURES_INCLUDING_SELF, PermanentNotPredicate(PermanentHasSubtypePredicate(ARCHON))) — sets base P/T only for matching creatures on both battlefields |
| Lockdown + sacrifice-to-ignore + self-bounce | `v/VolrathsCurse.java` | STATIC EnchantedCreatureCantAttackOrBlockEffect + EnchantedCreatureCantActivateAbilitiesEffect + `ActivatedAbility(false, null, [SacrificePermanentCost(PermanentTruePredicate, "a permanent"), IgnoreSourceAuraEffectsUntilEndOfTurnEffect()]).withActivatableByAnyPlayer().withActivatableOnlyByEnchantedPermanentController()` + `{1}{U}` `ReturnToHandEffect.self()` |
| Lockdown + self-destruct on target | `i/IceCage.java` | STATIC EnchantedCreatureCantAttackOrBlockEffect + EnchantedCreatureCantActivateAbilitiesEffect + ON_BECOMES_TARGET_OF_SPELL_OR_ABILITY DestroyReferencedPermanentEffect(SOURCE) |
| Lockdown + jump-to-entering-opponent-creature | `p/PrisonTerm.java` | STATIC EnchantedCreatureCantAttackOrBlockEffect + EnchantedCreatureCantActivateAbilitiesEffect + ON_OPPONENT_CREATURE_ENTERS_BATTLEFIELD AttachSourceAuraToEnteringCreatureEffect â€” "you may attach this Aura to that creature" moves the Aura onto each opponent creature that enters |
| Doesn't untap | `d/Dehydration.java` | STATIC EnchantedCreatureDoesntUntapEffect |
| Doesn't untap while Aura has a counter | `c/Cocoon.java` | STATIC `DoesntUntapWithCounterEffect.enchanted(CounterType.PUPA)` + UPKEEP_TRIGGERED `RemoveCounterOrSacrificeSelfEffect` with post-sacrifice counter and indefinite keyword payload |
| Aura hard untap and counter lock | `b/Blossombind.java` | STATIC `GrantEffectEffect(new CantBecomeUntappedEffect(), GrantScope.ENCHANTED_CREATURE)` + `GrantEffectEffect(new CantHaveCountersEffect(), GrantScope.ENCHANTED_CREATURE)`; ON_ENTER_BATTLEFIELD `TapPermanentsEffect(TapUntapScope.TARGET)` |
| Static boost | `h/HolyStrength.java` | STATIC StaticBoostEffect(X, Y, GrantScope.ENCHANTED_CREATURE) |
| Boost + can only attack alone | `e/Errantry.java` | STATIC StaticBoostEffect(3, 0, GrantScope.ENCHANTED_CREATURE) + EnchantedCreatureCanOnlyAttackAloneEffect â€” enchanted creature may be declared as an attacker only if it is the sole attacker (validated at declaration time in CombatAttackService); the mirror of Jackal Familiar's "can't attack alone" |
| Boost + keyword | `s/SerrasEmbrace.java` | Boost + GrantKeywordEffect(ENCHANTED_CREATURE) |
| Boost + keyword + ETB enchanted-creature fight | `c/CartoucheOfStrength.java` | STATIC StaticBoostEffect(1,1,ENCHANTED_CREATURE) + GrantKeywordEffect(TRAMPLE, ENCHANTED_CREATURE); group 0 = "creature you control", group 1 = opponent creature (`PermanentNotPredicate(PermanentControlledBySourceControllerPredicate())`) bound to ON_ENTER_BATTLEFIELD `MayEffect(EnchantedCreatureFightsTargetCreatureEffect())`. The fighter is the enchanted creature (Aura's `attachedTo`), not the Aura. Fight target chosen at resolution via the may path â€” `FightTargetsEffect(0,1)` won't work here (may path fills one target slot only) |
| Aura: grant flying + sac-to-bounce host | `p/PhantomWings.java` | STATIC GrantKeywordEffect(FLYING, ENCHANTED_CREATURE) + `ActivatedAbility(false, null, [SacrificeSelfCost(), ReturnToHandEffect.enchanted()])` — the enchanted permanent is snapshotted at activation, so the Aura sacrificing itself as the cost still returns its host (LKI) |
| Keyword + combat damage prevention | `g/GhostlyPossession.java` | GrantKeywordEffect(FLYING) + PreventAllCombatDamageToAndByEnchantedCreatureEffect (non-combat damage still goes through) |
| Aura shrink with toughness floor, from the enchanted creature's perspective | `s/Snowblind.java` | STATIC ShrinkEnchantedCreatureEffect(IfSourceAttacking(PermanentCount(AllOf(IsLand, HasSupertype(SNOW)), DEFENDING_PLAYER), PermanentCount(same, CONTROLLER))) â€” -X/-Y where Y = min(X, toughness - 1), so it never kills; the amount evaluates with the enchanted creature as amount source, so attacking/DEFENDING_PLAYER/CONTROLLER all read the creature, not the Aura |
| Aura boost per controlled subtype | `b/BlanchwoodArmor.java` | STATIC AttachedBoostEffect(PermanentCount(PermanentHasSubtypePredicate(FOREST), CONTROLLER), same, GrantScope.ENCHANTED_CREATURE) — enchanted creature gets +1/+1 per Forest you control |
| Aura: +1/+1 per other creature you control + other own creatures -1/-1 + delayed ETB draw | `v/Vampirism.java` | ON_ENTER_BATTLEFIELD RegisterDrawCardsAtNextUpkeepEffect + STATIC AttachedBoostEffect(PermanentCount(AllOf(IsCreature, Not(HostOfSourceAura)), CONTROLLER), same, ENCHANTED_CREATURE) + STATIC StaticBoostEffect(-1, -1, OWN_CREATURES, Not(HostOfSourceAura)) — "you" is Aura controller; Host exclude models "other" |
| Aura boost per card in host's hand + host-controller draw | `r/RighteousAuthority.java` | STATIC AttachedBoostEffect(CardsInHand(ATTACHED_CONTROLLER), same, ENCHANTED_CREATURE) + ENCHANTED_PERMANENT_CONTROLLER_DRAW_TRIGGERED DrawCardForTargetPlayerEffect(1) — "+1/+1 for each card in its controller's hand" uses ATTACHED_CONTROLLER (not CONTROLLER); extra draw fires on the enchanted permanent's controller's draw step |
| Aura color-evasion + conditional opponent-color boost | `b/BlackScarab.java` | STATIC CantBeBlockedByCreaturesMatchingPredicateEffect(PermanentColorInPredicate(BLACK)) on the Aura + ConditionalEffect(OpponentControlsPermanent(same color), StaticBoostEffect(2,2,ENCHANTED_CREATURE)) — Aura-granted negative evasion (read by GameQueryService like Treetop Bracers); +2/+2 while an opponent of the Aura's controller controls a black permanent |
| Flying + first strike while blocking if you control a snow land | `s/SnowDevil.java` | STATIC GrantKeywordEffect(FLYING, ENCHANTED_CREATURE) + ConditionalEffect(ControlsPermanent(AllOf(IsLand, HasSupertype(SNOW))), GrantKeywordEffect(FIRST_STRIKE, ENCHANTED_CREATURE, PermanentIsBlockingPredicate)) |
| Control enchanted | `p/Persuasion.java` | STATIC ControlEnchantedCreatureEffect |
| Grant activated ability | `a/ArcaneTeachings.java` | GrantActivatedAbilityEffect with GrantScope.ENCHANTED_CREATURE |
| Aura grants {T}: X damage where X = counters on the Aura | `a/ArcheryTraining.java` | UPKEEP_TRIGGERED MayEffect(PutCountersOnSelfEffect(ARROW)) + STATIC GrantActivatedAbilityEffect(ActivatedAbility(tap, null, DealDamageToTargetCreatureEffect(new CountersOnGrantingPermanent(ARROW))), ENCHANTED_CREATURE) — the granted ability's damage amount reads the Aura's arrow counters, not the activating creature's; `CountersOnGrantingPermanent` is bound to the granting permanent at activation in `ActivatedAbilityExecutionService.snapshotEffects` (mirrors `PutCountersOnGrantingEquipmentEffect`). Target filter = attacking/blocking creature |
| Redirect damage to creature | `p/Pariah.java` | STATIC RedirectPlayerDamageToEnchantedCreatureEffect |
| Redirect damage from unblocked creatures to self while untapped | `v/VeteranBodyguard.java` | STATIC `RedirectPlayerDamageToSelfEffect(false, true)` — source-restricted self redirect; combat uses the existing simultaneous unblocked-combat redirect path and noncombat damage uses source-aware player redirection |
| Redirect all your damage to self | `e/EmpyrialArchangel.java` | STATIC RedirectPlayerDamageToSelfEffect â€” "All damage that would be dealt to you is dealt to this creature instead." Flying/Shroud auto-loaded; queried in the same redirect path as Pariah (falls back when no aura variant present) |
| Redirect your + your permanents' damage to self | `p/PalisadeGiant.java` | STATIC `RedirectPlayerDamageToSelfEffect(true)` â€” "All damage that would be dealt to you and other permanents you control is dealt to this creature instead." Same player-half path as Empyrial Archangel plus the permanent half in `DamagePreventionService.applyStaticPermanentDamageRedirectToSelf` |
| Flash aura: redirect all your damage to enchanted creature this turn + boost | `s/SavingGrace.java` | ON_ENTER_BATTLEFIELD RedirectYourDamageToEnchantedCreatureThisTurnEffect + STATIC StaticBoostEffect(0, 3, ENCHANTED_CREATURE). Enters trigger installs a turn-scoped `TurnDamageRedirectToCreatureShield(controller, enchantedCreatureId)` (any source, unlimited, all four damage paths via `DamagePreventionService.applyTurnDamageRedirectToCreature`, reusing `pendingSourceRedirectDamage`). Destination creature is locked in at resolution; redirect skips while it's no longer a creature on the battlefield, and skips damage to that creature itself. Enchant creature you control via `ControlledPermanentPredicateTargetFilter` |
| Prevent X + redirect to player | `v/VengefulArchon.java` | Activated {X}: PreventXDamageToControllerAndRedirectToTargetPlayerEffect â€” prevent next X damage to you, source deals that much to target player |
| Enchanted land mana | `o/Overgrowth.java`, `f/FertileGround.java` | ON_ANY_PLAYER_TAPS_LAND `AddManaOnEnchantedLandTapEffect` + `AwardManaEffect` / `AwardAnyColorManaEffect` |
| Enchanted land grant mana ability + ETB counter | `n/NewHorizons.java` | STATIC GrantActivatedAbilityEffect(AwardAnyColorManaEffect(2), ENCHANTED_PERMANENT) + ON_ENTER_BATTLEFIELD PutCounterOnTargetPermanentEffect(PLUS_ONE_PLUS_ONE, 1) — multi-target aura: first target is land (aura attachment), second is creature (ETB +1/+1 counter). Uses GrantScope.ENCHANTED_PERMANENT for non-creature aura targets |
| Aura boost/keyword + optional ETB counter on ANY creature | `c/CartoucheOfAmbition.java` | STATIC StaticBoostEffect(1,1,ENCHANTED_CREATURE) + GrantKeywordEffect(LIFELINK, ENCHANTED_CREATURE) + ON_ENTER_BATTLEFIELD MayEffect(PutCounterOnTargetPermanentEffect(MINUS_ONE_MINUS_ONE, 1)). Two `target()` groups: group 0 = "creature you control" (enchant), group 1 = ANY creature (the ETB counter). The MayEffect defers its target to resolution and the may-target selection reads the group-1 filter (not the enchant filter) — see `MayAbilityHandlerService.mayAbilityTargetFilter`. Do NOT set `setAllowSharedTargets` (only the enchant is a cast-time target) |
| Enchanted land grant token ability + aura sac-token life | `c/CaribouRange.java` | STATIC GrantActivatedAbilityEffect(ActivatedAbility(tap+"{W}{W}", CreateTokenEffect(0/1 white Caribou)), ENCHANTED_PERMANENT) + the aura's own addActivatedAbility(SacrificePermanentCost(creature+token+Caribou subtype) + GainLifeEffect(1)) — "enchant land you control" via ControlledPermanentPredicateTargetFilter(PermanentIsLandPredicate); granted token ability lives on the land, sac-for-life ability lives on the aura |
| Enchanted land becomes basic type | `e/EvilPresence.java` | STATIC EnchantedPermanentBecomesTypeEffect(SWAMP) — land loses all land types/abilities, becomes the new basic land type |
| Enchanted land becomes several basic types | `l/LushGrowth.java` | STATIC EnchantedPermanentBecomesTypeEffect([MOUNTAIN, FOREST, PLAINS]) — type-replacing CR 305.7; taps for {R}, {G}, or {W} |
| Choose two basic land types; basics of first become second | `i/IllusionaryTerrain.java` | UPKEEP_TRIGGERED CumulativeUpkeepEffect("{2}") + ON_ENTER ChooseBasicLandTypeOnEnterEffect(2) + STATIC BasicLandsOfChosenTypesBecomeTypeEffect — only basics; stores from/to on chosenSubtype/secondChosenSubtype |
| Choose a land type; lands of it gain a keyword | `s/Shimmer.java` | ON_ENTER ChooseBasicLandTypeOnEnterEffect + STATIC GrantKeywordEffect(PHASING, ALL_LANDS, PermanentHasSourceChosenSubtypePredicate) — "each land of the chosen type has phasing" (CR 702.26a); ALL_LANDS covers every controller |
| Choose a basic land type; own lands gain it and its mana ability | `r/Realmwright.java` | ON_ENTER ChooseBasicLandTypeOnEnterEffect + STATIC GrantChosenBasicLandTypeToOwnLandsEffect — additive chosen land-type grant that preserves existing land abilities |
| Enchanted permanent becomes colorless land | `i/ImprisonedInTheMoon.java` | STATIC EnchantedPermanentBecomesOnlyLandEffect + BecomeColorlessEffect(ENCHANTED_PERMANENT) + LosesAllAbilitiesEffect(ENCHANTED_PERMANENT) + GrantActivatedAbilityEffect("{T}: Add {C}", ENCHANTED_PERMANENT) — "Enchanted permanent is a colorless land with '{T}: Add {C}' and loses all other card types and abilities." Keeps supertypes and land subtypes; type-replacing L4 (`cardTypeOverriding`) |
| Aura animates enchanted land into a creature | `l/LivingTerrain.java` | STATIC EnchantedPermanentBecomesCreatureEffect(5, 6, GREEN, [TREEFOLK]) — "Enchanted land is a 5/6 green Treefolk creature that's still a land." Continuous static animation (isCreature via aura scan, layer-4 type + handler colour/base-P/T); reverts when the aura leaves. Freshly-controlled land is summoning sick as a creature |
| Aura animates enchanted artifact into a creature (P/T = mana value) | `a/AnimateArtifact.java` | STATIC EnchantedPermanentBecomesCreatureEffect(0, 0, null, [], true) — "As long as enchanted artifact isn't a creature, it's an artifact creature with power and toughness each equal to its mana value." `powerToughnessEqualsManaValue=true`: base P/T track the enchanted artifact's mana value; skipped while it's already a creature. `PermanentIsArtifactPredicate` target filter |
| Aura that makes your creatures copies of the enchanted creature | `i/InfiniteReflection.java` | target(PermanentIsCreaturePredicate) + ON_ENTER_BATTLEFIELD OtherNontokenCreaturesBecomeCopyOfEnchantedCreatureEffect() + STATIC CreaturesEnterAsCopyOfSourceEffect(true, true) — the ETB half copies onto creatures already there (permanently), the STATIC half is the enters-as-copy replacement in `BattlefieldEntryService` pointed at the Aura's `attachedTo` and restricted to nontoken entrants |
| Aura that makes the enchanted creature a copy of a chosen creature | `m/MetamorphicAlteration.java` | target(PermanentIsCreaturePredicate) + ON_ENTER_BATTLEFIELD ChooseCreatureOnEnterEffect() + STATIC EnchantedCreatureIsCopyOfChosenCreatureEffect() — `AuraCopyService` prompts the choice from the Aura branch of `StackResolutionService`, applies the layer-1 card swap once the choice is made, and reverts it when the Aura leaves (WHILE_ATTACHED floating effect) |
| Aura with tap enchanted creature ability | `b/BurdenOfGuilt.java` | Activated TapPermanentsEffect(TapUntapScope.ENCHANTED) — aura's own ability pays {1} to tap the enchanted creature (no targeting, finds creature via attachedTo) |
| Aura phases out enchanted creature | `v/Vanishing.java` | Activated `{U}{U}` PhaseOutEffect(PhaseOutSubject.ATTACHED) — Aura's own ability; host + Aura leave for phasedOutPermanents (Aura indirectly); both return on host controller's next untap |
| Aura healing-counter shield | `f/Fylgja.java` | ETB `EnterWithCountersEffect(HEALING, Fixed(4))` + activated `RemoveCounterFromSourceCost(1, HEALING)` + `PreventDamageEffect.nextToEnchanted(1)` + `{2}{W}` `PutCountersOnSelfEffect(HEALING)` — remove healing counter to shield enchanted creature for next 1 damage this turn |
| Aura + self-bounce ability | `s/ShimmeringWings.java` | STATIC keyword + activated ReturnToHandEffect.self() |
| Aura boost + bounce enchanted | `s/SunClasp.java` | STATIC StaticBoostEffect(1, 3, ENCHANTED_CREATURE) + activated `{W}` ReturnToHandEffect.enchanted() — orphaned Aura dies as SBA |
| Aura boost + sacrifice for a bigger temporary boost | `b/BriarShield.java` | STATIC StaticBoostEffect(1, 1, ENCHANTED_CREATURE) + `SacrificeSelfCost` + `BoostEquippedCreatureUntilEndOfTurnEffect(Fixed(3), Fixed(3))` — the equipped-creature boost effect resolves against the attached permanent, captured at activation before the sacrifice severs the attachment |
| Grant keyword + upkeep counter + death return | `g/GlisteningOil.java` | STATIC GrantKeywordEffect(INFECT, ENCHANTED_CREATURE) + UPKEEP_TRIGGERED PutCounterOnReferencedPermanentEffect(CounterType.MINUS_ONE_MINUS_ONE) + ON_DEATH ReturnCardFromGraveyardEffect.builder().destination(HAND).filter(CardIsSelfPredicate).build() |
| Upkeep +1/+1 counter + sacrifice on combat | `p/PrimalCocoon.java` | UPKEEP_TRIGGERED PutCounterOnReferencedPermanentEffect(CounterType.PLUS_ONE_PLUS_ONE) + ON_ATTACK SacrificeSelfEffect + ON_BLOCK SacrificeSelfEffect |
| Doesn't untap + enchanted controller upkeep life loss | `n/NumbingDose.java` | STATIC DoesntUntapEffect.enchanted() + ENCHANTED_PERMANENT_CONTROLLER_UPKEEP_TRIGGERED EnchantedCreatureControllerLosesLifeEffect(1) — enchants artifact or creature, uses PermanentAnyOfPredicate target filter |
| Aura ETB taps enchanted creature and deals its power to you + untap lock | `b/BindTheMonster.java` | target(TargetFilters.creature()) + ON_ENTER_BATTLEFIELD TapPermanentsEffect(TapUntapScope.ENCHANTED) + EnchantedCreatureDealsPowerDamageToControllerEffect() + STATIC DoesntUntapEffect.enchanted() |
| ETB mass tap + permanent untap lock on a land subtype | `c/CurseOfMaritLage.java` | ON_ENTER_BATTLEFIELD TapPermanentsEffect(TapUntapScope.ALL_PERMANENTS, PermanentHasSubtypePredicate(ISLAND)) + STATIC MatchingPermanentsDoesntUntapEffect(same predicate) — taps all Islands on entry, then keeps them locked; one predicate reused by both slots |
| Color-wide untap lock + each-upkeep pay-to-untap | `m/MagneticMountain.java`, `t/ThelonsCurse.java` | STATIC MatchingPermanentsDoesntUntapEffect(blueCreature) + EACH_UPKEEP_TRIGGERED PayManaPerTappedCreatureToUntapEffect("{4}" / "{U}", blueCreature) — blue creatures don't untap; each player's upkeep, that player may pay the per-creature mana cost for tapped blue creatures they control to untap them (multi-permanent choice capped by colored-cost affordability; blueCreature = PermanentAllOfPredicate(IsCreature, ColorIn(BLUE))) |
| Enchant enchantment: upkeep damage to its controller | `f/Feedback.java` | target(PermanentIsEnchantmentPredicate) + ENCHANTED_PERMANENT_CONTROLLER_UPKEEP_TRIGGERED DealDamageToPlayersEffect(1, DamageRecipient.ENCHANTED_PERMANENT_CONTROLLER) — deals damage (not life loss) to the enchanted enchantment's controller at their upkeep; StepTriggerService bakes that controller as the stack targetId |
| Enchant non-Wall creature: +1/+0, upkeep counter that eventually kills the host | `c/ConsumingFerocity.java` | `target(PermanentPredicateTargetFilter(AllOf(IsCreature, Not(HasSubtype(WALL))), "Target must be a non-Wall creature"))` + STATIC `StaticBoostEffect(1, 0, ENCHANTED_CREATURE)` + UPKEEP_TRIGGERED `AddCounterToEnchantedCreatureThenDestroyAtThresholdEffect(CounterType.PLUS_ONE_PLUS_ZERO, 3)` — at three counters the creature deals its power to its own controller, then dies without regeneration |
| Enchant land: counter clock that destroys the land | `o/OrcishMine.java` | `target(PermanentPredicateTargetFilter(PermanentIsLandPredicate, "Target must be a land"))` + ON_ENTER_BATTLEFIELD `EnterWithCountersEffect(CounterType.ORE, Fixed(3))` + UPKEEP_TRIGGERED and ON_ENCHANTED_PERMANENT_TAPPED `RemoveCounterFromSourceThenDestroyEnchantedAtZeroEffect(CounterType.ORE, 2)` — "enters with three ore counters; at your upkeep and whenever enchanted land becomes tapped, remove one; when the last is removed, destroy enchanted land and this deals 2 damage to that land's controller" |
| Fading counter clock plus active-player tap choice | `t/TangleWire.java` | ON_ENTER_BATTLEFIELD `EnterWithCountersEffect(FADE, Fixed(4))` + own `UPKEEP_TRIGGERED` `RemoveCounterOrSacrificeSelfEffect(FADE)` and `TapPermanentsForAmountEffect(CountersOnSource(FADE), artifact-or-creature-or-land)` + `OPPONENT_UPKEEP_TRIGGERED` tap effect — the affected active player taps exactly the current fade-counter count when more matching untapped permanents are available; otherwise every available match is tapped |
| Enchant creature: upkeep pay-or-damage-and-tap | `m/MindWhip.java` | target(PermanentIsCreaturePredicate) + ENCHANTED_PERMANENT_CONTROLLER_UPKEEP_TRIGGERED `ForcedCostOrElseEffect.enchantedControllerMayPay(PayManaCost("{3}"), [DealDamageToPlayersEffect(2, ENCHANTED_PERMANENT_CONTROLLER), TapPermanentsEffect(ENCHANTED)])` — enchanted controller may pay {3}; decline deals 2 and taps the host |
| Enchant enchantment: upkeep pay-to-prevent damage | `p/PowerLeak.java` | target(PermanentIsEnchantmentPredicate) + ENCHANTED_PERMANENT_CONTROLLER_UPKEEP_TRIGGERED EnchantedControllerMayPayToPreventDamageEffect(2) — the enchanted enchantment's controller may pay any amount of mana to prevent that much of 2 damage dealt to them at their upkeep. Handler prompts that player (the stack targetId) for X (capped at the damage), pays generic mana, deals amount−X through the normal damage system |
| Enchant land: animate the land + self-return on its death (Genju) | `g/GenjuOfTheFields.java` | target(PermanentHasSubtypePredicate(PLAINS)) + `{2}` ActivatedAbility of `AnimatePermanentsEffect(2, 5, [SPIRIT], Set.of(), WHITE, Set.of(), GrantScope.ENCHANTED_PERMANENT, UNTIL_END_OF_TURN)` and `GrantEffectToTargetEffect.toEnchantedPermanent(ON_SELF_DEALS_DAMAGE, GainLifeEffect(new EventValue()), UNTIL_END_OF_TURN)` + ON_ENCHANTED_PERMANENT_PUT_INTO_GRAVEYARD `MayEffect(ReturnCardFromGraveyardEffect self -> HAND, returnAll)`. Both ability halves re-derive the enchanted land from the Aura's `attachedTo` at resolution (no targeting); the land stays a land, and the granted damage trigger is read off the permanent by `queueSourceDealsDamageReflections` |
| Genju that animates the land + grants it an activated pump ability | `g/GenjuOfTheFens.java` | target(PermanentHasSubtypePredicate(SWAMP)) + `{2}` ActivatedAbility of `AnimatePermanentsEffect(2, 2, [SPIRIT], Set.of(), BLACK, Set.of(), GrantScope.ENCHANTED_PERMANENT, UNTIL_END_OF_TURN)` and `GrantActivatedAbilityEffect(ActivatedAbility("{B}", BoostSelfEffect(1, 1)), GrantScope.ENCHANTED_PERMANENT, null, UNTIL_END_OF_TURN)` + the same ON_ENCHANTED_PERMANENT_PUT_INTO_GRAVEYARD self-return. `ENCHANTED_PERMANENT` is the one non-CONTINUOUS grant scope that skips the creature check, because the land only becomes a creature earlier in the same resolution |
| Enchant land: destroy on tap + self-return on death | `s/SpreadingAlgae.java` | target(PermanentHasSubtypePredicate(SWAMP)) + ON_ENCHANTED_PERMANENT_TAPPED DestroyReferencedPermanentEffect(ATTACHED) + ON_SELF_PUT_INTO_GRAVEYARD_FROM_BATTLEFIELD ReturnCardFromGraveyardEffect(self→HAND, returnAll). Tap destroys the enchanted land; the orphaned Aura dies and the battlefield-only graveyard slot returns it to hand |
| Enchant land: mill on tap | `c/ChronicFlooding.java` | target(TargetFilters.land()) + ON_ENCHANTED_PERMANENT_TAPPED MillEffect(3, MillRecipient.TARGET_PLAYER). "Whenever enchanted land becomes tapped, its controller mills three cards" — the collector bakes the tapped land's controller as `targetId`, so no player target is chosen |
| Enchant land: untaps during each other player's untap step | `u/UrbanBurgeoning.java` | `target(TargetFilters.land())` + STATIC `UntapAllPermanentsYouControlDuringEachOtherPlayersStepEffect(TurnStep.UNTAP, null, TapUntapScope.ENCHANTED)` — "Enchanted land has 'Untap this land during each other player's untap step.'" The `ENCHANTED` scope makes `UntapStepService` untap only the aura's host (keyed off the host's controller, not the aura's), unlike the `CONTROLLED` scope used by Seedborn Muse / Unwinding Clock |
| Enchant creature: -0/-2 counter on tap | `s/SpiritShackle.java` | target(PermanentIsCreaturePredicate) + ON_ENCHANTED_PERMANENT_TAPPED PutCounterOnReferencedPermanentEffect(CounterType.MINUS_ZERO_MINUS_TWO). "Whenever enchanted creature becomes tapped, put a -0/-2 counter on it" — the effect re-derives the enchanted creature from the Aura's `attachedTo` at resolution; the counter persists (accumulates on each tap) |
| Enchant opponent's creature: draw on tap | `b/Betrayal.java` | `target(TargetFilters.creatureAnOpponentControls())` + ON_ENCHANTED_PERMANENT_TAPPED `DrawCardEffect(1)`. "Whenever enchanted creature becomes tapped, you draw a card" — collector is registered for `DrawCardEffect` on this slot |
| Enchant creature: pay-or-damage on tap | `s/Seizures.java` | target(PermanentIsCreaturePredicate) + ON_ENCHANTED_PERMANENT_TAPPED ForcedCostOrElseEffect.enchantedControllerMayPay(PayManaCost("{3}"), [DealDamageToPlayersEffect(3, ENCHANTED_PERMANENT_CONTROLLER)]). "Whenever enchanted creature becomes tapped, deals 3 damage to that creature's controller unless that player pays {3}" — collector bakes controller as targetId; use ENCHANTED_PERMANENT_CONTROLLER (ForcedCostOrElse fallback) |
| Aura +2/+0 + enchanted-controller upkeep -0/-1 counter | `e/EssenceFlare.java` | STATIC StaticBoostEffect(2, 0, ENCHANTED_CREATURE) + ENCHANTED_PERMANENT_CONTROLLER_UPKEEP_TRIGGERED PutCounterOnReferencedPermanentEffect(MINUS_ZERO_MINUS_ONE) — Unstable Mutation pattern with toughness-only counters |
| Enchant opponent's artifact: modal tap trigger | `r/RelicBind.java` | target(PermanentAllOf(PermanentIsArtifactPredicate, PermanentNot(PermanentControlledBySourceControllerPredicate))) + ON_ENCHANTED_PERMANENT_TAPPED RelicBindTapEffect(). "Whenever enchanted artifact becomes tapped, choose one — 1 damage to target player/planeswalker; or target player gains 1 life." Modal *triggered* ability: no cast-time modal machinery exists for triggers, so `RelicBindTapEffectHandler` prompts the mode as it resolves (ChoiceContext.RelicBindModeChoice list pick), then routes the chosen mode's targeted effect (DealDamageToTargetPlayerOrPlaneswalkerEffect / TargetPlayerGainsLifeEffect) through the shared MayAbilityTriggerTarget target flow |
| Enchanted permanent death trigger (gain life) | `v/ViridianHarvest.java` | ON_ENCHANTED_PERMANENT_PUT_INTO_GRAVEYARD GainLifeEffect(6) — enchants artifact, aura controller gains life when enchanted permanent is put into graveyard |
| Aura self-return on enchanted creature death | `s/ScreamsFromWithin.java` | target(TargetFilters.creature()) + STATIC StaticBoostEffect(-1, -1, ENCHANTED_CREATURE) + ON_ENCHANTED_PERMANENT_PUT_INTO_GRAVEYARD ReturnSourceAuraToCreatureOnDeathEffect() — returns the Aura from its controller's graveyard and chooses a legal creature to enchant |
| Enchant black creature: granted life-CU + fixed death drain | `d/Decomposition.java` | target(PermanentAllOf(IsCreature, ColorIn(BLACK))) + STATIC GrantEffectEffect(CumulativeUpkeepEffect.life(1), ENCHANTED_CREATURE) + ON_ENCHANTED_PERMANENT_PUT_INTO_GRAVEYARD EnchantedCreatureControllerLosesLifeEffect(2) — the enchanted creature's own controller pays the CU (GrantedUpkeepEffectSupport); declining sacrifices it, which then triggers the 2-life drain |
| Enchant creature: death damages its controller = toughness | `c/CreatureBond.java` | target(PermanentIsCreaturePredicate) + ON_ENCHANTED_PERMANENT_PUT_INTO_GRAVEYARD DealDamageToPlayersEffect(0, DamageRecipient.ENCHANTED_PERMANENT_CONTROLLER) — deals damage (not life loss) equal to the dying creature's toughness to its controller; DeathTriggerCollectorService bakes the last-known toughness as the amount and that controller as the stack targetId. Damage sibling of Banewasp Affliction (life loss) |
| Enchant your creature: death drains target player by power | `d/DyingWish.java` | bare `target(TargetFilters.creatureYouControl())` + ON_ENCHANTED_PERMANENT_PUT_INTO_GRAVEYARD `SequenceEffect.of(LoseLifeEffect(EventValue, TARGET_PLAYER), GainLifeEffect(EventValue))` — the death collector snapshots last-known power into the trigger event value and chooses the player when the trigger is put on the stack |
| Enchant creature: death drains power / gains toughness | `d/DeathWatch.java` | target(creature) + ON_ENCHANTED_PERMANENT_PUT_INTO_GRAVEYARD EnchantedCreatureDiesLoseLifeEqualPowerGainLifeEqualToughnessEffect() — controller loses life = LKI power, you gain life = LKI toughness on one stack entry |
| Reanimation Aura (enchant creature card in a graveyard) | `a/AnimateDead.java` | SPELL ReturnCardFromGraveyardEffect.builder().destination(BATTLEFIELD).source(ALL_GRAVEYARDS).filter(CardTypePredicate(CREATURE)).targetGraveyard(true) + STATIC StaticBoostEffect(-1, 0, ENCHANTED_CREATURE) + ON_SELF_LEAVES_BATTLEFIELD SacrificeEnchantedCreatureOnLeaveEffect(). The graveyard-targeting SPELL effect makes the Aura target a creature card in a graveyard at cast (stack entry target zone = GRAVEYARD); resolveEnchantmentSpell reanimates it under the Aura's controller and attaches the Aura. When the Aura leaves, the enchanted creature is sacrificed |
| Non-Aura reanimation enchantment (becomes Aura on ETB) | `n/Necromancy.java` | STATIC FlashCastWithCleanupSacrificeEffect + ON_ENTER_BATTLEFIELD BecomeAuraReanimateFromGraveyardEffect + ON_SELF_LEAVES_BATTLEFIELD SacrificeEnchantedCreatureOnLeaveEffect. Not an Aura when cast (no cast-time target); ETB targets a creature card in any graveyard, becomes an Aura, reanimates under your control, and attaches. Empty graveyards → stays a non-Aura enchantment |
| Reanimation Aura + enter tapped + doesn't untap + pay to untap | `d/DanceOfTheDead.java` | Same as Animate Dead but SPELL `.enterTapped(true)` (resolveReanimationAura reads the flag), STATIC `StaticBoostEffect(1, 1, ENCHANTED_CREATURE)` + `DoesntUntapEffect.enchanted()`, and ENCHANTED_PERMANENT_CONTROLLER_UPKEEP_TRIGGERED `MayPayManaEffect("{1}{B}", UntapPermanentsEffect(TapUntapScope.ENCHANTED), …, MayPayPayer.ENCHANTED_CONTROLLER)` (Paralyze pay-to-untap) |
| Aura: doesn't untap + upkeep random-discard to untap | `a/Apathy.java` | STATIC `DoesntUntapEffect.enchanted()` + ENCHANTED_PERMANENT_CONTROLLER_UPKEEP_TRIGGERED `MayPayManaEffect(null, DiscardRandomCardThenUntapEnchantedCreatureEffect(), …, MayPayPayer.ENCHANTED_CONTROLLER)` — Dance of the Dead's pay-to-untap shape with a **null** mana cost, so the "cost" is the wrapped effect's random discard; an empty hand means no discard and no untap |
| Enchanted permanent LTB trigger (conditional draw) | `c/CuratorsWard.java` | STATIC GrantKeywordEffect(HEXPROOF, ENCHANTED_CREATURE) + ON_ENCHANTED_PERMANENT_LEAVES_BATTLEFIELD EnchantedPermanentLeavesConditionalEffect(CardIsHistoricPredicate, DrawCardEffect(2)) — enchants any permanent, grants hexproof, draws 2 when enchanted permanent leaves battlefield if historic |
| Enchanted creature LTB → its controller sacrifices | `f/FuneralMarch.java` | target creature + ON_ENCHANTED_PERMANENT_LEAVES_BATTLEFIELD EnchantedControllerSacrificesCreatureOnLeaveEffect() — when the enchanted creature leaves, its controller (baked at trigger time, not the Aura controller) sacrifices a creature of their choice |
| Enchanted creature dealt damage trigger | `s/SpitefulShadows.java` | ON_ENCHANTED_CREATURE_DEALT_DAMAGE EnchantedCreatureDealsDamageEqualToDealtDamageToControllerEffect — enchanted creature deals damage equal to amount dealt to its controller |
| Enchanted creature dealt damage → Aura burns its controller | `b/BindingAgony.java` | target creature + ON_ENCHANTED_CREATURE_DEALT_DAMAGE `EnchantedCreatureDealsDamageEqualToDealtDamageToControllerEffect(true)` — same as Spiteful Shadows but the Aura, not the creature, is the damage source |
| Boost + destroy host when it's targeted | `s/SpinalGraft.java` | target creature + STATIC `StaticBoostEffect(3, 3, ENCHANTED_CREATURE)` + ON_BECOMES_TARGET_OF_SPELL_OR_ABILITY `DestroyReferencedPermanentEffect(ATTACHED, true)` — destroys the enchanted creature (not the Aura, unlike Ice Cage); can't be regenerated |
| Enchanted creature dealt damage → destroy it | `m/MortalWound.java` | target creature + ON_ENCHANTED_CREATURE_DEALT_DAMAGE `DestroyReferencedPermanentEffect(ATTACHED)` — any damage (combat or noncombat) destroys the host; regeneration applies |
| Enchanted creature deals damage to you → reflect to its controller | `b/Backfire.java` | target creature + ON_ENCHANTED_CREATURE_DEALS_DAMAGE_TO_YOU EnchantedCreatureDealsDamageEqualToDealtDamageToControllerEffect — "whenever enchanted creature deals damage to you, this Aura deals that much to that creature's controller." Reuses the Spiteful Shadows resolution effect on a different slot; because the Aura is on its controller's battlefield the trigger only fires when the damaged player is the Aura's controller (combat + noncombat) |
| Enchanted-attacker unblocked → assign no damage + random discard | `c/CloakOfConfusion.java` | target(creature you control) + ON_ENCHANTED_CREATURE_ATTACKS_UNBLOCKED MayEffect(AssignNoCombatDamageAndDefendingPlayerDiscardsEffect) — "you may have it assign no combat damage this turn; if you do, defending player discards a card at random." Trigger bakes the enchanted attacker as `sourcePermanentId` and defender as `targetId` |
| Aura grants the enchanted creature a targeted attack trigger | `g/GhostlyTouch.java` | target(TargetFilters.creature()) + `ON_ATTACK` MayEffect(TapOrUntapTargetPermanentEffect) — "Enchanted creature has 'Whenever this creature attacks, you may tap or untap target permanent.'" Put the granted trigger in the Aura's own `ON_ATTACK` slot: `CombatTriggerService.checkAuraTriggersForCreature` scans attached permanents when the creature attacks, and a non-NONE `targetSpec()` routes it through `PermanentChoiceContext.AttackTriggerTarget` for target selection before it hits the stack |
| Any creature dealt damage → destroy it | `d/DeathPitsOfRath.java` | ON_ANY_CREATURE_DEALT_DAMAGE DestroyTargetPermanentEffect(true) — global enchantment; fires once per damaged creature (combat + noncombat), queued entry auto-targets the damaged creature and destroys it, no regeneration |
| Chosen-color creature damage punisher | `m/MangarasEquity.java` | ON_ENTER_BATTLEFIELD ChooseColorOnEnterEffect(BLACK, RED) + UPKEEP_TRIGGERED ForcedCostOrElseEffect(PayManaCost("{1}{W}"), List.of(SacrificeSelfEffect()), true) + ON_CREATURE_DEALS_DAMAGE_TO_YOU_OR_YOUR_PERMANENT ReflectDamageToChosenColorCreatureEffect(white creature filter) — whenever a creature of the chosen color damages you or a white creature you control, deals that much back to that creature |
| Reflect a color's damage to its controller | `j/Justice.java` | UPKEEP_TRIGGERED ForcedCostOrElseEffect(PayManaCost("{W}{W}"), List.of(SacrificeSelfEffect()), true) + ON_ANY_SOURCE_DEALS_DAMAGE ReflectSourceDamageToItsControllerEffect(CardColor.RED) — global watcher; whenever any red creature or spell deals damage, deals that much to the source's controller (all simultaneous damage from one source summed into one reflection) |
| Per-color-mana-symbol Swamp sacrifice tax | `d/Drought.java` | UPKEEP_TRIGGERED ForcedCostOrElseEffect(PayManaCost("{W}{W}"), List.of(SacrificeSelfEffect()), true) + STATIC AdditionalSacrificePerManaSymbolEffect(BLACK, SWAMP filter, taxes spells+abilities) — symmetric non-mana tax; cast pays via imposedSacrificePermanentIds; abilities synthesize SacrificeMultiplePermanentsCost |
| Grant all artifacts/creatures/enchantments an upkeep pay-or-sacrifice ability | `e/EnergyFlux.java`, `p/PendrellMists.java`, `a/AuraFlux.java` | STATIC AllPermanentsUpkeepSacrificeUnlessPayEffect(PermanentIsArtifactPredicate, "{2}") / (PermanentIsCreaturePredicate, "{1}") / (PermanentIsEnchantmentPredicate, "{2}", excludeSource=true) — global enchantment; StepTriggerService pushes one ForcedCostOrElseEffect(PayManaCost, [SacrificeSelfEffect], true) per matching permanent the active player controls (sourced at that permanent) during their upkeep. Fires regardless of who controls the enchantment; `excludeSource=true` models Aura Flux's "other enchantments" wording |
| Symmetric graveyard-fed token army, wiped each end step | `t/TombstoneStairwell.java` | UPKEEP_TRIGGERED CumulativeUpkeepEffect("{1}{B}") + EACH_UPKEEP_TRIGGERED EachPlayerCreatesTokenEffect(CreateTokenEffect(CardsInGraveyard(CardTypePredicate(CREATURE), CONTROLLER), "Tombspawn", 2, 2, BLACK, ZOMBIE, HASTE), recordAsCreatedWithSource=true) + END_STEP_TRIGGERED and ON_SELF_LEAVES_BATTLEFIELD DestroyTokensCreatedWithSourceEffect(true) — CONTROLLER-scoped count is re-evaluated per creating player, so each player counts their own graveyard; World supertype handled by the world rule in StateBasedActionService |
| End-step land sacrifice for mana taps + subtype damage | `d/Desolation.java` | END_STEP_TRIGGERED PlayersWhoTappedLandForManaSacrificeLandDamageIfSubtypeEffect(PLAINS, 2) — tracks taps in GameData.playersWhoTappedLandForManaThisTurn |
| CU enchantment with an escalating pay-life removal ability | `t/Tornado.java` | UPKEEP_TRIGGERED CumulativeUpkeepEffect("{G}") + ActivatedAbility("{2}{G}", [PayLifeCost.perSourceCounter(3, VELOCITY), DestroyTargetPermanentEffect(false), PutCountersOnSelfEffect(VELOCITY)], PermanentTruePredicate filter, maxActivationsPerTurn=1) — each use adds a velocity counter, so the *next* activation costs 3 more life; the first is free |
| CU with an unpaid-trigger sweep keyed to age counters | `h/HeartOfBogardan.java` | UPKEEP_TRIGGERED `CumulativeUpkeepEffect.withUnpaidEffects("{2}", [DealDamageToTargetAndTheirCreaturesEffect(Sum(Scaled(CountersOnSource(AGE), 2), Fixed(-2)))])` — "when a player doesn't pay this enchantment's cumulative upkeep" rides the unpaid branch; the amount is frozen to a `Fixed` before the sacrifice (CR 608.2h) and the damage lands on the source controller's opponent, since the fallback path chooses no target |
| CU + draw-step MV wipe keyed to age counters | `w/WaveOfTerror.java` | UPKEEP_TRIGGERED CumulativeUpkeepEffect("{1}") + DRAW_TRIGGERED DestroyAllPermanentsEffect(AllOf(IsCreature, ManaValueEqualsSourceCounters(AGE)), cantRegen) — the draw step follows the upkeep, so the age counter placed that turn already counts |
| CU + rust counters on target opponent's artifacts then global MV wipe | `c/Corrosion.java` | UPKEEP_TRIGGERED CumulativeUpkeepEffect("{1}") + target(opponent).UPKEEP_TRIGGERED SequenceEffect(PutCounterOnEachMatchingPermanentEffect(RUST, 1, IsArtifact, TARGET_PLAYER), DestroyAllPermanentsEffect(AllOf(IsArtifact, HasCounters(RUST), ManaValueAtMostOwnCounters(RUST)), cantRegen)) + ON_SELF_LEAVES_BATTLEFIELD RemoveAllCountersOfTypeFromAllPermanentsEffect(RUST) — puts rust only on target opponent's artifacts; destroys any artifact with rust whose MV ≤ rust count; LTB strips all rust |
| Grant matching creatures cumulative upkeep | `b/BreathOfDreams.java` | UPKEEP_TRIGGERED CumulativeUpkeepEffect("{U}") + STATIC GrantEffectEffect(CumulativeUpkeepEffect("{1}"), ALL_CREATURES, PermanentColorInPredicate(GREEN)) — continuous grant; GrantedUpkeepEffectSupport feeds granted CU into upkeep collection |
| Curse (enchant player + static debuff) | `c/CurseOfDeathsHold.java` | STATIC StaticBoostEffect(-1, -1, GrantScope.ENCHANTED_PLAYER_CREATURES) — enchant player aura (auto-detected from CURSE subtype via `isEnchantPlayer()`), creatures enchanted player controls get -1/-1 |
| Curse (enchant player + neuter creatures + ability lock) | `o/OverwhelmingSplendor.java` | STATIC LosesAllAbilitiesEffect(GrantScope.ENCHANTED_PLAYER_CREATURES) + SetBasePowerToughnessEffect(1, 1, GrantScope.ENCHANTED_PLAYER_CREATURES) + EnchantedPlayerCantActivateNonManaNonLoyaltyAbilitiesEffect() — enchant player curse; creatures enchanted player controls lose all abilities and are base 1/1, and the enchanted player can activate only mana/loyalty abilities |
| Curse (enchant player + combat trigger) | `c/CurseOfStalkedPrey.java` | ON_COMBAT_DAMAGE_TO_PLAYER PutCountersOnSourceEffect(1,1,1) — enchants player (auto-detected from CURSE subtype), whenever a creature deals combat damage to enchanted player, put +1/+1 counter on that creature. CombatDamageService checks curses attached to defending player |
| Modal upkeep: exact life lose-game then ±1 life | `t/Triskaidekaphobia.java` | UPKEEP_TRIGGERED ChooseOneEffect of two ExactLifeLoseGameThenAdjustLifeEffect(13, ±1) modes — mode picked as trigger resolves; simultaneous lose-at-13 first (both ⇒ draw via declareDraw), then each player gains/loses 1 if game continues |
| Modal upkeep: "choose one that hasn't been chosen" | `d/DemonicPact.java` | UPKEEP_TRIGGERED `ChooseModeNotYetChosenEffect` of four modes (any-target damage + gain 4 / target opponent discards 2 / draw 2 / `ControllerLosesGameEffect`) — mode AND its targets picked as the trigger goes on the stack, each mode consumed on `Permanent.chosenModeLabels`, so the fourth upkeep forces whatever is left. The discard mode carries its own "target opponent" `targetFilter`. Test: `handleListChoice` then the mode's own target answer |
| Curse (enchant player + upkeep damage) | `c/CurseOfThePiercedHeart.java` | ENCHANTED_PLAYER_UPKEEP_TRIGGERED DealDamageToPlayersEffect(1, DamageRecipient.ENCHANTED_PLAYER) — enchant player curse; at enchanted player's upkeep, deals 1 damage to that player (targetId baked to the enchanted player by StepTriggerService) |
| Curse (enchant player + upkeep damage = attached count) | `c/CurseOfThirst.java` | ENCHANTED_PLAYER_UPKEEP_TRIGGERED DealDamageToPlayersEffect.enchantedAttachedCount(new PermanentHasSubtypePredicate(CardSubtype.CURSE)) — at enchanted player's upkeep, deals damage equal to the number of permanents (here, Curses) attached to that player matching the predicate |
| Curse (enchant player + upkeep draws for any number of other players) | `c/CurseOfSurveillance.java` | ENCHANTED_PLAYER_UPKEEP_TRIGGERED EachTargetPlayerDrawsCardsEqualToAttachedCountEffect(new PermanentHasSubtypePredicate(CardSubtype.CURSE)) — the controller chooses zero or more target players other than the enchanted player; each target draws for the current Curse count attached to the enchanted player |
| Curse (enchant player + upkeep graveyard exile) | `c/CurseOfOblivion.java` | ENCHANTED_PLAYER_UPKEEP_TRIGGERED ExileGraveyardCardsEffect(2, GraveyardExileScope.OWN) — enchant player curse; at enchanted player's upkeep, that player exiles 2 cards from their graveyard (StepTriggerService bakes the enchanted player's id as affectedPlayerId) |
| Curse (enchant player + upkeep mill) | `c/CurseOfTheBloodyTome.java` | ENCHANTED_PLAYER_UPKEEP_TRIGGERED MillEffect(2, TARGET_PLAYER) — enchant player curse; at enchanted player's upkeep, that player mills 2 cards |
| Curse (enchant player + each-end-step scaling mill) | `f/FrayingSanity.java` | ENCHANTED_PLAYER_END_STEP_TRIGGERED MillEffect(CardsPutIntoGraveyardByTargetPlayerThisTurn(), TARGET_PLAYER) — enchant player curse; at EACH end step (any player's turn) the enchanted player mills X = the number of non-token cards put into their graveyard this turn. New slot fires every end step (not gated to the enchanted player's turn); the milled cards feed the same tracker, so stacked copies compound (4→8→16) |
| Curse (enchant player + upkeep sacrifice-or-lose-life) | `c/CruelReality.java` | ENCHANTED_PLAYER_UPKEEP_TRIGGERED SacrificePermanentOrLoseLifeEffect(PermanentAnyOfPredicate(creature, planeswalker), 5) — enchant player curse; at enchanted player's upkeep, that player sacrifices a creature or planeswalker of their choice, or loses 5 life if they control none (edict-with-punisher, acts on stack entry targetId) |
| Curse (enchant player + upkeep three-way punisher) | `t/TormentOfScarabs.java` | ENCHANTED_PLAYER_UPKEEP_TRIGGERED LoseLifeUnlessSacrificeNonlandOrDiscardEffect(3) — enchant player curse; at enchanted player's upkeep, that player loses 3 life unless they sacrifice a nonland permanent of their choice or discard a card. Single-player, single-pass slice of Torment of Hailfire, reusing the `ChoiceContext.TormentPenaltyChoice` / `PermanentChoiceContext.TormentSacrifice` plumbing; options pruned to what the enchanted player can do (life always offered); acts on stack entry targetId |
| Curse (enchant player + copy their instants/sorceries) | `c/CurseOfEchoes.java` | ON_ANY_PLAYER_CASTS_SPELL CopySpellForEachOtherPlayerEffect(true, StackEntryAllOfPredicate(StackEntryTypeInPredicate(INSTANT_SPELL, SORCERY_SPELL), StackEntryControlledByEnchantedPlayerPredicate)) — enchant player curse; reuses Hive Mind's copy machinery, the filter restricts firing to instant/sorcery spells cast by the enchanted player; each other player *may* copy and may choose new targets |
| Curse (enchant player + drain when their creature enters) | `t/TrespassersCurse.java` | ON_ENCHANTED_PLAYER_CREATURE_ENTERS_BATTLEFIELD LoseLifeEffect(1, TARGET_PLAYER) + GainLifeEffect(1) — enchant player curse; whenever a creature the enchanted player controls enters, that player loses 1 life and you gain 1 life. `TriggerCollectionService.checkEnchantedPlayerCreatureEntersTriggers` scans Curses attached to the entering creature's controller and bakes the enchanted player as the non-targeting targetId (drain reused, no new effect) |
| Enchanted creature LTB â†’ its controller sacrifices | `f/FuneralMarch.java` | target creature + ON_ENCHANTED_PERMANENT_LEAVES_BATTLEFIELD EnchantedControllerSacrificesCreatureOnLeaveEffect() â€” when the enchanted creature leaves, its controller (baked at trigger time, not the Aura controller) sacrifices a creature of their choice |
| Enchanted creature dealt damage â†’ Aura burns its controller | `b/BindingAgony.java` | target creature + ON_ENCHANTED_CREATURE_DEALT_DAMAGE `EnchantedCreatureDealsDamageEqualToDealtDamageToControllerEffect(true)` â€” same as Spiteful Shadows but the Aura, not the creature, is the damage source |
| Enchanted creature deals damage to you â†’ reflect to its controller | `b/Backfire.java` | target creature + ON_ENCHANTED_CREATURE_DEALS_DAMAGE_TO_YOU EnchantedCreatureDealsDamageEqualToDealtDamageToControllerEffect â€” "whenever enchanted creature deals damage to you, this Aura deals that much to that creature's controller." Reuses the Spiteful Shadows resolution effect on a different slot; because the Aura is on its controller's battlefield the trigger only fires when the damaged player is the Aura's controller (combat + noncombat) |
| Enchanted-attacker unblocked â†’ assign no damage + random discard | `c/CloakOfConfusion.java` | target(creature you control) + ON_ENCHANTED_CREATURE_ATTACKS_UNBLOCKED MayEffect(AssignNoCombatDamageAndDefendingPlayerDiscardsEffect) â€” "you may have it assign no combat damage this turn; if you do, defending player discards a card at random." Trigger bakes the enchanted attacker as `sourcePermanentId` and defender as `targetId` |
| Any creature dealt damage â†’ destroy it | `d/DeathPitsOfRath.java` | ON_ANY_CREATURE_DEALT_DAMAGE DestroyTargetPermanentEffect(true) â€” global enchantment; fires once per damaged creature (combat + noncombat), queued entry auto-targets the damaged creature and destroys it, no regeneration |
| Modal upkeep: exact life lose-game then Â±1 life | `t/Triskaidekaphobia.java` | UPKEEP_TRIGGERED ChooseOneEffect of two ExactLifeLoseGameThenAdjustLifeEffect(13, Â±1) modes â€” mode picked as trigger resolves; simultaneous lose-at-13 first (both â‡’ draw via declareDraw), then each player gains/loses 1 if game continues |
| Global block watcher: weaker combatant dies | `n/NoQuarter.java` | Two ON_ANY_CREATURE_BECOMES_BLOCKED registrations of `DestroyWeakerBlockParticipantEffect` (`BlockParticipant.BLOCKER` and `BlockParticipant.ATTACKER`) — "whenever a creature becomes blocked by a creature with lesser power, destroy the blocking creature" plus the mirrored attacker clause. The slot fires once per attacker/blocker pair on every battlefield, so it watches combats its controller isn't part of |
| Global block watcher: matching attackers boost blockers | `r/RighteousIndignation.java` | ON_ANY_CREATURE_BECOMES_BLOCKED `BoostBlockerWhenAttackerMatchesEffect(PermanentColorInPredicate(BLACK, RED), 1, 1)` — evaluates the attacking creature's colors at declaration and boosts the blocking creature once per matching attacker/blocker pair |
| Cast a colored spell by discarding a card that shares its color | `d/DreamHalls.java` | STATIC `SharedColorDiscardAlternativeCostEffect()` — symmetric battlefield permission; the selected hand card is a separate cast-request choice from any printed additional discard cost and is moved to the graveyard when paid |
| Global shared-color creature damage prevention | `w/WellLaidPlans.java` | STATIC `PreventDamageToCreaturesByCreaturesOfSharedColorEffect()` — prevents combat and noncombat damage dealt to a creature by another creature when their current colors overlap |


| Controller-scoped combat attack limit | `c/Crawlspace.java` | STATIC MaximumCombatCreaturesEffect(2, Integer.MAX_VALUE, CONTROLLER) — no more than two creatures can attack the effect's controller directly; attacks against that player's planeswalkers are unrestricted |
| Planeswalker-scoped combat attack limit | `t/TheEternalWanderer.java` | STATIC MaximumCombatCreaturesEffect(1, Integer.MAX_VALUE, SOURCE_PERMANENT) — no more than one creature can attack the effect planeswalker each combat |
