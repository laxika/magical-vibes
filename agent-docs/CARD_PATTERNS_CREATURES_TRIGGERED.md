# Card Patterns: Triggered Creatures
- Attack/block any-player graveyard exile + no combat damage: `c/CarrionRats.java` uses `ON_ATTACK` + `ON_BLOCK` with `AnyPlayerMayExileCardFromGraveyardAndAssignNoCombatDamageEffect()`; `c/CarrionWurm.java` uses the parameterized `(3)` form.
- Spell cast trigger — another player may pay the spell's mana cost to counter it: `i/IceCave.java` uses `ON_ANY_PLAYER_CASTS_SPELL CounterUnlessOtherPlayerPaysManaCostOnSpellCastEffect()`; the collector snapshots the printed mana cost and chosen X, then queues `MayPayManaEffect` with `MayPayPayer.ANY_OTHER_PLAYER` targeting the cast spell.

| Nontoken creature enters → controller creates copy; nontoken creature leaves → exile same-name tokens; source leaves → exile its created tokens | `d/DualNature.java` | ON_ANY_OTHER_CREATURE_ENTERS_BATTLEFIELD `TriggeringCardConditionalEffect(CardNotPredicate(CardIsTokenPredicate), CreateTokenCopyOfTargetPermanentEffect.trackedForTargetController())` + ON_ANOTHER_CREATURE_LEAVES_BATTLEFIELD `ExileTokensWithSameNameAsLeavingCreatureEffect()` + ON_SELF_LEAVES_BATTLEFIELD `ExileTokensCreatedWithSourceEffect()` |

| Each player's upkeep: active player mills and creates tokens by milled mana value | `i/InfernalGenesis.java` | EACH_UPKEEP_TRIGGERED `MillActivePlayerAndCreateTokensByManaValueEffect(CreateTokenEffect)` — uses the active player captured on the trigger entry, creates under that player's control, and creates nothing when the milled card has mana value 0 |

| Same-name graveyard instant/sorcery cast on hand spell | `h/HarnessTheStorm.java` | ON_CONTROLLER_CASTS_SPELL `CastSameNameCardFromGraveyardOnSpellCastEffect()` — accepts only an instant or sorcery cast from hand, chooses the matching controller-graveyard card as the trigger is queued, then the existing graveyard-cast effect offers its normal-cost cast |
| Attack: tap any number of creatures for self-boost | `l/LedevChampion.java` | `ON_ATTACK TapCreaturesBoostSelfEffect()` — controller chooses any number of untapped creatures they control at resolution; the source gets +1/+1 until end of turn per creature tapped |

| On death + reveal until bounded nonland + optional free cast | `t/TreasureKeeper.java` | ON_DEATH `RevealUntilNonlandCardWithManaValueAndCastEffect(3)` — reveals through the first nonland card with mana value at most 3, offers that card through the shared free-cast library interaction, and bottoms all other revealed cards randomly |

| Heroic may-pay X counters | `h/HeroOfLeinaTower.java` | ON_CONTROLLER_CASTS_SPELL `SpellCastTriggerEffect(null, List.of(PayXManaPutCountersOnSelfEffect(CounterType.PLUS_ONE_PLUS_ONE)), StackEntryTargetsSourcePredicate())` — on resolution, choose and pay X, then put X +1/+1 counters on the source; X=0 declines |

| Cards exiled during your turn → once-per-turn +1/+1 counter | `s/StonebindersFamiliar.java` | ON_CONTROLLER_CARDS_EXILED_DURING_TURN `OncePerTurnTriggerEffect(PutCountersOnSelfEffect(CounterType.PLUS_ONE_PLUS_ONE))` — the active player's exile event is watched regardless of the card's origin |

All paths relative to `cards/`.

| Graveyard ally creature dies -> may pay to return self to hand | `f/FuriousForebear.java` | `GRAVEYARD_ON_ALLY_CREATURE_DIES` + `MayPayManaEffect("{1}{W}", ReturnSourceCardFromGraveyardToOwnerHandEffect(), ...)`; the graveyard collector skips a source card that dies in the same event |

## Triggered creatures

| Pattern | Reference | Notes |
|---------|-----------|-------|
| Blocks/becomes-blocked by a low-power creature, destroy self at end of combat | `a/ArrogantBloodlord.java` | ON_BLOCK + ON_BECOMES_BLOCKED (PER_BLOCKER) `TriggeringPermanentConditionalEffect(PermanentPowerAtMostPredicate(1), DestroySelfAtEndOfCombatEffect())` - the combat opponent's power is checked when blockers are declared, and only the source is scheduled for destruction |
| Continuous creature-type and self-counter trigger grant | `m/MephidrossVampire.java` | STATIC `GrantSubtypeEffect(VAMPIRE, ALL_OWN_CREATURES)` + `GrantTriggeredAbilityEffect(ON_ALLY_CREATURE_DEALS_DAMAGE_TO_CREATURE, PutCountersOnSourceEffect(1,1,1), ALL_OWN_CREATURES)`; the damage collector adds the continuously granted trigger only for the creature that dealt the damage |
| This or another creature you control with flying enters (may gain life = its power) | `a/ArchonOfRedemption.java` | `ON_SELF_OR_ALLY_CREATURE_ENTERS_BATTLEFIELD` + `TriggeringPermanentConditionalEffect(PermanentHasKeywordPredicate(FLYING), MayEffect(GainLifeEqualToPowerEffect()))` — the collector preserves the entering permanent as resolution context so the optional life gain uses its effective power |
| Turns up with self or another controlled creature | `p/PineWalker.java` | `ON_SELF_OR_ALLY_CREATURE_TURNS_FACE_UP` + `UntapPermanentsEffect(TapUntapScope.SELF)`; the turn-face-up collector binds the turned creature as the non-targeting effect subject |
| Ally permanent transforms into a non-Human creature → Wolf | `c/CultOfTheWaxingMoon.java` | `ON_ALLY_PERMANENT_TRANSFORMS` + `TriggeringCardConditionalEffect(CardAllOfPredicate(CREATURE, CardNotPredicate(CardSubtypePredicate(HUMAN))), CreateTokenEffect(Wolf, 2/2, GREEN))`; the transform collector evaluates the destination face and only scans the transformed permanent's controller's battlefield |
| On death | `b/BogardanFirefiend.java` | ON_DEATH DealDamageToTargetCreatureEffect |
| Becomes the target of a spell or ability — damage its controller | `r/Retromancer.java` | `ON_BECOMES_TARGET_OF_SPELL_OR_ABILITY DealDamageToTriggeringSpellControllerEffect(3)` — a `TriggeringSpellReferencingEffect` queues the triggering spell or ability as an internal STACK reference, then deals damage (not life loss) to its controller |
| Sliver grant of optional ETB exile | `c/ConstrictingSliver.java` | STATIC `GrantTriggeredAbilityEffect(ON_ENTER_BATTLEFIELD, MayEffect(ExileTargetPermanentUntilSourceLeavesEffect(false, creature-opponent predicate)), ALL_OWN_CREATURES, PermanentHasSubtypePredicate(SLIVER))`; the granted ETB is collected from each entering Sliver and its target predicate is applied at resolution |
| Two-sided global grant of an optional ETB damage trigger | `c/ClashOfRealities.java` | two STATIC `GrantTriggeredAbilityEffect(ON_ENTER_BATTLEFIELD, MayEffect(DealDamageToTargetCreatureEffect(3, typed predicate)), ALL_CREATURES, subtype filter)` — one for Spirits shooting non-Spirits, its mirror for non-Spirits shooting Spirits. `ALL_CREATURES` is controller-agnostic; the damage effect's own predicate must include `PermanentIsCreaturePredicate` because the may-ability target path filters by the effect predicate alone |
| On death distribute +1/+1 among controlled creatures | `v/VastwoodHydra.java` | EnterWithCountersEffect(PLUS_ONE_PLUS_ONE, XValue) + ON_DEATH DistributeCountersAmongCreaturesOnDeathEffect(PLUS_ONE_PLUS_ONE) — "you may" at resolution; division via pendingETBDamageAssignments (non-targeting) |
| On death distribute a fixed number of +1/+1 among any creatures | `j/JuganTheRisingStar.java` | ON_DEATH DistributeCountersAmongCreaturesOnDeathEffect.fixedAmongAnyCreatures(PLUS_ONE_PLUS_ONE, 5) — "you may" at resolution; division via pendingETBDamageAssignments, any creature eligible |
| On death distribute -1/-1 counters among one to three target creatures, then remove them next end step | `s/ShamblingSwarm.java` | Targeted `DistributeCountersAmongTargetsEffect.chosenUntilNextEndStep(MINUS_ONE_MINUS_ONE, 3)`; death-trigger target selection and per-target assignment are handled through the multi-permanent choice flow, and each counter placed schedules its own delayed removal trigger |
| Enters with X counters + counter placement on another controlled non-Hydra creature | `w/WildwoodScourge.java` | `ON_ENTER_BATTLEFIELD EnterWithCountersEffect(PLUS_ONE_PLUS_ONE, XValue)` + `ON_ALLY_PLUS_ONE_PLUS_ONE_COUNTERS_PUT_ON_NON_HYDRA_CREATURE PutCountersOnSelfEffect(PLUS_ONE_PLUS_ONE)` |
| Each-combat keyword share (Odric) | `o/OdricLunarchMarshal.java` | `EACH_BEGINNING_OF_COMBAT_TRIGGERED` + per-keyword `ConditionalEffect(ControlsPermanent(AllOf(IsCreature, HasKeyword(kw))), GrantKeywordEffect(kw, OWN_CREATURES))` — not intervening-if; one batched ability; fires each combat |
| Attack with self + N others → choose blockers | `o/OdricMasterTactician.java` | `ON_ATTACK` + `ConditionalEffect(MinimumAttackers(4), ChooseBlockersThisCombatEffect())` — Odric + 3 others; first strike from Scryfall |
| Battalion grants a keyword to your creatures | `f/FrontlineMedic.java` | `ON_ATTACK` + `ConditionalEffect(MinimumAttackers(3), SequenceEffect.of(GrantKeywordEffect(INDESTRUCTIBLE, OWN_CREATURES), GrantKeywordEffect(INDESTRUCTIBLE, SELF)))` — `OWN_CREATURES` excludes the source, so the source is granted separately; the one-shot grant only affects creatures present when the trigger resolves |
| Each-combat two-target combat sculpting | `b/BorosBattleshaper.java` | `EACH_BEGINNING_OF_COMBAT_TRIGGERED` with two `target(TargetFilters.creature(), 0, 1)` groups — `SetCombatRequirementThisTurnEffect(MUST_ATTACK_OR_BLOCK)` on the first, `LockTargetPermanentEffect(true, true, false, UNTIL_END_OF_TURN)` on the second. Multiple target groups route the trigger through the slot-by-slot walker so each "up to one" is chosen or declined on its own |
| Each-combat may lock the source and prevent attack taps | `j/Johan.java` | `BEGINNING_OF_COMBAT_TRIGGERED` + `MayEffect(JohanCombatEffect())` — accepted resolution stamps one `UNTIL_END_OF_COMBAT` floating effect that locks Johan from attacking and lets the ability controller's creatures attack without tapping while Johan is untapped |
| Attack: unblockable unless defending player sacrifices a creature | `o/OgreMarauder.java` | `ON_ATTACK` + `ForcedCostOrElseEffect.defendingPlayerMayPay(SacrificePermanentCost(IsCreature, "Sacrifice a creature"), [MakeCreatureUnblockableEffect(true)])` — punisher edict on the defending player (trigger's `attackedTargetId`); declining or controlling no creature makes the attacker unblockable |
| Attack: defending player chooses an untapped creature to block | `c/CrashingBoars.java` | `ON_ATTACK` + `DefendingPlayerChoosesCreatureToBlockEffect()` — resolution-time non-targeting choice by the defending player; only untapped creatures they control are offered, and the chosen creature is marked to block the triggering attacker if able |
| Each-combat double own creatures P/T | `u/UnnaturalGrowth.java` | `EACH_BEGINNING_OF_COMBAT_TRIGGERED` + `DoubleAllOwnCreaturesPowerToughnessEffect()` — per-creature current effective P/T until EOT; fires each combat (any player's) |
| Combat damage → may -1/-1 target creature + counters→Snake payoff | `h/HapatraVizierOfPoisons.java` | `target(creatureFilter).addEffect(ON_COMBAT_DAMAGE_TO_PLAYER, MayEffect(PutCounterOnTargetPermanentEffect(MINUS_ONE_MINUS_ONE, 1)))` — the combat may targets **any** creature (chosen at resolution, since the wrapped effect's targetSpec includes permanents) — plus `ON_YOU_PUT_MINUS_ONE_MINUS_ONE_COUNTERS_ON_CREATURE CreateTokenEffect(Snake, deathtouch)` which fires once per creature per -1/-1 placement instance (not per counter; contrast Nest of Scarabs) |
| Combat damage → may deal damage to a creature the damaged player controls | `s/SparkMage.java` | `MayEffect(DealDamageToTargetCreatureDamagedPlayerControlsEffect(1))` on `ON_COMBAT_DAMAGE_TO_PLAYER` — after accepting the may, the controller chooses exactly one creature controlled by the damaged player; the source deals damage through the normal damage pipeline |
| Combat damage → -1/-1 on that creature + counters→drain | `o/ObeliskSpider.java` | `ON_COMBAT_DAMAGE_TO_CREATURE PutCounterOnTargetPermanentEffect(MINUS_ONE_MINUS_ONE, 1)` (damaged creature baked as non-targeting targetId) + `ON_YOU_PUT_MINUS_ONE_MINUS_ONE_COUNTERS_ON_CREATURE LoseLifeEffect(1, EACH_OPPONENT)` + `GainLifeEffect(1)` (Hapatra once-per-creature cadence) |
| Combat damage → damaged creature becomes a copy of the source | `p/PermeatingMass.java` | `ON_COMBAT_DAMAGE_TO_CREATURE BecomeTargetPermanentCopyOfSourceEffect()` — the damaged permanent is baked as a non-targeting target id and copies the source's current characteristics permanently |
| Any creature's combat damage to an opponent → face-down top-card exile for its controller | `g/GontiNightMinister.java` | `ON_ANY_CREATURE_COMBAT_DAMAGE_TO_OPPONENT ExileTopCardOfDamagedPlayerLibraryFaceDownAndGrantCreatureControllerPlayPermissionEffect()` — the collector captures the damaged player and damaging creature controller; direct exile permission lasts while the card remains exiled and supports any mana type for spells |
| Combat or noncombat damage → tap and lock the damaged creature | `m/MatsuTribeSniper.java` | `ON_ALLY_CREATURE_DEALS_DAMAGE_TO_CREATURE TapAndSkipUntapDamagedCreatureEffect()` — self-scoped; the collector expands it into tap + skip-next-untap effects aimed at the damaged creature |
| On death + target opponent discards | `b/BlackCat.java` | ON_DEATH DiscardEffect(1, TARGET_PLAYER, true) + PlayerPredicateTargetFilter(OPPONENT) — target opponent discards a card at random when this dies |
| On death + may-pay + target player discards | `i/InsidiousBookworms.java` | ON_DEATH MayPayManaEffect("{1}{B}", DiscardEffect(1, TARGET_PLAYER, true), prompt) — no target filter, so any player is legal; target chosen as the trigger stacks, payment prompted at resolution |
| Doesn't untap + tap-to-steal + destroy the stolen creature on leave/untap | `m/MeriekeRiBerit.java` | STATIC `DoesntUntapEffect.self()` + `{T}` ability `GainControlOfTargetEffect.linkingToSource(ControlDuration.WHILE_SOURCE_ON_BATTLEFIELD)` + `DestroyLinkedPermanentEffect(true)` on both ON_SELF_LEAVES_BATTLEFIELD and ON_SELF_BECOMES_UNTAPPED |
| May stay tapped + tap-to-reanimate + exile the reanimated creature on leave/untap | `c/CoffinQueen.java` | STATIC `MayNotUntapDuringUntapStepEffect()` + `{2}{B}`,`{T}` ability `ReturnCardFromGraveyardEffect.builder().destination(BATTLEFIELD).source(ALL_GRAVEYARDS).filter(CardTypePredicate(CREATURE)).targetGraveyard(true).linkToSource(true)` + `RemoveLinkedPermanentEffect(EXILE)` on both ON_SELF_LEAVES_BATTLEFIELD and ON_SELF_BECOMES_UNTAPPED. `linkToSource` bonds the reanimated permanent to the Queen via `chosenPermanentId` so the exile triggers can still name "that creature" |
| Reanimate from your graveyard, then exile the linked creature on death and return the enchantment | `d/DiabolicServitude.java` | ON_ENTER_BATTLEFIELD targeted `ReturnCardFromGraveyardEffect` with `linkToSource(true)` + ON_ANY_CREATURE_DIES `ExileTriggeringCreatureAndReturnSourceToHandEffect` (the collector matches the dying permanent ID to the source's `chosenPermanentId`) + ON_SELF_LEAVES_BATTLEFIELD `RemoveLinkedPermanentEffect(EXILE)` |
| Phasing + phase-out/phase-in triggers | `t/TeferisImp.java` | Printed `Phasing` keyword needs no code; `ON_SELF_PHASES_OUT DiscardEffect(1, CONTROLLER)` + `ON_SELF_PHASES_IN DrawCardEffect(1)`. Fired by `PhasingService` for both the untap-step turn-based action and effect-driven phase-outs |
| Phasing + targeted phase-in | `s/ShimmeringEfreet.java` | Printed Flying/Phasing; `target(TargetFilters.creature()).addEffect(ON_SELF_PHASES_IN, PhaseOutEffect(PhaseOutSubject.TARGET))` — "whenever this phases in, target creature phases out". Routes through `PhasesInTriggerTarget` (chosen at upkeep start) |
| Becomes untapped + may target-player discard | `h/Hollowsage.java` | `ON_SELF_BECOMES_UNTAPPED MayEffect(DiscardEffect(1, TARGET_PLAYER), "have target player discard a card")` — fires when the creature untaps (untap step or any untap effect); may + any-player target chosen at resolution via the MayEffect flow (no target filter = any player) |
| Becomes untapped + reveal top + may put a matching permanent onto the battlefield with modifications | `a/ArbiterOfTheIdeal.java` | `ON_SELF_BECOMES_UNTAPPED RevealTopCardMayPutMatchingOntoBattlefieldWithCounterAndTypeEffect(...)` — matching artifact, creature, or land may enter with a manifestation counter and persistent enchantment type |
| Ally permanent becomes untapped → self boost | `w/WakeThrasher.java` | `ON_ALLY_PERMANENT_BECOMES_UNTAPPED BoostSelfEffect(1, 1)` — +1/+1 until end of turn whenever a permanent you control becomes untapped; fires once per untapped permanent (untap step untaps stack multiple boosts), including this creature untapping itself |
| On death with cascading tokens | `m/MitoticSlime.java` | ON_DEATH CreateTokenEffect with TokenEffectEntry — tokens themselves have death triggers creating smaller tokens |
| Undying + "enters from a graveyard: target opponent gains control of it" | `t/TreacherousPitDweller.java` | `target(PlayerPredicateTargetFilter(PlayerRelationPredicate(OPPONENT)))` + `ON_SELF_ENTERS_FROM_GRAVEYARD` `TargetPlayerGainsControlOfSourceCreatureEffect()` — self-only enters-from-graveyard slot, so casting it from hand asks for nothing; the undying return targets at trigger time |
| On death exile self + random return | `m/MoldgrafMonstrosity.java` | ON_DEATH ReturnCardFromGraveyardEffect with `exileSourceFromGraveyard(true)` + `returnAtRandom(true)` + `randomCount(2)` + creature filter — exiles self then returns random creatures to battlefield |
| On death exile-or-return with tracker counter | `b/BogardanPhoenix.java` | ON_DEATH `ExileIfHadCounterElseReturnWithCounterEffect(CounterType.DEATH)` — first death returns under your control with a death counter; second death (had counter) exiles from graveyard |
| On death return face down + face-up damage | `a/AshcloudPhoenix.java` | ON_DEATH `ReturnSourceCardFromGraveyardToBattlefieldFaceDownEffect()` + ON_TURNED_FACE_UP `DealDamageToPlayersEffect(2, EACH_PLAYER)`; the returned permanent uses the standard face-down creature characteristics and its face-up ability is unavailable until it is turned up |
| Death + cycle both "you may exile target card from a graveyard" | `g/GrixisSojourners.java` | ON_DEATH `ExileUpToOneCardFromGraveyardEffect()` **and** the folded cycling ability `List.of(ExileUpToOneCardFromGraveyardEffect(), DrawCardEffect(1))`. Non-targeting resolution-time "up to one" cross-graveyard select (choose zero = the "you may") rides both paths; the cycling exile resolves, then the draw. Sibling to `e/EsperSojourners.java` (tap/untap permanent variant) |
| Upkeep sacrifice unless you exile the top creature card of your graveyard | `b/BarrowGhoul.java` | UPKEEP_TRIGGERED `ForcedCostOrElseEffect(ExileTopCardOfGraveyardCost(CREATURE), [SacrificeSelfEffect], true)` — the cost walks up from the graveyard top and skips noncreature cards; no creature card at all = unpayable, so the Ghoul is sacrificed without a prompt |
| Upkeep sacrifice unless you put two cards from your graveyard on the library bottom | `g/Gurzigost.java` | UPKEEP_TRIGGERED `ForcedCostOrElseEffect(PutCardsFromGraveyardOnBottomOfLibraryCost(2), [SacrificeSelfEffect], true)` — the controller chooses exactly two cards from their own graveyard; fewer than two cards is unpayable |
| Upkeep may-pay random reanimation with keyword grants and exile riders | `k/KheruLichLord.java` | UPKEEP_TRIGGERED `MayPayManaEffect("{2}{B}", ReturnCardFromGraveyardEffect.builder().destination(BATTLEFIELD).filter(CREATURE).returnAtRandom(true).grantKeywords(Set.of(FLYING, TRAMPLE, HASTE)).exileAtEndStep(true).exileIfLeavesBattlefield(true).build(), prompt)` |
| Upkeep source-on-battlefield gate + name-based mass reanimation | `m/MyrServitor.java` | UPKEEP_TRIGGERED `ConditionalEffect(SourceIsOnBattlefield, EachPlayerReturnsCardsFromGraveyardToBattlefieldEffect(Integer.MAX_VALUE, CardNamedPredicate("Myr Servitor")))` — returns every exact-name match from each player's graveyard, but does nothing if the triggering source left before resolution |
| Upkeep sacrifice unless you remove a counter from a permanent you control | `c/ChiseiHeartOfOceans.java` | UPKEEP_TRIGGERED `ForcedCostOrElseEffect(new RemoveCounterFromControlledPermanentCost(), [SacrificeSelfEffect], true)` — any counter kind on any permanent you control (source included) pays; no counters at all is unpayable and sacrifices with no prompt |
| Upkeep may sacrifice for a counter or remove a counter | `c/CrovaxTheCursed.java` | UPKEEP_TRIGGERED `MaySacrificePermanentForCounterOrRemoveSourceCounterEffect(PermanentIsCreaturePredicate, PLUS_ONE_PLUS_ONE, "a creature")` — sacrifice a creature to add a +1/+1 counter to the source; declining or having no matching creature removes one from the source |
| Upkeep sacrifice unless you pay {1} per other creature | `f/Fettergeist.java` | UPKEEP_TRIGGERED `ForcedCostOrElseEffect(PayManaCost.withGenericIncrease("{0}", PermanentCount(creature, CONTROLLER, excludeSource=true)), [SacrificeSelfEffect], true)` — dynamic generic *increase* (mirror of Draco's reduction); lone source pays `{0}` and survives |
| Upkeep escalating wage or lose the creature | `r/RogueSkycaptain.java` | UPKEEP_TRIGGERED SequenceEffect.of(PutCountersOnSelfEffect(WAGE), PayPerCounterOrElseEffect(WAGE, "{2}", [RemoveAllCountersEffect(WAGE), OpponentGainsControlOfSourceCreatureEffect(PERMANENT)])) — growing per-counter upkeep tax whose unpaid branch clears the counters and hands the creature to an opponent instead of destroying it |
| Upkeep counter countdown unlocking a boost + keyword | `r/RocHatchling.java` | ON_ENTER_BATTLEFIELD `EnterWithCountersEffect(SHELL, Fixed(4))` + UPKEEP_TRIGGERED `RemoveCounterFromSourceEffect(SHELL, 1)` + two STATIC `ConditionalEffect(NotCondition(SourceCounterThreshold(1, SHELL)), …)` wrapping `StaticBoostEffect(3, 2, GrantScope.SELF)` and `GrantKeywordEffect(FLYING, GrantScope.SELF)` — a four-turn clock that flips the creature on once the last counter comes off; the condition is layer-recomputed so re-adding a counter turns it back off |
| Upkeep symmetric reveal-top + life loss + draw | `d/DuskmantleSeer.java` | UPKEEP_TRIGGERED EachPlayerRevealsTopCardLosesLifeEqualToManaValueThenToHandEffect() — each player reveals their top card, loses life equal to its mana value, then puts it into their hand |
| End-step two-player cross-reveal + life loss + draw | `p/ParkerLuck.java` | CONTROLLER_END_STEP_TRIGGERED TargetPlayersRevealTopCardsLoseLifeEqualToOtherManaValueThenToHandEffect() — two target players reveal top cards, each loses life based on the other card, then puts their own card into hand |
| Upkeep sacrifice/discard | `r/RazormaneMasticore.java` / `a/ArgentumMasticore.java` | Razormane: `UPKEEP_TRIGGERED SacrificeUnlessDiscardCardTypeEffect(null)`. Argentum: pass a `DestroyTargetPermanentEffect` as the discard follow-up; the reflexive target is chosen after discard and `PermanentManaValueAtMostXPredicate` uses the discarded card's mana value |
| Empty-hand each-upkeep draw+life loss + Madness | `a/AsylumVisitor.java` | UPKEEP_TRIGGERED + OPPONENT_UPKEEP_TRIGGERED ConditionalEffect(ActivePlayerHandEmpty, SequenceEffect(DrawCardEffect(1), LoseLifeEffect(1))) + MadnessCast("{1}{B}") |
| Start-of-turn empty-hand upkeep branch | `m/MindstormCrown.java` | UPKEEP_TRIGGERED SequenceEffect(ConditionalEffect.unless(ControllerHadNoCardsInHandAtTurnStart, DrawCardEffect(1)), ConditionalEffect.unless(NotCondition(ControllerHadNoCardsInHandAtTurnStart), DealDamageToPlayersEffect(1, CONTROLLER))) |
| Historical damage-recipient upkeep trigger | `t/TheFallen.java` | UPKEEP_TRIGGERED `DealDamageToOpponentsAndPlaneswalkersDamagedBySourceEffect(1)`; GameData retains player/permanent recipients per source object across turns and phasing, while current opponent and planeswalker checks happen at resolution |
| ETB+attack empty-hand any-target damage + Madness | `b/BloodhallPriest.java` | ConditionalEffect(ControllerHandEmpty, DealDamageToAnyTargetEffect(2)) on ON_ENTER_BATTLEFIELD + ON_ATTACK + MadnessCast("{1}{B}{R}"). ControllerHandEmpty is an ETB gate; CombatAttackService filters it at attack-trigger time |
| Upkeep target creature other than self gets -1/-1 | `w/WeedPrunerPoplar.java` | `target(PermanentPredicateTargetFilter(PermanentAllOfPredicate(creature, PermanentNotPredicate(PermanentIsSourceCardPredicate)))).addEffect(UPKEEP_TRIGGERED, BoostTargetCreatureEffect(-1, -1))` — generic targeted-permanent upkeep trigger; target chosen at trigger time via `UpkeepPermanentTargetTrigger` |
| Upkeep may move counters from self to another creature | `s/ScroungingBandar.java` | `EnterWithCountersEffect(PLUS_ONE_PLUS_ONE, Fixed(2))` + targeted `UPKEEP_TRIGGERED MayEffect(MoveCountersFromSourceToTargetCreatureEffect(PLUS_ONE_PLUS_ONE))` — exclude the source with `PermanentNotPredicate(PermanentIsSourceCardPredicate)`; after accepting the may choice, choose 0..the source count through the shared amount-choice interaction |
| Upkeep sacrifice other + opponent life loss | `x/XathridDemon.java` | UPKEEP_TRIGGERED SacrificeOtherCreatureOpponentsLoseLifeOrTapAndLoseLifeEffect(7) — sacrifice other creature, opponents lose life equal to its power; or tap self + lose 7 life |
| Upkeep sacrifice a creature other than self | `d/DemonicTaskmaster.java` | UPKEEP_TRIGGERED SacrificePermanentsEffect(1, PermanentAllOfPredicate(creature, PermanentNotPredicate(PermanentIsSourceCardPredicate)), CONTROLLER) — excludes source; no other creature ⇒ no-op |
| Each player's upkeep: that player sacrifices a monocolored creature | `d/DefilerOfSouls.java` | EACH_UPKEEP_TRIGGERED SacrificePermanentsEffect(1, PermanentAllOfPredicate(PermanentIsCreaturePredicate, PermanentIsMonocoloredPredicate), TARGET_PLAYER) — EACH_UPKEEP sets the active player as the target that sacrifices; gold source is immune to itself |
| Exile-from-graveyard cost + damage | `m/MoltenTailMasticore.java` | ExileCardFromGraveyardCost(CREATURE) + DealDamageToAnyTargetEffect + RegenerateEffect |
| Exile-N-creatures-from-graveyard + cast from graveyard | `s/SkaabRuinator.java` | ExileNCardsFromGraveyardCost(3, CREATURE) + GraveyardCast — creature with "exile 3 creature cards from graveyard" additional cost and "you may cast from your graveyard" ability |
| Cast from graveyard + cast from exile | `s/SqueeTheImmortal.java` | GraveyardCast + ExileCast — creature castable from both graveyard and exile using normal mana cost |
| Attack token per creature in graveyard | `k/KessigCagebreakers.java` | ON_ATTACK CreateTokenEffect(canonical ctor, CardsInGraveyard(CardTypePredicate(CREATURE), CONTROLLER), tappedAndAttacking=true) — creates tapped-and-attacking tokens equal to creature cards in graveyard |
| Attack token + block token tied to the blocked attacker | `b/BrimazKingOfOreskos.java` | ON_ATTACK CreateTokenEffect(..., tappedAndAttacking=true) + ON_BLOCK CreateTokenBlockingCombatOpponentEffect(CreateTokenEffect(...)) — the block wrapper records each token as blocking the trigger's combat opponent |
| Attack creates Treasure + sacrifice-subtype boost | `c/CaptainLanneryStorm.java` | ON_ATTACK CreateTokenEffect.ofArtifactToken(1, "Treasure", TREASURE, tap+sacrifice->AwardAnyColorManaEffect) + ON_ALLY_PERMANENT_SACRIFICED TriggeringPermanentConditionalEffect(PermanentHasSubtypePredicate(TREASURE), BoostSelfEffect(1,0)) — creates Treasure artifact token on attack, gets +1/+0 when any Treasure is sacrificed |
| ETB may-put-land-tapped-draw-repeat + P/T = lands | `c/CultivatorColossus.java` | STATIC `SetPowerToughnessToAmountEffect(PermanentCount(IsLand, CONTROLLER), same)` + ON_ENTER_BATTLEFIELD `PutCardToBattlefieldEffect.tappedDrawAndRepeat(CardTypePredicate(LAND), "land")` — declinable hand choice (no MayEffect); each put draws 1 and re-offers until decline. ETB triggers from put lands wait until the ability finishes |
| ETB Blood token + gain life on Blood sacrifice | `g/GluttonousGuest.java` | ON_ENTER_BATTLEFIELD CreateTokenEffect.ofBloodToken(1) + ON_ALLY_PERMANENT_SACRIFICED TriggeringPermanentConditionalEffect(PermanentHasSubtypePredicate(BLOOD), GainLifeEffect(1)) — creates Blood artifact token on ETB; gains 1 life whenever you sacrifice a Blood token |
| Nontoken death → Blood; 5th Blood → transform DFC; back face animates Blood | `v/VoldarenBloodcaster.java` / `b/BloodbatSummoner.java` | Front: ON_ALLY_NONTOKEN_CREATURE_DIES + ON_DEATH CreateTokenEffect.ofBloodToken(1); ON_ALLY_ARTIFACT_ENTERS TriggeringCardConditionalEffect(token+BLOOD, ConditionalEffect(ControlsPermanentCount(5, token+BLOOD), TransformSelfEffect)). Back: BEGINNING_OF_COMBAT AnimatePermanentsEffect(2/2 Bat flying haste black, TARGET, PERMANENT) with target(Blood token you control, 0, 1) |
| Attack conditional may-pay token | `f/FathomFleetCaptain.java` | ON_ATTACK ConditionalEffect(new ControlsAnotherPermanent(PermanentAllOfPredicate(PermanentHasSubtypePredicate(PIRATE), PermanentNotPredicate(PermanentIsTokenPredicate()))), MayPayManaEffect("{2}", CreateTokenEffect("Pirate", 2, 2, BLACK, PIRATE, MENACE))) — intervening-if "another nontoken Pirate", may pay {2} to create 2/2 black Pirate token with menace |
| Attack may-pay {X}{R} deal X damage to any target | `f/FlameblastDragon.java` | ON_ATTACK PayXManaDealXDamageToAnyTargetEffect("{X}{R}") — target chosen when the ability goes on the stack (Attack pipeline); at resolution prompts X (capped by ManaCost.calculateMaxX), pays {X}{R}, deals X damage (X=0 = decline) |
| Attack: defending player may pay to blank this attacker's damage | `m/MtendaLion.java` | ON_ATTACK MayPayManaEffect("{U}", PreventDamageEffect.allCombatBySelf(), …, MayPayPayer.DEFENDING_PLAYER) — "defending player may pay {U}. If that player does, prevent all combat damage that would be dealt by this creature this turn." The payer is the attacked player (trigger `attackedTargetId`), not the controller; the wrapped prevention reads the attacker as `sourcePermanentId` |
| Attack may damage each defending creature | `r/RoninCliffrider.java` | ON_ATTACK `MayEffect(DealDamageToDefendingPlayerCreaturesEffect(1))` — the controller may have the attacker deal 1 damage to each creature controlled by the attacked player; the non-targeting attacked-player context survives the May choice |
| Combat damage → source-power damage to each blocking creature | `e/Electryte.java` | ON_COMBAT_DAMAGE_TO_PLAYER `DealDamageToDefendingPlayerCreaturesEffect(new SourcePower(), PermanentIsBlockingPredicate)` — the effect binds the damaged player and source creature through combat-damage trigger context, then filters that player’s blocking creatures |
| Attack: coin flip doubles or prevents one attacker's combat damage | `i/ImpulsiveManeuvers.java` | `ON_ANY_CREATURE_ATTACKS` `FlipCoinDoubleOrPreventNextCombatDamageFromAttackingCreatureEffect()` — the trigger carries the attacking creature as non-targeting `targetId`; the handler installs a combat-only one-shot source shield, so a noncombat damage event from that creature leaves it unused |
| Attack loot with self-boost by discard MV | `s/SpellboundDragon.java` | ON_ATTACK DrawDiscardBoostSelfByDiscardedManaValueEffect() — draw 1, discard 1, then this creature gets +X/+0 until end of turn where X = discarded card's mana value; boost applied by a pending hook after the interactive discard |
| Attack → mana at next main this turn | `c/ConduitOfStorms.java` / `c/ConduitOfEmrakul.java` | ON_ATTACK `RegisterDelayedManaAtNextMainPhaseEffect(color, amount)` — schedules mandatory mana at beginning of next main this turn (postcombat after attacking). Front also has `{3}{R}{R}: Transform` |
| Ally-creatures-attack mana per attacker | `g/GrandWarlordRadha.java` | ON_ALLY_CREATURES_ATTACK AddManaPerAttackingCreatureEffect(RED, GREEN) — fires once when 1+ creatures attack, adds mana = attacker count (player chooses R or G), prevents mana drain until end of turn |
| Per-creature attack trigger with subtype drain | `s/SanctumSeeker.java` | ON_ALLY_CREATURE_ATTACKS TriggeringCardConditionalEffect(CardSubtypePredicate(VAMPIRE), LoseLifeEffect(1, EACH_OPPONENT)) + TriggeringCardConditionalEffect(CardSubtypePredicate(VAMPIRE), GainLifeEffect(1)) — fires once per attacking Vampire, drains 1 life from each opponent |
| Per-creature attack trigger damage to attacked target | `h/Hellrider.java` | ON_ALLY_CREATURE_ATTACKS DealDamageToAttackedTargetEffect(1) — fires once per attacking creature you control; damages the player or planeswalker that creature is attacking |
| Per-creature attack trigger scaling boost by shared type | `s/SharedAnimosity.java` | ON_ALLY_CREATURE_ATTACKS BoostTargetCreatureEffect(new OtherAttackersSharingCreatureTypeWithTarget(), new Fixed(0)) — "it gets +1/+0 until end of turn for each other attacking creature that shares a creature type with it"; the triggering attacker is the (non-targeting) target, count computed at resolution |
| ETB move all Auras from target permanent to another same-controller permanent | `g/GlamerSpinners.java` | targeted ETB `PermanentPredicateTargetFilter(PermanentTruePredicate)` (any permanent) + ON_ENTER_BATTLEFIELD `AttachAllAurasToAnotherPermanentEffect()` — on resolution collects every Aura on the target and prompts the controller to pick another permanent with the same controller that all those Auras can legally enchant; if none, nothing moves |
| ETB counter-all-subtype + counter-bearing attacker may ping | `r/RageForger.java` | ON_ENTER_BATTLEFIELD PutCounterOnEachControlledPermanentEffect(PLUS_ONE_PLUS_ONE, 1, AllOf(IsCreature, HasSubtype(SHAMAN), Not(IsSourceCard))) for "each other Shaman creature you control" + ON_ALLY_CREATURE_ATTACKS TriggeringPermanentConditionalEffect(PermanentHasCountersPredicate(PLUS_ONE_PLUS_ONE), MayEffect(DealDamageToTargetPlayerOrPlaneswalkerEffect(1))) with card-level PermanentPredicateTargetFilter(PermanentIsPlaneswalkerPredicate) — the may becomes a CR 603.5 resolution-time may sourced by the attacking creature |
| Unblocked-attacker subtype boost | `s/StinkdrinkerBandit.java` | ON_ALLY_CREATURE_ATTACKS_UNBLOCKED TriggeringCardConditionalEffect(CardSubtypePredicate(ROGUE), BoostSelfEffect(2,1)) — fires once per unblocked attacking Rogue you control; "it gets +2/+1" applies to the unblocked creature. Also has prowl {1}{B} via AlternateHandCast(Set.of(GOBLIN, ROGUE)) |
| Self-boost when you activate a subtype's ability | `c/CeaselessSearblades.java` | ON_CONTROLLER_ACTIVATES_ABILITY TriggeringPermanentConditionalEffect(PermanentHasSubtypePredicate(ELEMENTAL), BoostSelfEffect(1, 0)) — fires once per activated-ability activation (incl. mana abilities) of a permanent you control; the conditional filters by the activated permanent's subtype |
| ETB tutor an eternalize/embalm creature to graveyard + draw on eternalize/embalm activation | `v/VizierOfTheAnointed.java` | ON_ENTER_BATTLEFIELD MayEffect(SearchLibraryEffect(Fixed(1), CardAllOfPredicate(CardTypePredicate(CREATURE), CardHasEmbalmOrEternalizePredicate()), LibrarySearchDestination.GRAVEYARD)) + ON_CONTROLLER_ACTIVATES_ETERNALIZE_OR_EMBALM DrawCardEffect(1). The draw slot fires from `AbilityActivationService.completeGraveyardAbilityActivation` when `ActivatedAbility.isEmbalmOrEternalize()`; controller-scoped, no conditional wrapper |
| Tap search to graveyard with selected-card conditional counter | `o/OriqLoremage.java` | ActivatedAbility(true, null, SearchLibraryAndConditionalEffect(null, GRAVEYARD, CardAnyOf(INSTANT, SORCERY), PutCountersOnSourceCardEffect(PLUS_ONE_PLUS_ONE)) — the post-search condition is bound to the exact selected card, so an unrelated instant or sorcery already in the graveyard cannot satisfy it |
| End-step corpse counters per death + remove-counter-to-regenerate | `s/ScavengingGhoul.java` | END_STEP_TRIGGERED PutCountersOnSelfEffect(CORPSE, CreatureDeathsThisTurn(ANY_PLAYER)) + ActivatedAbility(false, null, RemoveCounterFromSourceCost(1, CORPSE)+RegenerateEffect()) — accrues a corpse counter for each creature that died this turn at each end step; remove one corpse counter to regenerate. CORPSE is a plain resource counter (no P/T) |
| Enters tapped + doesn't untap + sac-another-creature untap + attack trigger destroy+counter | `g/GrimgrinCorpseBorn.java` | EntersTappedEffect + DoesntUntapEffect.self() + SacrificeCreatureCost(excludeSelf=true)+UntapPermanentsEffect(TapUntapScope.SELF)+PutCountersOnSourceEffect(1,1,1) activated ability + ON_ATTACK DestroyTargetPermanentEffect+PutCountersOnSourceEffect(1,1,1) with PermanentPredicateTargetFilter for opponent creatures |
| ETB -1/-1 on your creature + attack move-a-counter (two-target) | `d/DecimatorBeetle.java` | ETB `target(ControlledPermanentPredicateTargetFilter(IsCreature)).addEffect(ON_ENTER_BATTLEFIELD, PutCounterOnTargetPermanentEffect(MINUS_ONE_MINUS_ONE, 1))` (Baleful Ammit shape) + ON_ATTACK `RemoveAndPutCounterOnAttackEffect()`. The attack trigger needs **two** targets (a creature you control, then up to one creature the defending player controls) — the single-target attack pipeline can't do that, so it routes through the bespoke two-step `AttackCounterMoveFirstTarget`/`AttackCounterMoveSecondTarget` flow (marker interface `AttackCounterMoveEffect`, drained in `AutoPassService`). Independent halves: puts even with nothing to remove |
| Enters tapped + doesn't untap + upkeep sac-to-untap + sac-to-attack | `l/Leviathan.java` | EntersTappedEffect + DoesntUntapEffect.self() + UPKEEP_TRIGGERED `MayEffect(SacrificePermanentsToUntapSelfEffect(2, Island, "two Islands"))` + STATIC `CantAttackUnlessEffect(ControlsPermanentCount(2, Island), …)` gate + STATIC `CantAttackUnlessSacrificeEffect(2, Island, "two Islands")` (sacrifice paid on declare by `AttackSacrificeCostService`) |
| Pay life for keyword (no mana cost ability) | `a/AdantoVanguard.java` | STATIC StaticBoostEffect(2,0,SELF,PermanentIsAttackingPredicate) + ActivatedAbility(false, null, PayLifeCost(4)+GrantKeywordEffect(INDESTRUCTIBLE,SELF)) — "while attacking gets +2/+0" via static filter + "pay 4 life: gains indestructible until end of turn" with no mana cost |
| Attack trigger pays life for a keyword | `a/ArrogantPoet.java` | ON_ATTACK `MayPayLifeEffect(2, GrantKeywordEffect(FLYING, SELF), prompt)` — "Whenever this creature attacks, you may pay 2 life. If you do, it gains flying until end of turn." |
| Two abilities: sacrifice subtype for boost+life vs sacrifice another creature for boost | `t/ThallidOmnivore.java` | Ability 0: SacrificePermanentCost(creature AND SAPROLING, description, false)+BoostSelfEffect(2,2)+GainLifeEffect(2). Ability 1: SacrificeCreatureCost(excludeSelf=true)+BoostSelfEffect(2,2). Split "If a Saproling was sacrificed, gain life" into two separate activated abilities using existing effects |
| Sacrifice for keyword + conditional counter if sacrificed creature was a subtype | `f/FalkenrathAristocrat.java` | Ability 0: SacrificePermanentCost(creature AND HUMAN, description, false)+GrantKeywordEffect(INDESTRUCTIBLE,SELF)+PutCountersOnSourceEffect(1,1,1). Ability 1: SacrificePermanentCost(creature AND NOT HUMAN)+GrantKeywordEffect(INDESTRUCTIBLE,SELF). Split "Sacrifice a creature: gains indestructible. If it was a Human, put a +1/+1 counter" into a Human-sacrifice variant (with counter) and a non-Human-sacrifice variant (no counter) so the mandatory counter can't be avoided. Flying/haste auto-loaded from Scryfall |
| Upkeep bounce (filtered permanent) | `s/StampedingWildebeests.java`, `e/Esperzoa.java` | BouncePermanentOnUpkeepEffect — type via filters (creature+green for Wildebeests, `PermanentIsArtifactPredicate` for Esperzoa's "return an artifact you control") |
| Upkeep token | `v/VerdantForce.java` | EACH_UPKEEP_TRIGGERED CreateTokenEffect |
| Each player's upkeep: that player may pay mana or life to steal this creature | `e/EmberwildeDjinn.java` | EACH_UPKEEP_TRIGGERED `TargetPlayerMayPayManaOrLifeEffect("{R}{R}", 2, TargetPlayerGainsControlOfSourceCreatureEffect(), prompt)` — the slot puts the active player on `targetId`, so the same player is prompted to pay and gains control |
| End-step tokens per regeneration this turn | `s/SpinyStarfish.java` | END_STEP_TRIGGERED ConditionalEffect(SourceRegeneratedThisTurn, CreateTokenEffect(TimesSourceRegeneratedThisTurn(), 0/1 blue Starfish)) + `{U}` RegenerateEffect ability |
| Upkeep conditional self-bounce + tokens | `t/ThopterAssembly.java` | UPKEEP_TRIGGERED ConditionalEffect(new NoOtherPermanent(PermanentHasSubtypePredicate(THOPTER)), SequenceEffect.of(ReturnToHandEffect.self(), CreateTokenEffect(5 Thopters))) — intervening-if "no other Thopters"; one atomic entry (trigger collectors push one entry per slot effect, so the two steps must be one SequenceEffect), bounce no-ops if source already gone but tokens still created |
| Upkeep token per equipment | `k/KembaKhaRegent.java` | UPKEEP_TRIGGERED CreateTokenEffect(new AttachmentsOnSource(false, true), ...) — tokens equal to attached Equipment |
| Upkeep may target artifact charge | `v/VedalkenInfuser.java` | UPKEEP_TRIGGERED MayEffect(PutCounterOnTargetPermanentEffect(CounterType.CHARGE)) + PermanentPredicateTargetFilter(PermanentIsArtifactPredicate) — may put charge counter on target artifact |
| Upkeep may counter + counter-linked base-P/T & keyword rule | `a/AvenMimeomancer.java` | ON_ENTER_BATTLEFIELD GrantBaseStatsToCounterBearersEffect(CounterType.FEATHER, 3, 1, Set.of(Keyword.FLYING)) + UPKEEP_TRIGGERED MayEffect(PutCounterOnTargetPermanentEffect(CounterType.FEATHER)) + target(PermanentIsCreaturePredicate). "may put a feather counter on target creature; that creature has base P/T 3/1 and flying for as long as it has a feather counter" — the base-P/T set + flying is a source-independent continuous rule keyed off the counter (persists after Aven leaves, ends when the counter is removed). New FEATHER counter added to CounterType + the two `PermanentHasCountersPredicate` switches + `PermanentCounterSupport.placeCounterOnPermanent` |
| Graveyard Recover trigger | `s/SunsBounty.java` | GRAVEYARD_ON_CREATURE_PUT_INTO_CONTROLLER_GRAVEYARD_FROM_BATTLEFIELD `MayPayManaEffect(cost, ReturnSourceCardFromGraveyardToOwnerHandEffect(), prompt, ExileSourceCardFromGraveyardEffect())` — scan the graveyard after a creature enters its owner's graveyard; the source card, not the dying creature, is returned or exiled |
| Graveyard upkeep | `s/SqueeGoblinNabob.java` | GRAVEYARD_UPKEEP_TRIGGERED ReturnCardFromGraveyardEffect.builder().destination(HAND).filter(CardIsSelfPredicate).returnAll(true).build() |
| Graveyard metalcraft pay-to-return | `k/KuldothaPhoenix.java` | GRAVEYARD_UPKEEP_TRIGGERED ConditionalEffect(new Metalcraft(), MayPayManaEffect("{4}", ReturnCardFromGraveyardEffect.builder().destination(BATTLEFIELD).filter(CardIsSelfPredicate).build())) — metalcraft checked at trigger time, mana paid at resolution |
| Graveyard revenge trigger (combat damage to you) | `v/VengefulPharaoh.java` | `target(TargetFilters.attackingCreature()).addEffect(GRAVEYARD_ON_COMBAT_DAMAGE_TO_YOU_OR_YOUR_PLANESWALKER, ConditionalEffect(SourceCardInGraveyard, DestroyTargetPermanentEffect))` + `addEffect(same slot, PutSourceCardFromGraveyardIntoLibraryNFromTopEffect(0))` — "whenever combat damage is dealt to you or a planeswalker you control, if this card is in your graveyard, destroy target attacking creature, then put this card on top of your library". The only graveyard slot that targets; `SourceCardInGraveyard` is the resolution-time intervening-if re-check (CR 603.4) |
| Graveyard-order self-return | `n/NetherShadow.java` | GRAVEYARD_UPKEEP_TRIGGERED ConditionalEffect(new CardsAboveSelfInGraveyard(3, new CardTypePredicate(CardType.CREATURE)), MayEffect(ReturnCardFromGraveyardEffect.builder().destination(BATTLEFIELD).filter(CardIsSelfPredicate).returnAll(true).build())) — "with three or more creature cards above it"; condition counts cards higher in the ordered graveyard, checked at trigger time |
| Graveyard-order end-step self-return | `k/KrovikanHorror.java` | GRAVEYARD_END_STEP_TRIGGERED ConditionalEffect(new CardDirectlyAboveSelfInGraveyard(new CardTypePredicate(CardType.CREATURE)), MayEffect(ReturnCardFromGraveyardEffect.builder().destination(HAND).filter(CardIsSelfPredicate).returnAll(true).build())) — "with a creature card directly above it"; fires at EVERY end step from any player's graveyard, intervening-if checked at trigger time. Plus `{1}`+SacrificeCreatureCost+DealDamageToAnyTargetEffect(1) |
| Graveyard-order activated self-return | `a/AshenGhoul.java` | `addGraveyardActivatedAbility` + `{B}` + `ReturnCardFromGraveyardEffect` self-return + `ONLY_DURING_YOUR_UPKEEP` + `.withActivationCondition(CardsAboveSelfInGraveyard(3, CREATURE), …)` — same ordered-graveyard gate as Nether Shadow, but paid activation instead of upkeep trigger |
| Graveyard spell-cast pay-to-return | `l/LingeringPhantom.java` | GRAVEYARD_ON_CONTROLLER_CASTS_SPELL SpellCastTriggerEffect(CardIsHistoricPredicate, ReturnCardFromGraveyardEffect.builder().destination(HAND).filter(CardIsSelfPredicate).returnAll(true).build(), "{B}") — triggers from graveyard when controller casts historic spell, may pay {B} to return self to hand |
| Graveyard second creature-spell self-return | `v/Vengevine.java` | GRAVEYARD_ON_CONTROLLER_CASTS_SPELL `SpellCastTriggerEffect.nth(2, CardTypePredicate(CREATURE), List.of(MayEffect(ReturnCardFromGraveyardEffect.builder().destination(BATTLEFIELD).filter(CardIsSelfPredicate).returnAll(true).build(), prompt)))` — the noncreature spells do not advance the creature-spell count |
| Conditional indestructible + graveyard discard-to-return | `e/ErebossTitan.java` | STATIC `ConditionalEffect(NotCondition(OpponentControlsPermanent(PermanentIsCreaturePredicate)), GrantKeywordEffect(INDESTRUCTIBLE, SELF))` + GRAVEYARD_ON_CREATURE_CARD_LEAVES_OPPONENT_GRAVEYARD `MayEffect(DiscardCardThenEffect(null, ReturnSourceCardFromGraveyardToOwnerHandEffect(), "a card"), prompt)` — "as long as your opponents control no creatures" is a negated opponent-control condition; the return half functions only from the graveyard, and a `null` discard filter means any card |
| Graveyard activated ability (return to hand) | `m/MagmaPhoenix.java` | `addGraveyardActivatedAbility(ActivatedAbility(false, "{3}{R}{R}", ReturnCardFromGraveyardEffect.builder().destination(HAND).filter(CardIsSelfPredicate).build()))` — activated ability usable from graveyard zone, pays mana, puts on stack as ACTIVATED_ABILITY. Blocked by Pithing Needle |
| Same damage ability from battlefield and graveyard (multi-target) | `s/SoulOfShandalar.java` | `{3}{R}{R}` on the battlefield and `{3}{R}{R}` + `ExileSelfFromGraveyardCost()` from the graveyard, both `ActivatedAbility(false, "{3}{R}{R}", effects, desc, filters, 1, 2)` + `.withMultiTargetConstraint(CONTROLLED_BY_FIRST_TARGET)` around one `DealDamageToTargetAndUpToCreaturesThatPlayerControlsEffect(3, 3, 1, false, false)`; filters = `AnyTargetPredicateTargetFilter(PermanentIsPlaneswalkerPredicate, PlayerRelationPredicate(ANY))` + `PermanentPredicateTargetFilter(PermanentIsCreaturePredicate)`. Multi-target groups work from the graveyard zone too — harness `activateGraveyardAbilityWithTargets` |
| Graveyard activated ability with exile-cards cost (no mana) | `s/SalvageTitan.java` | `addGraveyardActivatedAbility(ActivatedAbility(false, null, List.of(new ExileNCardsFromGraveyardCost(3, CardType.ARTIFACT), ReturnCardFromGraveyardEffect.builder().destination(HAND).filter(CardIsSelfPredicate).returnAll(true).build()), "..."))` — "Exile three artifact cards from your graveyard: Return this from your graveyard to your hand." The graveyard-ability path pays `ExileNCardsFromGraveyardCost` by exiling N cards matching the type (`hasType`, so artifact creatures count), **excluding the source** so the return effect still finds it; requires N *other* matching cards |
| Graveyard activated ability (return to battlefield tapped) | `r/ReassemblingSkeleton.java` | `addGraveyardActivatedAbility(ActivatedAbility(false, "{1}{B}", ReturnCardFromGraveyardEffect.builder().destination(BATTLEFIELD).filter(CardIsSelfPredicate).enterTapped(true).build()))` — self-return to battlefield tapped from graveyard |
| Graveyard activated ability (discard N + return tapped) | `h/HauntedDead.java` | ETB `CreateTokenEffect.whiteSpirit(1)` + `addGraveyardActivatedAbility(ActivatedAbility(false, "{1}{B}", List.of(DiscardCardTypeCost(null, null, 2), ReturnCardFromGraveyardEffect…enterTapped(true))))` — discard two cards then return self tapped |
| Graveyard activated ability (return Aura attached to target creature) | `g/GryffsBoon.java` | `addGraveyardActivatedAbility(ActivatedAbility(false, "{3}{W}", List.of(ReturnSourceFromGraveyardAttachedToTargetEffect()), desc, PermanentPredicateTargetFilter(IsCreature), null, null, SORCERY_SPEED))` — targets a creature at activation; illegal target → fizzle, Aura stays in GY. Wire `targetId` through `activateGraveyardAbility` |
| Graveyard activated ability with raid restriction | `r/RepeatingBarrage.java` | `addGraveyardActivatedAbility(ActivatedAbility(false, "{3}{R}{R}", ReturnCardFromGraveyardEffect..., ActivationTimingRestriction.RAID))` — graveyard return to hand gated by raid (attacked this turn). Uses 5-arg ActivatedAbility constructor with timing restriction |
| Graveyard attack-trigger pay-to-return tapped and attacking | `w/WarcryPhoenix.java` | GRAVEYARD_ON_ALLY_CREATURES_ATTACK ConditionalEffect(new MinimumAttackers(3), MayPayManaEffect("{2}{R}", ReturnCardFromGraveyardEffect.builder().destination(BATTLEFIELD).filter(CardIsSelfPredicate).returnAll(true).enterTapped(true).enterAttacking(true).build())) — triggers from graveyard when 3+ creatures attack, may pay to return tapped and attacking |
| Graveyard creature entry or cast trigger — delayed tapped self-return | `p/PrizedAmalgam.java` | `GRAVEYARD_ON_CREATURE_ENTERS_FROM_GRAVEYARD_OR_CAST_FROM_GRAVEYARD` + `RegisterDelayedSelfReturnFromGraveyardEffect(null, 0, false, true)` — scans the source's graveyard and returns it tapped at the next end step |
| Attack-trigger targeted reanimation with a source-relative toughness restriction | `t/ThunderkinAwakener.java` | ON_ATTACK `ReturnCardFromGraveyardEffect.builder().destination(BATTLEFIELD).filter(CardAllOf(CREATURE, ELEMENTAL, CardToughnessLessThanSourceToughnessPredicate)).targetGraveyard(true).enterTapped(true).enterAttacking(true).sacrificeAtEndStep(true).build()` |
| Raid end-step targeted reanimation with a source-relative power restriction | `a/AleshaWhoLaughsAtFate.java` | ON_ATTACK `PutCountersOnSourceEffect(1, 1, 1)` + `CONTROLLER_END_STEP_TRIGGERED ConditionalEffect(Raid, ReturnCardFromGraveyardEffect.builder().destination(BATTLEFIELD).filter(CardAllOf(CREATURE, CardManaValueAtMostSourcePowerPredicate)).targetGraveyard(true).build())` |
| Combat damage to player | `t/ThievingMagpie.java` | ON_COMBAT_DAMAGE_TO_PLAYER DrawCardEffect |
| Attack: play from top for life | `g/GwenomRemorseless.java` | ON_ATTACK `MayPlayCardsFromTopOfLibraryForLifeUntilEndOfTurnEffect()` — grants a temporary controller permission; spells use life equal to mana value instead of mana, while lands use normal timing and land-play limits |
| Combat damage → optional dynamic-MV reanimation | `v/VenerableWarsinger.java` | ON_COMBAT_DAMAGE_TO_PLAYER `ReturnCardFromGraveyardEffect.builder().destination(BATTLEFIELD).filter(CREATURE).targetGraveyard(true).upTo(true).dynamicMaxManaValue(new EventValue()).build()` — combat trigger collection snapshots the actual damage dealt into a static mana-value cap before the graveyard target is chosen |
| Attack → reveal defending player's top card; land to hand | `g/GoblinGuide.java` | ON_ATTACK `RevealTopCardOfDefendingPlayerLibraryMatchingToHandEffect(CardTypePredicate(LAND))` — non-targeting combat context reads the defending player from `attackedTargetId`; a nonland remains on top |
| Combat damage with an intervening "if" clause | `l/LoneRevenant.java` | `ON_COMBAT_DAMAGE_TO_PLAYER ConditionalEffect(NoOtherPermanent(PermanentIsCreaturePredicate()), LookAtTopCardsEffect.chooseOneToHandRestOnBottom(Fixed(4)))` — any `Condition` works: `CombatDamageService` checks a `ConditionalEffect` wrapper at trigger time (CR 603.4) and `EffectResolutionService` re-checks it on resolution |
| Attacked: if has defender, lose defender + gain trample indefinitely | `g/GuardianOfTheAges.java` | `ON_CREATURE_ATTACKS_YOU ConditionalEffect(SelfHasKeyword(DEFENDER), SequenceEffect.of(RemoveKeywordEffect(DEFENDER, SELF, PERMANENT), SetSelfKeywordIndefinitelyEffect(TRAMPLE, true)))` — intervening-if at attack declaration; both changes indefinite |
| Combat damage untap chosen creature/land | `i/InitiatesCompanion.java` | ON_COMBAT_DAMAGE_TO_PLAYER `UntapChosenPermanentEffect(PermanentAnyOfPredicate(creature, land))` — "untap target creature or land"; combat-damage slot has no targeting pipeline, so the target is a resolution-time controller choice across all battlefields (`MultiPermanentChoiceContext.UntapChosenPermanent`) |
| Combat damage exile-top-of-library controller-may-play | `n/NightveilSpecter.java` | ON_COMBAT_DAMAGE_TO_PLAYER `ExileTopCardsToSourceEffect(1, false, false, LibraryScope.TARGET_OPPONENT)` (face up, tracked with the Specter) + STATIC `AllowCastFromCardsExiledWithSourceEffect(false)` — "you may play lands and cast spells from among cards exiled with this creature", normal costs, no expiry |
| Discard one-or-more nonland cards, exile one and cast it this turn | `c/ConspiracyTheorist.java` | ON_ATTACK `MayPayManaEffect("{1}", DiscardAndDrawCardEffect(), ...)` + ON_CONTROLLER_DISCARDS `MayEffect(ExileOneOfDiscardedCardsFromGraveyardEffect(), ...)` + STATIC `AllowCastFromCardsExiledWithSourceEffect(false, nonland, false, false, 0, null, false, true, false)` |
| Attack exile-top-of-each-library free-cast | `e/EtaliPrimalStorm.java` | ON_ATTACK `ExileTopCardOfEachLibraryAndMayCastSpellsEffect()` — exiles one face-up card from each library and offers any number of exiled spells to the controller for free; uncast cards remain exiled |
| Combat damage → sacrifice other permanents, discard hand, exile top six, free-cast spells | `h/HellcarverDemon.java` | ON_COMBAT_DAMAGE_TO_PLAYER `SequenceEffect.of(SacrificePermanentsEffect(new PermanentCount(otherPermanent, CONTROLLER), otherPermanent, CONTROLLER), DiscardHandEffect(), ExileTopCardsAndMayCastSpellsEffect(6))` — source-relative `PermanentNotPredicate(PermanentIsSourceCardPredicate)` keeps Hellcarver Demon out of the sweep; uncast exiled cards remain exiled |
| Combat damage exile-from-hand controller-may-play | `f/FiendOfTheShadows.java` | ON_COMBAT_DAMAGE_TO_PLAYER `TargetPlayerExilesFromHandEffect(1, true)` — damaged player exiles a card from hand (their choice), Fiend's controller may play it for as long as it remains exiled. Also has `SacrificePermanentCost(creature AND HUMAN, description, false) + RegenerateEffect` activated ability (null mana cost) |
| Combat damage may-pay-life draw plus X discard/exile ability | `g/GixYawgmothPraetor.java` | ON_ALLY_CREATURE_COMBAT_DAMAGE_TO_PLAYER `AllyCombatDamageTriggerEffect(PermanentIsCreaturePredicate, MayPayManaEffect("{0}", 1, DrawCardEffect(1)))` + STATIC free access to source-tracked exile + `ActivatedAbility(...).withXValue()` with `DiscardXCardsCost` and dynamic `ExileTopCardsToSourceEffect` |
| Combat damage awakening | `l/LiegeOfTheTangle.java` | ON_COMBAT_DAMAGE_TO_PLAYER PutAwakeningCountersOnTargetLandsEffect — multi-permanent choice on controller's lands, permanent 8/8 animation via awakening counters |
| Combat damage may-sacrifice-draw | `i/ImpalerShrike.java` | ON_COMBAT_DAMAGE_TO_PLAYER `MayEffect(SacrificeSelfThenEffect(new DrawCardEffect(3)))` — may sacrifice self, if you do draw N cards |
| Combat damage hidden hand-card guess | `m/MasterOfPredicaments.java` | ON_COMBAT_DAMAGE_TO_PLAYER `MasterOfPredicamentsEffect()` — controller chooses a hidden hand card, damaged player guesses whether its mana value is greater than 4, and a wrong guess offers a nonland card's free cast without revealing it if declined |
| Combat damage may-sacrifice-destroy | `b/BlindZealot.java` | ON_COMBAT_DAMAGE_TO_PLAYER `MayEffect(SacrificeSelfToDestroyCreatureDamagedPlayerControlsEffect)` — MayEffect wraps inner sacrifice+destroy. Legal creature targets are snapshotted when the trigger is created; after acceptance, the inner effect presents the required target choice. |
| Combat damage sacrifice-target-creature | `a/AshlingTheExtinguisher.java` | ON_COMBAT_DAMAGE_TO_PLAYER `SacrificePermanentDamagedPlayerControlsEffect(PermanentIsCreaturePredicate, 0)` — mandatory (no MayEffect); controller chooses target creature the damaged player controls, that player sacrifices it. Sacrifice analog of `DestroyPermanentDamagedPlayerControlsEffect` |
| Attack/block gather Auras from everywhere | `b/BrunaLightOfAlabaster.java` | ON_ATTACK + ON_BLOCK `AttachAurasToSourceEffect()` — one non-targeting prompt to attach any number of Auras (battlefield permanents plus own graveyard/hand Aura cards) to the source; only Auras that could legally enchant it are offered |
| Combat damage may-transform-attach | `s/SoulSeizer.java` (back face `g/GhastlyHaunting.java`) | ON_COMBAT_DAMAGE_TO_PLAYER `MayEffect(TransformSelfAndAttachToCreatureDamagedPlayerControlsEffect)` — optional transform; if accepted, choose target creature damaged player controls, transform to aura back face, attach, and steal via `ControlEnchantedCreatureEffect` on back face |
| Combat damage may-sacrifice-discard-per-poison | `w/WhisperingSpecter.java` | ON_COMBAT_DAMAGE_TO_PLAYER `MayEffect(SacrificeSelfAndTargetDiscardsPerPoisonCounterEffect)` — may sacrifice self, if you do damaged player discards cards equal to their poison counter count |
| Combat damage: exile damaged player's graveyard cards | `s/Skullsnatcher.java` | ON_COMBAT_DAMAGE_TO_PLAYER `ExileCardsFromGraveyardEffect(2, 0)` — "exile up to two target cards from that player's graveyard"; targets chosen from the damaged player's graveyard as the trigger goes on the stack (`CombatDamageService` → `GraveyardTargetingService.handleCombatDamageGraveyardChoiceTargeting`). Pairs with `addNinjutsu("{B}")` |
| Unblocked exile-defender-graveyard-creatures | `r/RysorianBadger.java` | ON_ATTACKS_UNBLOCKED `ExileCardsFromGraveyardEffect(2, 1, true, CardTypePredicate(CREATURE), true)` — "you may exile up to two target creature cards from defending player's graveyard; if you do, gain 1 life for each and this creature assigns no combat damage this turn". Targets chosen from the defending player's graveyard as the trigger goes on the stack (`GraveyardCardChoosingEffect` → `GraveyardTargetingService.handleUnblockedAttackGraveyardChoiceTargeting`); choosing zero is the "you may" |
| Unblocked may-sacrifice-discard-N | `m/MindstabThrull.java` | ON_ATTACKS_UNBLOCKED `MayEffect(SacrificeSelfThenEffect(new DiscardEffect(3, DiscardRecipient.TARGET_PLAYER)))` — may sacrifice self, if you do the defending player (baked onto the trigger's `targetId`) discards N cards |
| Unblocked may-sacrifice-destroy creature + land | `g/GoblinGrenadiers.java` | ON_ATTACKS_UNBLOCKED `MayEffect(SacrificeSelfThenEffect(new DestroyEachTargetPermanentEffect()))` + two `target()` groups (creature, land) — "you may sacrifice it. If you do, destroy target creature and target land". Two positional groups, so `CombatBlockService` routes the trigger through the `ETBTokenMultiTargetTrigger` slot-by-slot picker (targets chosen as the trigger goes on the stack, CR 603.3d); the may is still made at resolution and the entry's pre-chosen `targetIds` survive it |
| Unblocked may-reveal-defender-hand + no damage | `s/StromgaldSpy.java` | ON_ATTACKS_UNBLOCKED `MayEffect(SequenceEffect.of(MakeDefendingPlayerPlayWithHandRevealedEffect(), AssignNoCombatDamageEffect()))` — defending player plays with their hand revealed while the Spy stays on the battlefield; if you do, the Spy assigns no combat damage this turn |
| Unblocked may-power-damage-to-creature + no damage | `d/DwarvenVigilantes.java` | ON_ATTACKS_UNBLOCKED `MayEffect(SequenceEffect.of(DealDamageToTargetCreatureEffect(SourcePower()), AssignNoCombatDamageEffect()))` — same shape as Gaze of Pain's delayed trigger; permanent-targeting may is queued via `queueMayAbility` (null targetId, attacker as `sourcePermanentId`) so the creature target is chosen after accepting |
| Unblocked may-reanimate-from-defender-graveyard + no damage | `b/BoneDancer.java` | ON_ATTACKS_UNBLOCKED `MayEffect(ReturnTopCreatureCardFromGraveyardToBattlefieldEffect(GraveyardOwner.TARGET_PLAYER, true))` — puts the top creature card of the defending player's graveyard (trigger `targetId`) onto the battlefield under your control; the `true` flag applies "assigns no combat damage this turn" only if a card actually came back |
| Unblocked may-sacrifice-destroy (can't regen) | `n/Necrite.java` | ON_ATTACKS_UNBLOCKED `MayEffect(SacrificeSelfToDestroyCreatureDamagedPlayerControlsEffect(true))` — may sacrifice self, if you do destroy target creature the defending player (trigger `targetId`) controls; `cannotBeRegenerated=true`. The trigger is not created without a legal creature target, and its eligible target IDs are snapshotted at trigger creation. |
| Unblocked may-steal-permanent-and-assign-no-damage | `o/OrcishSquatters.java`, `k/KukemssaPirates.java` | ON_ATTACKS_UNBLOCKED `MayEffect(GainControlOfPermanentDefendingPlayerControlsAndAssignNoCombatDamageEffect(filter, duration, noun))` — may gain control of a permanent matching `filter` the defending player (trigger `targetId`) controls; if you do, the attacker (`sourcePermanentId`) assigns no combat damage this turn. Orcish Squatters = `PermanentIsLandPredicate`/`WHILE_SOURCE_ON_BATTLEFIELD`/"land"; Kukemssa Pirates = `PermanentIsArtifactPredicate`/`PERMANENT`/"artifact". Inner effect presents a multi-permanent choice (max 1); completion wraps a plain `GainControlOfTargetEffect(duration)` and adds the attacker to `creaturesPreventedFromDealingCombatDamage` |
| Unblocked may-pay-destroy-defender-artifact + no damage | `g/GoblinVandal.java` | ON_ATTACKS_UNBLOCKED `MayPayManaEffect("{R}", DestroyPermanentDefendingPlayerControlsAndAssignNoCombatDamageEffect(PermanentIsArtifactPredicate, "artifact"), prompt)` — pay {R} to destroy an artifact the defending player (trigger `targetId`) controls; the attacker (`sourcePermanentId`) then assigns no combat damage this turn. Destroy sibling of the Kukemssa Pirates gain-control effect |
| Attack-alone restriction + unblocked life-total-to-1 | `m/MasterOfCruelties.java` | STATIC `CanOnlyAttackAloneEffect()` + ON_ATTACKS_UNBLOCKED `SetLifeTotalEffect(1, SetLifeTotalRecipient.TARGET_PLAYER)` + `AssignNoCombatDamageEffect()` — the defending player is the trigger's non-targeting `targetId` |
| Unblocked no-damage + defender drain, upkeep discard-or-sac-and-draw | `l/LimDLsPaladin.java` | ON_ATTACKS_UNBLOCKED `AssignNoCombatDamageEffect()` + `LoseLifeEffect(4, LoseLifeRecipient.TARGET_PLAYER)` — the unblocked-attack trigger bakes the defending player as `targetId` (NOT `attackedTargetId`, so `DEFENDING_PLAYER` would no-op here). Plus UPKEEP_TRIGGERED `SacrificeUnlessDiscardCardTypeEffect(null, false, true)` and ON_BECOMES_BLOCKED `BoostSelfEffect(6, 3)` |
| On becomes blocked | `s/SylvanBasilisk.java` | ON_BECOMES_BLOCKED DestroyCreatureBlockingThisEffect |
| On becomes blocked — may destroy a defender's artifact/enchantment | `r/RustScarab.java` | ON_BECOMES_BLOCKED (plain) `MayEffect(DestroyTargetPermanentEffect())` + card-level `PermanentPredicateTargetFilter(AllOf(AnyOf(IsArtifact, IsEnchantment), ControlledByDefendingPlayer))` — "you may destroy target artifact or enchantment defending player controls". `CombatBlockService` routes permanent-targeting becomes-blocked mays through `queueMayAbility`, so the may and then the target are resolved on the stack (CR 603.5) honouring the card's filter |
| On becomes blocked — defending player drain | `v/VedalkenGhoul.java` | ON_BECOMES_BLOCKED `LoseLifeEffect(4, LoseLifeRecipient.DEFENDING_PLAYER)` — "defending player loses 4 life". `DEFENDING_PLAYER` reads the attacked player/planeswalker-controller baked as the trigger's `attackedTargetId`; no target chosen |
| On becomes blocked — defending player discards | `s/SlateStreetRuffian.java` | ON_BECOMES_BLOCKED `DiscardEffect(1, DiscardRecipient.DEFENDING_PLAYER)` — "defending player discards a card". Same `attackedTargetId` routing as the drain above; the defending player picks the card |
| On becomes blocked — defending player redraws their hand | `r/RobberFly.java` | ON_BECOMES_BLOCKED `DefendingPlayerDiscardsHandThenDrawsThatManyEffect()` — the defending player discards their entire hand, then draws that many; the effect reads the combat trigger's `attackedTargetId` |
| Ally creature becomes blocked (self-boost "it") | `u/UnstoppableAsh.java` | ON_ALLY_CREATURE_BECOMES_BLOCKED `BoostSelfEffect(0, 5)` — "whenever a creature you control becomes blocked, it gets +0/+5 until end of turn". Fires once per blocked attacker; "it" = the blocked creature via non-targeting sourcePermanentId. Wrap in `TriggeringCardConditionalEffect` to filter |
| Global subtype creature becomes blocked | `s/SpinedSliver.java` | ON_ANY_CREATURE_BECOMES_BLOCKED `TriggeringCardConditionalEffect(CardSubtypePredicate(SLIVER), BoostSelfEffect(CreaturesBlockingSource(), CreaturesBlockingSource()))` — fires once per matching blocked attacker across all controllers; the blocked attacker is both source and non-targeting target, and the blocker count is read at resolution. |
| Becomes blocked by a filtered creature, shrink it | `c/CatacombDragon.java` | ON_BECOMES_BLOCKED (PER_BLOCKER) `TriggeringPermanentConditionalEffect(filter, BoostTargetCreatureEffect(Scaled(Divided(TargetPower(), 2), -1), Fixed(0)))` — "becomes blocked by a nonartifact, non-Dragon creature, that creature gets -X/-0 where X is half its power, rounded down". The wrapper filters per blocker in `CombatBlockService` |
| Blocks/becomes-blocked destroy at end of combat (filtered) | `d/Deathgazer.java` | ON_BLOCK + ON_BECOMES_BLOCKED (PER_BLOCKER) `DestroyCombatOpponentAtEndOfCombatEffect(filter, cannotBeRegenerated)` — "blocks or becomes blocked by a [filter] creature, destroy that creature at end of combat" (Basilisk-style). Deathgazer's filter is nonblack (`PermanentNotPredicate(PermanentColorInPredicate({BLACK}))`). Destroys at end of combat, not immediately |
| Basilisk-style destroy at end of combat as an **Aura** | `v/Venom.java` | `target(creature)` + same ON_BLOCK + ON_BECOMES_BLOCKED (PER_BLOCKER) `DestroyCombatOpponentAtEndOfCombatEffect(nonWall, false)` where `nonWall = PermanentNotPredicate(PermanentHasSubtypePredicate(WALL))`. The effect implements `CombatOpponentReferencingEffect`, so `CombatTriggerService` auto-targets the enchanted creature's combat opponent (aura/equipment flow) exactly as Deathgazer's creature triggers do |
| Attack self-boost + becomes-blocked tap/untap-lock at end of combat | `j/JovensFerrets.java` | ON_ATTACK `BoostSelfEffect(0, 2)` + ON_BECOMES_BLOCKED (PER_BLOCKER) `TapCombatOpponentAtEndOfCombatEffect()` — "At end of combat, tap all creatures that blocked this creature this turn. They don't untap during their controller's next untap step." Scheduled per blocker as it becomes blocked; the tap + `skipUntapCount` bump happen at end of combat, so the blocker still deals its combat damage |
| Becomes-blocked sacrifice at end of combat + token rider | `b/BasaltGolem.java` | ON_BECOMES_BLOCKED (PER_BLOCKER) `SacrificeCombatOpponentAtEndOfCombatEffect(CreateTokenEffect)` — "that creature's controller sacrifices it at end of combat. If the player does, they create a 0/2 colorless Wall artifact creature token with defender." Sacrifice sibling of Deathgazer's destroy. Also `STATIC CantBeBlockedByCreaturesMatchingPredicateEffect(PermanentIsArtifactPredicate)` for "can't be blocked by artifact creatures" |
| Blocks/becomes-blocked -0/-2 counter at end of combat | `g/GreaterWerewolf.java` | ON_BLOCK + ON_BECOMES_BLOCKED (PER_BLOCKER) `PutCounterOnCombatOpponentAtEndOfCombatEffect(CounterType.MINUS_ZERO_MINUS_TWO, 1)` — "At end of combat, put a -0/-2 counter on each creature blocking or blocked by this creature". The counter-analog of Deathgazer; combat opponent is queued a `PutCounterOnPermanentAtEndOfCombat` delayed action. `MINUS_ZERO_MINUS_TWO` counters subtract 2 toughness each |
| Attacked-or-blocked self -0/-1 counter + token at end of combat | `k/KjeldoranHomeGuard.java` | ON_ATTACK + ON_BLOCK `PutCounterOnSourceAtEndOfCombatEffect(CounterType.MINUS_ZERO_MINUS_ONE, 1, deserterToken)` — "At end of combat, if this creature attacked or blocked this combat, put a -0/-1 counter on this creature and create a 0/1 white Deserter creature token". The two combat slots encode the intervening-if; both halves are skipped if the creature already left the battlefield |
| Attacked-or-blocked self bounce at end of combat | `p/PhantomWhelp.java` | ON_ATTACK + ON_BLOCK `ReturnSelfToHandAtEndOfCombatEffect()` — "When this creature attacks or blocks, return it to its owner's hand at end of combat". The source permanent is scheduled after the trigger resolves, and the delayed action skips it if it has already left the battlefield |
| Blocks/becomes-blocked paralyzation counter + tap at end of combat | `d/DreadWight.java` | ON_BLOCK + ON_BECOMES_BLOCKED (PER_BLOCKER) `PutCounterOnCombatOpponentAtEndOfCombatEffect(CounterType.PARALYZATION, 1, true)` — tap + paralyzation counter; counter prevents untap during untap step and grants `{4}: Remove a paralyzation counter` on placement |
| Blocks: counters plus persistent granted abilities | `m/MindbenderSpores.java` | ON_BLOCK `PutCounterOnCombatOpponentEffect(FUNGUS, 4, DoesntUntapWithCounterEffect(FUNGUS), RemoveCounterFromSourceEffect(FUNGUS, 1))` — immediate counters on the blocked attacker plus the two abilities granted by the source's wording. The grants outlive the Spores; fungus counters placed by unrelated cards have no intrinsic rules |
| Becomes-blocked gain control of blockers at end of combat | `t/TheWretched.java` | ON_BECOMES_BLOCKED (PER_BLOCKER) `GainControlOfCombatOpponentAtEndOfCombatEffect()` — "At end of combat, gain control of all creatures blocking this creature for as long as you control this creature". Blockers only (no ON_BLOCK). Each blocker queues a `GainControlOfPermanentAtEndOfCombat` delayed action; control applied `WHILE_SOURCE_ON_BATTLEFIELD` in `CombatService.processEndOfCombatControlGains()`. Pass `ControlDuration.PERMANENT` for the durationless "gain control of that creature at end of combat" (`t/TolarianEntrancer.java`), which also applies if the source has since left |
| Blocks/becomes-blocked grant keyword to that creature | `w/WitherscaleWurm.java` | ON_BLOCK + ON_BECOMES_BLOCKED (PER_BLOCKER) `GrantKeywordEffect(keyword, GrantScope.TARGET)` — "blocks or becomes blocked by a creature, that creature gains [keyword] until end of turn". The combat opponent is carried as the trigger's non-targeting target (a `GrantScope.TARGET` grant on ON_BLOCK auto-targets the blocked attacker). Also removes its own -1/-1 counters on damage via ON_DAMAGE_TO_PLAYER `RemoveAllCountersEffect(MINUS_ONE_MINUS_ONE)` |
| Blocks/becomes-blocked boost that creature | `f/FlailingDrake.java` | ON_BLOCK + ON_BECOMES_BLOCKED (PER_BLOCKER) `BoostTargetCreatureEffect(1, 1)` — "blocks or becomes blocked by a creature, that creature gets +1/+1 until end of turn". The boosted creature is the combat opponent, baked in as the trigger's non-targeting target (`BoostTargetCreatureEffect` is a `CombatOpponentReferencingEffect`) |
| Blocks/becomes-blocked can't regenerate that creature | `l/LimDLsCohort.java` | ON_BLOCK + ON_BECOMES_BLOCKED (PER_BLOCKER) `PreventTargetCreatureRegenerationThisTurnEffect()` — "blocks or becomes blocked by a creature, that creature can't be regenerated this turn". Combat opponent is the non-targeting target (`CombatOpponentReferencingEffect`) |
| Blocks/becomes-blocked phase both out | `d/DreamFighter.java` | ON_BLOCK + ON_BECOMES_BLOCKED (PER_BLOCKER) `PhaseOutSelfAndCombatOpponentEffect()` — "blocks or becomes blocked by a creature, this creature and that creature phase out". Source + combat opponent both move to `GameData.phasedOutPermanents` via `PhasingService.phaseOut(...)` and are removed from combat (CR 506.4), so no combat damage happens; each phases in during its own controller's next untap step |
| On block (mutual destroy) | `l/LoyalSentry.java` | ON_BLOCK DestroyBlockedCreatureAndSelfEffect |
| On block, blocked creature can't attack next turn | `w/WallOfDust.java` | ON_BLOCK `CantAttackNextTurnCombatOpponentEffect()` — "Whenever this creature blocks a creature, that creature can't attack during its controller's next turn". Auto-targets the blocked attacker (a `CombatOpponentReferencingEffect`, non-targeting); sets `Permanent.cantAttackNextTurn`, promoted to `cantAttackThisTurn` at that creature's controller's next turn start and self-clears after one turn. Defender is auto-loaded from Scryfall |
| On block conditional boost | `e/EzurisArchers.java` | ON_BLOCK BoostSelfWhenBlockingKeywordEffect(FLYING, 3, 0) — conditional trigger checked at block time |
| On block, damage blocked creature's controller = power | `m/Meglonoth.java` | ON_BLOCK `DealDamageToCombatOpponentControllerEffect(new SourcePower())` — "Whenever this creature blocks a creature, it deals damage to that creature's controller equal to this creature's power". THIS creature is the damage source; recipient is the blocked creature's controller. Auto-targets the blocked attacker (a `CombatOpponentReferencingEffect`, non-targeting). Vigilance/trample auto-load from Scryfall |
| Blocks/becomes-blocked self-boost (opponent-filtered) | `d/DwarvenSoldier.java` | ON_BLOCK + ON_BECOMES_BLOCKED `BoostSelfWhenCombatOpponentMatchesEffect(PermanentPredicate, power, toughness)` — "blocks or becomes blocked by one or more [filter] creatures, this creature gets +X/+Y until end of turn". Non-targeting; the trigger always goes on the stack and the handler applies the boost only if a combat opponent (a creature this blocks, or a creature blocking it) matches the filter. Fires once regardless of how many opponents match. Dwarven Soldier's filter is `PermanentHasSubtypePredicate(ORC)`, +0/+2. Plain (non-PER_BLOCKER) on both slots Optional 4th arg `Set<Keyword> grantedKeywords` also grants those keywords until end of turn — Crimson Roc (`PermanentNotPredicate(PermanentHasKeywordPredicate(FLYING))`, +1/+0, `Set.of(FIRST_STRIKE)`, ON_BLOCK only, for "whenever this creature blocks a creature without flying"). |
| On damaged creature dies | `s/SengirVampire.java` | ON_DAMAGED_CREATURE_DIES PutPlusOnePlusOneCounterOnSourceEffect |
| Opponent creature dies → may cast a targeted instant from your graveyard (for its cost), exiled instead of returning | `t/ToshiroUmezawa.java` | ON_OPPONENT_CREATURE_DIES `GrantTargetGraveyardCardCastEffect(CardTypePredicate(INSTANT), CONTROLLERS_GRAVEYARD, true)` — target picked as the trigger is stacked; resolution records a one-shot graveyard-cast permission the player spends through the normal cast path |
| Opponent creature dies → steal it at end step with a counter + type grant (enchantment) | `g/GraveBetrayal.java` | ON_OPPONENT_CREATURE_DIES `RegisterDelayedReturnDyingCreatureUnderControlEffect(false, CounterType.PLUS_ONE_PLUS_ONE, 1, CardColor.BLACK, CardSubtype.ZOMBIE)` — same delayed-return infra as Seraph, with `sacrificeOnSourceControlLoss=false` (no control-loss linkage) plus entry counters and permanent color/subtype grants. A dedicated `@CollectsTrigger` on the opponent-death slot bakes the dying card id into `StackEntry.triggeringCardId` |
| On damaged creature dies → steal it at end step | `s/Seraph.java` | ON_DAMAGED_CREATURE_DIES `RegisterDelayedReturnDyingCreatureUnderControlEffect()` — "put that card onto the battlefield under your control at the beginning of the next end step; sacrifice it when you lose control of this creature". The trigger bakes the dying card id into `StackEntry.triggeringCardId`; the handler queues `DelayedGraveyardToBattlefieldUnderControl` (drained at end step, returns under your control with a PERMANENT control effect + owner recorded via `stolenCreatures` so it dies to its owner's graveyard). Linkage tracked in `GameData.seraphReturnedCreatures`; `StateBasedActionService` sacrifices linked creatures when their controller loses control of the still-on-battlefield Seraph |
| Ally nontoken creature dies → transform this creature into a planeswalker, "if you do" make a token | `l/LilianaHereticalHealer.java` | ON_ALLY_NONTOKEN_CREATURE_DIES `ExileSelfAndReturnTransformedEffect(CreateTokenEffect.blackZombie(1))` — the slot already excludes tokens and the source itself. The "if you do" token is the effect's `thenEffect` rider (resolved by `ExileSelfAndReturnTransformedEffectHandler` only when the exile-and-return actually happened), not a second entry in the effect list |
| End step: if damaged creature died → steal it | `k/KrovikanVampire.java` | END_STEP_TRIGGERED `ConditionalEffect(SourceDamagedCreatureDiedThisTurn, ReturnDamagedCreaturesThatDiedUnderControlEffect)` — intervening-if (source must be on the battlefield at end step). Tracks deaths in `GameData.sourcesWhoseDamagedCreaturesDiedThisTurn` / `creatureCardsDamagedBySourceThatDiedThisTurn` (pruned on leave-graveyard). Same `seraphReturnedCreatures` control-loss sacrifice as Seraph |
| On controller gains life | `a/AjanisPridemate.java` | ON_CONTROLLER_GAINS_LIFE PutCountersOnSourceEffect(1,1,1) — +1/+1 counter whenever controller gains life |
| On controller gains life (draw) | `d/DrogskolReaver.java` | ON_CONTROLLER_GAINS_LIFE DrawCardEffect(1) — draw a card whenever controller gains life (fires once per life-gain event, regardless of amount). Flying/Double strike/Lifelink auto-load from Scryfall |
| On ally creature with subtype enters | `c/ChampionOfTheParish.java` | ON_ALLY_CREATURE_ENTERS_BATTLEFIELD TriggeringCardConditionalEffect(CardSubtypePredicate(HUMAN), PutCountersOnSourceEffect(1,1,1)) — +1/+1 counter whenever another Human you control enters |
| Elf/Faerie enters — exile and one free cast this turn | `m/MaralenFaeAscendant.java` | ON_SELF_OR_ALLY_CREATURE_ENTERS_BATTLEFIELD `TriggeringCardConditionalEffect(CardAnyOfPredicate(ELF, FAERIE), ExileTopCardsToSourceEffect(2, false, false, TARGET_OPPONENT, true))` with an opponent target + STATIC `AllowCastFromCardsExiledWithSourceEffect(false, nonland, false, false, 0, PermanentCount(PermanentHasAnySubtypePredicate({ELF, FAERIE}), CONTROLLER), true, true, true)` |
| Subtype lord + on ally subtype enters destroy (targeted) | `r/ReaperKing.java` | STATIC StaticBoostEffect(1, 1, OWN_CREATURES, PermanentHasSubtypePredicate(SCARECROW)) + ON_ALLY_CREATURE_ENTERS_BATTLEFIELD TriggeringCardConditionalEffect(CardSubtypePredicate(SCARECROW), DestroyTargetPermanentEffect(false)) — +1/+1 to other Scarecrows, and whenever another Scarecrow you control enters, destroy target permanent (mandatory targeted enter trigger routed through the `EntersTriggerTarget` pipeline; controller picks any permanent) |
| On dealt damage to self | `n/NestedGhoul.java` | ON_DEALT_DAMAGE CreateTokenEffect — fires per source, both combat and non-combat |
| Another ally creature enters (pump it + haste) | `o/OgreBattledriver.java` | ON_ALLY_CREATURE_ENTERS_BATTLEFIELD BoostEnteringCreatureEffect(2, 0, Set.of(Keyword.HASTE)) — "that creature gets +2/+0 and gains haste until end of turn"; mandatory, baked onto the entering creature |
| Combat damage: make tokens + sacrifice self | `k/KathariBomber.java` | ON_COMBAT_DAMAGE_TO_PLAYER SequenceEffect.of(CreateTokenEffect(2 Goblin 1/1 red), SacrificeSelfEffect()) — one atomic entry. SacrificeSelfEffect reports CombatDamageTriggerContextEffect.SOURCE_SELF and SequenceEffect forwards its steps' richest context, so the entry binds the source for the sacrifice (a plain combat-damage entry has no source). Also has Unearth {3}{B}{R} |
| Combat damage reveal-hand pick + exile all copies | `s/ShimianSpecter.java` | ON_COMBAT_DAMAGE_TO_PLAYER `RevealHandChooseCardFromItAndExileAllCopiesEffect(List.of(CardType.LAND))` — damaged player reveals hand, controller picks a nonland card from it, every copy of that name is exiled from hand/graveyard/library, then they shuffle |
| On dealt damage: deal that much to the chosen player | `s/StuffyDoll.java` | ON_DEALT_DAMAGE DealDamageToPlayersEffect(new EventValue(), EACH_OPPONENT) + `{T}:` DealDamageToSourceEffect(1); Indestructible from Scryfall. Damage dealt snapshots onto the trigger's eventValue |
| On dealt damage: reflect damage to a creature source or its spell controller | `t/Tephraderm.java` | ON_DEALT_DAMAGE DealDamageToDamageSourceCreatureOrSpellControllerEffect() marker — damage amount and source identity bind at trigger time; the creature or spell controller is damaged without a target |
| On dealt damage: deal that much to any target | `s/Spitemare.java` | ON_DEALT_DAMAGE DealDamageToAnyTargetEffect(new XValue()) — damage dealt snapshots to xValue, controller picks any target (combat + non-combat) |
| On dealt damage: may destroy target nonland permanent | `h/HighPriestOfPenance.java` | `target(TargetFilters.nonlandPermanent())` + ON_DEALT_DAMAGE MayEffect(DestroyTargetPermanentEffect) — a permanent-targeting MayEffect in this slot routes through `SpellTargetTriggerAnyTarget`, so the target is chosen when the trigger stacks and the "may" is answered on resolution (combat + non-combat, once per source) |
| On dealt damage: source controller poison | `r/ReaperOfSheoldred.java` | ON_DEALT_DAMAGE DamageSourceControllerGetsPoisonCounterEffect(null) marker — UUID filled at trigger time, gives source's controller 1 poison counter |
| On dealt damage: source controller mills that many | `b/BelltowerSphinx.java` | ON_DEALT_DAMAGE DamageSourceControllerMillsEffect() marker — damage amount + source controller bound at trigger time, that player mills that many cards |
| On dealt damage: source controller sacrifices | `p/PhyrexianObliterator.java` | ON_DEALT_DAMAGE DamageSourceControllerSacrificesPermanentsEffect(0,null) marker — count+playerId filled at trigger time, multi-permanent choice for sacrifice |
| Whenever this creature deals damage: gain that much life | `e/ElHajjJ.java` | ON_SELF_DEALS_DAMAGE GainLifeEffect(new EventValue()) — fires only for this creature's own damage (combat + non-combat, any target), summed total snapshotted to eventValue; still fires when it dies dealing the damage |
| Whenever an instant/sorcery you control deals damage: create two tokens | `b/BlazeCommando.java` | ON_ALLY_INSTANT_OR_SORCERY_DEALS_DAMAGE CreateTokenEffect(2, "Soldier", 1, 1, RED, Set.of(RED, WHITE), List.of(SOLDIER), Set.of(HASTE), Set.of()) — one trigger per damage event however many objects the spell hit at once; combat damage and opponents' spells never qualify |
| Whenever an instant/sorcery you control damages an opponent: damage that opponent's creature for that much | `s/SatyrFiredancer.java` | ON_ALLY_INSTANT_OR_SORCERY_DEALS_DAMAGE DealDamageToTargetCreatureDamagedPlayerControlsEffect(new EventValue()) — the batched damage event preserves each damaged player's effective damage; one trigger is queued per opponent hit, and the controller chooses that player's creature on resolution |
| Opponent's source damages you: may add that many +1/+1 counters | `r/RetaliatorGriffin.java` | ON_CONTROLLER_DEALT_DAMAGE_BY_OPPONENT MayEffect(PutCountersOnSelfEffect(PLUS_ONE_PLUS_ONE, new EventValue())) — like Living Artifact's ON_CONTROLLER_DEALT_DAMAGE but gated to opponent-controlled sources (own self-damage ignored); damage snapshotted to eventValue, read after the "you may" is accepted. Register the trigger on MayEffect.class (registry unwraps a bare inner effect) |
| On opponent creature dealt damage | `k/KazarovSengirPureblood.java` | ON_OPPONENT_CREATURE_DEALT_DAMAGE PutCountersOnSelfEffect(PLUS_ONE_PLUS_ONE) — fires per damaged creature (combat + non-combat), scans all battlefields |
| Other creature enters | `s/SoulWarden.java` | ON_ANY_OTHER_CREATURE_ENTERS_BATTLEFIELD GainLifeEffect |
| Other creature enters (may put X +1/+1 counters = its power) | `h/HamletbackGoliath.java` | ON_ANY_OTHER_CREATURE_ENTERS_BATTLEFIELD PutCountersOnSourceEqualToEnteringPowerEffect(1, 1, true) — you may put X +1/+1 counters on this creature where X = entering creature's power |
| Another creature dies (may put X +1/+1 counters = its power) | `k/KreshTheBloodbraided.java` | ON_ANY_CREATURE_DIES PutCountersOnSourceEqualToDyingPowerEffect(1, 1, true) — you may put X +1/+1 counters on this creature where X = the dying creature's last-known effective power |
| Opponent searches their library (edict + big life loss) + another creature dies (grow) | `o/ObNixilisUnshackled.java` | ON_OPPONENT_SEARCHES_LIBRARY SacrificePermanentsEffect(1, creature, TARGET_PLAYER) + LoseLifeEffect(10, TARGET_PLAYER) — searching player baked in as `targetId`; plus ON_ANY_CREATURE_DIES PutCountersOnSourceEffect(1, 1, 1) |
| This or another creature you control enters (it fights an opponent creature) | `g/GruulRagebeast.java` | ON_SELF_OR_ALLY_CREATURE_ENTERS_BATTLEFIELD EnteringCreatureFightsTargetCreatureEffect() + `target(TargetFilters.creatureAnOpponentControls())` — the slot is the ally-enter scan without the self-exclusion, so the Ragebeast's own ETB triggers it too; the entering permanent rides along as `triggeringPermanentId` so "that creature" is the fighter, not the source. No opponent creature ⇒ the trigger is removed for want of a legal target (CR 603.3d) |
| Other creature enters (deal damage to controller) + static no life gain | `r/RampagingFerocidon.java` | STATIC PlayersCantGainLifeEffect + ON_ANY_OTHER_CREATURE_ENTERS_BATTLEFIELD DealDamageToPlayersEffect(1, DamageRecipient.TARGET_PLAYER) — targetId set to entering creature's controller at trigger time |
| Any creature enters (deal N damage to it) | `a/AetherFlash.java` | ON_ANY_OTHER_CREATURE_ENTERS_BATTLEFIELD DealDamageToTargetCreatureEffect(2) — enter collector bakes the entering creature's permanent id in as the (non-chosen) target; this permanent is the damage source. Enchantment, not a creature |
| Ally subtype enters (may transform it) | `v/VildinPackAlpha.java` (front `g/GeierReachBandit.java`) | ON_ALLY_CREATURE_ENTERS_BATTLEFIELD TriggeringCardConditionalEffect(CardSubtypePredicate(WEREWOLF), TransformEnteringCreatureEffect()) — may transform the entering Werewolf (baked target), not the source. Front: EACH_UPKEEP NoSpellsCastLastTurn→TransformSelf; back also EACH_UPKEEP TwoOrMoreSpellsCastLastTurn→TransformSelf |
| Creature you control with power N+ enters (may put M +1/+1 counters on it) | `m/MightyEmergence.java` | ON_ALLY_CREATURE_ENTERS_BATTLEFIELD EnteringCreatureMinPowerConditionalEffect(5, PutCountersOnEnteringCreatureEffect(2)) — counters go on the entering creature (baked target), not the source. Enchantment, not a creature |
| 1/1 creature you control enters (mandatory two +1/+1 counters on it) | `s/SigilCaptain.java` | ON_ALLY_CREATURE_ENTERS_BATTLEFIELD EnteringCreatureExactStatsConditionalEffect(1, 1, PutCountersOnEnteringCreatureEffect(2, false)) — intervening-if "is 1/1" gate (exact printed power AND toughness); `optional=false` bakes the mandatory counter placement on the entering creature directly onto the stack (no "you may" prompt) |
| Creature you control with power N+ enters (may put a +1/+1 counter on THIS creature) | `g/GodtrackerOfJund.java` | ON_ALLY_CREATURE_ENTERS_BATTLEFIELD EnteringCreatureMinPowerConditionalEffect(5, MayEffect(PutCountersOnSourceEffect(1,1,1), "…")) — counter goes on the source, not the entering creature. handleEnterMay binds the source permanent for ally scans, so the may-ability resolves PutCountersOnSourceEffect against Godtracker itself |
| Ally creature enters (may gain life) + opponent creature enters (may lose life) | `s/SuturePriest.java` | ON_ALLY_CREATURE_ENTERS_BATTLEFIELD MayEffect(GainLifeEffect(1)) + ON_OPPONENT_CREATURE_ENTERS_BATTLEFIELD MayEffect(LoseLifeEffect(1, TARGET_PLAYER)) |
| Ally subtype creature enters (may gain life = its toughness) | `o/OrchardWarden.java` | ON_ALLY_CREATURE_ENTERS_BATTLEFIELD TriggeringCardConditionalEffect(CardSubtypePredicate(TREEFOLK), MayEffect(GainLifeEqualToToughnessEffect())) — the collector reads the entering creature's toughness at trigger time and queues the may-ability as MayEffect(GainLifeEffect(toughness)) |
| Ally nontoken dies → return it at next end step; opponents' creatures exiled instead of dying | `l/LiesaForgottenArchangel.java` | ON_ALLY_NONTOKEN_CREATURE_DIES `RegisterDelayedReturnCardFromGraveyardToHandEffect(null)` (the death collector binds the dying card id) + STATIC `ExileOpponentCreaturesInsteadOfDyingEffect()`. The exile replacement is keyed on the dying permanent's controller, so it lives in `PermanentRemovalService.processGraveyardAndTriggers`, not `GraveyardService` |
| Opponents' creatures die → exile with this creature; return them during your upkeep with decayed | `g/GisaGloriousResurrector.java` | STATIC `ExileOpponentCreaturesInsteadOfDyingEffect.withSourceTracking()` + UPKEEP_TRIGGERED `ReturnAllCardsExiledWithSourceEffect(true, CardTypePredicate(CREATURE), false, Set.of(DECAYED))`. Source tracking is applied at the replacement site; returned cards receive the indefinite keyword grant |
| Ally power-1 creature dies → may reanimate it at next end step | `s/ShireiShizosCaretaker.java` | ON_ALLY_CREATURE_DIES `TriggeringPermanentConditionalEffect(PermanentPowerAtMostPredicate(1), MayEffect(RegisterDelayedReturnDyingCreatureUnderControlEffect(true)))` — last-known power gates the trigger; the `true` is `requireSourceOnBattlefield`, so the delayed return is skipped unless that exact source permanent is still around at the end step |
| Ally creature dies | `g/GravePact.java` | ON_ALLY_CREATURE_DIES SacrificePermanentsEffect(1, PermanentIsCreaturePredicate, EACH_OPPONENT) |
| Ally nontoken dies → exile it; upkeep pay mana+life to reanimate (enchantment) | `p/Purgatory.java` | ON_ALLY_NONTOKEN_CREATURE_DIES ExileTriggeringCreatureAndTrackWithSourceEffect() (mandatory — no MayEffect; the collector binds the dying card id) + UPKEEP_TRIGGERED MayPayManaEffect("{4}", 2, ReturnCardExiledWithSourceToBattlefieldEffect(), prompt) — the `int lifeCost` overload is mana **and** life; the returned card enters under the ability controller's control (CR 110.2a) |
| Ally dies → opponents lose life; opponent dies → you gain life (enchantment) | `t/TheMeathookMassacre.java` | ON_ENTER_BATTLEFIELD BoostAllCreaturesEffect(Scaled(XValue,-1), same) + ON_ALLY_CREATURE_DIES LoseLifeEffect(1, EACH_OPPONENT) + ON_OPPONENT_CREATURE_DIES GainLifeEffect(1). Cast X snapshotted onto ETB stack entry |
| Non-subtype ally dies → transform at next upkeep (DFC) | `a/ArchangelAvacyn.java` / `a/AvacynThePurifier.java` | Front: ON_ENTER_BATTLEFIELD GrantKeywordEffect(INDESTRUCTIBLE, OWN_CREATURES) + ON_ALLY_CREATURE_DIES TriggeringCardConditionalEffect(CardNotPredicate(CardSubtypePredicate(ANGEL)), RegisterTransformSourceAtNextUpkeepEffect()). Back: ON_TRANSFORM_TO_BACK_FACE MassDamageEffect(3, false, false, Not(IsSource)) + DealDamageToPlayersEffect(3, EACH_OPPONENT). Flash/flying/vigilance auto-loaded |
| Ally creature dies (subtype-filtered, multi-effect) | `s/Slimefoot.java` | ON_ALLY_CREATURE_DIES TriggeringCardConditionalEffect(CardSubtypePredicate(SAPROLING), DealDamageToPlayersEffect(1, DamageRecipient.EACH_OPPONENT)) + TriggeringCardConditionalEffect(CardSubtypePredicate(SAPROLING), GainLifeEffect(1)) — only fires when dying creature matches the predicate; multiple effects grouped into single stack entry |
| Another subtype creature dies — target opponent loses life (lord) | `d/DiregrafCaptain.java` | STATIC StaticBoostEffect(1,1,OWN_CREATURES,PermanentHasAnySubtypePredicate(ZOMBIE)) anthem + `target(PlayerPredicateTargetFilter(OPPONENT)).addEffect(ON_ALLY_CREATURE_DIES, TriggeringCardConditionalEffect(CardSubtypePredicate(ZOMBIE), LoseLifeEffect(1, TARGET_PLAYER)))`. Targeting ON_ALLY_CREATURE_DIES routes through the Death target pipeline (controller picks the opponent as the ability is stacked). "another" is automatic — the dying creature is off the battlefield before the watcher's trigger is checked. Deathtouch is auto-applied from the Scryfall keyword |
| This or another subtype dies — look at top, may reveal matching to hand else may graveyard | `a/ArchghoulOfThraben.java` | `ON_ALLY_CREATURE_DIES` TriggeringCardConditionalEffect(CardSubtypePredicate(ZOMBIE), LookAtTopCardMayRevealMatchingToHandEffect(…)) + same effect on `ON_DEATH`. Ally slot = "another"; death slot = self. Matching top → may reveal to hand; decline/non-match → may put into graveyard |
| Another subtype dies — may ping a player/planeswalker (enchantment) | `b/BoggartShenanigans.java` | ON_ALLY_CREATURE_DIES TriggeringCardConditionalEffect(CardSubtypePredicate(GOBLIN), MayEffect(DealDamageToTargetPlayerOrPlaneswalkerEffect(1))). MayEffect-wrapped targeted effect: the collector queues a may-ability (not the Death target pipeline) and the target is chosen during may-resolution. Non-creature source satisfies "another" automatically |
| Any creature dies (may loot) | `m/MurderOfCrows.java` | ON_ANY_CREATURE_DIES MayEffect(SequenceEffect.of(DrawCardEffect(1), DiscardEffect(1, DiscardRecipient.CONTROLLER))) — may draw+discard on any creature death |
| Another creature LEAVES battlefield — may mill target player | `e/ExtractorDemon.java` | ON_ANOTHER_CREATURE_LEAVES_BATTLEFIELD MayEffect(MillEffect(2, TARGET_PLAYER), "have target player mill two cards"). "Leaves the battlefield" is broader than "dies" — fires on destroy/exile/bounce/sacrifice/tuck (global watcher in `PermanentRemovalService`). Non-targeting slot: the "may" and player target resolve on the stack (Hollowsage pattern). Also has Unearth {2}{B} |
| Growing upkeep tax + gives the opponent an X/X on leave | `p/PhantasmalSphere.java` | UPKEEP_TRIGGERED `SequenceEffect.of(PutCountersOnSourceEffect(1,1,1), DestroyUnlessPaysPerCounterEffect(PLUS_ONE_PLUS_ONE, "{1}", true))` + `target(PlayerPredicateTargetFilter(OPPONENT))` ON_SELF_LEAVES_BATTLEFIELD `CreateTokenForTargetPlayerEffect(CreateTokenEffect("Orb", CountersOnSource(PLUS_ONE_PLUS_ONE), …, FLYING))`. One sequence on the upkeep slot so the counter lands before the cost is sized; the token's X is frozen from the leaving permanent by the self-leaves collector |
| CU + gain life equal to its own age counters on leave | `r/ReveredUnicorn.java` | UPKEEP_TRIGGERED `CumulativeUpkeepEffect("{1}")` + ON_SELF_LEAVES_BATTLEFIELD `GainLifeEffect(CountersOnSource(AGE))`. The source is gone by resolution, so `DeathTriggerCollectorService.handleSelfLeavesGainLife` evaluates the source-relative amount against the leaving permanent and freezes it into a `Fixed` before the ordinary self-leaves routing |
| Ally creature LEAVES — gain 1 life (Disturb back) | `l/LuminousPhantom.java` | ON_ALLY_CREATURE_LEAVES_BATTLEFIELD GainLifeEffect(1) + STATIC ExileInsteadOfGraveyardReplacementEffect. Controller-scoped leave watcher; pair with front-face DisturbCast |
| Disturb die-mill → ETB shuffle GY (creature) | `c/CovetousCastaway.java` + `g/GhostlyCastigator.java` | Front: ON_DEATH MillEffect(3, CONTROLLER) + DisturbCast. Back: ON_ENTER_BATTLEFIELD ShuffleTargetCardsFromControllerGraveyardIntoLibraryEffect(null, 3) + STATIC ExileInsteadOfGraveyardReplacementEffect |
| Disturb creature → Aura (grant keyword) | `t/TwinbladeGeist.java` + `t/TwinbladeInvocation.java` | Front: DisturbCast only (keywords auto-loaded). Back: target(creature) + STATIC GrantKeywordEffect(DOUBLE_STRIKE, ENCHANTED_CREATURE) + ExileInsteadOfGraveyardReplacementEffect. Resolve via ENCHANTMENT_SPELL + castWithDisturb |
| Another artifact you control ENTERS or LEAVES — may pay {1} to drain | `s/SludgeStrider.java` | One shared effect on both `ON_ALLY_ARTIFACT_ENTERS_BATTLEFIELD` and `ON_ANOTHER_ARTIFACT_LEAVES_BATTLEFIELD`: `MayPayManaEffect("{1}", SequenceEffect.of(LoseLifeEffect(1, TARGET_PLAYER), GainLifeEffect(1)), "pay {1}")`. The leave slot is controller-scoped ("you control"); "leaves" is broader than "dies" (destroy/exile/bounce/sacrifice/tuck). Non-targeting slots — the "may pay {1}" and player target resolve on the stack at resolution |
| Any creature dies — its controller may draw (enchantment) | `f/Fecundity.java` | ON_ANY_CREATURE_DIES DyingCreatureControllerMayDrawCardEffect() — the DYING creature's controller (may be an opponent) may draw, unlike a plain MayEffect which offers it to the source's controller |
| Enchanted creature dies — its controller may search for a creature (Aura) | `p/PatternOfRebirth.java` | ON_ENCHANTED_PERMANENT_PUT_INTO_GRAVEYARD EnchantedCreatureControllerMaySearchLibraryForCreatureToBattlefieldEffect() — queues a resolution-time `MayEffect(SearchLibraryEffect(CREATURE, BATTLEFIELD))` for the dying creature's controller, who may differ from the Aura's controller |
| Any creature dies — its controller sacrifices a land (enchantment) | `e/Earthlink.java` | ON_ANY_CREATURE_DIES DyingCreatureControllerSacrificesPermanentsEffect(1, PermanentIsLandPredicate()) + UPKEEP_TRIGGERED ForcedCostOrElseEffect(PayManaCost("{2}"), List.of(SacrificeSelfEffect()), true) — the DYING creature's controller sacrifices, not the source's controller |
| Opponent creature dies | `g/GlissaTheTraitor.java` | ON_OPPONENT_CREATURE_DIES MayEffect(ReturnCardFromGraveyardEffect.builder().destination(HAND).filter(CardTypePredicate(ARTIFACT)).build()) — includes tokens |
| Opponent creature dies → gain life equal to its toughness (enchantment) | `g/GrimFeast.java` | UPKEEP_TRIGGERED DealDamageToPlayersEffect(1, DamageRecipient.CONTROLLER) + ON_OPPONENT_CREATURE_DIES GainLifeEqualToDyingCreatureToughnessEffect() — the collector snapshots the dying creature's last-known effective toughness and bakes a concrete GainLifeEffect |
| Opponent creature with a -1/-1 counter dies — may reanimate under your control | `n/Necroskitter.java` | ON_OPPONENT_CREATURE_DIES TriggeringPermanentConditionalEffect(PermanentHasCountersPredicate(MINUS_ONE_MINUS_ONE), ReturnDyingOpponentCreatureUnderYourControlEffect()) — intervening-if on the dying permanent's counters; the dedicated collector queues a "you may" that steals the dying card from its owner's graveyard onto your battlefield (tracked as a stolen creature). Do NOT wrap the return in MayEffect yourself — the collector adds it and bakes the dying card id into the effect |
| Any artifact goes to graveyard from battlefield | `m/MolderBeast.java` | ON_ANY_ARTIFACT_PUT_INTO_GRAVEYARD_FROM_BATTLEFIELD BoostSelfEffect |
| Any artifact goes to graveyard — damage controller | `m/MagneticMine.java` | ON_ANY_ARTIFACT_PUT_INTO_GRAVEYARD_FROM_BATTLEFIELD DealDamageToPlayersEffect(2, DamageRecipient.TRIGGERING_PERMANENT_CONTROLLER) — target pre-set to artifact's controller at trigger time |
| Own artifact goes to graveyard — target opponent loses source power | `m/MarionetteMaster.java` | target opponent + ON_ANY_ARTIFACT_PUT_INTO_GRAVEYARD_FROM_BATTLEFIELD TriggeringArtifactControllerConditionalEffect(LoseLifeEffect(SourcePower, TARGET_PLAYER)) — controller check uses the artifact's controller at trigger time; source power is evaluated when the ability resolves |
| Self or another artifact you control goes to graveyard — return a lesser-mana-value artifact card | `s/ScrapTrawler.java` | ON_DEATH + ON_ANY_ARTIFACT_PUT_INTO_GRAVEYARD_FROM_BATTLEFIELD `ReturnCardFromGraveyardEffect.builder().destination(HAND).filter(CardTypePredicate(ARTIFACT)).targetGraveyard(true).dynamicMaxManaValue(SourceManaValueMinusOne).build()`; the self trigger snapshots one less than its source mana value, while the controller-conditional artifact trigger snapshots one less than the triggering artifact's last-known mana value and queues a graveyard-card target |
| Any enchantment goes to graveyard — draw a card | `f/FemerefEnchantress.java` | ON_ANY_ENCHANTMENT_PUT_INTO_GRAVEYARD_FROM_BATTLEFIELD DrawCardEffect — fires for any player's enchantment |
| Any permanent another player owns dies — draw + lose life | `k/KothophedSoulHoarder.java` | ON_OTHER_PLAYER_OWNED_PERMANENT_PUT_INTO_GRAVEYARD_FROM_BATTLEFIELD SequenceEffect.of(DrawCardEffect(1), LoseLifeEffect(1)) — ownership-based |
| Artifact put into opponent's graveyard from battlefield | `v/ViridianRevel.java` | ON_ARTIFACT_PUT_INTO_OPPONENT_GRAVEYARD_FROM_BATTLEFIELD MayEffect(DrawCardEffect) |
| Black card put into opponent's graveyard from anywhere | `c/Compost.java` | ON_BLACK_CARD_PUT_INTO_OPPONENT_GRAVEYARD_FROM_ANYWHERE MayEffect(DrawCardEffect) — fires from any zone (battlefield, hand, library, stack) via `GraveyardService.addCardToGraveyard` |
| Creature card into opponent's graveyard → become a copy | `l/LazavDimirMastermind.java` | ON_CREATURE_CARD_PUT_INTO_OPPONENT_GRAVEYARD_FROM_ANYWHERE MayEffect(BecomeCopyOfCreatureCardInOpponentGraveyardEffect) — the triggering card is baked in at trigger time (last-known information, no target, no re-check on resolution); the copy keeps Lazav's name, gains legendary + hexproof, and re-gains the trigger |
| Any land goes to graveyard — damage its controller | `d/DingusEgg.java` | ON_ANY_LAND_PUT_INTO_GRAVEYARD_FROM_BATTLEFIELD DealDamageToPlayersEffect(2, DamageRecipient.TRIGGERING_PERMANENT_CONTROLLER) — target pre-set to land's controller at trigger time |
| Any land goes to graveyard — self gets +1/+0 until EOT | `a/AkkiRaider.java` | ON_ANY_LAND_PUT_INTO_GRAVEYARD_FROM_BATTLEFIELD BoostSelfEffect(1, 0) |
| Any player casts a spell from a graveyard â€” damage that player | `a/AshZealot.java` | ON_ANY_PLAYER_CASTS_SPELL SpellCastTriggerEffect(null, DealDamageToPlayersEffect(3, TRIGGERING_PLAYER), StackEntryCastFromZonePredicate(Zone.GRAVEYARD)) |
| Opponent dealt noncombat damage — self boost | `c/ChandrasSpitfire.java` | ON_OPPONENT_DEALT_NONCOMBAT_DAMAGE BoostSelfEffect(3, 0) — +3/+0 whenever an opponent is dealt noncombat damage |
| Opponent damaged by your red spell/planeswalker — return this from graveyard | `c/ChandrasPhoenix.java` | GRAVEYARD_ON_OPPONENT_DAMAGED_BY_RED_SPELL_OR_PLANESWALKER `ReturnCardFromGraveyardEffect.builder().destination(HAND).filter(CardIsSelfPredicate).returnAll(true)` — source gating (red instant/sorcery spell, or ability whose source card is a red planeswalker; entry controller must be an opponent of the damaged player) lives in `TriggerCollectionService.checkRedSpellOrPlaneswalkerDamageToOpponentTriggers`, called from the noncombat player-damage path in `DamageSupport` |
| Opponent draws | `u/UnderworldDreams.java` | ON_OPPONENT_DRAWS DealDamageToPlayersEffect(1, DamageRecipient.TARGET_PLAYER) — targetId set to the drawing player |
| Opponent activates a non-mana ability of an artifact/creature/land — damage them | `h/HarshMentor.java` | ON_OPPONENT_ACTIVATES_NONMANA_ABILITY TriggeringPermanentConditionalEffect(PermanentAnyOfPredicate(artifact/creature/land), DealDamageToPlayersEffect(2, DamageRecipient.TARGET_PLAYER)) — fires only on the non-mana activation path (mana abilities excluded automatically); conditional wrapper filters by the activated permanent's type; activating opponent pre-set as the non-targeting targetId |
| Opponent loses life — you gain that much life | `e/ExquisiteBlood.java` | `addEffect(ON_OPPONENT_LOSES_LIFE, GainLifeEffect(new EventValue()))` — the collector stacks the trigger and snapshots the life lost onto the entry's event value, so `EventValue()` reads "that much" at resolution. Fires from damage (spell + combat) and life-loss effects |
| Opponent loses life — mill | `m/Mindcrank.java` | ON_OPPONENT_LOSES_LIFE MillOpponentOnLifeLossEffect — whenever opponent loses life, mills that many cards. Trigger fires from damage (spell + combat) and life loss effects |
| Opponent discards — damage | `m/Megrim.java` | ON_OPPONENT_DISCARDS DealDamageToDiscardingPlayerEffect |
| Opponent discards — life loss | `l/LilianasCaress.java` | ON_OPPONENT_DISCARDS LoseLifeEffect |
| Opponent discards — gated on the discarded card's type | `w/WasteNot.java` | Three `ON_OPPONENT_DISCARDS` effects, each wrapped in `TriggeringCardConditionalEffect(CardPredicate, wrapped)`: `CardTypePredicate(CREATURE)` → `CreateTokenEffect.blackZombie(1)`, `CardTypePredicate(LAND)` → `AwardManaEffect(BLACK, 2)`, `CardAllOfPredicate(Not(CREATURE), Not(LAND))` → `DrawCardEffect()`. `TriggerCollectionService.checkDiscardTriggers` unwraps the conditional against the discarded card; `DiscardTriggerCollectorService` enqueues the survivor as a `TRIGGERED_ABILITY` (it uses the stack — not a mana ability, CR 605.1b) |
| Spell cast trigger | `q/QuirionDryad.java` | ON_ANY_PLAYER_CASTS_SPELL +1/+1 counter |
| Magecraft — temporary exile replacement plus exile trigger | `e/ElementalExpressionist.java` | ON_CONTROLLER_CASTS_SPELL + ON_CONTROLLER_COPIES_SPELL with `GrantExileIfLeavesBattlefieldUntilEndOfTurnEffect()` and `GrantEffectToTargetUntilEndOfTurnEffect(ON_SELF_LEAVES_BATTLEFIELD, SelfExiledFromBattlefieldEffect(CreateTokenEffect(...)))`, targeted by `TargetFilters.creatureYouControl()` |
| Heroic exile and source-name counter | `m/Mindreaver.java` | ON_CONTROLLER_CASTS_SPELL `SpellCastTriggerEffect(null, List.of(ExileTopCardsToSourceEffect(3, false, false, LibraryScope.TARGET_PLAYER)), StackEntryTargetsSourcePredicate())` + activated `SacrificeSelfCost` and targeted `CounterSpellEffect` filtered by `StackEntrySharesNameWithCardExiledWithSourcePredicate` |
| Spell cast trigger — caster gains for same-name graveyard cards | `a/AvenShrine.java` | ON_ANY_PLAYER_CASTS_SPELL `GainLifeForSameNameCardsInGraveyardsOnSpellCastEffect()` — collector snapshots the cast spell's name and queues a `GainLifeEffect(CardsInGraveyard(CardNamedPredicate(spellName), ANY_PLAYER), TRIGGERING_PLAYER)` |
| Spell cast trigger — caster creates Squirrels for same-name graveyard cards | `n/NantukoShrine.java` | ON_ANY_PLAYER_CASTS_SPELL `CreateSquirrelTokensForSameNameCardsInGraveyardsOnSpellCastEffect()` — collector snapshots the cast spell's name and queues `CreateTokenForTargetPlayerEffect(CreateTokenEffect(CardsInGraveyard(CardNamedPredicate(spellName), ANY_PLAYER), Squirrel 1/1))` with the caster as the non-target recipient |
| Spell cast trigger — counter unless caster pays for same-name graveyard cards | `c/CephalidShrine.java` | ON_ANY_PLAYER_CASTS_SPELL `CounterUnlessPaysForSameNameCardsInGraveyardsOnSpellCastEffect()` — collector snapshots the cast spell's name and queues `CounterUnlessPaysEffect(CardsInGraveyard(CardNamedPredicate(spellName), ANY_PLAYER))` targeting the cast spell |
| Spell cast trigger — caster discards for same-name graveyard cards | `c/CabalShrine.java` | ON_ANY_PLAYER_CASTS_SPELL `DiscardForSameNameCardsInGraveyardsOnSpellCastEffect()` — collector snapshots the cast spell's name and queues `DiscardEffect(CardsInGraveyard(CardNamedPredicate(spellName), ANY_PLAYER), TARGET_PLAYER)` with the caster bound as a non-target |
| Spell cast trigger — self-boost by mana value | `m/Manaplasm.java` | ON_CONTROLLER_CASTS_SPELL BoostSelfByCastSpellManaValueEffect(null) — "whenever you cast a spell, this gets +X/+X until end of turn where X is that spell's mana value" (null filter = any spell) |
| Spirit/Arcane spell cast trigger — may mill target player by mana value | `c/CloudhoofKirin.java` | ON_CONTROLLER_CASTS_SPELL `MayEffect(MillTargetPlayerByCastSpellManaValueEffect(CardAnyOf(SPIRIT, ARCANE)))` — snapshots the triggering spell's mana value, then the controller may choose any player to mill that many cards |
| Spirit/Arcane spell cast trigger — may gain life by mana value | `b/BounteousKirin.java` | ON_CONTROLLER_CASTS_SPELL `MayEffect(GainLifeEqualToCastSpellManaValueEffect(CardAnyOf(SPIRIT, ARCANE)))` — snapshots the triggering spell's full mana value, then the controller may gain that much life |
| Spirit/Arcane spell cast trigger — target player discards by mana value | `i/InfernalKirin.java` | ON_CONTROLLER_CASTS_SPELL `DiscardAllCardsWithCastSpellManaValueEffect(CardAnyOf(SPIRIT, ARCANE))` — snapshots the triggering spell's mana value, then the controller chooses any player whose revealed hand discards every card with that mana value |
| Spell cast trigger — destroy permanents by mana value | `c/CelestialKirin.java` | ON_CONTROLLER_CASTS_SPELL DestroyAllPermanentsWithCastSpellManaValueEffect(filter) — the collector snapshots the cast spell's mana value and reuses DestroyAllPermanentsEffect with exact min/max mana-value predicates |
| Spell cast trigger — self-animation by mana value | `v/VeiledSentry.java` | ON_OPPONENT_CASTS_SPELL BecomeCreatureByCastSpellManaValueEffect() — "when an opponent casts a spell, if this permanent is an enchantment, it becomes an Illusion creature with power and toughness each equal to that spell's mana value"; the collector snapshots the cast spell's mana value into `BecomeCreatureEffect` and gates the trigger on the source remaining an enchantment |
| Spell cast modal choose-up-to-one bounce | `h/HullbreakerHorror.java` | STATIC CantBeCounteredEffect + ON_CONTROLLER_CASTS_SPELL SpellCastTriggerEffect(null, HullbreakerHorrorTriggerEffect) — mode at resolution (spell you don't control / nonland permanent / none) |
| Bloodthirst + grants bloodthirst on Vampire cast | `b/BloodlordOfVaasgoth.java` | ON_ENTER_BATTLEFIELD ConditionalEffect(OpponentDealtDamageThisTurn(1), EnterWithCountersEffect(PLUS_ONE_PLUS_ONE, Fixed(3))) + ON_CONTROLLER_CASTS_SPELL SpellCastTriggerEffect(CardAllOfPredicate(CardTypePredicate(CREATURE), CardSubtypePredicate(VAMPIRE)), GrantBloodthirstToCastSpellEffect(3)) — printed bloodthirst is an as-enters replacement; the grant rides on the spell's StackEntry and becomes counters when that creature enters |
| Historic spell cast trigger (damage) | `c/CabalPaladin.java` | ON_CONTROLLER_CASTS_SPELL SpellCastTriggerEffect(CardIsHistoricPredicate, DealDamageToPlayersEffect(2, DamageRecipient.EACH_OPPONENT)) — deals 2 damage to each opponent when casting historic spells |
| Kicked spell cast trigger (counter + damage) | `h/HallarTheFirefletcher.java` | ON_CONTROLLER_CASTS_SPELL KickedSpellCastTriggerEffect(PutCountersOnSourceEffect(1,1,1), DealDamageToPlayersEffect(new CountersOnSource(PLUS_ONE_PLUS_ONE), DamageRecipient.EACH_OPPONENT)) — on kicked spell cast, add +1/+1 counter then deal damage equal to total counters to each opponent |
| Knowledge Pool-style cast intercept | `k/KnowledgePool.java` | ON_ENTER_BATTLEFIELD ExileTopCardsToSourceEffect(3, false, false, LibraryScope.EACH_PLAYER) + ON_ANY_PLAYER_CASTS_SPELL KnowledgePoolCastTriggerEffect — exiles cast-from-hand spells, lets caster pick a nonland card from pool to cast free. Uses per-permanent exile tracking (permanentExiledCards) and KNOWLEDGE_POOL_CAST_CHOICE awaiting input |
| Knowledge Pool-style cast intercept | `k/KnowledgePool.java` | ON_ENTER_BATTLEFIELD EachPlayerExilesTopCardsToSourceEffect(3) + ON_ANY_PLAYER_CASTS_SPELL KnowledgePoolCastTriggerEffect — exiles cast-from-hand spells, lets caster pick a nonland card from pool to cast free. Uses per-permanent exile tracking (permanentExiledCards) and KNOWLEDGE_POOL_CAST_CHOICE awaiting input |
| Possibility Storm cast intercept | `p/PossibilityStorm.java` | ON_ANY_PLAYER_CASTS_SPELL PossibilityStormCastTriggerEffect — exiles the cast-from-hand spell, digs the caster's library until a card shares a card type with it, offers it as a free cast (`MayCastExiledCardThenBottomRestEffect`), then bottoms everything exiled with the enchantment in a random order |
| May gain life on spell cast | `a/AngelsFeather.java` | MayEffect(GainLifeOnSpellCastEffect(CardColorPredicate)) — also `g/GolemsHeart.java` with CardTypePredicate |
| May pay to deal damage on artifact cast | `e/Embersmith.java` | MayEffect(DealDamageToAnyTargetOnArtifactCastEffect) — pay mana + any-target trigger |
| May pay to create token on artifact cast | `m/Myrsmith.java` | MayEffect(CreateTokenOnOwnSpellCastWithCostEffect) — pay mana + create token trigger |
| May loot on spell cast | `r/Riddlesmith.java` | MayEffect(DrawAndDiscardOnOwnSpellCastEffect(CardTypePredicate)) — may draw+discard on matching spell cast |
| ETB any-opponent take-damage-or-keep | `v/VexingDevil.java` | ON_ENTER_BATTLEFIELD AnyOpponentMayTakeDamageSacrificeSourceEffect(4) — any opponent may take 4 damage from it; if any do, sacrifice (even if damage prevented) |
| Each-combat any-opponent sacrifice-or-attack | `d/DesecrationDemon.java` | EACH_BEGINNING_OF_COMBAT_TRIGGERED AnyOpponentMaySacrificeCreatureTapAndCounterSourceEffect() — any opponent may sacrifice a creature; if a player does, tap the Demon and put one +1/+1 counter on it |
| Opponent spell punisher (discard or life) | `p/PainfulQuandary.java` | ON_OPPONENT_CASTS_SPELL LoseLifeUnlessDiscardEffect(5) — opponent chooses discard or lose life |
| Opponent spell tax (counter unless pays) | `c/ChancellorOfTheAnnex.java` | ON_OPPONENT_CASTS_SPELL CounterUnlessPaysEffect(1) — auto-targets the triggering spell. Also has ON_OPENING_HAND_REVEAL MayEffect(RegisterDelayedCounterTriggerEffect(1)) for Chancellor-style delayed counter trigger on first opponent spell |
| Opponent creature spell punisher (pay or life) | `i/IsolationCell.java` | ON_OPPONENT_CASTS_SPELL LoseLifeUnlessPaysEffect(2, 2, CardTypePredicate(CREATURE)) — filtered to creature spells only, pay mana or lose life |
| Opponent creature spell punisher (pay or damage) | `s/SoulBarrier.java` | ON_OPPONENT_CASTS_SPELL DamageUnlessPaysEffect(2, 2, CardTypePredicate(CREATURE)) — damage counterpart of IsolationCell (goes through the damage system: preventable, infect, etc.), pay mana or take damage |
| Any spell +1/+1 counter | `m/ManagorgerHydra.java` | ON_ANY_PLAYER_CASTS_SPELL PutPlusOnePlusOneCounterOnSourceOnColorSpellCastEffect(Set.of(), 1, false) — empty color set = any spell, colorless included |
| Opponent color spell +1/+1 counter (may) | `m/MoldAdder.java` | ON_OPPONENT_CASTS_SPELL MayEffect(PutPlusOnePlusOneCounterOnSourceOnColorSpellCastEffect(BLUE+BLACK, 1, false)) — may put +1/+1 counter when opponent casts blue or black spell |
| Opponent spell reveal-creature-to-battlefield | `l/LurkingPredators.java` | ON_OPPONENT_CASTS_SPELL RevealTopCardCreatureToBattlefieldOrMayBottomEffect — reveal top card, creature goes to battlefield, non-creature may go to bottom |
| Ally land enters (landfall — life + draw) | `t/TatyovaBenthicDruid.java` | ON_ALLY_LAND_ENTERS_BATTLEFIELD GainLifeEffect(1) + DrawCardEffect(1) — triggers when controller plays a land, both effects bundled in one triggered ability |
| Ally land-subtype enters + Treefolk cast — self boost | `b/BattlewandOak.java` | ON_ALLY_LAND_ENTERS_BATTLEFIELD TriggeringCardConditionalEffect(CardSubtypePredicate(FOREST), BoostSelfEffect(2,2)) + ON_CONTROLLER_CASTS_SPELL SpellCastTriggerEffect(CardSubtypePredicate(TREEFOLK), BoostSelfEffect(2,2)) — "Whenever a Forest you control enters" filters the landfall trigger by land subtype (ally-land-enters now unwraps `TriggeringCardConditionalEffect` like the opponent-land slot) |
| Opponent land enters (punisher) | `t/TunnelIgnus.java` | ON_OPPONENT_LAND_ENTERS_BATTLEFIELD ConditionalEffect(new PermanentEnteredThisTurn(CardTypePredicate(LAND), 2), DealDamageToPlayersEffect(3, DamageRecipient.TARGET_PLAYER)) — conditional wrapper checks 2+ lands entered this turn |
| Imprint ETB exile land + opponent land name match | `i/InvaderParasite.java` | ON_ENTER_BATTLEFIELD ExileTargetPermanentAndImprintEffect + ON_OPPONENT_LAND_ENTERS_BATTLEFIELD ConditionalEffect(new ImprintedCardNameMatchesEnteringPermanent(), DealDamageToPlayersEffect(2, DamageRecipient.TARGET_PLAYER)) — exile target land on ETB, deal 2 damage when opponent plays land with same name |
| Land tap trigger | `m/Manabarbs.java` | ON_ANY_PLAYER_TAPS_LAND DealDamageOnLandTapEffect |
| Nonbasic land tap trigger | `b/BurningEarth.java` | ON_ANY_PLAYER_TAPS_LAND `DealDamageOnLandTapEffect(1, PermanentNotPredicate(PermanentHasSupertypePredicate(BASIC)))` — the filter narrows which tapped lands trigger it |
| Land tap mana doubling + opponent untap lock | `v/VorinclexVoiceOfHunger.java` | ON_ANY_PLAYER_TAPS_LAND AddOneOfEachManaTypeProducedByLandEffect + OpponentTappedLandDoesntUntapEffect — doubles controller's land mana, opponent lands skip next untap step |
| End step self-destruct | `s/SparkElemental.java` | END_STEP_TRIGGERED SacrificeSelfEffect |
| End step morbid destroy | `r/ReaperFromTheAbyss.java` | END_STEP_TRIGGERED ConditionalEffect(new Morbid(), DestroyTargetPermanentEffect()) + target(PermanentPredicateTargetFilter(non-Demon creature)) — intervening-if morbid check at trigger time, targeting via `EndStepTriggerTarget` |
| End step counter if it dealt damage to opponent | `w/WhirlingDervish.java` | END_STEP_TRIGGERED ConditionalEffect(new SelfDealtDamageToOpponentThisTurn(), PutCountersOnSelfEffect(PLUS_ONE_PLUS_ONE)) — "each end step" intervening-if that the source dealt (combat) damage to an opponent of its current controller this turn; at most one counter/turn since it's a single trigger |
| End step counter if it was dealt damage | `w/WallOfResistance.java` | END_STEP_TRIGGERED ConditionalEffect(new SelfWasDealtDamageThisTurn(), PutCountersOnSelfEffect(PLUS_ZERO_PLUS_ONE)) — "each end step" intervening-if that the source was dealt damage this turn (any source, combat or not); one +0/+1 counter per end step at most |
| Upkeep win if N creature cards in graveyard | `m/MortalCombat.java` | UPKEEP_TRIGGERED ConditionalEffect(new GraveyardCardThreshold(20, new CardTypePredicate(CardType.CREATURE)), new WinGameEffect()) — intervening-if (CR 603.4) checked at trigger time in `StepTriggerService` and again at resolution. The threshold counts nontoken cards only |
| End step win if four same-named creatures | `b/Biovisionary.java` | END_STEP_TRIGGERED ConditionalEffect(new ControlsPermanentCount(4, PermanentAllOfPredicate(PermanentIsCreaturePredicate, PermanentNamedPredicate("Biovisionary"))), WinGameEffect()) — "each end step" intervening-if on the count of same-named creatures **you** control (the permanent's controller, not the active player); checked at trigger time in `StepTriggerService` and again at resolution |
| Controller end step draw | `j/JinGitaxiasCoreAugur.java` | CONTROLLER_END_STEP_TRIGGERED DrawCardEffect(7) — "your end step" trigger (only fires on controller's turn) + STATIC ReduceOpponentMaxHandSizeEffect(7) |
| Controller end step raid loot | `m/MaraudingLooter.java` | CONTROLLER_END_STEP_TRIGGERED ConditionalEffect(new Raid(), MayEffect(SequenceEffect.of(DrawCardEffect(1), DiscardEffect(1, DiscardRecipient.CONTROLLER)))) — raid intervening-if at trigger time, may draw-then-discard at resolution |
| Controller end step meld | `g/GiselaTheBrokenBlade.java` | CONTROLLER_END_STEP_TRIGGERED ConditionalEffect(AllOf(own+control self, own+control named Bruna), MeldWithNamedCreatureEffect("Bruna, the Fading Light")) + setBackFaceCard(Brisela) — exile both then meld; components tracked on Permanent.meldComponentCards |
| Beginning of combat meld | `g/GrafRats.java` / `c/ChitteringHost.java` | BEGINNING_OF_COMBAT_TRIGGERED same ConditionalEffect+MeldWithNamedCreatureEffect shape (`"Midnight Scavengers"` → Chittering Host). Host ETB: BoostAllOwnCreaturesEffect(1,0,Not(IsSource)) + GrantKeywordEffect(MENACE, OWN_CREATURES, Not(IsSource)) |
| Beginning of combat may-discard self-boost | `f/FurybladeVampire.java` | BEGINNING_OF_COMBAT_TRIGGERED `MayEffect(DiscardCardAndBoostSelfEffect(3, 0))` — may discard a card; if you do, this creature gets +3/+0 until end of turn (`DiscardFollowUp.boost`; empty hand → no boost) |
| Dies — self-reanimate at end step + skip next draw step | `i/IvoryGargoyle.java` | two ON_DEATH effects: `RegisterDelayedSelfReturnFromGraveyardEffect()` (no counters, returns under owner control at the next end step) + `SkipNextEffect(SkipKind.DRAW_STEP)`. `{4}{W}: Exile this creature.` is an `ActivatedAbility` with `ExileSelfEffect()` as its resolution effect (not a cost), so exiling dodges the dies-trigger |
| Dies — self-reanimate tapped at owner's next upkeep | `p/Phytotitan.java` | ON_DEATH `RegisterDelayedSelfReturnFromGraveyardEffect.tappedAtOwnersNextUpkeep()` — same delayed action as Ivory Gargoyle but with `atNextUpkeep`/`tapped` set, so it is drained in `StepTriggerService.handleUpkeepTriggers` and only at the owner's own upkeep |
| Discarded by opponent | `g/GuerrillaTactics.java` | ON_SELF_DISCARDED_BY_OPPONENT DealDamageToAnyTargetEffect |
| Discarded by opponent — self-reanimate at end step | `s/SandGolem.java` | ON_SELF_DISCARDED_BY_OPPONENT `RegisterDelayedSelfReturnFromGraveyardEffect(CounterType.PLUS_ONE_PLUS_ONE, 1)` — returns itself from the graveyard to the battlefield with a +1/+1 counter at the beginning of the next end step |
| Dies with a counter — self-reanimate at end step with one fewer | `n/NineLivesFamiliar.java` | ON_DEATH `RegisterDelayedSelfReturnFromGraveyardWithOneFewerCounterEffect(CounterType.REVIVAL)` — the death collector snapshots the dying permanent's counter count, skips the trigger at zero, and registers the existing delayed self-return with count minus one |
| Imprint ETB + dies | `c/CloneShell.java` | Artifact Creature — ON_ENTER_BATTLEFIELD ImprintFromTopCardsEffect + ON_DEATH PutImprintedCreatureOntoBattlefieldEffect |
| Equipment enters may-draw + metalcraft equip {0} | `p/PuresteelPaladin.java` | ON_ALLY_EQUIPMENT_ENTERS_BATTLEFIELD MayEffect(DrawCardEffect) + STATIC ConditionalEffect(new Metalcraft(), GrantActivatedAbilityEffect(EquipActivatedAbility("{0}"), OWN_PERMANENTS, PermanentHasSubtypePredicate(EQUIPMENT))) |
| Attack boost per other attacking subtype | `d/DireFleetCaptain.java` | ON_ATTACK BoostSelfEffect(PermanentCount(AllOf(IsAttacking, HasSubtype(PIRATE)), CONTROLLER, excludeSource=true), same) — +1/+1 per other attacking Pirate, excludes self |
| Battle cry (attack boost others) | `a/AccorderPaladin.java` | Engine-handled via `BATTLE_CRY` keyword — CombatService auto-generates BoostAllOwnCreaturesEffect(1, 0, attacking + not-self filter) trigger for any creature with the keyword. Card class is empty-body. |
| Training (attack with greater-power ally → +1/+1) | `h/HopefulInitiate.java` | Engine-handled via `TRAINING` keyword — CombatAttackService auto-generates PutCountersOnSelfEffect(+1/+1) when this creature attacks with another attacker of strictly greater power. Card wires only the activated ability. |
| Flanking (blocker without flanking gets -1/-1) | `m/MtendaHerder.java` | Engine-handled via `FLANKING` keyword (CR 702.25a) — CombatBlockService auto-generates a non-targeting BoostTargetCreatureEffect(-1, -1) trigger per blocker that lacks flanking. Card class is empty-body. |
| Battle cry + attack can't-block | `h/HeroOfOxidRidge.java` | ON_ATTACK CantBlockThisTurnEffect(TapUntapScope.ALL_CREATURES, PermanentPowerAtMostPredicate(1)) — battle cry keyword auto-handled, plus predicate-filtered mass can't-block on attack |
| Damage-to-controller bounce | `d/DissipationField.java` | ON_ANY_PERMANENT_DEALS_DAMAGE_TO_YOU ReturnDamageSourcePermanentToHandEffect — bounces any permanent that deals damage to controller |
| Damage-to-controller steal | `c/ContestedWarZone.java` | ON_ANY_PERMANENT_DEALS_DAMAGE_TO_YOU DamageSourceControllerGainsControlOfThisPermanentEffect(true, true) — combat creature damage causes opponent to gain control of this land |
| Damage-to-controller destroy matching source | `m/MikaeusTheUnhallowed.java` | ON_ANY_PERMANENT_DEALS_DAMAGE_TO_YOU DestroyDamageSourcePermanentEffect(filter) — destroys the permanent that dealt damage if it matches the predicate |
| Damage-to-controller exile until this leaves | `h/HixusPrisonWarden.java` | ON_ANY_PERMANENT_DEALS_DAMAGE_TO_YOU ExileDamageSourcePermanentUntilSourceLeavesEffect(PermanentIsCreaturePredicate, combatOnly=true, new SourceEnteredThisTurn()) — O-ring exile of the damaging creature; the intervening-"if" is checked at trigger time and again on resolution (CR 603.4) |
| Combat-damage-to-you revenge + token | `t/TeysaEnvoyOfGhosts.java` | ON_CREATURE_DEALS_COMBAT_DAMAGE_TO_YOU DestroyTargetPermanentEffect() + CreateTokenEffect(...) — "whenever a creature deals combat damage to you, destroy that creature. Create a token." Both effects share the one stack entry whose baked-in `targetId` is the damaging creature; creature- and combat-only, unlike ON_ANY_PERMANENT_DEALS_DAMAGE_TO_YOU |
| Combat-damage trigger with a source-leaves land-type grant | `g/GracefulAntelope.java` | `target(TargetFilters.land()).addEffect(ON_COMBAT_DAMAGE_TO_PLAYER, MayEffect(TargetLandBecomesBasicLandTypeUntilSourceLeavesEffect(PLAINS), ...))` + `TrackedLandsBecomeBasicLandTypeEffect(PLAINS)` in STATIC; target is chosen after accepting the resolution-time may ability |
| Upkeep become-copy | `c/Cryptoplasm.java` | UPKEEP_TRIGGERED BecomeCopyOfTargetCreatureEffect — mandatory target at trigger time (CR 603.3d), may choice at resolution. Uses UpkeepCopyTriggerTarget queue for target selection, then CopyResolutionService queues pendingMayAbility for the may choice |
| Whenever this creature deals damage to a creature: exile that creature | `p/PitSpawn.java` | ON_ALLY_CREATURE_DEALS_DAMAGE_TO_CREATURE `ExileDamagedCreatureEffect()` — self-scoped, combat and noncombat; expands to a non-targeting `ExileTargetPermanentEffect` for the damaged creature |
| Opponent source damages you or your permanent: may damage that source's controller | `f/FlamebladeAngel.java` | `ON_OPPONENT_SOURCE_DEALS_DAMAGE_TO_YOU_OR_YOUR_PERMANENT` + `MayEffect(DealDamageToDamageSourceControllerEffect(1))` — fires once per opponent-controlled source per damage event, including damage to controlled permanents |
| On death + fixed may-pay self-return + same-resolution pay-X subtype reanimation | `r/RelentlessDead.java` | ON_DEATH `MayPayManaEffect("{B}", ReturnCardFromGraveyardEffect` self-to-hand) + `PayXManaReturnTargetCreatureWithManaValueXEffect(filter)`; the pay-X effect chooses the exact-mana-value graveyard target only after payment and returns it before the resolution yields priority |
| Upkeep reveal-top opponent choice | `s/SinProdder.java` | UPKEEP_TRIGGERED RevealTopCardAnyOpponentMayGraveyardOrDealDamageEffect() — reveal the controller's top card; opponents choose in turn order whether to put it into the graveyard, with the first acceptance dealing its mana value to that opponent; all declines put it into hand |
| Becomes the target of a spell or ability — its controller sacrifices a land | `l/LavaRunner.java` | `ON_BECOMES_TARGET_OF_SPELL_OR_ABILITY TriggeringSpellControllerSacrificesPermanentsEffect(1, PermanentIsLandPredicate)` — a `TriggeringSpellReferencingEffect` queues the triggering spell or ability as an internal STACK reference, then reuses the forced-sacrifice choice for that object's controller |
