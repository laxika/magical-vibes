package com.github.laxika.magicalvibes.cards.m;

import com.github.laxika.magicalvibes.cards.CardRegistration;
import com.github.laxika.magicalvibes.model.Card;

/**
 * Islandwalk and phasing are both keyword-driven; the engine handles them from the
 * Scryfall-loaded keyword set, so no engine logic is needed here.
 */
@CardRegistration(set = "MIR", collectorNumber = "75")
public class MerfolkRaiders extends Card {
}
