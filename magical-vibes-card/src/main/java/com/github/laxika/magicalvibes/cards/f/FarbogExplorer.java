package com.github.laxika.magicalvibes.cards.f;

import com.github.laxika.magicalvibes.cards.CardRegistration;
import com.github.laxika.magicalvibes.model.Card;

/**
 * Farbog Explorer — {2}{W} Creature — Human Scout (2/3).
 * Swampwalk (auto-loaded from Scryfall). No card-specific engine logic.
 */
@CardRegistration(set = "AVR", collectorNumber = "21")
public class FarbogExplorer extends Card {

    public FarbogExplorer() {
    }
}
