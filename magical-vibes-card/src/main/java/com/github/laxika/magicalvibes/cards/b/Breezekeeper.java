package com.github.laxika.magicalvibes.cards.b;

import com.github.laxika.magicalvibes.cards.CardRegistration;
import com.github.laxika.magicalvibes.model.Card;

/**
 * Flying and phasing are both keyword-driven; the engine handles them from the
 * Scryfall-loaded keyword set, so no engine logic is needed here.
 */
@CardRegistration(set = "VIS", collectorNumber = "27")
public class Breezekeeper extends Card {
}
