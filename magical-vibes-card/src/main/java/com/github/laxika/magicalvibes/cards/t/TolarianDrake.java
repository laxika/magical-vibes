package com.github.laxika.magicalvibes.cards.t;

import com.github.laxika.magicalvibes.cards.CardRegistration;
import com.github.laxika.magicalvibes.model.Card;

/**
 * Flying and phasing are keyword-driven; the engine handles both from the
 * Scryfall-loaded keyword set, so no engine logic is needed here.
 */
@CardRegistration(set = "WTH", collectorNumber = "55")
public class TolarianDrake extends Card {
}
