package com.github.laxika.magicalvibes.model.effect;

/** Each player secretly chooses a positive item count, then the revealed counts determine life loss. */
public record GoblinGameEffect() implements CardEffect {
}
