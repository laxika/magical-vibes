package com.github.laxika.magicalvibes.model.effect;

/**
 * Static replacement effect: the controller's unspent mana becomes colorless when it would drain.
 */
public record ReplaceManaDrainWithColorlessEffect() implements CardEffect {
}
