package com.github.laxika.magicalvibes.model.amount;

/** The mana value of the top card of the controller's library, or zero for an empty library. */
public record TopCardOfLibraryManaValue() implements DynamicAmount {
}
