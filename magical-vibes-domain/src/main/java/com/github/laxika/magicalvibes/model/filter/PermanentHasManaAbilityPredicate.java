package com.github.laxika.magicalvibes.model.filter;

/** Matches permanents that currently have at least one mana ability. */
public record PermanentHasManaAbilityPredicate() implements PermanentPredicate {
}
