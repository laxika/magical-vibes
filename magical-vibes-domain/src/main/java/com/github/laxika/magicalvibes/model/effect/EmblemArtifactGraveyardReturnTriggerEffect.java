package com.github.laxika.magicalvibes.model.effect;

/** Marker for an emblem that returns an artifact from its controller's graveyard at the next end step. */
public record EmblemArtifactGraveyardReturnTriggerEffect() implements CardEffect {
}
