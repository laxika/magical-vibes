package com.github.laxika.magicalvibes.model.effect;

/** Gives the controller the city's blessing when they control at least ten permanents. */
public record AscendEffect() implements CardEffect {
}
