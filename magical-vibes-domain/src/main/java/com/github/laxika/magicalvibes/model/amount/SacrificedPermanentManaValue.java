package com.github.laxika.magicalvibes.model.amount;

/** The mana value of the permanent whose last-known snapshot was captured by a sacrifice effect. */
public record SacrificedPermanentManaValue() implements DynamicAmount {
}
