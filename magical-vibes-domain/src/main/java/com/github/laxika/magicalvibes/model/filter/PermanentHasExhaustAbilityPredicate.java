package com.github.laxika.magicalvibes.model.filter;

/** Matches permanents that currently have at least one effective exhaust ability. */
public record PermanentHasExhaustAbilityPredicate() implements PermanentPredicate {
}
