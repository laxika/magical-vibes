package com.github.laxika.magicalvibes.model.effect;

/** Marker granted to a permanent that cannot be sacrificed while the granting effect applies. */
public record CantBeSacrificedEffect() implements CardEffect {
}
