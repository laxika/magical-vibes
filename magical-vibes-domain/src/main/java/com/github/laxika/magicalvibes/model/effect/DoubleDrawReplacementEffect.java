package com.github.laxika.magicalvibes.model.effect;

/**
 * Static draw-replacement effect: while a source with this effect is on the battlefield,
 * if its controller would draw a card, they draw two cards instead (Thought Reflection).
 */
public record DoubleDrawReplacementEffect() implements CardEffect {
}
