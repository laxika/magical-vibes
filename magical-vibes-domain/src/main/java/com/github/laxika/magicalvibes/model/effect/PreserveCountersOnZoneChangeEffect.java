package com.github.laxika.magicalvibes.model.effect;

/** Marks a permanent whose counters remain as it changes zones, except into a hand or library. */
public record PreserveCountersOnZoneChangeEffect() implements CardEffect {
}
