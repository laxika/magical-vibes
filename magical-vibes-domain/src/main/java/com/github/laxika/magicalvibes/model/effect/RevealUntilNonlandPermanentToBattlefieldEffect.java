package com.github.laxika.magicalvibes.model.effect;

/**
 * Reveals cards from the top of a library until a nonland permanent card is found, puts that card
 * onto the battlefield, and puts the other revealed cards on the bottom in a random order.
 */
public record RevealUntilNonlandPermanentToBattlefieldEffect() implements CardEffect {
}
