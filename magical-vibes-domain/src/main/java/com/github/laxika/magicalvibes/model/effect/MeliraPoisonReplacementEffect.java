package com.github.laxika.magicalvibes.model.effect;

/**
 * Melira's replacement effect: the first poison-counter event affecting this permanent's
 * controller becomes one poison counter, and later poison-counter events that turn are ignored.
 */
public record MeliraPoisonReplacementEffect() implements CardEffect {
}
