package com.github.laxika.magicalvibes.model.effect;

/**
 * Emblem trigger for returning an artifact put into the controller's graveyard from the battlefield
 * at the beginning of the next end step.
 */
public record RegisterDelayedReturnDyingArtifactUnderControlEffect() implements CardEffect {
}
