package com.github.laxika.magicalvibes.model.effect;

/** Resolves a training ability and fires the source's "whenever this creature trains" triggers. */
public record TrainEffect() implements CardEffect {
}
