package com.github.laxika.magicalvibes.model.effect;

/** Exiles every card from the controller's hand face down and returns those cards at the next end step. */
public record ExileControllerHandFaceDownAndReturnAtNextEndStepEffect() implements CardEffect {
}
