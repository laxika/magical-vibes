package com.github.laxika.magicalvibes.model.effect;

/**
 * Hostile Negotiations: exile two groups of up to three cards face down, let the controller turn
 * one group face up, and have an opponent choose which group goes to the controller's hand. The
 * other group goes to the controller's graveyard.
 */
public record HostileNegotiationsEffect() implements CardEffect {
}
