package com.github.laxika.magicalvibes.model.effect;

/** Each opponent draws a card, then the controller draws for each opponent who drew this way. */
public record EachOpponentDrawsThenControllerDrawsEffect() implements CardEffect {
}
