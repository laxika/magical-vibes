package com.github.laxika.magicalvibes.model.effect;

/** Static replacement effect that adds one die to a roll and ignores the lowest result. */
public record RollWithAdvantageEffect() implements CardEffect {
}
