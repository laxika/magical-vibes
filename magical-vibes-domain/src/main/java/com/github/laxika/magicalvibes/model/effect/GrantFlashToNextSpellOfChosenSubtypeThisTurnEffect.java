package com.github.laxika.magicalvibes.model.effect;

/**
 * One-shot effect: the next spell of the source permanent's chosen creature subtype the
 * resolving controller casts this turn can be cast as though it had flash.
 */
public record GrantFlashToNextSpellOfChosenSubtypeThisTurnEffect() implements CardEffect {
}
