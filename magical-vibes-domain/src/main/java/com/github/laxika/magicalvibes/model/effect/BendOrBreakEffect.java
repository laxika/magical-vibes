package com.github.laxika.magicalvibes.model.effect;

/**
 * Each player separates their nontoken lands into two piles. An opponent chosen by that player
 * chooses one pile to destroy; the lands in the other pile are tapped.
 */
public record BendOrBreakEffect() implements CardEffect {
}
