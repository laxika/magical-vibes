package com.github.laxika.magicalvibes.model.filter;

/** Matches permanents whose current controller is the monarch. */
public record PermanentControlledByMonarchPredicate() implements PermanentPredicate {
}
