package com.github.laxika.magicalvibes.model.effect;

/** Searches the controller's outside-the-game card pool for a creature card to put into hand. */
public record SearchOutsideGameForCreatureToHandEffect() implements CardEffect {
}
