package com.github.laxika.magicalvibes.model.effect;

/** Declares a target group that is consumed by a separate effect without changing the game state. */
public record TargetGroupMarkerEffect() implements CardEffect {
}
