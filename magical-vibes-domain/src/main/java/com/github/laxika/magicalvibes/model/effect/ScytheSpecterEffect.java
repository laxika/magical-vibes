package com.github.laxika.magicalvibes.model.effect;

/** Makes each opponent discard one card, then causes the highest-mana-value discarding opponents to lose that value in life. */
public record ScytheSpecterEffect() implements CardEffect {
}
