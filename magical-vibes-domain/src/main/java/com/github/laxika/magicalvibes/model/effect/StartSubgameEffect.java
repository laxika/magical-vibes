package com.github.laxika.magicalvibes.model.effect;

/** Suspends resolution until a game played using the players' libraries has ended. */
public record StartSubgameEffect() implements CardEffect { }
