package com.github.laxika.magicalvibes.model.effect;

/** Registers a one-shot delayed trigger for a Vehicle that was crewed earlier in the turn. */
public record RegisterDelayedVehicleAttackEffect(CardEffect effect) implements CardEffect {
}
