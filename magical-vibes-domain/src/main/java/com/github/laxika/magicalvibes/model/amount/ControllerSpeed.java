package com.github.laxika.magicalvibes.model.amount;

/** The resolving effect's controller's current speed, or zero before they start their engines. */
public record ControllerSpeed() implements DynamicAmount {
}
