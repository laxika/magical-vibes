package com.github.laxika.magicalvibes.model.effect;

/** Exchanges the controller's graveyard and library, then shuffles the new library. */
public record ExchangeGraveyardAndLibraryEffect() implements CardEffect {
}
