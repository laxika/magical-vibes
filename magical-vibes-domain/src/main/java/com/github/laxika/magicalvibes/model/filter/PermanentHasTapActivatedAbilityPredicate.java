package com.github.laxika.magicalvibes.model.filter;

/** Matches permanents that currently have an effective activated ability with a tap cost. */
public record PermanentHasTapActivatedAbilityPredicate() implements PermanentPredicate {
}
