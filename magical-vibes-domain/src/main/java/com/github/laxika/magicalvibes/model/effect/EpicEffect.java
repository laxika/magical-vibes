package com.github.laxika.magicalvibes.model.effect;

/**
 * Resolves the Epic ability: the controller cannot cast spells for the rest of the game, and a
 * copy of the spell is created at the beginning of each of that player's upkeeps.
 */
public record EpicEffect() implements CardEffect {
}
