package com.github.laxika.magicalvibes.model.effect;

/**
 * Each player separates all permanents they control into three piles, then sacrifices the pile
 * chosen at random for them.
 */
public record WhimsOfTheFatesEffect() implements CardEffect {
}
