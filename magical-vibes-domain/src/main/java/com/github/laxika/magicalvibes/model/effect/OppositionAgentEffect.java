package com.github.laxika.magicalvibes.model.effect;

/** Static marker for controlling an opponent while that opponent searches their library. */
public record OppositionAgentEffect() implements CardEffect {
}
