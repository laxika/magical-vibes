package com.github.laxika.magicalvibes.model.effect;

/** Looks at the top two cards, manifests one, and puts the other on top or bottom. */
public record ManifestOneOfTopTwoEffect() implements CardEffect {
}
