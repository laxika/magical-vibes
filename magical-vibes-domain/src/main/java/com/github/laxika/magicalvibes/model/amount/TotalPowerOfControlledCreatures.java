package com.github.laxika.magicalvibes.model.amount;

/**
 * The sum of the effective power of the creatures controlled by the amount's controller.
 */
public record TotalPowerOfControlledCreatures() implements DynamicAmount {
}
