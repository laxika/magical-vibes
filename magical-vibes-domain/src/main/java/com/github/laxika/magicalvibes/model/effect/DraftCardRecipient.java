package com.github.laxika.magicalvibes.model.effect;

/** Identifies who receives and chooses a card offered by a spellbook draft. */
public enum DraftCardRecipient {
    CONTROLLER,
    TARGET_PLAYER
}
