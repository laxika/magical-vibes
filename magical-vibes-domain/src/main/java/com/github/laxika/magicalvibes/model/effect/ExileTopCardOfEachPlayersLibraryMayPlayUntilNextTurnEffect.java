package com.github.laxika.magicalvibes.model.effect;

/** Exiles the top card of each player's library and lets the effect controller play those cards
 * until the end of their next turn. */
public record ExileTopCardOfEachPlayersLibraryMayPlayUntilNextTurnEffect() implements CardEffect {
}
