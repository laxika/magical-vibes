package com.github.laxika.magicalvibes.model.filter;

/** Matches a spell controlled by the player chosen by the source permanent as it entered. */
public record StackEntryControlledByChosenPlayerPredicate() implements StackEntryPredicate {
}
