package com.github.laxika.magicalvibes.model.filter;

/** Matches creatures that saddled the source Mount during the current turn. */
public record PermanentThatSaddledSourceThisTurnPredicate() implements PermanentPredicate {
}
