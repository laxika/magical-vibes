package com.github.laxika.magicalvibes.model.effect;

/**
 * Morphic Tide: each player shuffles their owned permanents into their library, then reveals
 * that many cards and puts the specified permanent types onto the battlefield.
 */
public record MorphicTideEffect() implements CardEffect {
}
