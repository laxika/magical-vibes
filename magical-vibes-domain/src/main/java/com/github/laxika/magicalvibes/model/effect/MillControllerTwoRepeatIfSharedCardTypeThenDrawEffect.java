package com.github.laxika.magicalvibes.model.effect;

/** The controller mills two cards, then draws and repeats if two milled cards share a card type. */
public record MillControllerTwoRepeatIfSharedCardTypeThenDrawEffect() implements CardEffect {
}
