package com.github.laxika.magicalvibes.model.amount;

/** The number of legal targets chosen for one declared target group. */
public record TargetGroupCount(int groupIndex) implements DynamicAmount {
}
