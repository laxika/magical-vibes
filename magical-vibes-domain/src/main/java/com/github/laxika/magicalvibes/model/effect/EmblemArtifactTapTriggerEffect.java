package com.github.laxika.magicalvibes.model.effect;

/** Marker for an emblem that triggers whenever an artifact its controller controls becomes tapped. */
public record EmblemArtifactTapTriggerEffect() implements CardEffect {
}
