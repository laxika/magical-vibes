package com.github.laxika.magicalvibes.model.effect;

/** Reveals six cards, lets an opponent exile a nonland, and puts the rest into hand. */
public record AllureOfTheUnknownEffect() implements CardEffect {
}
