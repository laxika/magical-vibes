package com.github.laxika.magicalvibes.model.amount;

/** The number of cards currently exiled and tracked with the source permanent. */
public record CardsExiledWithSource() implements DynamicAmount {
}
