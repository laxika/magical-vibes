package com.github.laxika.magicalvibes.model.effect;

/** Each player secretly chooses a number, then the highest and lowest choices determine the result. */
public record WheelOfMisfortuneEffect() implements CardEffect {
}
