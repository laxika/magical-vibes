package com.github.laxika.magicalvibes.model.effect;

/**
 * Each player exiles the top card of their library and may play that card until the next end step
 * of the effect's controller.
 */
public record EachPlayerExilesTopCardMayPlayUntilNextEndStepEffect() implements CardEffect {
}
