package com.github.laxika.magicalvibes.model.effect;

/** Exiles the source permanent, then immediately returns it under an opponent's control. */
public record ExileSelfAndReturnUnderOpponentControlEffect() implements CardEffect {
}
