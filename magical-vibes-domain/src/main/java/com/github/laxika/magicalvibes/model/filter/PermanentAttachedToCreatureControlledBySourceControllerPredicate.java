package com.github.laxika.magicalvibes.model.filter;

/**
 * Matches a permanent attached to a creature controlled by the source's controller.
 */
public record PermanentAttachedToCreatureControlledBySourceControllerPredicate()
        implements PermanentPredicate {
}
