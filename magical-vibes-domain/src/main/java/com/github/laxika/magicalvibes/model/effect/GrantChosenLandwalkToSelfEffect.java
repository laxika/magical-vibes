package com.github.laxika.magicalvibes.model.effect;

/** Prompts for a land type, then grants the source landwalk of that type until end of turn. */
public record GrantChosenLandwalkToSelfEffect() implements CardEffect {
}
