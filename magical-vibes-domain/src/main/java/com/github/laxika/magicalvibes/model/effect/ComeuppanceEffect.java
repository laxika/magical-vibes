package com.github.laxika.magicalvibes.model.effect;

/**
 * Prevents damage to the controller and their planeswalkers from sources they do not control for
 * the turn, then deals the prevented damage back to each source creature or its controller.
 */
public record ComeuppanceEffect() implements CardEffect {
}
