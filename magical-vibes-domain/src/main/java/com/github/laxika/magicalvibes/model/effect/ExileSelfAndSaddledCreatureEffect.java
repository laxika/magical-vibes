package com.github.laxika.magicalvibes.model.effect;

/** Exiles the source and up to one creature that saddled it this turn, then returns both cards. */
public record ExileSelfAndSaddledCreatureEffect() implements CardEffect {
}
