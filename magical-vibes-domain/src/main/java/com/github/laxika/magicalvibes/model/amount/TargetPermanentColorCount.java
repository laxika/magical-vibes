package com.github.laxika.magicalvibes.model.amount;

/**
 * The number of colors in the target permanent's current effective color set.
 */
public record TargetPermanentColorCount() implements DynamicAmount {
}
