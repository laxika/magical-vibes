package com.github.laxika.magicalvibes.model.effect;

/** Choose a creature controlled by the effect's controller, remove one counter from it, then draw a card. */
public record RemoveCounterFromControlledCreatureThenDrawEffect() implements CardEffect {
}
