package com.github.laxika.magicalvibes.model.effect;

/**
 * Capability marker for a static replacement effect that skips its controller's draw only while
 * that controller's library is empty.
 */
public interface SkipDrawIfEmptyLibraryReplacementEffect extends CardEffect {
}
