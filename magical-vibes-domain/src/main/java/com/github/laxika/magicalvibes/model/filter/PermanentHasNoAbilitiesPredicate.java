package com.github.laxika.magicalvibes.model.filter;

/** Matches permanents with no currently effective abilities. */
public record PermanentHasNoAbilitiesPredicate() implements PermanentPredicate {
}
