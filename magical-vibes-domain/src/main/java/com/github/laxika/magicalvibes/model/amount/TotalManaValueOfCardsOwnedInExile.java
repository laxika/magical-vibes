package com.github.laxika.magicalvibes.model.amount;

/** The total mana value of the non-token cards owned by the controller in exile. */
public record TotalManaValueOfCardsOwnedInExile() implements DynamicAmount {
}
