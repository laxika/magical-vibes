package com.github.laxika.magicalvibes.model.effect;

/**
 * Static draw-replacement effect: if an opponent of the source's controller would draw two or
 * more cards, that opponent and the controller each draw one card instead (Alms Collector).
 */
public record OpponentDrawTwoOrMoreReplacedEffect() implements CardEffect {
}
