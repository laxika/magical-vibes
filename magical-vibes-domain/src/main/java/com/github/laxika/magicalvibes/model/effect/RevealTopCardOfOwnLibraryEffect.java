package com.github.laxika.magicalvibes.model.effect;

/** Reveals the top card of the resolving controller's library; the card stays on top. */
public record RevealTopCardOfOwnLibraryEffect() implements CardEffect {
}
