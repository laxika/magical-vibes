package com.github.laxika.magicalvibes.model.amount;

/**
 * The number of distinct mana values among nonland cards owned by the controller in exile that
 * have study counters on them.
 */
public record DistinctManaValuesAmongStudyCounterCardsInExile() implements DynamicAmount {
}
