package com.github.laxika.magicalvibes.model.filter;

/** Matches permanents whose name is shared by a token controlled by the source's controller. */
public record PermanentSharesNameWithControlledTokenPredicate() implements PermanentPredicate {
}
