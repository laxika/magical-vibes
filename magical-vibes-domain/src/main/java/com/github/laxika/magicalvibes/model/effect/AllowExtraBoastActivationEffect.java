package com.github.laxika.magicalvibes.model.effect;

/**
 * Static effect allowing the controller's creatures to activate marked boast abilities twice
 * each turn rather than once.
 */
public record AllowExtraBoastActivationEffect() implements CardEffect {
}
