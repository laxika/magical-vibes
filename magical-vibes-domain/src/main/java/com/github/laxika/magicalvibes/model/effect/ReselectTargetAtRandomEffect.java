package com.github.laxika.magicalvibes.model.effect;

/**
 * Reselects the only target of the stack entry referenced by the containing trigger at random
 * from that spell or ability's legal targets.
 */
public record ReselectTargetAtRandomEffect() implements CardEffect {
}
