package com.github.laxika.magicalvibes.model.effect;

/**
 * The controller chooses a nonnegative integer during resolution and stores it on the source
 * permanent for later effects in the same resolution.
 */
public record ChooseAnyNumberEffect() implements CardEffect {
}
