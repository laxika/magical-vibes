package com.github.laxika.magicalvibes.model.filter;

/** Matches permanents whose spell was cast for its Warp cost. */
public record PermanentCastForWarpCostPredicate() implements PermanentPredicate {
}
