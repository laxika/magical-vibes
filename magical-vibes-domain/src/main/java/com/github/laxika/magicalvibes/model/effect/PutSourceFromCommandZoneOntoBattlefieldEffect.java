package com.github.laxika.magicalvibes.model.effect;

/** Puts the source card from its controller's command zone onto the battlefield. */
public record PutSourceFromCommandZoneOntoBattlefieldEffect() implements CardEffect {
}
