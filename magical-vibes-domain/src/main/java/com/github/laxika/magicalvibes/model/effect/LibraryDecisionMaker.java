package com.github.laxika.magicalvibes.model.effect;

/** Which player decides the order of cards in a library-inspection effect. */
public enum LibraryDecisionMaker {
    CONTROLLER,
    TARGET_PLAYER
}
