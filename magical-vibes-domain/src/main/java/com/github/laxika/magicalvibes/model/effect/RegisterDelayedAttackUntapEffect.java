package com.github.laxika.magicalvibes.model.effect;

/** Registers a one-shot trigger that untaps the controller's creatures when they next attack this turn. */
public record RegisterDelayedAttackUntapEffect() implements CardEffect {
}
