package com.github.laxika.magicalvibes.model.effect;

/** Advances the controller's Ring emblem and lets them choose their Ring-bearer. */
public record TemptTheRingEffect() implements CardEffect {
}
